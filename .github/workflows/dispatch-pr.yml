name: SDLC Dispatch / PR

on:
  pull_request:
    # `sdlc-test-base` is a temporary sandbox base for smoke-testing the
    # dispatcher end-to-end before the platform lands on `development-1.0`.
    # Delete the entry — and the `sdlc-test-base` branch on origin — once
    # validation is done.
    branches: [development-1.0, sdlc-test-base]
    types: [opened, synchronize, reopened, labeled, unlabeled]

permissions:
  contents: read
  pull-requests: write
  checks: write
  security-events: write
  id-token: write   # required by anthropics/claude-code-action for OIDC auth
  actions: read     # caller cap: run-agent.yml's `gh run download` needs this

jobs:
  dispatch:
    uses: ./.github/workflows/dispatch-core.yml
    secrets: inherit