name: PR Workflow

on:
  pull_request:
    branches: [development, development-1.0, release-*]

permissions:
  contents: read
  security-events: write

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number }}
  cancel-in-progress: true

jobs:
  run_tests:
    uses: epam/ai-dial-ci/.github/workflows/node_pr.yml@4.3.1
    secrets: inherit
    with:
      node-version: 24
      maximize-build-space: true
      ort-config-revision: "5650fed55a20397b9abb9017a0c6bb674dc8481d"
