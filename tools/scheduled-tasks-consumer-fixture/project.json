{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "scheduled-tasks-consumer-fixture",
  "targets": {
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc --project tsconfig.consumer.json --noEmit",
        "cwd": "tools/scheduled-tasks-consumer-fixture"
      },
      "dependsOn": ["pack-lib"],
      "outputs": []
    },
    "pack-lib": {
      "executor": "nx:run-commands",
      "options": {
        "command": "node scripts/pack-and-install.mjs",
        "cwd": "tools/scheduled-tasks-consumer-fixture"
      },
      "dependsOn": [
        {
          "projects": ["@epam/ai-dial-scheduled-tasks"],
          "target": "build"
        },
        {
          "projects": ["@epam/ai-dial-catalog", "@epam/ai-dial-chat-hooks"],
          "target": "build"
        }
      ]
    },
    "build": {
      "dependsOn": ["pack-lib"],
      "options": {
        "env": { "NODE_ENV": "production" }
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "node scripts/verify-package.mjs",
        "cwd": "tools/scheduled-tasks-consumer-fixture"
      },
      "dependsOn": ["pack-lib"]
    },
    "test-browser": {
      "executor": "nx:run-commands",
      "options": {
        "command": "node scripts/verify.mjs",
        "cwd": "tools/scheduled-tasks-consumer-fixture"
      },
      "dependsOn": ["build"]
    }
  }
}
