/** Arrangement `LimitsTab` uses for each usage-limit row. */
export enum LimitRowLayout {
  /** Label column beside a fixed-width column holding the value above a narrow progress bar. */
  Inline = 'inline',
  /** Label and value on one line, with a full-width progress bar and the reset caption beneath it. */
  Stacked = 'stacked',
}
