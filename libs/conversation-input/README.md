# conversation-input

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build conversation-input` to build the library.

## Running unit tests

Run `nx test conversation-input` to execute the unit tests via [Vitest](https://vitest.dev/).
