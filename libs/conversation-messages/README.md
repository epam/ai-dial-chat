# conversation-messages

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build conversation-messages` to build the library.

## Running unit tests

Run `nx test conversation-messages` to execute the unit tests via [Vitest](https://vitest.dev/).
