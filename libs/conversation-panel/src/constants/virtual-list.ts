/** Pixel height of each conversation item row (h-8 = 32px + 4px gap above). */
export const ITEM_ROW_HEIGHT = 36;
/** Pixel height of a group header row with group-separation space above (h-6 = 24px + 32px gap above). */
export const GROUP_HEADER_ROW_HEIGHT = 56;
/** Pixel height of the first group header row (h-6 = 24px + 16px gap above). */
export const FIRST_GROUP_HEADER_ROW_HEIGHT = 40;
