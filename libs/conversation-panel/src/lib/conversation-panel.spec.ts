import { conversationPanel } from './conversation-panel.js';

describe('conversationPanel', () => {
  it('should work', () => {
    expect(conversationPanel()).toEqual('conversation-panel');
  });
});
