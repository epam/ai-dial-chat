# conversation-panel

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build conversation-panel` to build the library.

## Running unit tests

Run `nx test conversation-panel` to execute the unit tests via [Vitest](https://vitest.dev/).
