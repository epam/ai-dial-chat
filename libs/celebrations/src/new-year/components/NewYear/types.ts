/** Every New Year scene; hosts enable or disable them by these ids. */
export enum NewYearScene {
  /** Snowflakes drift down the viewport. */
  Snow = 'snow',
  /** A confetti burst; also the secret-phrase scene. */
  Confetti = 'confetti',
  /** A flying sleigh crosses the sky. */
  Sleigh = 'sleigh',
}
