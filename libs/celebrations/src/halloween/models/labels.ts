/** Visible text of the Halloween event; `{{phrase}}` becomes the secret hint. */
export interface HalloweenLabels {
  /** Title of every scene notification. */
  toastTitle: string;
  /** Notification message of the spiders scene. */
  spidersToastMessage: string;
  /** Notification message of the ghost scene. */
  ghostToastMessage: string;
  /** Notification message of the web scene. */
  webToastMessage: string;
  /** Notification message of the bats scene. */
  batsToastMessage: string;
  /** Notification message of the cat scene. */
  catToastMessage: string;
  /** Notification message of the witches scene. */
  witchesToastMessage: string;
  /** Accessible name of the pumpkin trigger. */
  pumpkinLabel: string;
  /** Notification message of the train scene. */
  trainToastMessage: string;
  /** Notification message of the portal scene. */
  portalToastMessage: string;
  /** Notification message of the ravens scene. */
  ravensToastMessage: string;
  /** Notification message of the candy scene. */
  candyToastMessage: string;
  /** Notification message of the footprints scene. */
  footprintsToastMessage: string;
  /** Notification message of the skeletons scene. */
  skeletonsToastMessage: string;
  /** Notification message of the cauldron scene. */
  cauldronToastMessage: string;
  /** Notification message of the mimic scene. */
  mimicToastMessage: string;
  /** Notification message of the bowling scene. */
  bowlingToastMessage: string;
  /** Notification message of the mummy scene. */
  mummyToastMessage: string;
}
