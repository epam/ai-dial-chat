/** Stable public class names the library emits, for host styling hooks. */
export const CELEBRATIONS_CLASS = {
  /** Root of the active event's decoration. */
  decor: 'dial-celebrations-decor',
  /** The event's trigger button (pumpkin, gift). */
  trigger: 'dial-celebrations-trigger',
  /** Viewport layer a playing scene renders into. */
  sceneLayer: 'dial-celebrations-scene-layer',
} as const;
