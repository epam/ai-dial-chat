{
  "info": {
    "name": "Chat API (1.0.0)",
    "description": "REST API for the chat application. Provides endpoints for theme configuration, authentication, and management. All endpoints return appropriate HTTP status codes (200, 400, 401, 403, 404, 502, 503) with descriptive error messages.\n\nGenerated from libs/chat-api-client/openapi.json. Contains 107 requests grouped by OpenAPI tag.\n\nAuthentication uses the Postman cookie jar. Start the OIDC flow with the login request, then call “Get current user”; its response supplies the CSRF token used by authenticated write requests.",
    "schema": "https://schema.getpostman.com/json/collection/v2.1.0/collection.json"
  },
  "event": [
    {
      "listen": "test",
      "script": {
        "type": "text/javascript",
        "exec": [
          "const csrfToken = pm.response.headers.get('X-CSRF-Token');",
          "if (csrfToken) {",
          "  pm.collectionVariables.set('csrfToken', csrfToken);",
          "}"
        ]
      }
    }
  ],
  "variable": [
    { "key": "baseUrl", "value": "http://localhost:5000", "type": "string" },
    { "key": "origin", "value": "http://localhost:4207", "type": "string" },
    { "key": "csrfToken", "value": "", "type": "string" },
    { "key": "providerId", "value": "local", "type": "string" },
    {
      "key": "callbackUrl",
      "value": "http://localhost:4207",
      "type": "string"
    },
    { "key": "deployment", "value": "deployment-name", "type": "string" },
    { "key": "modelName", "value": "model-name", "type": "string" },
    { "key": "id", "value": "resource-id", "type": "string" },
    { "key": "bucket", "value": "bucket-name", "type": "string" },
    { "key": "path", "value": "path/to/file", "type": "string" }
  ],
  "item": [
    {
      "name": "app-config",
      "item": [
        {
          "name": "Get client-safe application configuration and feature flags",
          "request": {
            "method": "GET",
            "header": [{ "key": "Accept", "value": "application/json" }],
            "url": {
              "raw": "{{baseUrl}}/api/v1/client-config?appId=chat-ui",
              "host": ["{{baseUrl}}"],
              "path": ["api", "v1", "client-config"],
              "query": [
                {
                  "key": "appId",
                  "value": "chat-ui",
                  "description": "Application identifier. Must be a known app ID."
                }
              ]
            }
          }
        }
      ]
    },
    {
      "name": "applications",
      "item": [
        {
          "name": "List application type schemas",
          "request": {
            "method": "GET",
            "header": [{ "key": "Accept", "value": "application/json" }],
            "url": {
              "raw": "{{baseUrl}}/api/v1/application-schemas",
              "host": ["{{baseUrl}}"],
              "path": ["api", "v1", "application-schemas"]
            },
            "description": "Returns DIAL Core application type schemas visible to the authenticated user. Results are cached server-side for 60 seconds per user."
          }
        },
        {
          "name": "Get application type schema by id",
          "request": {
            "method": "GET",
            "header": [{ "key": "Accept", "value": "application/json" }],
            "url": {
              "raw": "{{baseUrl}}/api/v1/application-schemas/{{id}}",
              "host": ["{{baseUrl}}"],
              "path": ["api", "v1", "application-schemas", "{{id}}"]
            },
            "description": "Returns one DIAL Core application type schema by its $id. Results are cached server-side for 60 seconds per user per schema."
          }
        },
        {
          "name": "List available applications",
          "request": {
            "method": "GET",
            "header": [{ "key": "Accept", "value": "application/json" }],
            "url": {
              "raw": "{{baseUrl}}/api/v1/applications",
              "host": ["{{baseUrl}}"],
              "path": ["api", "v1", "applications"]
            },
            "description": "Returns the list of DIAL Core applications visible to the authenticated session user. Proxies GET /openai/applications using the caller's session access token. Results are cached server-side for 30 seconds per user."
          }
        },
        {
          "name": "Create a new application",
          "request": {
            "method": "POST",
            "header": [
              { "key": "Accept", "value": "application/json" },
              { "key": "Origin", "value": "{{origin}}" },
              { "key": "X-CSRF-Token", "value": "{{csrfToken}}" }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"name\": \"My App\",\n  \"type\": \"https://mydial.epam.com/custom_application_schemas/quickapps2\",\n  \"description\": \"A custom application.\",\n  \"iconUrl\": \"https://example.com/icon.svg\",\n  \"version\": \"1.0\",\n  \"topics\": [\n    \"nlp\",\n    \"assistant\"\n  ],\n  \"applicationProperties\": {\n    \"orchestrator\": {\n      \"system_prompt\": {\n        \"type\": \"custom\",\n        \"variables\": {},\n        \"content\": \"\"\n      }\n    },\n    \"contexts\": [],\n    \"tool_sets\": []\n  }\n}\n",
              "options": { "raw": { "language": "json" } }
            },
            "url": {
              "raw": "{{baseUrl}}/api/v1/applications",
              "host": ["{{baseUrl}}"],
              "path": ["api", "v1", "applications"]
            },
            "description": "Creates a new application for the authenticated session user by proxying DIAL Core. Invalidates the applications list cache on success.\n\nCSRF: run an authenticated GET request first (for example “Get current user”) so the collection captures X-CSRF-Token."
          }
        },
        {
          "name": "Update an application",
          "request": {
            "method": "PATCH",
            "header": [
              { "key": "Accept", "value": "application/json" },
              { "key": "Origin", "value": "{{origin}}" },
              { "key": "X-CSRF-Token", "value": "{{csrfToken}}" }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"name\": \"My App\",\n  \"description\": \"A custom application.\",\n  \"iconUrl\": \"https://example.com/icon.svg\",\n  \"topics\": [\n    \"nlp\",\n    \"assistant\"\n  ],\n  \"version\": \"1.0.0\",\n  \"endpoint\": \"https://api.example.com/chat\",\n  \"features\": {},\n  \"inputAttachmentTypes\": [\n    \"image/png\"\n  ],\n  \"maxInputAttachments\": 5\n}\n",
              "options": { "raw": { "language": "json" } }
            },
            "url": {
              "raw": "{{baseUrl}}/api/v1/applications/{{applicationName}}",
              "host": ["{{baseUrl}}"],
              "path": ["api", "v1", "applications", "{{applicationName}}"]
            },
            "description": "Updates the General-step fields (name, description, iconUrl, topics) of an existing application for the authenticated session user, by proxying DIAL Core. Settings-step configuration (application_properties, version) is preserved untouched. Invalidates the applications and deployments list caches on success.\n\nCSRF: run an authenticated GET request first (for example “Get current user”) so the collection captures X-CSRF-Token."
          }
        },
        {
          "name": "Delete an application",
          "request": {
            "method": "DELETE",
            "header": [
              { "key": "Accept", "value": "application/json" },
              { "key": "Origin", "value": "{{origin}}" },
              { "key": "X-CSRF-Token", "value": "{{csrfToken}}" }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/v1/applications/{{applicationName}}",
              "host": ["{{baseUrl}}"],
              "path": ["api", "v1", "applications", "{{applicationName}}"]
            },
            "description": "Deletes an application for the authenticated session user by proxying DIAL Core. Invalidates the applications list cache on success.\n\nCSRF: run an authenticated GET request first (for example “Get current user”) so the collection captures X-CSRF-Token."
          }
        }
      ]
    },
    {
      "name": "auth",
      "item": [
        {
          "name": "List configured identity providers",
          "request": {
            "method": "GET",
            "header": [{ "key": "Accept", "value": "application/json" }],
            "url": {
              "raw": "{{baseUrl}}/api/v1/auth/providers",
              "host": ["{{baseUrl}}"],
              "path": ["api", "v1", "auth", "providers"]
            }
          }
        },
        {
          "name": "Start OIDC login flow",
          "request": {
            "method": "GET",
            "header": [{ "key": "Accept", "value": "application/json" }],
            "url": {
              "raw": "{{baseUrl}}/api/v1/auth/login/{{providerId}}",
              "host": ["{{baseUrl}}"],
              "path": ["api", "v1", "auth", "login", "{{providerId}}"]
            }
          }
        },
        {
          "name": "OIDC authorization callback",
          "request": {
            "method": "GET",
            "header": [{ "key": "Accept", "value": "application/json" }],
            "url": {
              "raw": "{{baseUrl}}/api/v1/auth/callback/{{providerId}}",
              "host": ["{{baseUrl}}"],
              "path": ["api", "v1", "auth", "callback", "{{providerId}}"]
            }
          }
        },
        {
          "name": "Log out and clear session cookie",
          "request": {
            "method": "POST",
            "header": [
              { "key": "Accept", "value": "application/json" },
              { "key": "Origin", "value": "{{origin}}" },
              { "key": "X-CSRF-Token", "value": "{{csrfToken}}" }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/v1/auth/logout",
              "host": ["{{baseUrl}}"],
              "path": ["api", "v1", "auth", "logout"]
            },
            "description": "CSRF: run an authenticated GET request first (for example “Get current user”) so the collection captures X-CSRF-Token."
          }
        },
        {
          "name": "Get current user profile",
          "request": {
            "method": "GET",
            "header": [{ "key": "Accept", "value": "application/json" }],
            "url": {
              "raw": "{{baseUrl}}/api/v1/auth/me",
              "host": ["{{baseUrl}}"],
              "path": ["api", "v1", "auth", "me"]
            },
            "description": "Authentication: session cookie (managed automatically by the Postman cookie jar)."
          }
        }
      ]
    },
    {
      "name": "chat",
      "item": [
        {
          "name": "Send a chat completion request to DIAL Core",
          "request": {
            "method": "POST",
            "header": [
              { "key": "Accept", "value": "application/json" },
              { "key": "Origin", "value": "{{origin}}" },
              { "key": "X-CSRF-Token", "value": "{{csrfToken}}" }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"deployment\": \"gpt-4o\",\n  \"messages\": [\n    {\n      \"id\": \"cfeaf733-4ecd-4898-ad3b-d6835c0b5fc8\",\n      \"role\": \"user\",\n      \"content\": \"Hello, how can you help me today?\",\n      \"timestamp\": \"2024-01-01T00:00:00.000Z\",\n      \"custom_content\": {\n        \"attachments\": [\n          {\n            \"index\": 0,\n            \"type\": \"type\",\n            \"title\": \"title\",\n            \"data\": \"data\",\n            \"url\": \"url\",\n            \"reference_type\": \"reference_type\",\n            \"reference_url\": \"reference_url\"\n          }\n        ],\n        \"configuration_value\": {\n          \"button\": 1\n        },\n        \"form_value\": {\n          \"field1\": \"value\",\n          \"field2\": 42\n        }\n      }\n    }\n  ],\n  \"temperature\": 0.7,\n  \"max_tokens\": 1024\n}\n",
              "options": { "raw": { "language": "json" } }
            },
            "url": {
              "raw": "{{baseUrl}}/api/v1/chat/completions",
              "host": ["{{baseUrl}}"],
              "path": ["api", "v1", "chat", "completions"]
            },
            "description": "CSRF: run an authenticated GET request first (for example “Get current user”) so the collection captures X-CSRF-Token."
          }
        }
      ]
    },
    {
      "name": "client-channel",
      "item": [
        {
          "name": "Subscribe to the DIAL Core client channel",
          "request": {
            "method": "POST",
            "header": [
              { "key": "Accept", "value": "application/json" },
              {
                "key": "x-dial-client-channel-id",
                "value": "x-dial-client-channel-id",
                "disabled": true
              },
              { "key": "Origin", "value": "{{origin}}" },
              { "key": "X-CSRF-Token", "value": "{{csrfToken}}" }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/v1/client-channel/subscribe",
              "host": ["{{baseUrl}}"],
              "path": ["api", "v1", "client-channel", "subscribe"]
            },
            "description": "Opens an SSE stream proxying DIAL Core's client-channel RPC events (e.g. `toolset/signin`) for the authenticated session. Relays the upstream stream without buffering and echoes the assigned X-DIAL-CLIENT-CHANNEL-ID response header. Send the same header on reconnect to resume an existing channel.\n\nCSRF: run an authenticated GET request first (for example “Get current user”) so the collection captures X-CSRF-Token."
          }
        },
        {
          "name": "Report an RPC response on the client channel",
          "request": {
            "method": "POST",
            "header": [
              { "key": "Accept", "value": "application/json" },
              {
                "key": "x-dial-client-channel-id",
                "value": "x-dial-client-channel-id"
              },
              { "key": "Origin", "value": "{{origin}}" },
              { "key": "X-CSRF-Token", "value": "{{csrfToken}}" }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"id\": \"a1b2c3d4-e5f6-4789-a012-3456789abcde\",\n  \"result\": \"success\"\n}\n",
              "options": { "raw": { "language": "json" } }
            },
            "url": {
              "raw": "{{baseUrl}}/api/v1/client-channel/report",
              "host": ["{{baseUrl}}"],
              "path": ["api", "v1", "client-channel", "report"]
            },
            "description": "Forwards a `{ id, result }` RPC response (e.g. a toolset sign-in success/denial) to DIAL Core for the given channel, so Core can resume or terminate the blocked tool call.\n\nCSRF: run an authenticated GET request first (for example “Get current user”) so the collection captures X-CSRF-Token."
          }
        },
        {
          "name": "Unsubscribe from the client channel",
          "request": {
            "method": "POST",
            "header": [
              { "key": "Accept", "value": "application/json" },
              {
                "key": "x-dial-client-channel-id",
                "value": "x-dial-client-channel-id"
              },
              { "key": "Origin", "value": "{{origin}}" },
              { "key": "X-CSRF-Token", "value": "{{csrfToken}}" }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/v1/client-channel/unsubscribe",
              "host": ["{{baseUrl}}"],
              "path": ["api", "v1", "client-channel", "unsubscribe"]
            },
            "description": "Closes the given client channel on DIAL Core. Treats an already-gone channel (404) as idempotent success.\n\nCSRF: run an authenticated GET request first (for example “Get current user”) so the collection captures X-CSRF-Token."
          }
        }
      ]
    },
    {
      "name": "conversations",
      "item": [
        {
          "name": "Create a new conversation",
          "request": {
            "method": "POST",
            "header": [
              { "key": "Accept", "value": "application/json" },
              { "key": "Origin", "value": "{{origin}}" },
              { "key": "X-CSRF-Token", "value": "{{csrfToken}}" }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"firstMessage\": \"Hello, how can you help me today?\",\n  \"deploymentId\": \"applications/catalog/Untitled%20app%201__0.0.1\",\n  \"custom_content\": {\n    \"attachments\": [\n      {\n        \"index\": 0,\n        \"type\": \"type\",\n        \"title\": \"title\",\n        \"data\": \"data\",\n        \"url\": \"url\",\n        \"reference_type\": \"reference_type\",\n        \"reference_url\": \"reference_url\"\n      }\n    ],\n    \"configuration_value\": {\n      \"button\": 1\n    },\n    \"form_value\": {\n      \"field1\": \"value\",\n      \"field2\": 42\n    }\n  }\n}\n",
              "options": { "raw": { "language": "json" } }
            },
            "url": {
              "raw": "{{baseUrl}}/api/v1/conversations",
              "host": ["{{baseUrl}}"],
              "path": ["api", "v1", "conversations"]
            },
            "description": "Creates a new conversation with an initial user message and returns it with a server-assigned ID.\n\nCSRF: run an authenticated GET request first (for example “Get current user”) so the collection captures X-CSRF-Token."
          }
        },
        {
          "name": "Get a conversation by path",
          "request": {
            "method": "GET",
            "header": [{ "key": "Accept", "value": "application/json" }],
            "url": {
              "raw": "{{baseUrl}}/api/v1/conversations?path={{path}}",
              "host": ["{{baseUrl}}"],
              "path": ["api", "v1", "conversations"],
              "query": [
                {
                  "key": "path",
                  "value": "{{path}}",
                  "description": "Conversation path (uuid__name). May contain slashes."
                }
              ]
            }
          }
        },
        {
          "name": "Save (overwrite) a conversation by path",
          "request": {
            "method": "PUT",
            "header": [
              { "key": "Accept", "value": "application/json" },
              { "key": "Origin", "value": "{{origin}}" },
              { "key": "X-CSRF-Token", "value": "{{csrfToken}}" }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"conversation\": {\n    \"id\": \"test-bucket/uuid__Hello\",\n    \"folderId\": \"test-bucket\",\n    \"name\": \"Hello\",\n    \"model\": {\n      \"id\": \"anthropic.claude-v3-sonnet\"\n    },\n    \"prompt\": \"\",\n    \"temperature\": 1,\n    \"messages\": [\n      {\n        \"id\": \"cfeaf733-4ecd-4898-ad3b-d6835c0b5fc8\",\n        \"role\": \"user\",\n        \"content\": \"Hello!\",\n        \"timestamp\": \"2026-05-19T16:00:00.000Z\",\n        \"custom_content\": {\n          \"attachments\": [\n            {\n              \"index\": 0,\n              \"type\": \"type\",\n              \"title\": \"title\",\n              \"data\": \"data\",\n              \"url\": \"url\",\n              \"reference_type\": \"reference_type\",\n              \"reference_url\": \"reference_url\"\n            }\n          ],\n          \"configuration_value\": {\n            \"button\": 1\n          },\n          \"form_value\": {\n            \"field1\": \"value\",\n            \"field2\": 42\n          },\n          \"event_type\": \"model_changed\",\n          \"previous_deployment_id\": \"gpt-4o\",\n          \"new_deployment_id\": \"claude-3\"\n        },\n        \"streamErrorMessage\": \"You have exceeded your daily token limit.\"\n      }\n    ],\n    \"lastActivityDate\": 1779206400000,\n    \"updatedAt\": 1779206400000,\n    \"selectedAddons\": [],\n    \"assistantModelId\": \"anthropic.claude-v3-sonnet\",\n    \"responseFormat\": \"markdown\",\n    \"llmNamingDone\": false\n  }\n}\n",
              "options": { "raw": { "language": "json" } }
            },
            "url": {
              "raw": "{{baseUrl}}/api/v1/conversations?path={{path}}",
              "host": ["{{baseUrl}}"],
              "path": ["api", "v1", "conversations"],
              "query": [
                {
                  "key": "path",
                  "value": "{{path}}",
                  "description": "Conversation path (uuid__name). May contain slashes."
                }
              ]
            },
            "description": "CSRF: run an authenticated GET request first (for example “Get current user”) so the collection captures X-CSRF-Token."
          }
        },
        {
          "name": "Rename a conversation by path",
          "request": {
            "method": "PATCH",
            "header": [
              { "key": "Accept", "value": "application/json" },
              { "key": "Origin", "value": "{{origin}}" },
              { "key": "X-CSRF-Token", "value": "{{csrfToken}}" }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"newTitle\": \"My renamed chat\"\n}\n",
              "options": { "raw": { "language": "json" } }
            },
            "url": {
              "raw": "{{baseUrl}}/api/v1/conversations?path={{path}}",
              "host": ["{{baseUrl}}"],
              "path": ["api", "v1", "conversations"],
              "query": [
                {
                  "key": "path",
                  "value": "{{path}}",
                  "description": "Conversation path (uuid__name). May contain slashes."
                }
              ]
            },
            "description": "CSRF: run an authenticated GET request first (for example “Get current user”) so the collection captures X-CSRF-Token."
          }
        },
        {
          "name": "Delete a conversation by path",
          "request": {
            "method": "DELETE",
            "header": [
              { "key": "Accept", "value": "application/json" },
              { "key": "Origin", "value": "{{origin}}" },
              { "key": "X-CSRF-Token", "value": "{{csrfToken}}" }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/v1/conversations?path={{path}}",
              "host": ["{{baseUrl}}"],
              "path": ["api", "v1", "conversations"],
              "query": [
                {
                  "key": "path",
                  "value": "{{path}}",
                  "description": "Conversation path (uuid__name). May contain slashes."
                }
              ]
            },
            "description": "CSRF: run an authenticated GET request first (for example “Get current user”) so the collection captures X-CSRF-Token."
          }
        },
        {
          "name": "List conversations",
          "request": {
            "method": "GET",
            "header": [{ "key": "Accept", "value": "application/json" }],
            "url": {
              "raw": "{{baseUrl}}/api/v1/conversations/list",
              "host": ["{{baseUrl}}"],
              "path": ["api", "v1", "conversations", "list"],
              "query": [
                {
                  "key": "limit",
                  "value": "100",
                  "disabled": true,
                  "description": "Maximum number of conversations to return."
                },
                {
                  "key": "nextToken",
                  "value": "eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9",
                  "disabled": true,
                  "description": "Pagination cursor returned in the previous response as `nextToken`. Omit for the first page."
                }
              ]
            },
            "description": "Returns a flat, paginated list of all conversations for the authenticated user by calling the DIAL Core metadata endpoint with `recursive=true` on the root path."
          }
        },
        {
          "name": "Get metadata for a conversation",
          "request": {
            "method": "GET",
            "header": [{ "key": "Accept", "value": "application/json" }],
            "url": {
              "raw": "{{baseUrl}}/api/v1/conversations/metadata?path={{path}}",
              "host": ["{{baseUrl}}"],
              "path": ["api", "v1", "conversations", "metadata"],
              "query": [
                {
                  "key": "path",
                  "value": "{{path}}",
                  "description": "Conversation path (uuid__name). May contain slashes."
                },
                {
                  "key": "permissions",
                  "value": "true",
                  "disabled": true,
                  "description": "Include READ/WRITE/SHARE permissions for the requesting user"
                }
              ]
            }
          }
        },
        {
          "name": "Stream a chat completion",
          "request": {
            "method": "POST",
            "header": [
              { "key": "Accept", "value": "application/json" },
              { "key": "Origin", "value": "{{origin}}" },
              { "key": "X-CSRF-Token", "value": "{{csrfToken}}" }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"generationId\": \"cfeaf733-4ecd-4898-ad3b-d6835c0b5fc8\",\n  \"path\": \"gpt-4o__My Conversation__cfeaf733-4ecd-4898-ad3b-d6835c0b5fc8\",\n  \"model\": \"anthropic.claude-v3-sonnet\",\n  \"mode\": \"append\",\n  \"message\": \"What is the capital of France?\",\n  \"messageIndex\": 3,\n  \"custom_content\": {\n    \"attachments\": [\n      {\n        \"index\": 0,\n        \"type\": \"type\",\n        \"title\": \"title\",\n        \"data\": \"data\",\n        \"url\": \"url\",\n        \"reference_type\": \"reference_type\",\n        \"reference_url\": \"reference_url\"\n      }\n    ],\n    \"configuration_value\": {\n      \"button\": 1\n    },\n    \"form_value\": {\n      \"field1\": \"value\",\n      \"field2\": 42\n    }\n  },\n  \"clientChannelId\": \"a1b2c3d4-e5f6-4789-a012-3456789abcde\"\n}\n",
              "options": { "raw": { "language": "json" } }
            },
            "url": {
              "raw": "{{baseUrl}}/api/v1/conversations/completions",
              "host": ["{{baseUrl}}"],
              "path": ["api", "v1", "conversations", "completions"]
            },
            "description": "Appends the user message to the conversation history, streams a completion from DIAL Core as SSE, persists the result, and returns the raw event stream. Backend owns persistence.\n\nCSRF: run an authenticated GET request first (for example “Get current user”) so the collection captures X-CSRF-Token."
          }
        },
        {
          "name": "Stop an active generation",
          "request": {
            "method": "POST",
            "header": [
              { "key": "Accept", "value": "application/json" },
              { "key": "Origin", "value": "{{origin}}" },
              { "key": "X-CSRF-Token", "value": "{{csrfToken}}" }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"generationId\": \"cfeaf733-4ecd-4898-ad3b-d6835c0b5fc8\",\n  \"path\": \"gpt-4o__My Conversation__cfeaf733-4ecd-4898-ad3b-d6835c0b5fc8\"\n}\n",
              "options": { "raw": { "language": "json" } }
            },
            "url": {
              "raw": "{{baseUrl}}/api/v1/conversations/completions/stop",
              "host": ["{{baseUrl}}"],
              "path": ["api", "v1", "conversations", "completions", "stop"]
            },
            "description": "CSRF: run an authenticated GET request first (for example “Get current user”) so the collection captures X-CSRF-Token."
          }
        },
        {
          "name": "Subscribe to conversation resource updates via SSE",
          "request": {
            "method": "POST",
            "header": [
              { "key": "Accept", "value": "application/json" },
              { "key": "Origin", "value": "{{origin}}" },
              { "key": "X-CSRF-Token", "value": "{{csrfToken}}" }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"path\": \"gpt-4o__My Chat\"\n}\n",
              "options": { "raw": { "language": "json" } }
            },
            "url": {
              "raw": "{{baseUrl}}/api/v1/conversations/watch",
              "host": ["{{baseUrl}}"],
              "path": ["api", "v1", "conversations", "watch"]
            },
            "description": "Opens an SSE stream that proxies DIAL Core resource-update events for the given conversation path. Used by the frontend to detect when LLM naming completes.\n\nCSRF: run an authenticated GET request first (for example “Get current user”) so the collection captures X-CSRF-Token."
          }
        },
        {
          "name": "Generate an LLM-based title suggestion for a conversation",
          "request": {
            "method": "POST",
            "header": [
              { "key": "Accept", "value": "application/json" },
              { "key": "Origin", "value": "{{origin}}" },
              { "key": "X-CSRF-Token", "value": "{{csrfToken}}" }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/v1/conversations/generate-title?path={{path}}",
              "host": ["{{baseUrl}}"],
              "path": ["api", "v1", "conversations", "generate-title"],
              "query": [
                {
                  "key": "path",
                  "value": "{{path}}",
                  "description": "Conversation path (uuid__name). May contain slashes."
                }
              ]
            },
            "description": "Generates a title for an existing conversation using the operator-configured utility model, based on the most recent messages. The suggestion is returned but NOT persisted — the caller confirms the rename separately. Does not read or set the llmNamingDone flag.\n\nCSRF: run an authenticated GET request first (for example “Get current user”) so the collection captures X-CSRF-Token."
          }
        },
        {
          "name": "Duplicate a conversation into the user's own bucket",
          "request": {
            "method": "POST",
            "header": [
              { "key": "Accept", "value": "application/json" },
              { "key": "Origin", "value": "{{origin}}" },
              { "key": "X-CSRF-Token", "value": "{{csrfToken}}" }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/v1/conversations/duplicate?path={{path}}",
              "host": ["{{baseUrl}}"],
              "path": ["api", "v1", "conversations", "duplicate"],
              "query": [
                {
                  "key": "path",
                  "value": "{{path}}",
                  "description": "Conversation path (uuid__name). May contain slashes."
                }
              ]
            },
            "description": "CSRF: run an authenticated GET request first (for example “Get current user”) so the collection captures X-CSRF-Token."
          }
        },
        {
          "name": "Delete selected conversations",
          "request": {
            "method": "POST",
            "header": [
              { "key": "Accept", "value": "application/json" },
              { "key": "Origin", "value": "{{origin}}" },
              { "key": "X-CSRF-Token", "value": "{{csrfToken}}" }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"ids\": [\n    \"ids\"\n  ]\n}\n",
              "options": { "raw": { "language": "json" } }
            },
            "url": {
              "raw": "{{baseUrl}}/api/v1/conversations/deletions",
              "host": ["{{baseUrl}}"],
              "path": ["api", "v1", "conversations", "deletions"]
            },
            "description": "Deletes up to 100 owned conversations in one request. Returns a result counting deleted, already-absent, and failed items. Already-absent IDs are treated as success. IDs outside the authenticated bucket are rejected with code FORBIDDEN.\n\nCSRF: run an authenticated GET request first (for example “Get current user”) so the collection captures X-CSRF-Token."
          }
        },
        {
          "name": "Delete all conversations in the user bucket",
          "request": {
            "method": "POST",
            "header": [
              { "key": "Accept", "value": "application/json" },
              { "key": "Origin", "value": "{{origin}}" },
              { "key": "X-CSRF-Token", "value": "{{csrfToken}}" }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"confirm\": true\n}\n",
              "options": { "raw": { "language": "json" } }
            },
            "url": {
              "raw": "{{baseUrl}}/api/v1/conversations/deletions/all",
              "host": ["{{baseUrl}}"],
              "path": ["api", "v1", "conversations", "deletions", "all"]
            },
            "description": "Deletes every conversation in the authenticated user's bucket. Requires { confirm: true } in the request body to prevent accidental deletion. Returns a result counting deleted, already-absent, and failed items.\n\nCSRF: run an authenticated GET request first (for example “Get current user”) so the collection captures X-CSRF-Token."
          }
        },
        {
          "name": "Mark a conversation as viewed",
          "request": {
            "method": "PATCH",
            "header": [
              { "key": "Accept", "value": "application/json" },
              { "key": "Origin", "value": "{{origin}}" },
              { "key": "X-CSRF-Token", "value": "{{csrfToken}}" }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/v1/conversations/viewed?path={{path}}",
              "host": ["{{baseUrl}}"],
              "path": ["api", "v1", "conversations", "viewed"],
              "query": [
                {
                  "key": "path",
                  "value": "{{path}}",
                  "description": "Conversation path (uuid__name). May contain slashes."
                }
              ]
            },
            "description": "Idempotently records that the authenticated user has opened this conversation. Used to clear the unread indicator for scheduler-created conversations.\n\nCSRF: run an authenticated GET request first (for example “Get current user”) so the collection captures X-CSRF-Token."
          }
        },
        {
          "name": "Publish a conversation to an Organization folder",
          "request": {
            "method": "POST",
            "header": [
              { "key": "Accept", "value": "application/json" },
              { "key": "Origin", "value": "{{origin}}" },
              { "key": "X-CSRF-Token", "value": "{{csrfToken}}" }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"folderPath\": \"Organization/Data Science/Shared chats\",\n  \"rules\": []\n}\n",
              "options": { "raw": { "language": "json" } }
            },
            "url": {
              "raw": "{{baseUrl}}/api/v1/conversations/publish?path={{path}}",
              "host": ["{{baseUrl}}"],
              "path": ["api", "v1", "conversations", "publish"],
              "query": [
                {
                  "key": "path",
                  "value": "{{path}}",
                  "description": "Conversation path (uuid__name). May contain slashes."
                }
              ]
            },
            "description": "Publishes an owned conversation to a folder under the Organization/public bucket by proxying DIAL Core's Publication API (`createPublication`). This endpoint keeps no publish records of its own — DIAL Core is the sole source of truth. The conversation title is re-fetched server-side and used as the publication name.\n\nCSRF: run an authenticated GET request first (for example “Get current user”) so the collection captures X-CSRF-Token."
          }
        },
        {
          "name": "Get publish history for a conversation",
          "request": {
            "method": "GET",
            "header": [{ "key": "Accept", "value": "application/json" }],
            "url": {
              "raw": "{{baseUrl}}/api/v1/conversations/publish-history?path={{path}}",
              "host": ["{{baseUrl}}"],
              "path": ["api", "v1", "conversations", "publish-history"],
              "query": [
                {
                  "key": "path",
                  "value": "{{path}}",
                  "description": "Conversation path (uuid__name). May contain slashes."
                }
              ]
            },
            "description": "Returns every folder this conversation has been published to, most recent first, derived from DIAL Core's Publication API (`getPublications`) — never from chat-api-side storage."
          }
        }
      ]
    },
    {
      "name": "deployments",
      "item": [
        {
          "name": "List deployments by interface type",
          "request": {
            "method": "GET",
            "header": [{ "key": "Accept", "value": "application/json" }],
            "url": {
              "raw": "{{baseUrl}}/api/v1/deployments",
              "host": ["{{baseUrl}}"],
              "path": ["api", "v1", "deployments"],
              "query": [
                {
                  "key": "refresh",
                  "value": "false",
                  "disabled": true,
                  "description": "Bypass the short server-side deployments list cache and refresh from DIAL Core"
                },
                {
                  "key": "interface_type",
                  "value": "chat",
                  "disabled": true,
                  "description": "Filter by interface type (repeatable)"
                }
              ]
            }
          }
        },
        {
          "name": "Get JSON Schema configuration for a deployment",
          "request": {
            "method": "GET",
            "header": [{ "key": "Accept", "value": "application/json" }],
            "url": {
              "raw": "{{baseUrl}}/api/v1/deployments/{{deployment}}/configuration",
              "host": ["{{baseUrl}}"],
              "path": [
                "api",
                "v1",
                "deployments",
                "{{deployment}}",
                "configuration"
              ]
            },
            "description": "Returns the JSON Schema of configuration supported by the deployment. Only available for deployments whose `features.configuration` flag is `true`. Results are cached server-side for 60 seconds per user."
          }
        },
        {
          "name": "Get deployment usage limits",
          "request": {
            "method": "GET",
            "header": [{ "key": "Accept", "value": "application/json" }],
            "url": {
              "raw": "{{baseUrl}}/api/v1/deployments/{{deployment}}/limits",
              "host": ["{{baseUrl}}"],
              "path": ["api", "v1", "deployments", "{{deployment}}", "limits"]
            },
            "description": "Returns spent/limit statistics for a single deployment. Proxies GET /v1/deployments/{deployment_name}/limits using the caller's session access token. Not cached — every request hits DIAL Core for real-time usage data."
          }
        },
        {
          "name": "Get full details for a single deployment",
          "request": {
            "method": "GET",
            "header": [{ "key": "Accept", "value": "application/json" }],
            "url": {
              "raw": "{{baseUrl}}/api/v1/deployments/{{deployment}}/details",
              "host": ["{{baseUrl}}"],
              "path": ["api", "v1", "deployments", "{{deployment}}", "details"]
            },
            "description": "Fetches the full per-entity payload for a model, application, or toolset by id (dispatching to DIAL Core's getModel/getApplication/getToolset based on the resolved deployment type) and maps it into a frontend-safe DeploymentDetailsDto. Results are cached server-side for 60 seconds per user; the response carries no client-facing Cache-Control so a browser never serves a stale copy of another user's cache window or of credentials that changed since the last fetch."
          }
        }
      ]
    },
    {
      "name": "external-services",
      "item": [
        {
          "name": "Get application external-service metadata",
          "request": {
            "method": "GET",
            "header": [{ "key": "Accept", "value": "application/json" }],
            "url": {
              "raw": "{{baseUrl}}/api/v1/external-services/{{appId}}/{{serviceId}}",
              "host": ["{{baseUrl}}"],
              "path": [
                "api",
                "v1",
                "external-services",
                "{{appId}}",
                "{{serviceId}}"
              ]
            },
            "description": "Returns an application external service's display metadata and authentication type by proxying DIAL Core (GET /v1/applications/{appId}/external-services/{id}) using the caller's session access token."
          }
        },
        {
          "name": "Submit external-service credentials",
          "request": {
            "method": "POST",
            "header": [
              { "key": "Accept", "value": "application/json" },
              { "key": "Origin", "value": "{{origin}}" },
              { "key": "X-CSRF-Token", "value": "{{csrfToken}}" }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"credentialsLevel\": \"USER\",\n  \"authenticationType\": \"API_KEY\",\n  \"apiKey\": \"apiKey\",\n  \"code\": \"code\",\n  \"redirectUri\": \"redirectUri\"\n}\n",
              "options": { "raw": { "language": "json" } }
            },
            "url": {
              "raw": "{{baseUrl}}/api/v1/external-services/{{appId}}/{{serviceId}}/signin",
              "host": ["{{baseUrl}}"],
              "path": [
                "api",
                "v1",
                "external-services",
                "{{appId}}",
                "{{serviceId}}",
                "signin"
              ]
            },
            "description": "Submits API key or OAuth authorization-code credentials for an application external service by proxying DIAL Core (POST /v1/ops/external-service/signin). Credential payloads are never logged.\n\nCSRF: run an authenticated GET request first (for example “Get current user”) so the collection captures X-CSRF-Token."
          }
        },
        {
          "name": "Revoke external-service credentials",
          "request": {
            "method": "POST",
            "header": [
              { "key": "Accept", "value": "application/json" },
              { "key": "Origin", "value": "{{origin}}" },
              { "key": "X-CSRF-Token", "value": "{{csrfToken}}" }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"credentialsLevel\": \"USER\",\n  \"authenticationType\": \"API_KEY\"\n}\n",
              "options": { "raw": { "language": "json" } }
            },
            "url": {
              "raw": "{{baseUrl}}/api/v1/external-services/{{appId}}/{{serviceId}}/signout",
              "host": ["{{baseUrl}}"],
              "path": [
                "api",
                "v1",
                "external-services",
                "{{appId}}",
                "{{serviceId}}",
                "signout"
              ]
            },
            "description": "Revokes an application external service's credentials by proxying DIAL Core (POST /v1/ops/external-service/signout). A Core 404 (nothing to revoke) is treated as idempotent success.\n\nCSRF: run an authenticated GET request first (for example “Get current user”) so the collection captures X-CSRF-Token."
          }
        }
      ]
    },
    {
      "name": "files",
      "item": [
        {
          "name": "uploadFile",
          "request": {
            "method": "POST",
            "header": [
              { "key": "Accept", "value": "application/json" },
              { "key": "Origin", "value": "{{origin}}" },
              { "key": "X-CSRF-Token", "value": "{{csrfToken}}" }
            ],
            "body": {
              "mode": "formdata",
              "formdata": [
                { "key": "file", "type": "file", "src": [] },
                { "key": "bucket", "type": "text", "value": "{{bucket}}" },
                { "key": "path", "type": "text", "value": "{{path}}" },
                {
                  "key": "uploadMode",
                  "type": "text",
                  "value": "overwrite",
                  "disabled": true
                }
              ]
            },
            "url": {
              "raw": "{{baseUrl}}/api/v1/files",
              "host": ["{{baseUrl}}"],
              "path": ["api", "v1", "files"]
            },
            "description": "CSRF: run an authenticated GET request first (for example “Get current user”) so the collection captures X-CSRF-Token."
          }
        },
        {
          "name": "Upload a ZIP archive and extract its contents to a destination folder",
          "request": {
            "method": "POST",
            "header": [
              { "key": "Accept", "value": "application/json" },
              { "key": "Origin", "value": "{{origin}}" },
              { "key": "X-CSRF-Token", "value": "{{csrfToken}}" }
            ],
            "body": {
              "mode": "formdata",
              "formdata": [
                { "key": "file", "type": "file", "src": [] },
                { "key": "bucket", "type": "text", "value": "{{bucket}}" },
                {
                  "key": "destinationPath",
                  "type": "text",
                  "value": "destinationPath"
                }
              ]
            },
            "url": {
              "raw": "{{baseUrl}}/api/v1/files/upload-archive",
              "host": ["{{baseUrl}}"],
              "path": ["api", "v1", "files", "upload-archive"]
            },
            "description": "CSRF: run an authenticated GET request first (for example “Get current user”) so the collection captures X-CSRF-Token."
          }
        },
        {
          "name": "List files and folders",
          "request": {
            "method": "GET",
            "header": [{ "key": "Accept", "value": "application/json" }],
            "url": {
              "raw": "{{baseUrl}}/api/v1/files/list?bucket={{bucket}}",
              "host": ["{{baseUrl}}"],
              "path": ["api", "v1", "files", "list"],
              "query": [
                {
                  "key": "bucket",
                  "value": "{{bucket}}",
                  "description": "DIAL Core bucket name"
                },
                {
                  "key": "path",
                  "value": "{{path}}",
                  "disabled": true,
                  "description": "Folder path within bucket (no leading slash, no ..)"
                },
                {
                  "key": "token",
                  "value": "token",
                  "disabled": true,
                  "description": "Pagination token from previous response"
                },
                {
                  "key": "limit",
                  "value": "100",
                  "disabled": true,
                  "description": "Max items to return"
                },
                {
                  "key": "recursive",
                  "value": "false",
                  "disabled": true,
                  "description": "Return items recursively"
                },
                {
                  "key": "permissions",
                  "value": "true",
                  "disabled": true,
                  "description": "Include item permissions"
                }
              ]
            },
            "description": "Returns a page of file and folder items from DIAL Core storage, normalized for FileManager compatibility."
          }
        },
        {
          "name": "List files from the organization (public) bucket",
          "request": {
            "method": "GET",
            "header": [{ "key": "Accept", "value": "application/json" }],
            "url": {
              "raw": "{{baseUrl}}/api/v1/files/public",
              "host": ["{{baseUrl}}"],
              "path": ["api", "v1", "files", "public"],
              "query": [
                {
                  "key": "path",
                  "value": "{{path}}",
                  "disabled": true,
                  "description": "Folder path within public bucket (no leading slash, no ..)"
                },
                {
                  "key": "token",
                  "value": "token",
                  "disabled": true,
                  "description": "Pagination token from previous response"
                },
                {
                  "key": "limit",
                  "value": "100",
                  "disabled": true,
                  "description": "Max items to return"
                },
                {
                  "key": "recursive",
                  "value": "false",
                  "disabled": true,
                  "description": "Return items recursively"
                }
              ]
            },
            "description": "Returns files from the fixed public bucket. Permissions are always false — users cannot write to the public bucket."
          }
        },
        {
          "name": "List files shared with the current user",
          "request": {
            "method": "GET",
            "header": [{ "key": "Accept", "value": "application/json" }],
            "url": {
              "raw": "{{baseUrl}}/api/v1/files/shared",
              "host": ["{{baseUrl}}"],
              "path": ["api", "v1", "files", "shared"],
              "query": [
                {
                  "key": "path",
                  "value": "{{path}}",
                  "disabled": true,
                  "description": "Folder path filter (no leading slash, no ..)"
                },
                {
                  "key": "token",
                  "value": "token",
                  "disabled": true,
                  "description": "Pagination token from previous response"
                },
                {
                  "key": "limit",
                  "value": "100",
                  "disabled": true,
                  "description": "Max items to return"
                }
              ]
            },
            "description": "Proxies the DIAL Core sharing API to return files shared with the authenticated user."
          }
        },
        {
          "name": "List files and folders shared by the caller with others",
          "request": {
            "method": "GET",
            "header": [{ "key": "Accept", "value": "application/json" }],
            "url": {
              "raw": "{{baseUrl}}/api/v1/files/shared-by-me?bucket={{bucket}}",
              "host": ["{{baseUrl}}"],
              "path": ["api", "v1", "files", "shared-by-me"],
              "query": [
                {
                  "key": "bucket",
                  "value": "{{bucket}}",
                  "description": "DIAL Core bucket name"
                }
              ]
            }
          }
        },
        {
          "name": "Get file metadata",
          "request": {
            "method": "GET",
            "header": [{ "key": "Accept", "value": "application/json" }],
            "url": {
              "raw": "{{baseUrl}}/api/v1/files/metadata?bucket={{bucket}}&path={{path}}",
              "host": ["{{baseUrl}}"],
              "path": ["api", "v1", "files", "metadata"],
              "query": [
                {
                  "key": "bucket",
                  "value": "{{bucket}}",
                  "description": "DIAL Core bucket name"
                },
                {
                  "key": "path",
                  "value": "{{path}}",
                  "description": "File path within the bucket (no leading slash, no .., no trailing /)"
                }
              ]
            },
            "description": "Returns metadata for a single named file from DIAL Core. Path must not end with /."
          }
        },
        {
          "name": "Create a folder",
          "request": {
            "method": "POST",
            "header": [
              { "key": "Accept", "value": "application/json" },
              { "key": "Origin", "value": "{{origin}}" },
              { "key": "X-CSRF-Token", "value": "{{csrfToken}}" }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"bucket\": \"my-bucket\",\n  \"parentPath\": \"reports/\",\n  \"name\": \"reports\"\n}\n",
              "options": { "raw": { "language": "json" } }
            },
            "url": {
              "raw": "{{baseUrl}}/api/v1/files/folders",
              "host": ["{{baseUrl}}"],
              "path": ["api", "v1", "files", "folders"]
            },
            "description": "CSRF: run an authenticated GET request first (for example “Get current user”) so the collection captures X-CSRF-Token."
          }
        },
        {
          "name": "Download files and folders as a ZIP archive",
          "request": {
            "method": "POST",
            "header": [
              { "key": "Accept", "value": "application/zip" },
              { "key": "Origin", "value": "{{origin}}" },
              { "key": "X-CSRF-Token", "value": "{{csrfToken}}" }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"items\": [\n    {\n      \"bucket\": \"my-bucket\",\n      \"path\": \"reports/\",\n      \"name\": \"reports\",\n      \"nodeType\": \"item\"\n    }\n  ]\n}\n",
              "options": { "raw": { "language": "json" } }
            },
            "url": {
              "raw": "{{baseUrl}}/api/v1/files/download-archive",
              "host": ["{{baseUrl}}"],
              "path": ["api", "v1", "files", "download-archive"]
            },
            "description": "CSRF: run an authenticated GET request first (for example “Get current user”) so the collection captures X-CSRF-Token."
          }
        },
        {
          "name": "Delete files and folders",
          "request": {
            "method": "POST",
            "header": [
              { "key": "Accept", "value": "application/json" },
              { "key": "Origin", "value": "{{origin}}" },
              { "key": "X-CSRF-Token", "value": "{{csrfToken}}" }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"items\": [\n    {\n      \"bucket\": \"my-bucket\",\n      \"path\": \"reports/\",\n      \"name\": \"reports\",\n      \"nodeType\": \"item\"\n    }\n  ]\n}\n",
              "options": { "raw": { "language": "json" } }
            },
            "url": {
              "raw": "{{baseUrl}}/api/v1/files/delete",
              "host": ["{{baseUrl}}"],
              "path": ["api", "v1", "files", "delete"]
            },
            "description": "CSRF: run an authenticated GET request first (for example “Get current user”) so the collection captures X-CSRF-Token."
          }
        },
        {
          "name": "Rename files and folders",
          "request": {
            "method": "POST",
            "header": [
              { "key": "Accept", "value": "application/json" },
              { "key": "Origin", "value": "{{origin}}" },
              { "key": "X-CSRF-Token", "value": "{{csrfToken}}" }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"items\": [\n    {\n      \"bucket\": \"my-bucket\",\n      \"sourcePath\": \"reports/q1.pdf\",\n      \"destinationPath\": \"reports/q1-final.pdf\",\n      \"nodeType\": \"item\",\n      \"name\": \"q1.pdf\"\n    }\n  ]\n}\n",
              "options": { "raw": { "language": "json" } }
            },
            "url": {
              "raw": "{{baseUrl}}/api/v1/files/rename",
              "host": ["{{baseUrl}}"],
              "path": ["api", "v1", "files", "rename"]
            },
            "description": "CSRF: run an authenticated GET request first (for example “Get current user”) so the collection captures X-CSRF-Token."
          }
        },
        {
          "name": "Copy files and folders",
          "request": {
            "method": "POST",
            "header": [
              { "key": "Accept", "value": "application/json" },
              { "key": "Origin", "value": "{{origin}}" },
              { "key": "X-CSRF-Token", "value": "{{csrfToken}}" }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"items\": [\n    {\n      \"bucket\": \"my-bucket\",\n      \"sourcePath\": \"reports/q1.pdf\",\n      \"destinationPath\": \"archive/q1.pdf\",\n      \"overwrite\": true,\n      \"nodeType\": \"item\",\n      \"name\": \"q1.pdf\"\n    }\n  ]\n}\n",
              "options": { "raw": { "language": "json" } }
            },
            "url": {
              "raw": "{{baseUrl}}/api/v1/files/copy",
              "host": ["{{baseUrl}}"],
              "path": ["api", "v1", "files", "copy"]
            },
            "description": "CSRF: run an authenticated GET request first (for example “Get current user”) so the collection captures X-CSRF-Token."
          }
        },
        {
          "name": "Move files and folders across folders",
          "request": {
            "method": "POST",
            "header": [
              { "key": "Accept", "value": "application/json" },
              { "key": "Origin", "value": "{{origin}}" },
              { "key": "X-CSRF-Token", "value": "{{csrfToken}}" }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"items\": [\n    {\n      \"bucket\": \"my-bucket\",\n      \"sourcePath\": \"inbox/draft.pdf\",\n      \"destinationPath\": \"reports/draft.pdf\",\n      \"overwrite\": true,\n      \"nodeType\": \"item\",\n      \"name\": \"draft.pdf\"\n    }\n  ]\n}\n",
              "options": { "raw": { "language": "json" } }
            },
            "url": {
              "raw": "{{baseUrl}}/api/v1/files/move",
              "host": ["{{baseUrl}}"],
              "path": ["api", "v1", "files", "move"]
            },
            "description": "CSRF: run an authenticated GET request first (for example “Get current user”) so the collection captures X-CSRF-Token."
          }
        },
        {
          "name": "Revoke all shared access to files and folders",
          "request": {
            "method": "POST",
            "header": [
              { "key": "Accept", "value": "application/json" },
              { "key": "Origin", "value": "{{origin}}" },
              { "key": "X-CSRF-Token", "value": "{{csrfToken}}" }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"items\": [\n    {\n      \"bucket\": \"my-bucket\",\n      \"path\": \"reports/q1.pdf\"\n    }\n  ]\n}\n",
              "options": { "raw": { "language": "json" } }
            },
            "url": {
              "raw": "{{baseUrl}}/api/v1/files/revoke-access",
              "host": ["{{baseUrl}}"],
              "path": ["api", "v1", "files", "revoke-access"]
            },
            "description": "CSRF: run an authenticated GET request first (for example “Get current user”) so the collection captures X-CSRF-Token."
          }
        },
        {
          "name": "Discard resources shared with the caller",
          "request": {
            "method": "POST",
            "header": [
              { "key": "Accept", "value": "application/json" },
              { "key": "Origin", "value": "{{origin}}" },
              { "key": "X-CSRF-Token", "value": "{{csrfToken}}" }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"items\": [\n    {\n      \"bucket\": \"my-bucket\",\n      \"path\": \"reports/q1.pdf\"\n    }\n  ]\n}\n",
              "options": { "raw": { "language": "json" } }
            },
            "url": {
              "raw": "{{baseUrl}}/api/v1/files/discard-shared",
              "host": ["{{baseUrl}}"],
              "path": ["api", "v1", "files", "discard-shared"]
            },
            "description": "CSRF: run an authenticated GET request first (for example “Get current user”) so the collection captures X-CSRF-Token."
          }
        },
        {
          "name": "downloadFile",
          "request": {
            "method": "GET",
            "header": [
              { "key": "Accept", "value": "application/octet-stream" }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/v1/files/download?bucket={{bucket}}&path={{path}}",
              "host": ["{{baseUrl}}"],
              "path": ["api", "v1", "files", "download"],
              "query": [
                {
                  "key": "bucket",
                  "value": "{{bucket}}",
                  "description": "DIAL Core bucket name"
                },
                {
                  "key": "path",
                  "value": "{{path}}",
                  "description": "File path within the bucket (no leading slash, no ..)"
                }
              ]
            }
          }
        }
      ]
    },
    {
      "name": "health",
      "item": [
        {
          "name": "Check application health status",
          "request": {
            "method": "GET",
            "header": [{ "key": "Accept", "value": "application/json" }],
            "url": {
              "raw": "{{baseUrl}}/api/health",
              "host": ["{{baseUrl}}"],
              "path": ["api", "health"]
            },
            "description": "Returns a simple health check response indicating the application is running. Use this endpoint for load balancer health checks, monitoring, and deployment verification."
          }
        }
      ]
    },
    {
      "name": "models",
      "item": [
        {
          "name": "List available models",
          "request": {
            "method": "GET",
            "header": [{ "key": "Accept", "value": "application/json" }],
            "url": {
              "raw": "{{baseUrl}}/api/v1/models",
              "host": ["{{baseUrl}}"],
              "path": ["api", "v1", "models"]
            },
            "description": "Returns the list of DIAL Core deployments visible to the authenticated session user. Proxies GET /openai/models using the caller's session access token. Results are cached server-side for 30 seconds per user."
          }
        },
        {
          "name": "Get model by name",
          "request": {
            "method": "GET",
            "header": [{ "key": "Accept", "value": "application/json" }],
            "url": {
              "raw": "{{baseUrl}}/api/v1/models/{{modelName}}",
              "host": ["{{baseUrl}}"],
              "path": ["api", "v1", "models", "{{modelName}}"]
            },
            "description": "Returns a single DIAL Core deployment by name for the authenticated session user. Proxies GET /openai/models/{model_name} using the caller's session access token. Results are cached server-side for 60 seconds per user per model."
          }
        }
      ]
    },
    {
      "name": "prompts",
      "item": [
        {
          "name": "List personal prompts",
          "request": {
            "method": "GET",
            "header": [{ "key": "Accept", "value": "application/json" }],
            "url": {
              "raw": "{{baseUrl}}/api/v1/prompts",
              "host": ["{{baseUrl}}"],
              "path": ["api", "v1", "prompts"]
            },
            "description": "Returns all personal prompts and the folder hierarchy."
          }
        },
        {
          "name": "Create a personal prompt",
          "request": {
            "method": "POST",
            "header": [
              { "key": "Accept", "value": "application/json" },
              { "key": "Origin", "value": "{{origin}}" },
              { "key": "X-CSRF-Token", "value": "{{csrfToken}}" }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"name\": \"My Prompt\",\n  \"description\": \"description\",\n  \"content\": \"content\",\n  \"folderId\": \"Work/AI\"\n}\n",
              "options": { "raw": { "language": "json" } }
            },
            "url": {
              "raw": "{{baseUrl}}/api/v1/prompts",
              "host": ["{{baseUrl}}"],
              "path": ["api", "v1", "prompts"]
            },
            "description": "CSRF: run an authenticated GET request first (for example “Get current user”) so the collection captures X-CSRF-Token."
          }
        },
        {
          "name": "Update a personal prompt",
          "request": {
            "method": "PUT",
            "header": [
              { "key": "Accept", "value": "application/json" },
              { "key": "Origin", "value": "{{origin}}" },
              { "key": "X-CSRF-Token", "value": "{{csrfToken}}" }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"name\": \"name\",\n  \"description\": \"description\",\n  \"content\": \"content\"\n}\n",
              "options": { "raw": { "language": "json" } }
            },
            "url": {
              "raw": "{{baseUrl}}/api/v1/prompts?path={{path}}",
              "host": ["{{baseUrl}}"],
              "path": ["api", "v1", "prompts"],
              "query": [
                {
                  "key": "path",
                  "value": "{{path}}",
                  "description": "Prompt path to update"
                }
              ]
            },
            "description": "CSRF: run an authenticated GET request first (for example “Get current user”) so the collection captures X-CSRF-Token."
          }
        },
        {
          "name": "Delete a personal prompt",
          "request": {
            "method": "DELETE",
            "header": [
              { "key": "Accept", "value": "application/json" },
              { "key": "Origin", "value": "{{origin}}" },
              { "key": "X-CSRF-Token", "value": "{{csrfToken}}" }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/v1/prompts?path={{path}}",
              "host": ["{{baseUrl}}"],
              "path": ["api", "v1", "prompts"],
              "query": [
                {
                  "key": "path",
                  "value": "{{path}}",
                  "description": "Prompt path to delete"
                }
              ]
            },
            "description": "CSRF: run an authenticated GET request first (for example “Get current user”) so the collection captures X-CSRF-Token."
          }
        },
        {
          "name": "Get a personal prompt",
          "request": {
            "method": "GET",
            "header": [{ "key": "Accept", "value": "application/json" }],
            "url": {
              "raw": "{{baseUrl}}/api/v1/prompts/item?path={{path}}",
              "host": ["{{baseUrl}}"],
              "path": ["api", "v1", "prompts", "item"],
              "query": [
                {
                  "key": "path",
                  "value": "{{path}}",
                  "description": "Prompt path within the prompts namespace"
                }
              ]
            }
          }
        },
        {
          "name": "List organisation prompts",
          "request": {
            "method": "GET",
            "header": [{ "key": "Accept", "value": "application/json" }],
            "url": {
              "raw": "{{baseUrl}}/api/v1/prompts/public",
              "host": ["{{baseUrl}}"],
              "path": ["api", "v1", "prompts", "public"]
            }
          }
        },
        {
          "name": "Get an organisation prompt",
          "request": {
            "method": "GET",
            "header": [{ "key": "Accept", "value": "application/json" }],
            "url": {
              "raw": "{{baseUrl}}/api/v1/prompts/public/item?path={{path}}",
              "host": ["{{baseUrl}}"],
              "path": ["api", "v1", "prompts", "public", "item"],
              "query": [
                {
                  "key": "path",
                  "value": "{{path}}",
                  "description": "Prompt path within the prompts namespace"
                }
              ]
            }
          }
        },
        {
          "name": "Create a prompt folder",
          "request": {
            "method": "POST",
            "header": [
              { "key": "Accept", "value": "application/json" },
              { "key": "Origin", "value": "{{origin}}" },
              { "key": "X-CSRF-Token", "value": "{{csrfToken}}" }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"name\": \"AI\",\n  \"parentId\": \"Work\"\n}\n",
              "options": { "raw": { "language": "json" } }
            },
            "url": {
              "raw": "{{baseUrl}}/api/v1/prompts/folders",
              "host": ["{{baseUrl}}"],
              "path": ["api", "v1", "prompts", "folders"]
            },
            "description": "CSRF: run an authenticated GET request first (for example “Get current user”) so the collection captures X-CSRF-Token."
          }
        },
        {
          "name": "Rename a prompt folder",
          "request": {
            "method": "PUT",
            "header": [
              { "key": "Accept", "value": "application/json" },
              { "key": "Origin", "value": "{{origin}}" },
              { "key": "X-CSRF-Token", "value": "{{csrfToken}}" }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"name\": \"Machine Learning\"\n}\n",
              "options": { "raw": { "language": "json" } }
            },
            "url": {
              "raw": "{{baseUrl}}/api/v1/prompts/folders?path={{path}}",
              "host": ["{{baseUrl}}"],
              "path": ["api", "v1", "prompts", "folders"],
              "query": [
                {
                  "key": "path",
                  "value": "{{path}}",
                  "description": "Folder path to rename"
                }
              ]
            },
            "description": "CSRF: run an authenticated GET request first (for example “Get current user”) so the collection captures X-CSRF-Token."
          }
        },
        {
          "name": "Delete a prompt folder",
          "request": {
            "method": "DELETE",
            "header": [
              { "key": "Accept", "value": "application/json" },
              { "key": "Origin", "value": "{{origin}}" },
              { "key": "X-CSRF-Token", "value": "{{csrfToken}}" }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/v1/prompts/folders?path={{path}}",
              "host": ["{{baseUrl}}"],
              "path": ["api", "v1", "prompts", "folders"],
              "query": [
                {
                  "key": "path",
                  "value": "{{path}}",
                  "description": "Folder path to delete"
                }
              ]
            },
            "description": "CSRF: run an authenticated GET request first (for example “Get current user”) so the collection captures X-CSRF-Token."
          }
        },
        {
          "name": "Move a personal prompt to a different folder",
          "request": {
            "method": "POST",
            "header": [
              { "key": "Accept", "value": "application/json" },
              { "key": "Origin", "value": "{{origin}}" },
              { "key": "X-CSRF-Token", "value": "{{csrfToken}}" }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"targetFolderId\": \"Work/AI\"\n}\n",
              "options": { "raw": { "language": "json" } }
            },
            "url": {
              "raw": "{{baseUrl}}/api/v1/prompts/move?path={{path}}",
              "host": ["{{baseUrl}}"],
              "path": ["api", "v1", "prompts", "move"],
              "query": [
                {
                  "key": "path",
                  "value": "{{path}}",
                  "description": "Prompt path to move"
                }
              ]
            },
            "description": "CSRF: run an authenticated GET request first (for example “Get current user”) so the collection captures X-CSRF-Token."
          }
        }
      ]
    },
    {
      "name": "publish",
      "item": [
        {
          "name": "Publish a catalog entity to an Organization folder",
          "request": {
            "method": "POST",
            "header": [
              { "key": "Accept", "value": "application/json" },
              { "key": "Origin", "value": "{{origin}}" },
              { "key": "X-CSRF-Token", "value": "{{csrfToken}}" }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"folderPath\": \"Organization/Data Science/Published models\",\n  \"version\": \"1.2.0\",\n  \"rules\": []\n}\n",
              "options": { "raw": { "language": "json" } }
            },
            "url": {
              "raw": "{{baseUrl}}/api/v1/catalog/{{entityType}}/{{entityId}}/publish",
              "host": ["{{baseUrl}}"],
              "path": [
                "api",
                "v1",
                "catalog",
                "{{entityType}}",
                "{{entityId}}",
                "publish"
              ]
            },
            "description": "Publishes a catalog entity (Toolset or Application) to a folder under the Organization/public bucket by proxying DIAL Core's Publication API (`createPublication`). This endpoint keeps no publish records of its own — DIAL Core is the sole source of truth.\n\nCSRF: run an authenticated GET request first (for example “Get current user”) so the collection captures X-CSRF-Token."
          }
        },
        {
          "name": "Get publish history for a catalog entity",
          "request": {
            "method": "GET",
            "header": [{ "key": "Accept", "value": "application/json" }],
            "url": {
              "raw": "{{baseUrl}}/api/v1/catalog/{{entityType}}/{{entityId}}/publish-history",
              "host": ["{{baseUrl}}"],
              "path": [
                "api",
                "v1",
                "catalog",
                "{{entityType}}",
                "{{entityId}}",
                "publish-history"
              ]
            },
            "description": "Returns every folder this catalog entity has been published to, most recent first, derived from DIAL Core's Publication API (`getPublications`) — never from chat-api-side storage."
          }
        },
        {
          "name": "Get a destination folder's already-configured access rules",
          "request": {
            "method": "GET",
            "header": [{ "key": "Accept", "value": "application/json" }],
            "url": {
              "raw": "{{baseUrl}}/api/v1/publish/rules?folderPath=Organization/Data Science/Shared chats",
              "host": ["{{baseUrl}}"],
              "path": ["api", "v1", "publish", "rules"],
              "query": [
                {
                  "key": "folderPath",
                  "value": "Organization/Data Science/Shared chats",
                  "description": "Destination folder under the Organization/public bucket to look up existing access rules for."
                }
              ]
            },
            "description": "Returns the exact requested folder's own access-restriction rules by proxying DIAL Core's `getPublicationRules` — ancestor-folder rules in the underlying response are discarded, never returned."
          }
        }
      ]
    },
    {
      "name": "rate",
      "item": [
        {
          "name": "Rate an assistant message",
          "request": {
            "method": "POST",
            "header": [
              { "key": "Accept", "value": "application/json" },
              { "key": "Origin", "value": "{{origin}}" },
              { "key": "X-CSRF-Token", "value": "{{csrfToken}}" }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"conversationId\": \"bucket/gpt-4o__My Conversation__UUID\",\n  \"responseId\": \"22222222-2222-2222-2222-222222222222\",\n  \"modelId\": \"anthropic.claude-v3-sonnet\",\n  \"rate\": 1,\n  \"comment\": \"The response was too brief\"\n}\n",
              "options": { "raw": { "language": "json" } }
            },
            "url": {
              "raw": "{{baseUrl}}/api/v1/rate",
              "host": ["{{baseUrl}}"],
              "path": ["api", "v1", "rate"]
            },
            "description": "Forwards a like/dislike rating for an assistant message to DIAL Core. Uses the authenticated session's access token as a Bearer credential.\n\nCSRF: run an authenticated GET request first (for example “Get current user”) so the collection captures X-CSRF-Token."
          }
        }
      ]
    },
    {
      "name": "scheduled-tasks",
      "item": [
        {
          "name": "List scheduled tasks",
          "request": {
            "method": "GET",
            "header": [{ "key": "Accept", "value": "application/json" }],
            "url": {
              "raw": "{{baseUrl}}/api/v1/scheduled-tasks",
              "host": ["{{baseUrl}}"],
              "path": ["api", "v1", "scheduled-tasks"],
              "query": [
                {
                  "key": "limit",
                  "value": "20",
                  "disabled": true,
                  "description": "Maximum number of scheduled tasks to return."
                },
                {
                  "key": "offset",
                  "value": "0",
                  "disabled": true,
                  "description": "Offset of the first scheduled task to return."
                },
                {
                  "key": "search",
                  "value": "daily",
                  "disabled": true,
                  "description": "Case-insensitive substring match against the scheduled task display name."
                },
                {
                  "key": "sort",
                  "value": "firstToRun",
                  "disabled": true,
                  "description": "Sort order for the returned scheduled tasks. Defaults to `firstToRun`."
                }
              ]
            },
            "description": "Returns the DIAL Scheduler schedules visible to the authenticated session user. Proxies the DIAL Scheduler routed-deployment API using the session access token. Results are cached server-side for 30 seconds per user."
          }
        },
        {
          "name": "Create a scheduled task",
          "request": {
            "method": "POST",
            "header": [
              { "key": "Accept", "value": "application/json" },
              { "key": "Origin", "value": "{{origin}}" },
              { "key": "X-CSRF-Token", "value": "{{csrfToken}}" }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"displayName\": \"Daily summary\",\n  \"trigger\": {\n    \"date\": \"2026-07-24T09:00:00.000Z\",\n    \"cron\": {\n      \"fields\": {\n        \"minute\": \"0\",\n        \"hour\": \"9\"\n      },\n      \"startDate\": \"2026-08-01T00:00:00.000Z\",\n      \"endDate\": \"2026-12-31T23:59:59.999Z\"\n    }\n  },\n  \"model\": \"gpt-4.1-mini-2025-04-14\",\n  \"prompt\": \"Summarize my inbox\",\n  \"description\": \"Summarizes unread inbox items every morning\"\n}\n",
              "options": { "raw": { "language": "json" } }
            },
            "url": {
              "raw": "{{baseUrl}}/api/v1/scheduled-tasks",
              "host": ["{{baseUrl}}"],
              "path": ["api", "v1", "scheduled-tasks"]
            },
            "description": "Creates a DIAL Scheduler schedule that runs a chat completion on the given model and prompt, using the OAuth external-service id configured via SCHEDULER_SERVICE_ID. Invalidates the scheduled tasks list cache on success.\n\nCSRF: run an authenticated GET request first (for example “Get current user”) so the collection captures X-CSRF-Token."
          }
        },
        {
          "name": "Get a scheduled task by id",
          "request": {
            "method": "GET",
            "header": [{ "key": "Accept", "value": "application/json" }],
            "url": {
              "raw": "{{baseUrl}}/api/v1/scheduled-tasks/{{scheduleId}}",
              "host": ["{{baseUrl}}"],
              "path": ["api", "v1", "scheduled-tasks", "{{scheduleId}}"]
            },
            "description": "Returns a single DIAL Scheduler schedule by id, proxying DIAL Scheduler using the session user's access token. Not cached."
          }
        },
        {
          "name": "Update a scheduled task",
          "request": {
            "method": "PUT",
            "header": [
              { "key": "Accept", "value": "application/json" },
              { "key": "Origin", "value": "{{origin}}" },
              { "key": "X-CSRF-Token", "value": "{{csrfToken}}" }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"displayName\": \"Daily summary\",\n  \"trigger\": {\n    \"date\": \"2026-07-24T09:00:00.000Z\",\n    \"cron\": {\n      \"fields\": {\n        \"minute\": \"0\",\n        \"hour\": \"9\"\n      },\n      \"startDate\": \"2026-08-01T00:00:00.000Z\",\n      \"endDate\": \"2026-12-31T23:59:59.999Z\"\n    }\n  },\n  \"model\": \"gpt-4.1-mini-2025-04-14\",\n  \"prompt\": \"Summarize my inbox\",\n  \"description\": \"Summarizes unread inbox items every morning\"\n}\n",
              "options": { "raw": { "language": "json" } }
            },
            "url": {
              "raw": "{{baseUrl}}/api/v1/scheduled-tasks/{{scheduleId}}",
              "host": ["{{baseUrl}}"],
              "path": ["api", "v1", "scheduled-tasks", "{{scheduleId}}"]
            },
            "description": "Updates an existing DIAL Scheduler schedule for the authenticated session user. Invalidates the scheduled tasks list cache on success.\n\nCSRF: run an authenticated GET request first (for example “Get current user”) so the collection captures X-CSRF-Token."
          }
        },
        {
          "name": "List a scheduled task run history",
          "request": {
            "method": "GET",
            "header": [{ "key": "Accept", "value": "application/json" }],
            "url": {
              "raw": "{{baseUrl}}/api/v1/scheduled-tasks/{{scheduleId}}/runs",
              "host": ["{{baseUrl}}"],
              "path": [
                "api",
                "v1",
                "scheduled-tasks",
                "{{scheduleId}}",
                "runs"
              ],
              "query": [
                {
                  "key": "limit",
                  "value": "20",
                  "disabled": true,
                  "description": "Maximum number of runs to return."
                },
                {
                  "key": "offset",
                  "value": "0",
                  "disabled": true,
                  "description": "Offset of the first run to return."
                }
              ]
            },
            "description": "Returns the paginated run history for a single DIAL Scheduler schedule, proxying DIAL Scheduler using the session user's access token. Always requests upstream ordering of created_at desc explicitly. Not cached."
          }
        }
      ]
    },
    {
      "name": "share",
      "item": [
        {
          "name": "Create a share link",
          "request": {
            "method": "POST",
            "header": [
              { "key": "Accept", "value": "application/json" },
              { "key": "Origin", "value": "{{origin}}" },
              { "key": "X-CSRF-Token", "value": "{{csrfToken}}" }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"itemId\": \"gpt-4o\",\n  \"access\": [\n    \"view\"\n  ]\n}\n",
              "options": { "raw": { "language": "json" } }
            },
            "url": {
              "raw": "{{baseUrl}}/api/v1/share",
              "host": ["{{baseUrl}}"],
              "path": ["api", "v1", "share"]
            },
            "description": "Creates a share link for a DIAL Core resource (catalog entity — agent, application, skill, toolset, or model — conversation, or prompt) by proxying DIAL Core's resource-sharing API. Returns the share URL, access level, and expiry.\n\nCSRF: run an authenticated GET request first (for example “Get current user”) so the collection captures X-CSRF-Token."
          }
        },
        {
          "name": "Accept a share invitation",
          "request": {
            "method": "GET",
            "header": [{ "key": "Accept", "value": "application/json" }],
            "url": {
              "raw": "{{baseUrl}}/api/v1/share/invitations/{{invitationId}}",
              "host": ["{{baseUrl}}"],
              "path": ["api", "v1", "share", "invitations", "{{invitationId}}"]
            },
            "description": "Accepts a share invitation via DIAL Core, granting the authenticated user the invitation's access level, and returns the shared entity's identifier so the frontend can navigate to it."
          }
        },
        {
          "name": "Discard a shared catalog resource or conversation",
          "request": {
            "method": "POST",
            "header": [
              { "key": "Accept", "value": "application/json" },
              { "key": "Origin", "value": "{{origin}}" },
              { "key": "X-CSRF-Token", "value": "{{csrfToken}}" }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"itemId\": \"applications/owner-bucket/my-app\"\n}\n",
              "options": { "raw": { "language": "json" } }
            },
            "url": {
              "raw": "{{baseUrl}}/api/v1/share/discard",
              "host": ["{{baseUrl}}"],
              "path": ["api", "v1", "share", "discard"]
            },
            "description": "Discards the authenticated user's own access to a shared catalog entity (application or toolset) or conversation, via DIAL Core's discardSharedResources operation. Only affects the caller's own access — removing access for everyone else is a separate operation.\n\nCSRF: run an authenticated GET request first (for example “Get current user”) so the collection captures X-CSRF-Token."
          }
        }
      ]
    },
    {
      "name": "skills",
      "item": [
        {
          "name": "List skills and grouping folders",
          "request": {
            "method": "GET",
            "header": [{ "key": "Accept", "value": "application/json" }],
            "url": {
              "raw": "{{baseUrl}}/api/v1/skills?bucket={{bucket}}",
              "host": ["{{baseUrl}}"],
              "path": ["api", "v1", "skills"],
              "query": [
                {
                  "key": "bucket",
                  "value": "{{bucket}}",
                  "description": "DIAL Core bucket name"
                },
                {
                  "key": "path",
                  "value": "{{path}}",
                  "disabled": true,
                  "description": "Grouping folder path within the bucket (no leading slash, no ..); empty lists the bucket root"
                },
                {
                  "key": "token",
                  "value": "token",
                  "disabled": true,
                  "description": "Pagination token from a previous response"
                },
                {
                  "key": "limit",
                  "value": "100",
                  "disabled": true,
                  "description": "Max items to return (0-1000, default 100)"
                },
                {
                  "key": "recursive",
                  "value": "false",
                  "disabled": true,
                  "description": "If true, lists the whole subtree; otherwise only immediate children"
                }
              ]
            },
            "description": "Proxies DIAL Core listSkillMetadata to list the grouping folders and skills at or under the given path."
          }
        },
        {
          "name": "Create or replace a whole skill from a ZIP archive",
          "request": {
            "method": "PUT",
            "header": [
              { "key": "Accept", "value": "application/json" },
              { "key": "If-Match", "value": "If-Match", "disabled": true },
              { "key": "Origin", "value": "{{origin}}" },
              { "key": "X-CSRF-Token", "value": "{{csrfToken}}" }
            ],
            "body": {
              "mode": "formdata",
              "formdata": [
                { "key": "file", "type": "file", "src": [] },
                { "key": "bucket", "type": "text", "value": "{{bucket}}" },
                { "key": "path", "type": "text", "value": "{{path}}" }
              ]
            },
            "url": {
              "raw": "{{baseUrl}}/api/v1/skills",
              "host": ["{{baseUrl}}"],
              "path": ["api", "v1", "skills"]
            },
            "description": "Validates the uploaded ZIP (path safety, reserved markers, duplicate paths, a root SKILL.md) then forwards it unchanged to DIAL Core uploadSkillFolder.\n\nCSRF: run an authenticated GET request first (for example “Get current user”) so the collection captures X-CSRF-Token."
          }
        },
        {
          "name": "Delete a whole skill",
          "request": {
            "method": "DELETE",
            "header": [
              { "key": "Accept", "value": "application/json" },
              { "key": "If-Match", "value": "If-Match", "disabled": true },
              { "key": "Origin", "value": "{{origin}}" },
              { "key": "X-CSRF-Token", "value": "{{csrfToken}}" }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/v1/skills?bucket={{bucket}}&path={{path}}",
              "host": ["{{baseUrl}}"],
              "path": ["api", "v1", "skills"],
              "query": [
                {
                  "key": "bucket",
                  "value": "{{bucket}}",
                  "description": "DIAL Core bucket name"
                },
                {
                  "key": "path",
                  "value": "{{path}}",
                  "description": "The skill's resource path within the bucket"
                }
              ]
            },
            "description": "Proxies DIAL Core deleteSkillFolder.\n\nCSRF: run an authenticated GET request first (for example “Get current user”) so the collection captures X-CSRF-Token."
          }
        },
        {
          "name": "List files inside a skill",
          "request": {
            "method": "GET",
            "header": [{ "key": "Accept", "value": "application/json" }],
            "url": {
              "raw": "{{baseUrl}}/api/v1/skills/files?bucket={{bucket}}&filePath=",
              "host": ["{{baseUrl}}"],
              "path": ["api", "v1", "skills", "files"],
              "query": [
                {
                  "key": "bucket",
                  "value": "{{bucket}}",
                  "description": "DIAL Core bucket name"
                },
                {
                  "key": "path",
                  "value": "{{path}}",
                  "disabled": true,
                  "description": "Grouping folder path within the bucket (no leading slash, no ..); empty lists the bucket root"
                },
                {
                  "key": "token",
                  "value": "token",
                  "disabled": true,
                  "description": "Pagination token from a previous response"
                },
                {
                  "key": "limit",
                  "value": "100",
                  "disabled": true,
                  "description": "Max items to return (0-1000, default 100)"
                },
                {
                  "key": "recursive",
                  "value": "false",
                  "disabled": true,
                  "description": "If true, lists the whole subtree; otherwise only immediate children"
                },
                {
                  "key": "filePath",
                  "value": "",
                  "description": "The relative path of a subfolder inside the skill to scope the listing (empty string for the skill's root)"
                }
              ]
            },
            "description": "Proxies DIAL Core listSkillFileMetadata."
          }
        },
        {
          "name": "Add or replace one file in a skill",
          "request": {
            "method": "PUT",
            "header": [
              { "key": "Accept", "value": "application/json" },
              { "key": "If-Match", "value": "If-Match", "disabled": true },
              { "key": "Origin", "value": "{{origin}}" },
              { "key": "X-CSRF-Token", "value": "{{csrfToken}}" }
            ],
            "body": {
              "mode": "formdata",
              "formdata": [
                { "key": "file", "type": "file", "src": [] },
                { "key": "bucket", "type": "text", "value": "{{bucket}}" },
                { "key": "path", "type": "text", "value": "{{path}}" },
                { "key": "filePath", "type": "text", "value": "filePath" }
              ]
            },
            "url": {
              "raw": "{{baseUrl}}/api/v1/skills/files",
              "host": ["{{baseUrl}}"],
              "path": ["api", "v1", "skills", "files"]
            },
            "description": "Proxies DIAL Core uploadSkillFile.\n\nCSRF: run an authenticated GET request first (for example “Get current user”) so the collection captures X-CSRF-Token."
          }
        },
        {
          "name": "Delete one file from a skill",
          "request": {
            "method": "DELETE",
            "header": [
              { "key": "Accept", "value": "application/json" },
              { "key": "If-Match", "value": "If-Match", "disabled": true },
              { "key": "Origin", "value": "{{origin}}" },
              { "key": "X-CSRF-Token", "value": "{{csrfToken}}" }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/v1/skills/files?bucket={{bucket}}&path={{path}}&filePath=scripts/helper.py",
              "host": ["{{baseUrl}}"],
              "path": ["api", "v1", "skills", "files"],
              "query": [
                {
                  "key": "bucket",
                  "value": "{{bucket}}",
                  "description": "DIAL Core bucket name"
                },
                {
                  "key": "path",
                  "value": "{{path}}",
                  "description": "The skill's resource path within the bucket"
                },
                {
                  "key": "filePath",
                  "value": "scripts/helper.py",
                  "description": "Relative path of the file within the skill"
                }
              ]
            },
            "description": "Proxies DIAL Core deleteSkillFile. Rejects deleting SKILL.md.\n\nCSRF: run an authenticated GET request first (for example “Get current user”) so the collection captures X-CSRF-Token."
          }
        },
        {
          "name": "Download a whole skill as a ZIP archive",
          "request": {
            "method": "GET",
            "header": [{ "key": "Accept", "value": "application/zip" }],
            "url": {
              "raw": "{{baseUrl}}/api/v1/skills/download?bucket={{bucket}}&path={{path}}",
              "host": ["{{baseUrl}}"],
              "path": ["api", "v1", "skills", "download"],
              "query": [
                {
                  "key": "bucket",
                  "value": "{{bucket}}",
                  "description": "DIAL Core bucket name"
                },
                {
                  "key": "path",
                  "value": "{{path}}",
                  "description": "The skill's resource path within the bucket"
                }
              ]
            },
            "description": "Proxies DIAL Core downloadSkillFolder and streams the response. Returns 400 when the path resolves to a grouping folder instead of a skill."
          }
        },
        {
          "name": "Download one file from a skill",
          "request": {
            "method": "GET",
            "header": [
              { "key": "Accept", "value": "application/octet-stream" }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/v1/skills/files/download?bucket={{bucket}}&path={{path}}&filePath=scripts/helper.py",
              "host": ["{{baseUrl}}"],
              "path": ["api", "v1", "skills", "files", "download"],
              "query": [
                {
                  "key": "bucket",
                  "value": "{{bucket}}",
                  "description": "DIAL Core bucket name"
                },
                {
                  "key": "path",
                  "value": "{{path}}",
                  "description": "The skill's resource path within the bucket"
                },
                {
                  "key": "filePath",
                  "value": "scripts/helper.py",
                  "description": "Relative path of the file within the skill"
                }
              ]
            },
            "description": "Proxies DIAL Core downloadSkillFile and streams the response."
          }
        },
        {
          "name": "Create a grouping folder",
          "request": {
            "method": "POST",
            "header": [
              { "key": "Accept", "value": "application/json" },
              { "key": "Origin", "value": "{{origin}}" },
              { "key": "X-CSRF-Token", "value": "{{csrfToken}}" }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/v1/skills/grouping-folders?bucket={{bucket}}&path={{path}}",
              "host": ["{{baseUrl}}"],
              "path": ["api", "v1", "skills", "grouping-folders"],
              "query": [
                {
                  "key": "bucket",
                  "value": "{{bucket}}",
                  "description": "DIAL Core bucket name"
                },
                {
                  "key": "path",
                  "value": "{{path}}",
                  "description": "The skill's resource path within the bucket"
                }
              ]
            },
            "description": "Proxies DIAL Core createSkillGroupingFolder. Accepts no conditional request headers — the verified SDK schema declares none for this operation.\n\nCSRF: run an authenticated GET request first (for example “Get current user”) so the collection captures X-CSRF-Token."
          }
        },
        {
          "name": "Delete an empty grouping folder",
          "request": {
            "method": "DELETE",
            "header": [
              { "key": "Accept", "value": "application/json" },
              { "key": "If-Match", "value": "If-Match", "disabled": true },
              { "key": "Origin", "value": "{{origin}}" },
              { "key": "X-CSRF-Token", "value": "{{csrfToken}}" }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/v1/skills/grouping-folders?bucket={{bucket}}&path={{path}}",
              "host": ["{{baseUrl}}"],
              "path": ["api", "v1", "skills", "grouping-folders"],
              "query": [
                {
                  "key": "bucket",
                  "value": "{{bucket}}",
                  "description": "DIAL Core bucket name"
                },
                {
                  "key": "path",
                  "value": "{{path}}",
                  "description": "The skill's resource path within the bucket"
                }
              ]
            },
            "description": "Proxies DIAL Core deleteSkillGroupingFolder.\n\nCSRF: run an authenticated GET request first (for example “Get current user”) so the collection captures X-CSRF-Token."
          }
        }
      ]
    },
    {
      "name": "themes",
      "item": [
        {
          "name": "Get themes configuration",
          "request": {
            "method": "GET",
            "header": [{ "key": "Accept", "value": "application/json" }],
            "url": {
              "raw": "{{baseUrl}}/api/themes",
              "host": ["{{baseUrl}}"],
              "path": ["api", "themes"]
            },
            "description": "Fetches the complete theme configuration from the external themes service. Returns a JSON object containing all available themes with their properties, colors, and icon references. Results are cached for 5 minutes."
          }
        },
        {
          "name": "Get theme icon",
          "request": {
            "method": "GET",
            "header": [{ "key": "Accept", "value": "image/svg+xml" }],
            "url": {
              "raw": "{{baseUrl}}/api/themes/icon?iconName=icon-light.svg",
              "host": ["{{baseUrl}}"],
              "path": ["api", "themes", "icon"],
              "query": [
                {
                  "key": "iconName",
                  "value": "icon-light.svg",
                  "description": "Icon filename (alphanumeric, dash, underscore, and dot only)"
                }
              ]
            },
            "description": "Fetches a theme icon as SVG or PNG content from the external themes service. The icon name is validated to prevent path traversal attacks - only alphanumeric characters, dashes, underscores, and dots are allowed. Results are cached for 5 minutes."
          }
        }
      ]
    },
    {
      "name": "toolsets",
      "item": [
        {
          "name": "List available toolsets",
          "request": {
            "method": "GET",
            "header": [{ "key": "Accept", "value": "application/json" }],
            "url": {
              "raw": "{{baseUrl}}/api/v1/toolsets",
              "host": ["{{baseUrl}}"],
              "path": ["api", "v1", "toolsets"]
            },
            "description": "Returns the list of DIAL Core toolsets visible to the authenticated session user. Proxies GET /openai/toolsets using the caller's session access token. Results are cached server-side for 30 seconds per user; the response carries no client-facing Cache-Control so a browser never serves a stale copy across a login/logout that already invalidated that cache."
          }
        },
        {
          "name": "Create a new toolset",
          "request": {
            "method": "POST",
            "header": [
              { "key": "Accept", "value": "application/json" },
              { "key": "Origin", "value": "{{origin}}" },
              { "key": "X-CSRF-Token", "value": "{{csrfToken}}" }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"name\": \"My toolset\",\n  \"version\": \"0.0.1\",\n  \"description\": \"My toolset description\",\n  \"iconUrl\": \"https://example.com/icon.svg\",\n  \"topics\": [\n    \"keyword1\",\n    \"keyword2\"\n  ],\n  \"endpoint\": \"https://my-toolset.example.com/mcp\",\n  \"transport\": \"HTTP\",\n  \"allowedTools\": [\n    \"tool1\",\n    \"tool2\"\n  ],\n  \"reference\": \"ff5584b7-a82b-4f4f-bf42-5bf74a3893d6\",\n  \"authSettings\": {\n    \"authenticationType\": \"NONE\",\n    \"apiKeyHeader\": \"X-Api-Key\",\n    \"clientId\": \"my-client-id\",\n    \"clientSecret\": \"my-client-secret\",\n    \"authorizationEndpoint\": \"https://auth.example.com/authorize\",\n    \"tokenEndpoint\": \"https://auth.example.com/token\",\n    \"scopesSupported\": [\n      \"read\",\n      \"write\"\n    ],\n    \"redirectUri\": \"https://chat.example.com/auth/toolset-signin\",\n    \"codeChallengeMethod\": \"S256\",\n    \"codeChallenge\": \"abc123\"\n  }\n}\n",
              "options": { "raw": { "language": "json" } }
            },
            "url": {
              "raw": "{{baseUrl}}/api/v1/toolsets",
              "host": ["{{baseUrl}}"],
              "path": ["api", "v1", "toolsets"]
            },
            "description": "Creates a new toolset for the authenticated session user by proxying DIAL Core. Invalidates the toolset list cache on success.\n\nCSRF: run an authenticated GET request first (for example “Get current user”) so the collection captures X-CSRF-Token."
          }
        },
        {
          "name": "Get toolset by name",
          "request": {
            "method": "GET",
            "header": [{ "key": "Accept", "value": "application/json" }],
            "url": {
              "raw": "{{baseUrl}}/api/v1/toolsets/{{toolsetName}}",
              "host": ["{{baseUrl}}"],
              "path": ["api", "v1", "toolsets", "{{toolsetName}}"]
            },
            "description": "Returns a single DIAL Core toolset by name for the authenticated session user. Proxies GET /openai/toolsets/{toolset_name} using the caller's session access token. Results are cached server-side for 60 seconds per user per toolset; the response carries no client-facing Cache-Control so a browser never serves a stale copy across a login/logout that already invalidated that cache."
          }
        },
        {
          "name": "Update a toolset",
          "request": {
            "method": "PATCH",
            "header": [
              { "key": "Accept", "value": "application/json" },
              { "key": "Origin", "value": "{{origin}}" },
              { "key": "X-CSRF-Token", "value": "{{csrfToken}}" }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"name\": \"My toolset\",\n  \"version\": \"0.0.1\",\n  \"description\": \"My toolset description\",\n  \"iconUrl\": \"https://example.com/icon.svg\",\n  \"topics\": [\n    \"keyword1\",\n    \"keyword2\"\n  ],\n  \"endpoint\": \"https://my-toolset.example.com/mcp\",\n  \"transport\": \"HTTP\",\n  \"allowedTools\": [\n    \"tool1\",\n    \"tool2\"\n  ],\n  \"reference\": \"ff5584b7-a82b-4f4f-bf42-5bf74a3893d6\",\n  \"authSettings\": {\n    \"authenticationType\": \"NONE\",\n    \"apiKeyHeader\": \"X-Api-Key\",\n    \"clientId\": \"my-client-id\",\n    \"clientSecret\": \"my-client-secret\",\n    \"authorizationEndpoint\": \"https://auth.example.com/authorize\",\n    \"tokenEndpoint\": \"https://auth.example.com/token\",\n    \"scopesSupported\": [\n      \"read\",\n      \"write\"\n    ],\n    \"redirectUri\": \"https://chat.example.com/auth/toolset-signin\",\n    \"codeChallengeMethod\": \"S256\",\n    \"codeChallenge\": \"abc123\"\n  }\n}\n",
              "options": { "raw": { "language": "json" } }
            },
            "url": {
              "raw": "{{baseUrl}}/api/v1/toolsets/{{toolsetName}}",
              "host": ["{{baseUrl}}"],
              "path": ["api", "v1", "toolsets", "{{toolsetName}}"]
            },
            "description": "Updates an existing toolset for the authenticated session user by proxying DIAL Core. Invalidates the relevant caches on success.\n\nCSRF: run an authenticated GET request first (for example “Get current user”) so the collection captures X-CSRF-Token."
          }
        },
        {
          "name": "Delete a toolset",
          "request": {
            "method": "DELETE",
            "header": [
              { "key": "Accept", "value": "application/json" },
              { "key": "Origin", "value": "{{origin}}" },
              { "key": "X-CSRF-Token", "value": "{{csrfToken}}" }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/v1/toolsets/{{toolsetName}}",
              "host": ["{{baseUrl}}"],
              "path": ["api", "v1", "toolsets", "{{toolsetName}}"]
            },
            "description": "Deletes a toolset for the authenticated session user by proxying DIAL Core. Invalidates the relevant caches on success.\n\nCSRF: run an authenticated GET request first (for example “Get current user”) so the collection captures X-CSRF-Token."
          }
        },
        {
          "name": "Submit toolset credentials",
          "request": {
            "method": "POST",
            "header": [
              { "key": "Accept", "value": "application/json" },
              { "key": "Origin", "value": "{{origin}}" },
              { "key": "X-CSRF-Token", "value": "{{csrfToken}}" }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"url\": \"toolsets/encrypted-bucket/My%20toolset__0.0.1\",\n  \"credentialsLevel\": \"USER\",\n  \"authenticationType\": \"API_KEY\",\n  \"apiKey\": \"apiKey\",\n  \"code\": \"code\",\n  \"redirectUri\": \"redirectUri\"\n}\n",
              "options": { "raw": { "language": "json" } }
            },
            "url": {
              "raw": "{{baseUrl}}/api/v1/toolsets/{{toolsetName}}/login",
              "host": ["{{baseUrl}}"],
              "path": ["api", "v1", "toolsets", "{{toolsetName}}", "login"]
            },
            "description": "Submits API key or OAuth authorization-code credentials for a toolset by proxying DIAL Core (POST /v1/ops/toolset/signin). Credential payloads are never logged.\n\nCSRF: run an authenticated GET request first (for example “Get current user”) so the collection captures X-CSRF-Token."
          }
        },
        {
          "name": "Revoke toolset credentials",
          "request": {
            "method": "POST",
            "header": [
              { "key": "Accept", "value": "application/json" },
              { "key": "Origin", "value": "{{origin}}" },
              { "key": "X-CSRF-Token", "value": "{{csrfToken}}" }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"url\": \"toolsets/encrypted-bucket/My%20toolset__0.0.1\",\n  \"credentialsLevel\": \"USER\",\n  \"authenticationType\": \"OAUTH\"\n}\n",
              "options": { "raw": { "language": "json" } }
            },
            "url": {
              "raw": "{{baseUrl}}/api/v1/toolsets/{{toolsetName}}/logout",
              "host": ["{{baseUrl}}"],
              "path": ["api", "v1", "toolsets", "{{toolsetName}}", "logout"]
            },
            "description": "Revokes a toolset's credentials by proxying DIAL Core (POST /v1/ops/toolset/signout). When the request body omits `authenticationType`, the toolset's own stored authentication type is looked up first (same lookup as `GET /api/v1/toolsets/{toolsetName}`).\n\nCSRF: run an authenticated GET request first (for example “Get current user”) so the collection captures X-CSRF-Token."
          }
        }
      ]
    },
    {
      "name": "transcription",
      "item": [
        {
          "name": "Transcribe audio using the configured ASR model",
          "request": {
            "method": "POST",
            "header": [
              { "key": "Accept", "value": "application/json" },
              { "key": "Origin", "value": "{{origin}}" },
              { "key": "X-CSRF-Token", "value": "{{csrfToken}}" }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"audioUrl\": \"audioUrl\",\n  \"mimeType\": \"mimeType\"\n}\n",
              "options": { "raw": { "language": "json" } }
            },
            "url": {
              "raw": "{{baseUrl}}/api/v1/transcription",
              "host": ["{{baseUrl}}"],
              "path": ["api", "v1", "transcription"]
            },
            "description": "CSRF: run an authenticated GET request first (for example “Get current user”) so the collection captures X-CSRF-Token."
          }
        }
      ]
    },
    {
      "name": "user-config",
      "item": [
        {
          "name": "Get current user configuration",
          "request": {
            "method": "GET",
            "header": [{ "key": "Accept", "value": "application/json" }],
            "url": {
              "raw": "{{baseUrl}}/api/v1/user-config",
              "host": ["{{baseUrl}}"],
              "path": ["api", "v1", "user-config"]
            }
          }
        },
        {
          "name": "Pin or unpin a conversation",
          "request": {
            "method": "PATCH",
            "header": [
              { "key": "Accept", "value": "application/json" },
              { "key": "Origin", "value": "{{origin}}" },
              { "key": "X-CSRF-Token", "value": "{{csrfToken}}" }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"path\": \"conversations/default-bucket/gpt-4__My chat__cfeaf733-4ecd-4898-ad3b-d6835c0b5fc8\",\n  \"isPinned\": true\n}\n",
              "options": { "raw": { "language": "json" } }
            },
            "url": {
              "raw": "{{baseUrl}}/api/v1/user-config/pins",
              "host": ["{{baseUrl}}"],
              "path": ["api", "v1", "user-config", "pins"]
            },
            "description": "CSRF: run an authenticated GET request first (for example “Get current user”) so the collection captures X-CSRF-Token."
          }
        },
        {
          "name": "Install or uninstall a toolset",
          "request": {
            "method": "PATCH",
            "header": [
              { "key": "Accept", "value": "application/json" },
              { "key": "Origin", "value": "{{origin}}" },
              { "key": "X-CSRF-Token", "value": "{{csrfToken}}" }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"id\": \"toolset-abc\",\n  \"isInstalled\": true\n}\n",
              "options": { "raw": { "language": "json" } }
            },
            "url": {
              "raw": "{{baseUrl}}/api/v1/user-config/toolsets",
              "host": ["{{baseUrl}}"],
              "path": ["api", "v1", "user-config", "toolsets"]
            },
            "description": "CSRF: run an authenticated GET request first (for example “Get current user”) so the collection captures X-CSRF-Token."
          }
        },
        {
          "name": "Install or uninstall a deployment",
          "request": {
            "method": "PATCH",
            "header": [
              { "key": "Accept", "value": "application/json" },
              { "key": "Origin", "value": "{{origin}}" },
              { "key": "X-CSRF-Token", "value": "{{csrfToken}}" }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"id\": \"toolset-abc\",\n  \"isInstalled\": true\n}\n",
              "options": { "raw": { "language": "json" } }
            },
            "url": {
              "raw": "{{baseUrl}}/api/v1/user-config/deployments",
              "host": ["{{baseUrl}}"],
              "path": ["api", "v1", "user-config", "deployments"]
            },
            "description": "CSRF: run an authenticated GET request first (for example “Get current user”) so the collection captures X-CSRF-Token."
          }
        },
        {
          "name": "Set the selected deployment",
          "request": {
            "method": "PATCH",
            "header": [
              { "key": "Accept", "value": "application/json" },
              { "key": "Origin", "value": "{{origin}}" },
              { "key": "X-CSRF-Token", "value": "{{csrfToken}}" }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"id\": \"{{id}}\"\n}\n",
              "options": { "raw": { "language": "json" } }
            },
            "url": {
              "raw": "{{baseUrl}}/api/v1/user-config/deployments/selected",
              "host": ["{{baseUrl}}"],
              "path": ["api", "v1", "user-config", "deployments", "selected"]
            },
            "description": "CSRF: run an authenticated GET request first (for example “Get current user”) so the collection captures X-CSRF-Token."
          }
        }
      ]
    }
  ]
}
