import { ThemesConfig } from '@/src/types/themes';

export const FALLBACK_THEME_CONFIG: ThemesConfig = {
  themes: [],
  images: {},
};

export const DEFAULT_MODEL_IMAGE = 'default-model';
export const DEFAULT_TOOLSET_IMAGE = 'default-toolset';
