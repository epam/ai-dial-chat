# Local Vite override for the apps/chat instance to embed via
# @epam/ai-dial-chat-overlay. Deployed builds are served by chat-api under
# /overlay-sandbox/ and embed the current origin by default.
VITE_CHAT_OVERLAY_HOST=http://localhost:4207
