# Local Vite fallback for the apps/chat instance to embed via
# @epam/ai-dial-chat-overlay. Deployed containers use CHAT_OVERLAY_HOST instead.
# The embedded chat must run with OVERLAY_ENABLED=true and this sandbox's origin
# added to ALLOWED_IFRAME_ORIGINS.
VITE_CHAT_OVERLAY_HOST=http://localhost:4207
