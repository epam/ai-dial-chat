/** Lowercased mirror of DIAL Core's `MetadataBase.nodeType` (`'FOLDER' | 'ITEM'`), matching `ListFilesItemDto`'s existing normalization convention. */
export enum SkillNodeType {
  Folder = 'folder',
  Item = 'item',
}
