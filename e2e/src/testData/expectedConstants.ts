export enum ExpectedConstants {
  newConversationTitle = 'New conversation',
  defaultTalkToModel = 'Default GPT-3.5',
  emptyString = '',
  defaultTemperature = '1',
  signInButtonTitle = 'Sign in with Credentials',
}
