apiVersion: v1
kind: Service
metadata:
  name: {{ include "app.fullname" . }}
spec:
  selector:
    app: {{ include "app.fullname" . }}
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 3000
