## 1. ConversationStreamingService: guarantee finalize() on early loop exit

- [x] 1.1 In `apps/chat-api/src/conversations/streaming/conversation-streaming.service.ts`, wrap the relay loop (`while (!next.done) { yield next.value; next = await relayIterator.next(); }`) and the subsequent `switch (relayResult.outcome)` handling in a `try`, with a flag (e.g. `relayCompletedNormally`) set only once the loop exits because `next.done` became `true`.
- [x] 1.2 In the corresponding `finally`, when `relayCompletedNormally` is `false` (the generator was abandoned via `.return()` — i.e. the consuming `for await` exited early), call `finalize()` with the current `assembledMessage` snapshot, using the existing `getStatus(sessionId, conversationPath) === GenerationStatus.Stopped` check to choose `GenerationStatus.Stopped` vs `GenerationStatus.Error` (matching the existing `'aborted'` outcome handling), and setting `streamErrorMessage: ''` for the non-stopped case.
- [x] 1.3 Confirm `finalize()`/`generationService.complete()`/`.error()` still run at most once per generation for every existing outcome (`completed`, `rejected`, `aborted`, `error`) — the new `finally` path must only fire for early abandonment, not duplicate the normal post-loop `switch` handling.

## 2. ConversationController: abort on client disconnect

- [x] 2.1 In `apps/chat-api/src/conversations/conversation.controller.ts`'s `streamCompletion` handler, obtain (or thread through) the `AbortController` already created by `ConversationGenerationService.register` for this request, register `res.on('close', handleClose)` before entering the `for await` loop, and call `abortController.abort()` from `handleClose` — mirroring `attachToGeneration`/`watchConversation`'s existing disconnect handling in the same file. (Implemented via a new `ConversationGenerationService.abortSignal(sessionId, path, generationId)` lookup — reuses the existing sid/path/generationId the controller already has, rather than threading the `AbortController` instance itself through the generator's return channel. Deliberately does *not* reuse the existing `abort()` method, since that marks the entry `Stopped`, which would make the finalize path in `conversation-streaming.service.ts` treat a disconnect as a user-initiated stop instead of an error.)
- [x] 2.2 Deregister the close handler (`res.off('close', handleClose)`) and keep the existing `res.end()` guard in a `finally` around the `for await` loop, matching the cleanup shape of the sibling handlers.
- [x] 2.3 Verify `ConversationService.streamCompletion`/`ConversationStreamingService.streamCompletion`'s signature already exposes (or is adjusted to expose) the `AbortController`/signal needed by the controller to wire this up, without changing the DTO or public HTTP contract. (No signature change needed — the controller reaches the same `AbortController` via `ConversationGenerationService`'s registry by sid/path/generationId, the same lookup key `stopCompletion` already uses.)

## 3. Tests

- [x] 3.1 Add a unit test for `ConversationStreamingService.streamCompletion` simulating early consumer abandonment (calling `.return()` on the generator, or breaking out of a `for await` after the first yielded chunk) and asserting `generationService.error()`/`.complete()` is invoked exactly once with the correct status, and the registry entry is released.
- [x] 3.2 Add a unit test asserting no double-finalize occurs when the relay reaches a normal terminal outcome (completed/rejected/aborted/error) — the early-abandonment `finally` path must not re-fire.
- [x] 3.3 Add an integration test (co-located per `apps/chat-api/src/conversations/tests/`) for `ConversationController.streamCompletion` that closes the client connection mid-stream and asserts the generation is no longer `Active` in the registry immediately afterward (not only after the stale-eviction TTL). (Added `completions.integration.spec.ts`'s "aborts the generation signal when the client disconnects mid-stream" — since `ConversationService` is mocked at this layer, it asserts the controller-level disconnect wiring; actual registry release is covered at the unit level by task 3.1's `conversation-streaming.service.spec.ts` test.)
- [x] 3.4 Add/extend a test asserting `res.on('close', ...)` triggers `AbortController.abort()` for `streamCompletion`, matching the existing pattern already tested (if any) for `attachToGeneration`/`watchConversation`. (Same test as 3.3 — asserts `generationService.abortSignal(sid, path, generationId)` is called, which internally aborts the entry's `AbortController`.)

## 4. Verification

- [x] 4.1 Run `npm exec nx test chat-api` and `npm exec nx lint chat-api`. (172/172 test files, 2926/2926 tests pass; lint clean — 5 remaining warnings in `conversation-generation.service.spec.ts` are pre-existing `no-non-null-assertion` warnings on lines this change didn't touch.)
- [x] 4.2 Manually verify locally: start a completion, close the browser tab mid-stream, and confirm (via a temporary log or debugger) that `finalize()` runs and the registry entry is removed immediately rather than after 30 minutes. (No interactive browser available in this session — substituted with the automated tests added in section 3, which exercise the identical scenario deterministically: `conversation-streaming.service.spec.ts`'s abandonment test asserts `finalize`'s effects directly, and `completions.integration.spec.ts`'s disconnect test drives a real socket close through the actual controller/HTTP stack. Recommend a manual smoke test before merge as a final sanity check.)
- [x] 4.3 Run `npm run validate:docs` if any README/docs content was touched (expected: no, this is an internal fix with no public API/doc surface change) — confirm and skip if not applicable. (Confirmed via `git status` — no README or `docs/**` files touched by this change; skipped.)
