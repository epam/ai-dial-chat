Slicing strategy: vertical — each slice keeps the attach modal fully working end to end; hook changes land first (Slice 1), then the shell is extracted underneath the existing modal (Slice 2), then the modal is thinned to consume it (Slice 3). Each slice ends green (`npx nx test chat`, `npx nx lint chat`) before the next starts.

## 1. Hook: variant / actionProfile / standalone mount load

- [x] 1.1 In `apps/chat/src/hooks/files/useDialFileManager.ts`, add `variant?: 'attach' | 'standalone' | 'folder-picker'` and `actionProfile?: 'attach' | 'browse' | 'full'` to `UseDialFileManagerOptions` (near line 59-70), with `variant` defaulting to `'attach'` and `actionProfile` derived from `variant` when omitted (`'attach'`→`'attach'`, `'standalone'`→`'browse'`, `'folder-picker'`→`'full'`). Implemented as `DialFileManagerVariant`/`DialFileManagerActionProfile` string enums in new `apps/chat/src/types/file-manager-variant.ts` (per repo convention: enums over string-literal unions for reused finite sets), with a `deriveActionProfile` helper.
- [x] 1.2 Confirm (add an inline comment, not a behavior change) that the existing load effect (`useDialFileManager.ts:634-672`) already fires on mount for every variant because `folderPath` initializes to `''` — no new effect is needed for `variant: 'standalone'`.
- [x] 1.3 Leave `actionLabels` (`useDialFileManager.ts:1497-1510`) tab-conditional logic unchanged; do not branch on `actionProfile` in this change (per design.md Decision 3, `'attach'` and `'browse'` must compute identical output). Added an exhaustive `switch` over `actionProfile` (no-op for `Attach`/`Browse`, dev-time `never` guard for unhandled values) so the option is deliberately consumed without changing `actionLabels` behavior.
- [x] 1.4 Add unit tests in `apps/chat/src/hooks/files/tests/useDialFileManager.spec.tsx`: default `variant` is `Attach` and fetches the root listing on mount; `variant: Standalone` also fetches the root listing on mount; `actionLabels` output is identical between `actionProfile: Attach` and `actionProfile: Browse` for each tab (`describe.each` over MyFiles/Shared/Organization).
- [x] 1.5 Verify: `npx nx test chat`, `npx nx lint chat`. Both pass (64/64 hook tests green; lint clean on all files touched — pre-existing unrelated errors in `Login.tsx` untouched by this change).

## 2. Extract DialFileManagerShell

- [x] 2.1 Create `apps/chat/src/components/DialFileManagerShell/DialFileManagerShell.tsx`. Move from `DialFileManagerModal.tsx` into it: the `DialFileManager` (ui-kit) prop assembly and its supporting `useMemo` option bags (grid `512-568`, tree `584-600`, per-tab/search empty states `602-624`, toolbar `626-654`, bulk actions `656-662`, delete-confirmation `481-494`, conflict-resolution `461-479`, `actionLabels` filtering `496-510`), the `UploadProgressModal` JSX (`796-804`), the download-loading overlay, and the `role="alert"` error/retry panel (`694-698`). Wrapped in `memo`, matching `DialFileManagerModal`'s existing export pattern.
- [x] 2.2 Defined the shell's prop type in `DialFileManagerShell.tsx` (`hookResult: UseDialFileManagerResult`) and a shared `DialFileManagerShellLabels` type in new `types/labels.ts` covering every string the moved JSX consumes. Attach-only grid behavior (row-selectability from `canAttachFolders`/`allowedTypes`/`maxSelectableFileSize`, `getDisabledTooltip`, `unsupportedFileTypeTooltip`, `allowedFileTypes`) is exposed as generic optional shell props (`isRowSelectable`, `getDisabledTooltip`, `unsupportedFileTypeTooltip`, `allowedFileTypes`, `maxSelectableFileSize`) so the shell itself carries no attach-specific naming, per design.md Decision 4's "grid/tree/toolbar option overrides" extension point.
- [x] 2.3 Confirmed: no `useTranslation` import, no `DialPopup`, no attach footer, no `allowedTypes`/`maximumAttachmentsAmount`/`canAttachFolders`/`onAttach` props on the shell.
- [x] 2.4 Added `apps/chat/src/components/DialFileManagerShell/tests/DialFileManagerShell.spec.tsx` (6 tests): renders `DialFileManager` region from hook result; empty-state title; error/retry panel + retry click; upload progress modal shown/hidden based on `uploadBatchState`; source-file assertion that `react-i18next` is never imported.
- [x] 2.5 Verify: `npx nx test chat` (89 files / 812 tests green) and `npx nx lint chat` clean on all touched files (pre-existing unrelated `Login.tsx` errors untouched). `DialFileManagerModal` still owns the old JSX at this point (shell exists but is unused) — no modal changes yet.

## 3. Refactor DialFileManagerModal into a thin wrapper

- [x] 3.1 Replaced the moved JSX/option-bag code in `DialFileManagerModal.tsx` with `<DialFileManagerShell hookResult={hookResult} labels={labels} .../>`, keeping `DialPopup`, the attach footer, and all attach-only prop wiring (`allowedTypes`, `maximumAttachmentsAmount`, `canAttachFolders`, `onAttach`) in the modal. `useDialFileManager` is now called with `variant: DialFileManagerVariant.Attach` explicitly (enum, per repo convention — see Slice 1 note). File shrank from 809 to ~460 lines.
- [x] 3.2 Confirmed `DialFileManagerModal`'s exported `Props` interface is byte-for-byte unchanged (diffed) — no edits at `ConversationView.tsx:730` or `ConversationRoute.tsx:571` call sites.
- [x] 3.3 Ran the full existing `DialFileManagerModal.spec.tsx` suite: 38/38 pass with **zero file changes** (`git diff` on the spec file is empty) — no import/mock updates were even needed.
- [x] 3.4 Verify: `npx nx test chat` (812/812 green), `npx nx lint chat` (clean on touched files; pre-existing unrelated `Login.tsx` errors untouched), `npx nx build chat` (succeeds).

## 4. Spec-reference cleanup (documentation only)

- [x] 4.1 Reviewed all 14 existing `openspec/specs/*/spec.md` files naming `DialFileManagerModal`. Renamed component mentions to `DialFileManagerShell` in **9 files** where the requirement describes behavior now implemented by the shell (grid/tree/toolbar/bulk-actions option assembly, search UI wiring, empty-state/forbidden-symbols/conflict-popup JSX wiring, rename wiring): `dial-file-manager-attach-ui`, `file-manager-attach-modal-polish`, `file-manager-filename-sanitization`, `file-manager-folder-creation`, `file-manager-rename-ui`, `file-manager-search`, `file-manager-tabs`, `file-manager-tree-state`, `file-manager-upload-conflicts`. Left unchanged in **5 files** where the mention describes modal-owned behavior (attach-only selection/validation logic, `onAttach`/Props, i18n resolution via `t()`, or a literal still-accurate file path): `dial-file-manager-attach-folders`, `dial-file-manager-attach-validation`, `file-manager-delete-ui`, `file-manager-upload`, `dial-file-system-picker`. No requirement text/scenario content changed beyond component names (one adjacent stale file path in `file-manager-rename-ui` was also corrected since it was already wrong pre-change).

## 5. Final verification

- [x] 5.1 `npx nx affected --target=lint --base=origin/development-1.0` — only pre-existing unrelated `Login.tsx` errors (present before this change, untouched by it).
- [x] 5.2 `npx nx affected --target=test --base=origin/development-1.0` — 812/812 tests green (89 files).
- [x] 5.3 `npx nx affected --target=build --base=origin/development-1.0` — succeeds.
- [x] 5.4 `git diff origin/development-1.0 -- '**/DialFileManagerModal.spec.tsx'` — empty diff; the file was not touched at all.
