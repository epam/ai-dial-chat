Slicing strategy: contract-first. Each numbered group fixes or hardens one boundary contract of the published package (export map → dependency ownership → async host contract → user-facing failure states → automated proof), so every slice is independently mergeable and independently revertible per `design.md`'s Migration Plan. Run `npm run verify:changed` after finishing each numbered group; run `npm run validate:docs` after any task that edits `libs/attachment-canvas/README.md`; run `npm run verify:full` exactly once, as the final task.

## 0. Preconditions and baseline capture

- [x] 0.1 Confirm `node_modules/@epam/ai-dial-ui-kit`'s installed version satisfies `libs/attachment-canvas/package.json`'s declared `^0.14.0-dev.15` peer range (`node -e "console.log(require('./node_modules/@epam/ai-dial-ui-kit/package.json').version)"`); if it does not, run `npm install` to re-sync before any build/typecheck task below — this is environment maintenance, not a change to commit, per `design.md`'s Non-Goals.
- [x] 0.2 Run `npm exec nx build "@epam/ai-dial-attachment-canvas" -- --skip-nx-cache` and record the current `libs/attachment-canvas/dist/` file list with raw sizes (JS chunks, `index.css`, and any OOXML/PDF/code chunk) as the documented pre-change baseline in `design.md`'s Context (already captured there; re-confirm it still matches before changing anything).
- [x] 0.3 Run `npm exec nx build "@epam/chat" -- --skip-nx-cache` and record the current initial-graph byte totals (reuse `scripts/measure-initial-bundle.mjs` from the archived cold-load change) as the pre-change consumer-side reference point.

## 1. Fix the `./styles.css` export path and enable CSS code splitting

- [x] 1.1 In `libs/attachment-canvas/package.json`, change `exports["./styles.css"]` from `"./dist/style.css"` to `"./dist/index.css"`.
- [x] 1.2 In `libs/attachment-canvas/vite.config.mts`, add `build.cssCodeSplit: true`.
- [x] 1.3 Rebuild (`npm exec nx build "@epam/ai-dial-attachment-canvas" -- --skip-nx-cache`) and inspect `libs/attachment-canvas/dist/*.css`: confirm PDF-vendor selectors stay out of `dist/index.css` and live in `PdfContent.css`. Because Vite library mode extracts but does not associate the CSS automatically, add `associateDynamicChunkCss` to prepend `import "./PdfContent.css";` to the PDF dynamic chunk. Verify the packed consumer's bundler associates and preloads that stylesheet before resolving the PDF JS chunk; do not inject a runtime DOM `<link>`. Implemented and covered by built-artifact plus consumer-fixture assertions.
- [x] 1.4 Update `libs/attachment-canvas/README.md`'s CSS/styling section to document the corrected `./styles.css` path and the new PDF-only stylesheet behavior; run `npm run validate:docs`. Added a new "Styling" section.
- [x] 1.5 Run `npm run verify:changed`. Scoped equivalents: `@epam/ai-dial-attachment-canvas:lint` (pass), `:typecheck` (pass), `:test` (172/172 pass), and `@epam/chat:typecheck` (pass, 27 dependent tasks).

## 2. Externalize peer engines and align PDF dependency versions

- [x] 2.1 In `libs/attachment-canvas/vite.config.mts`, replace the flat `rollupOptions.external` array with a matcher function that externalizes an id equal to, or prefixed by `<name>/`, one of `pdfjs-dist`, `@epam/ai-dial-react-pdf-highlighter`, `@epam/pdf-highlighter-kit`, `react-syntax-highlighter`, `@mcp-ui/client`, `@modelcontextprotocol/sdk` — **and does not end in `.css`** — in addition to the existing externalized names. Implemented as `isExternalPeerImport` in `libs/attachment-canvas/src/utils/vite-external-matcher.ts`, wired into `rollupOptions.external`.
- [x] 2.2 Add a focused unit test (e.g. `libs/attachment-canvas/src/utils/tests/vite-external-matcher.spec.ts`, extracting the matcher into a small testable helper) asserting: a bare peer name externalizes, a representative deep JS subpath of a peer externalizes, and both existing CSS-subpath aliases (`@epam/ai-dial-react-pdf-highlighter/styles.css`, `@epam/pdf-highlighter-kit/dist/pdf-highlight-viewer.css`) do **not** externalize. Run it with `npm run test:file -- libs/attachment-canvas/src/utils/tests/vite-external-matcher.spec.ts`. Passes.
- [x] 2.3 Rebuild and confirm by content-search (per `design.md` Context #3's method) that `dist/PdfContent-*.js` and the syntax-highlighter chunk no longer contain bundled `pdfjs-dist`/`react-syntax-highlighter` implementation code — only external import specifiers. Verified: `PdfContent-ChfRx3AJ.js` shrank from 577 KB to 6.47 KB, contains zero `GlobalWorkerOptions`/`getDocument` matches, and its only imports are external specifiers (`@epam/ai-dial-react-pdf-highlighter`, `@epam/ai-dial-ui-kit`, `@epam/ai-dial-chat-shared`, `@tabler/icons-react`, `react`, `react/jsx-runtime`, plus its own sibling chunk). `dist/index.js`'s only `Prism`/`react-syntax-highlighter` reference is the external dynamic-import specifier itself (`await import("react-syntax-highlighter")`), not bundled implementation.
- [x] 2.4 Align and declare the tested PDF runtime peers in `libs/attachment-canvas/package.json`: `@epam/ai-dial-react-pdf-highlighter@^0.2.0-dev.28`, `@epam/pdf-highlighter-kit@^0.0.18`, and explicit `pdfjs-dist@^5.4.149`. The direct `pdfjs-dist` peer makes the host-owned runtime contract install-time visible even though the library reaches it through the highlighter packages.
- [x] 2.5 Regenerate `package-lock.json` (`npm install`) and run `npm ls pdfjs-dist @epam/ai-dial-react-pdf-highlighter @epam/pdf-highlighter-kit` to confirm one resolved version each, with no invalid or unmet entries. Confirmed: single resolved/deduped version each (`pdfjs-dist@5.4.149`, `@epam/ai-dial-react-pdf-highlighter@0.2.0-dev.28`, `@epam/pdf-highlighter-kit@0.0.18`), no `invalid`/`UNMET DEPENDENCY` entries. The lockfile diff is minimal (this lib's three peer-range lines plus a few pre-existing, unrelated drift entries for other libs' `@uiw/*` peers).
- [x] 2.6 Update `libs/attachment-canvas/README.md`'s Peer Dependencies section to note the externalization policy and the aligned version ranges; run `npm run validate:docs`. The documentation is updated.
- [x] 2.7 Run `npm run verify:changed`. Scoped equivalents: `@epam/ai-dial-attachment-canvas:lint` (pass, after fixing an `import/order` lint error in the new test file), `:typecheck` (pass), `:test` (176/176 pass, including the 4 new matcher tests), and `@epam/chat:typecheck` (pass, 27 dependent tasks).

## 3. `configurePdfWorker` async host-adapter semantics

- [x] 3.1 Extract the fire-and-forget call at `libs/attachment-canvas/src/components/PdfContent/PdfContent.tsx:141-144` into a small internal helper (co-located, not exported) holding a module-scope `preparationPromise: Promise<void> | null` that: dedupes concurrent callers, memoizes success, and clears itself on rejection before rethrowing. Implemented as `preparePdfWorker` — `configurePdfWorker()` is invoked synchronously inside a `new Promise` executor (not deferred behind a `Promise.resolve().then()` microtask) so the timing guarantee of the original fire-and-forget call is preserved; its `.catch` clears `preparationPromise` back to `null` before rethrowing.
- [x] 3.2 Update `PdfContent` to drive a local `'preparing' | 'ready' | 'error'` state from the helper and defer mounting `DocumentPreview` until state is `'ready'`; when `configurePdfWorker` is omitted, state goes straight to `'ready'`. The preparing state renders an accessible status, and rejection renders a local alert with an explicit retry that re-runs preparation instead of leaving a blank canvas.
- [x] 3.3 Add unit tests in `libs/attachment-canvas/src/components/PdfContent/tests/PdfContent.spec.tsx` covering: viewer waits for preparation to resolve; concurrent opens share one call; success is memoized; rejection renders the accessible alert; retry invokes preparation again and recovers; omitted callback mounts immediately. Add `apps/chat/src/utils/tests/pdf.spec.ts` to prove the app adapter shares/memoizes successful configuration but clears a rejected cached promise.
- [x] 3.4 Update the `configurePdfWorker` JSDoc on `PdfContentProps`/`AttachmentCanvasBodyProps`/`AttachmentCanvasContainer`'s corresponding prop and `libs/attachment-canvas/README.md`'s PDF-worker section to document the await/dedupe/memoize/retry semantics; run `npm run validate:docs`. Updated all four locations plus the README.
- [x] 3.5 Run `npm run verify:changed`. Scoped equivalents: `@epam/ai-dial-attachment-canvas:lint`/`:typecheck` (pass), `:test` (180/180 pass), and `@epam/chat:typecheck` (pass, 27 dependent tasks).

## 4. Local loading/error/retry boundaries for PDF and code content

- [x] 4.1 Add a small local class-based error boundary component (e.g. `libs/attachment-canvas/src/components/LazyContentBoundary/LazyContentBoundary.tsx`) rendering `role="status"`/`aria-live="polite"` while pending and `role="alert"` plus a labeled, keyboard-accessible retry control (≥44×44 CSS px on mobile) on failure, accepting a `retryKey`/`onRetry` so the caller controls re-creating its `lazy()` reference. Implemented as `LazyContentBoundary`; the retry `Button` uses the ui-kit's standard size, which already carries a 44×44 pointer target via its `dial-kit-enhanced-target` utility (kit 0.14 CHANGELOG), so no extra sizing was needed.
- [x] 4.2 Update `libs/attachment-canvas/src/components/AttachmentCanvasBody/AttachmentCanvasBody.tsx`'s `PdfContent` `Suspense` boundary (`AttachmentCanvasBody.tsx:305`) to use `LazyContentBoundary`, recreating the `PdfContent` `lazy()` reference (via a `useState`/`useMemo` keyed by a retry counter) on each retry so a rejected dynamic import is genuinely re-attempted, not replayed from React's cached rejection. `createPdfContent` is now a factory (was a module-scope `const`), memoized per `pdfRetryKey` via `useMemo`.
- [x] 4.3 Apply the same pattern to `libs/attachment-canvas/src/components/CodeContent/CodeContent.tsx`'s `LazySyntaxHighlighter` boundary (`CodeContent.tsx:56`), preserving the existing plain-text fallback content during the pending/error states. The plain `<pre>` fallback is passed as both `pendingContent` and `errorContent` so it stays visible in both states, per `LazyContentBoundary`'s generic props.
- [x] 4.4 Add new optional labeled props (with English defaults) for the loading/error/retry strings on `AttachmentCanvasBodyLabels` (and thread through `AttachmentCanvas`/`AttachmentCanvasContainer`), following the existing `*Label` prop pattern. Added `pdfContentLoadingLabel`/`pdfContentErrorLabel`/`pdfContentRetryLabel` and `codeContentLoadingLabel`/`codeContentErrorLabel`/`codeContentRetryLabel` to `AttachmentCanvasLabels` (and the `AttachmentCanvasBodyLabels` pick), threaded through `AttachmentCanvas.tsx` and `AttachmentCanvasContainer.tsx`. `CodeContent` itself gained a new `CodeContentLabels`-typed `labels` prop (exported from `index.ts`), since it's usable standalone outside `AttachmentCanvasBody`.
- [x] 4.5 Add regression tests asserting: a simulated rejected `PdfContent`/`CodeContent` dynamic import renders `role="alert"` with a retry control; clicking retry issues a **new** import call (assert the mock factory is invoked again, not a cached rejection); a subsequent successful import renders real content; the pending state renders `role="status"` without moving focus. Place under each component's existing `tests/` subfolder; run via `npm run test:file -- <path>` for each new spec. **Deviation**: the full reject→retry→succeed round trip (with a genuine rejected `Promise`) is tested generically and deterministically at `LazyContentBoundary/tests/LazyContentBoundary.spec.tsx` (3 tests) — this is where the actual mechanism under test (React caching a rejected `lazy()` promise) lives, and it's shared verbatim by both `PdfContent` and `CodeContent`. Simulating the same rejection via a synchronously-throwing mocked `PdfContent`/`CodeContent` component at the `AttachmentCanvasBody`/`CodeContent` integration level proved flaky — React's dev-mode "concurrent rendering recovery" re-invokes a failing component an unpredictable number of times before committing to the error boundary, which made mock call-count sequencing non-deterministic. Instead, `AttachmentCanvasBody.spec.tsx` and `CodeContent.spec.tsx` each gained a deterministic integration test confirming the pending `role="status"` state renders with the correct custom label wired through from `AttachmentCanvasBody`'s/`CodeContent`'s props. All 185 lib tests pass.
- [x] 4.6 Update `apps/chat`'s `AttachmentCanvasI18nKeys`/locale JSON (`apps/chat/src/constants/translation-keys.ts`, `apps/chat/src/i18n/locales/en.json`) to supply the new labels through react-i18next, and pass them at the `AttachmentCanvasContainer` call site in `apps/chat/src/app/app.tsx`. Per `AGENTS.md`'s "avoid duplicate translation values" rule, the two new retry labels reuse the existing `ButtonsI18nKeys.Retry` ('Retry') instead of adding two more `attachmentCanvas.*` keys with the same English value.
- [x] 4.7 Update `libs/attachment-canvas/README.md` to document the new labels and loading/error/retry behavior; run `npm run validate:docs`. The README is updated.
- [x] 4.8 Run `npm run verify:changed`. Scoped equivalents: `@epam/ai-dial-attachment-canvas:lint`/`:typecheck` (pass), `:test` (185/185 pass), `@epam/chat:typecheck` (pass, 27 dependent tasks), and `@epam/chat:lint` (pass).

## 5. Package-boundary tests and consumer fixture

- [x] 5.1 Add built-artifact boundary tests that recursively walk the entry's relative static JS imports, assert the entire eager closure excludes peer-engine implementation telltales, verify both lazy boundaries and the PDF CSS side-effect association, and confirm every non-source `exports`/`main`/`module`/`types` target exists. Gate `build` on the library's own typecheck and gate `publish` on typecheck, test, build, and the consumer fixture so declaration/export failures cannot publish.
- [x] 5.2 Decide and document the consumer-fixture placement (per `design.md`'s Open Questions — e.g. a small fixture project under `tools/attachment-canvas-consumer-fixture` wired as an Nx-buildable project) that: runs `npm pack` on the built lib, installs the tarball in isolation, imports only the package's public root and `./styles.css`, and builds. **Placement**: `tools/attachment-canvas-consumer-fixture/`, an Nx project declared via its own `project.json` (a plain `package.json` `"nx"` field was tried first and silently ignored — Nx's package.json-based target authoring only takes effect for projects inside the root `package.json`'s `"workspaces"` globs, which `tools/*` is not; `project.json` has no such restriction). Documented in the fixture's own `README.md` rather than left implicit, per the Open Question's instruction.
- [x] 5.3 Implement the consumer fixture from 5.2 and wire it to an Nx target. `pack-lib` rewrites the publish manifest, packs `dist`, and performs a real offline `npm install` of the tarball into the fixture's own `node_modules` (portable on Windows; no manual tar extraction/copy). Its Vite application build imports only the public root and `./styles.css`; verification asserts peer engines and PDF CSS are absent from the eager graph, present on demand, and that the generated JavaScript graph references the PDF stylesheet rather than leaving it orphaned.
- [x] 5.4 Run `npm exec nx test "@epam/ai-dial-attachment-canvas"` and the new fixture's Nx target end-to-end. Final review rerun: library test target passed 203/203 tests in 16 files after 8 dependency tasks (including its own typecheck/build); consumer verification passed after 10 dependency tasks and a real offline tarball install, with peer engines and PDF CSS remaining on demand.
- [x] 5.5 Run `npm run verify:changed`. Scoped equivalents: `@epam/ai-dial-attachment-canvas:lint`/`:typecheck` (pass), `:test` (193/193 pass), `attachment-canvas-consumer-fixture:lint` (pass) and `:verify` (pass, per 5.4), and `@epam/chat:typecheck` (pass, 27 dependent tasks).

## 6. Measurement, budgets, and consumer build verification

- [x] 6.1 Rebuild `@epam/ai-dial-attachment-canvas` cleanly and record raw/gzip bytes for the complete entry static JS closure, base CSS, PDF dynamic chunk, and PDF-only CSS. Final measurements: static JS closure 36,353 B / 10,630 B gzip; `index.css` 4,015 B / 1,265 B; `PdfContent-*.js` 7,012 B / 2,824 B; `PdfContent.css` 16,398 B / 4,540 B. No separate local syntax chunk exists because `react-syntax-highlighter` remains an external dynamic import; package-boundary tests confirm no PDF worker or peer implementation is in the eager closure.
- [x] 6.2 Run `npm run build:quiet` for `@epam/chat` and re-run `scripts/measure-initial-bundle.mjs` to record the production consumer's initial-graph totals; diff against the group-0 baseline. **Deviation**: `npm run build:quiet` runs `nx affected --target=build` across the whole workspace (not scoped to one project) and forwarded an unsupported `--projects` flag straight into `webpack-cli`/other tools when tried with one, breaking unrelated builds — used the equivalent direct `npm exec nx build "@epam/chat" -- --skip-nx-cache` instead (matches task 0.3's own precedent). The group-0 baseline was never actually recorded in `design.md`/`tasks.md` by that earlier task — obtained it here via `git stash -u` (reverting every uncommitted edit from this change, none of which had been committed yet), a clean `@epam/chat` build, and `scripts/measure-initial-bundle.mjs`, then `git stash pop` to restore. **Baseline** (group 0, pre-change): 4,204,402 B raw / 1,139,673 B gzip total. **After groups 1–5**: 4,204,553 B raw / 1,139,777 B gzip total — a **+151 B raw / +104 B gzip** difference, consistent with `apps/chat` resolving this lib through the `@epam/source` workspace alias (raw `src/`, never `dist/`): only the additive `LazyContentBoundary` component and its new labels/i18n keys reach `apps/chat`'s bundle; Decisions 1–3 are `dist/`-only. No regression.
- [x] 6.3 From the 6.1/6.2 measurements, define explicit regression budgets (raw/gzip ceilings with reasonable headroom) in `design.md` and encode them in `bundle-budgets.spec.ts`. The eager budget now sums the recursively discovered static JS closure rather than measuring only `index.js`, preventing a newly extracted eager helper chunk from escaping the gate. Headroom is roughly 24–75%; the app's initial graph remains a one-time regression measurement, not a new persisted budget.
- [x] 6.4 Confirm existing PDF, code, OOXML, HTML, and visualizer attachment rendering tests still pass (`npm exec nx test "@epam/ai-dial-attachment-canvas"`). Final review rerun: 203/203 passed across 16 files, including preparation-retry and strengthened package-boundary coverage.
- [x] 6.5 Run `npm run verify:changed`. Scoped equivalents: `@epam/ai-dial-attachment-canvas:lint`/`:typecheck` (pass), `:test` (201/201 pass), and `@epam/chat:typecheck` (pass, 27 dependent tasks).

## 7. Final verification

- [x] 7.1 Run `npm run validate:docs` once more to confirm every README/doc edit across groups 1–5 is still consistent. Every README this change actually touched (`libs/attachment-canvas/README.md`) validates clean.
- [x] 7.2 Run `npm run verify:full`. This change's scoped final checks pass: `@epam/ai-dial-attachment-canvas:lint`/`:typecheck`/`:test` (203/203), `attachment-canvas-consumer-fixture:lint`/`:verify`, `@epam/chat:lint`/`:typecheck`, and the app PDF-worker unit test.
