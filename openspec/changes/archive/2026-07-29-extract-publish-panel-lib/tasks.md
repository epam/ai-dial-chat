## 1. Scaffold the `publish-panel` library

- [x] 1.1 Create `libs/publish-panel/` scaffolded from `libs/conversation-input`/`libs/catalog`: `package.json` (`name: "@epam/ai-dial-publish-panel"`, `private: true`, `nx.tags: ["publishable"]`, `nx.targets.publish`, peer deps for `@epam/ai-dial-ui-kit`, `@epam/ai-dial-kit`, `@epam/ai-dial-sidebar`, `@epam/ai-dial-chat-shared`, `react`/`react-dom`), `vite.config.mts` (React + `dts()` plugins, `resolve.alias` for the same sibling libs, `build.lib` → `src/index.ts`, `formats: ['es']`), `tsconfig.json` / `tsconfig.lib.json` / `tsconfig.spec.json`, `eslint.config.mjs`, `tailwind.config.js`/`postcss.config.js`, empty `src/index.ts`, `src/test-setup.ts`
- [x] 1.2 Register `"@epam/ai-dial-publish-panel/*": ["./libs/publish-panel/*"]` in `tsconfig.base.json` `compilerOptions.paths`
- [x] 1.3 Add `libs/publish-panel/README.md` documenting the package's purpose, public exports, and the `renderSummary` extension point
- [x] 1.4 Verify: `npm exec nx build "@epam/ai-dial-publish-panel"` succeeds on the empty scaffold (project is registered under its package.json name, not the directory name)

## 2. Move models and pure utils (no UI consumers switched yet)

- [x] 2.1 Move `libs/catalog/src/models/publish.ts` → `libs/publish-panel/src/models/publish.ts` verbatim (no behavior change)
- [x] 2.2 Move `publish-state.ts`, `publish-folder-tree.ts`, `format-published-date.ts` (and their co-located tests) verbatim into `libs/publish-panel/src/utils/`
- [x] 2.3 Move `use-publish-flow.ts` (and its tests) into `libs/publish-panel/src/utils/`; change the generic default from `<TItem extends PublishFlowItem = CatalogItem>` to `<TItem extends PublishFlowItem = PublishFlowItem>`; remove the `CatalogItem` import (test fixture switched from a `CatalogItem` literal to a minimal `PublishFlowItem` literal)
- [x] 2.4 Populate `libs/publish-panel/src/index.ts` with the moved models/utils exports
- [x] 2.5 Verify: `npm exec nx test "@epam/ai-dial-publish-panel"` passes (66 tests); lint passes for the moved files (remaining `@nx/dependency-checks` errors on `package.json` peer deps are expected until task 3 moves the components that use them)

## 3. Move UI components; decouple `CatalogItem`/`EntityHeader`

- [x] 3.1 Move `PublishFoldersTree`, `PublishHistoryList`, `PublishFooter` (and their tests) into `libs/publish-panel/src/components/` verbatim — none of these import `CatalogItem`
- [x] 3.2 Move `PublishPanel.tsx` (and its tests); replace the `item?: CatalogItem` prop with `renderSummary?: () => ReactNode`; render the slot's output where the `EntityHeader` block currently renders; remove the `CatalogItem`/`EntityHeader` imports. Version-derived behavior (replace-warning callout, history-section visibility) now keys off `resource?.version` instead of `item?.version`, since `resource` is passed alongside `renderSummary` rather than being mutually exclusive with it. Also added `src/vite-env.d.ts` (`/// <reference types="vite/client" />`) so `*.module.scss` imports type-check, matching `libs/catalog`.
- [x] 3.3 Move `StandalonePublishPanel.tsx` (and its tests); replace its `item?: CatalogItem` prop with the same `renderSummary?` pass-through to `PublishPanel`; remove the `CatalogItem` import
- [x] 3.4 Update `libs/publish-panel/src/index.ts` to export all moved components and their `*Props`/`*Texts` types
- [x] 3.5 Verify: `npm exec nx test "@epam/ai-dial-publish-panel"` (137 tests), `npm exec nx lint "@epam/ai-dial-publish-panel"`, `npm exec nx build "@epam/ai-dial-publish-panel"` all pass; grep `libs/publish-panel/src` confirms no `CatalogItem`/`EntityHeader`/`@epam/ai-dial-catalog` reference remains (project is registered under its package.json name, not the `publish-panel` directory name)

## 4. Switch `PublishConversationPanelContainer` to the new library

- [x] 4.1 Update `apps/chat/src/components/PublishConversationPanelContainer/PublishConversationPanelContainer.tsx` imports from `@epam/ai-dial-catalog` to `@epam/ai-dial-publish-panel` (no other code changes — it already only uses the `resource` path). Also switched `apps/chat/src/utils/publish.ts` (`PublishHistoryEntry`) and `apps/chat/src/hooks/publish/usePublishFolders.ts` (`PublishFolderNode`) to import from the new package, since both were identified as additional straggler import sites during implementation.
- [x] 4.2 Verify: existing container tests pass unchanged (updated the test's `vi.mock('@epam/ai-dial-catalog', ...)` to target `@epam/ai-dial-publish-panel` instead)
- [x] 4.3 Verify: full affected suite deferred to task 6.4 (catalog had to be fixed first since it re-exported the just-moved files — see task 5 note)

## 5. Switch `DetailsPanel` / `CatalogView` wiring

- [x] 5.1 Update `libs/catalog/src/components/Details/DetailsPanel.tsx` imports (`PublishPanel`, `PublishFooter`, `usePublishFlow`, `derivePublishState`, publish models) to `@epam/ai-dial-publish-panel`. Note: this had to happen immediately after task 3's file moves — `libs/catalog/src/index.ts`, `models/item-details-props.ts`, and `models/catalog-props.ts` also still referenced the now-moved relative paths and were fixed in the same pass (pulled forward from task 6.1), since catalog would not build otherwise. Also added `@epam/ai-dial-publish-panel` to catalog's `package.json` peerDependencies, `vite.config.mts` `resolve.alias`, and `tsconfig.lib.json` references (mirroring the existing `ai-dial-kit` pattern); ran `npm install` to create the new workspace symlink; removed `@epam/ai-dial-sidebar` from catalog's package.json/vite.config.mts/tsconfig.lib.json since `PublishPanel`'s `SearchInput` import (the only user) moved out with it.
- [x] 5.2 Replace `<PublishPanel item={item} ... />` with `resource={{ title: item.name, version: item.version, iconUrl: item.iconUrl }}` + `renderSummary={() => (<><EntityHeader .../><DialTag .../></>)}` — the existing `EntityHeader` + version-tag JSX, moved verbatim from `PublishPanel.tsx` into `DetailsPanel.tsx`'s `renderSummary` callback
- [x] 5.3 Update the `usePublishFlow` call site to explicitly instantiate `usePublishFlow<CatalogItem>({ ... })`
- [x] 5.4 Confirmed `apps/chat/src/components/CatalogView/CatalogView.tsx` only imports non-`Publish*` symbols from `@epam/ai-dial-catalog` (`Catalog`, `CatalogEntityType`, `CatalogItem`, etc.) — no edits needed
- [x] 5.5 Verify: `npm exec nx run-many -t typecheck lint test build -p "@epam/ai-dial-catalog"` passes (206 tests, 0 lint errors); updated `DetailsPanel.spec.tsx`'s `vi.mock` to target `@epam/ai-dial-publish-panel` (spreading `importOriginal` so the real `usePublishFlow` still runs, only `PublishPanel`/`PublishFooter` are stubbed)

## 6. Remove old exports and source from `libs/catalog`

- [x] 6.1 Remove the publish-related re-exports from `libs/catalog/src/index.ts` (done as part of task 5.1, since catalog would not build with dangling exports pointing at the already-moved files)
- [x] 6.2 Delete the now-moved source files from `libs/catalog`: `components/PublishPanel/`, `components/PublishFoldersTree/`, `components/PublishHistoryList/`, `utils/use-publish-flow.ts`, `utils/publish-state.ts`, `utils/publish-folder-tree.ts`, `utils/format-published-date.ts`, `models/publish.ts` (done via `git mv` in tasks 2–3, preserving file history)
- [x] 6.3 Grepped the repo for remaining `@epam/ai-dial-catalog` imports of `Publish*` symbols and stale relative imports into the deleted paths — none found (the two matches for the string "publish" were `CatalogEntityType` imports in files merely named `publish.ts`/`publish.spec.ts`, not stale `Publish*` symbol imports)
- [x] 6.4 Verify: `npm exec nx affected --target=test,lint,build --base=origin/development-1.0` — 1840 tests passed (2 pre-existing skips), all affected builds succeeded. Lint surfaced pre-existing failures in files this change never touched (`apps/chat-api/src/app-config/config-registry/env-config.provider.ts`, three unrelated `apps/chat` spec files) — confirmed via `git status`/`git log` that these predate this change; linting only the files this change modified in `apps/chat` returns zero issues. `@epam/ai-dial-catalog` and `@epam/ai-dial-publish-panel` both pass test/lint/build/typecheck cleanly.

## 7. Update OpenSpec specs and finalize

- [x] 7.1 Reconciled this change's delta specs (`catalog-publish-flow`, `conversation-publish-flow`, `publish-panel-library`) against the final code — no divergence found; the `renderSummary` mechanism, import-source requirements, and library-isolation contract all match the implementation as written
- [x] 7.2 Ran the full affected suite: `npm exec nx affected --target=test,lint,build --base=origin/development-1.0` — see note on task 6.4 (tests/builds all green; lint has pre-existing unrelated failures on the base branch)
- [x] 7.3 A dev server for this workspace was already running (port 4207, started outside this session); navigated to it via Playwright. The app shell serves with no module-resolution errors for `@epam/ai-dial-publish-panel` (Vite dev-mode resolves the new lib via its `@epam/source` export condition correctly). Full interactive click-through of both publish flows (folder tree, submit, RTL toggle) was **not possible** in this sandbox: the backend (`chat-api`/DIAL Core) is not running here, so every `/api/*` call returns 502 and the app never gets past its auth bootstrap to render any authenticated view. Correctness of the moved/decoupled behavior is instead evidenced by the automated suites: 137 `publish-panel` tests (including `StandalonePublishPanel`'s dialog role, `inert`, focus-management, and RTL slide-in assertions), 206 `catalog` tests (including the full `DetailsPanel` publish sub-view flow: open, select folder, create folder, submit success/failure, Back-button-disabled-while-submitting), and the `apps/chat` suite's `PublishConversationPanelContainer` tests (title-only summary render, folder selection/creation, publish success/failure, pending-approval notification) — all passing unchanged after the extraction.
