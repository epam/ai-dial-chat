## 1. Selection-agnostic overlay data hook (app-owned, no chat-input change)

- [x] 1.1 Add `apps/chat/src/components/DeploymentSelector/useDeploymentSelectorFieldOverlay.tsx`: a hook mirroring `useDeploymentSelectorOverlay.tsx`'s data binding (`useDeployments()`, `useFavoriteApplications()`, `mapDeploymentToCatalogItem`, `findDeploymentByIdOrReference`) but parameterized by `selectedId: string | null` and `onSelect: (id: string) => void` props instead of reading/writing `DeploymentsContext.selectedItemId`/`setSelectedItemId`. Export `{ renderOverlay, catalogModal, isLoading, error, resolvedLabel }` (`resolvedLabel` is the selected deployment's display name, or the raw `selectedId` when unresolvable).
- [x] 1.2 Add a unit test `apps/chat/src/components/DeploymentSelector/tests/useDeploymentSelectorFieldOverlay.spec.tsx` covering: resolves a favorited selection, resolves a non-favorited/not-initially-loaded selection (via `findDeploymentByIdOrReference`), falls back to the raw id when `selectedId` doesn't resolve, and never calls `setSelectedItemId`/reads `DeploymentsContext.selectedItemId`.
- [x] 1.3 Verify: `npm exec nx test chat && npm exec nx lint chat`

## 2. DeploymentSelectorFieldTrigger component

- [x] 2.1 Add `apps/chat/src/components/DeploymentSelector/DeploymentSelectorFieldTrigger.tsx`: full-width outlined `<button type="button">` trigger (Tailwind field chrome matching `Input`/`Select`), trailing `IconChevronDown` from `@tabler/icons-react` that rotates on open, wrapped in the ui-kit `Dropdown` (default `matchReferenceWidth={true}`) rendering `renderOverlay` from `useDeploymentSelectorFieldOverlay`. Props: `selectedId`, `onSelect`, `placeholder`, `isDisabled?`, `labelledById` (for `aria-labelledby`), `isInvalid?`.
- [x] 2.2 Implement `aria-haspopup="listbox"`, `aria-expanded`, `aria-labelledby={labelledById}` on the trigger button; ensure a ≥44×44px hit area via padding, not fixed height overrides that could shrink it.
- [x] 2.3 Implement loading (disabled + busy affordance), empty (interactive, panel shows existing empty hint), error (error affordance, still focusable), disabled (`isDisabled` blocks opening), and unavailable-selected-deployment (raw-id fallback label, no `onSelect` call, no clearing) states per `design.md` §6.
- [x] 2.4 Apply logical Tailwind properties throughout; confirm in code review whether the chevron rotation needs an explicit `rtl:` class (per `design.md` Open Questions) and document the decision in a one-line code comment if a class is added.
- [x] 2.5 Add `apps/chat/src/components/DeploymentSelector/tests/DeploymentSelectorFieldTrigger.spec.tsx` covering: placeholder when unset, resolved name when set, opening renders search/favorites/Browse, selecting calls `onSelect` and closes, selection does not touch `DeploymentsContext.selectedItemId`, `Highlight` renders on a search match, loading/empty/error/disabled/unavailable states, keyboard open→select→close with focus restored to the trigger, Escape closes and restores focus, `aria-expanded`/`aria-labelledby` correctness, ≥44×44px target, and RTL layout (render with `dir="rtl"` and assert no hard-coded left/right offset breaks layout).
- [x] 2.6 Verify: `npm exec nx test chat && npm exec nx lint chat && npm exec nx build chat`

## 3. libs/scheduled-tasks: replace modelOptions with a modelSelector slot

- [x] 3.1 In `libs/scheduled-tasks/src/models/scheduled-task-create-form-props.ts`, remove `modelOptions: ScheduledTaskCreateFormModelOption[]` from `ScheduledTaskCreateFormProps` and add `modelSelector: ReactNode` with JSDoc documenting it as an opaque host-supplied control; remove the now-unused `ScheduledTaskCreateFormModelOption` type/export if nothing else references it (grep before removing).
- [x] 3.2 In `libs/scheduled-tasks/src/components/ScheduledTaskCreateForm/ScheduledTaskCreateForm.tsx`, remove the `Select` block bound to `values.modelId`/`modelOptions` (current lines ~151-161) and render `modelSelector` instead, wrapped in the lib's own "Model or Agent" required label + `errors.modelId` markup (reuse the `withRequiredMarker`/inline-error-paragraph pattern already used for `Calendar` fields in this file); remove the `Select` import if it becomes unused elsewhere in the file (check other `Select` usages, e.g. the Repeat field, before removing the import).
- [x] 3.3 Update `libs/scheduled-tasks/README.md`'s `ScheduledTaskCreateForm` usage example to show `modelSelector` instead of `modelOptions`.
- [x] 3.4 Update `libs/scheduled-tasks/src/components/ScheduledTaskCreateForm/tests/ScheduledTaskCreateForm.spec.tsx` (or equivalent existing test file): replace any `modelOptions`-based assertions with `modelSelector` slot assertions (renders the passed node verbatim, required-label/error markup wraps it, required-field guard still reads `values.modelId`).
- [x] 3.5 Verify: `npm exec nx test scheduled-tasks && npm exec nx lint scheduled-tasks && npm exec nx build scheduled-tasks`

## 4. Wire ScheduledTaskCreatePage to the shared selector

- [x] 4.1 In `apps/chat/src/pages/ScheduledTaskCreatePage/ScheduledTaskCreatePage.tsx`, remove the `modelOptions` construction from `useDeployments().items` (current lines ~94-101) and instead render `<DeploymentSelectorFieldTrigger selectedId={values.modelId} onSelect={(id) => onFieldChange('modelId', id)} placeholder={t(ScheduledTasksI18nKeys.ModelPlaceholder)} labelledById="scheduled-task-model-label" isDisabled={isSubmitting} isInvalid={Boolean(errors.modelId)} labels={{...}} />`, passed as `ScheduledTaskCreateForm`'s `modelSelector` prop.
- [x] 4.2 Confirm/add the `id="scheduled-task-model-label"` (or equivalent) on the lib's rendered label element so `aria-labelledby` resolves; if the lib doesn't yet expose a label id, add one in the same edit as task 3.2 rather than inventing a duplicate label here.
- [x] 4.3 Update `apps/chat/src/pages/ScheduledTaskCreatePage/tests/ScheduledTaskCreatePage.spec.tsx`: replace `modelOptions`-shape assertions with assertions that the page renders `DeploymentSelectorFieldTrigger` bound to `values.modelId`, that selecting a deployment calls `onFieldChange('modelId', ...)`, and that submit still sends `model: <selected id>` in the `POST` body (integration boundary test per proposal requirement 9's "connects to routing, data loading, and form submission").
- [x] 4.4 Verify: `npm exec nx test chat && npm exec nx lint chat`

## 5. Wire ScheduledTaskEditPage to the shared selector with preselection

- [x] 5.1 In `apps/chat/src/pages/ScheduledTaskEditPage/ScheduledTaskEditPage.tsx`, remove the equivalent `modelOptions` construction (current line ~132) and render the same `DeploymentSelectorFieldTrigger` wiring as task 4.1, with `selectedId` sourced from the reverse-mapped `values.modelId` so the task's current deployment is preselected on load.
- [x] 5.2 Update `apps/chat/src/pages/ScheduledTaskEditPage/tests/ScheduledTaskEditPage.spec.tsx`: add/adjust cases for preselection of a favorited deployment, preselection of a deployment absent from favorites/initial subset (unavailable-fallback path per `design.md` §6), changing the deployment before Save, and that `PUT` sends the newly selected `model` id.
- [x] 5.3 Verify: `npm exec nx test chat && npm exec nx lint chat && npm exec nx build chat`

## 6. i18n

- [x] 6.1 Grep `apps/chat/src/i18n/locales/en.json` and `apps/chat/src/constants/translation-keys.ts` for an existing "Select Model or Agent" / model-selector placeholder string before adding a new key; a matching key already existed (`scheduledTasks.create.modelPlaceholder` / `ScheduledTasksI18nKeys.CreateModelPlaceholder`) — its value was updated from `"Select a model"` to `"Select Model or Agent"` and reused by both create and edit pages instead of adding a new key.
- [x] 6.2 Add/reuse i18n keys for the trigger's loading/error/empty labels, preferring existing `DeploymentSelectorLabels` defaults already used by the chat selector over new duplicates; `DeploymentSelectorFieldTrigger` reuses `DeploymentSelectorI18nKeys.Loading`/`.Error` verbatim — no new keys added.
- [x] 6.3 Verify: `npm exec nx lint chat` (catches unresolved/unused translation-key enum references)

## 7. Full affected verification

- [x] 7.1 Run `npm exec nx affected --target=lint --base=origin/development-1.0` — `nx affected`/`nx show projects --affected` returned an empty project list in this session's nested git-worktree checkout (`.claude/worktrees/...`), which confuses Nx's workspace-root detection for git-diff-based affected computation (see the `createGlobPatternsForDependencies` warning logged on every run). Substituted `npx nx run-many --target=lint --projects=@epam/chat,@epam/ai-dial-scheduled-tasks` plus the individual per-project lint runs already done in tasks 1.3/2.6/3.5/4.4/5.3/6.3 — all pass with 0 errors (pre-existing warnings only, unrelated to this change).
- [x] 7.2 Run `npm exec nx affected --target=test --base=origin/development-1.0` — same substitution as 7.1. All new/modified test suites pass (hook: 6, trigger: 10, lib form: 35, create page: 20, edit page: 16). One pre-existing, unrelated failure was found and left untouched: `apps/chat/src/components/CatalogView/tests/CatalogView.spec.tsx` (3 unshare-notification-variant assertions) fails in complete isolation with zero diff against `origin/development-1.0` on that file or its component — confirmed pre-existing on the base branch, not a regression from this change.
- [x] 7.3 Run `npm exec nx affected --target=build --base=origin/development-1.0` — same substitution as 7.1. `@epam/chat` and `@epam/ai-dial-scheduled-tasks` both build successfully.
- [x] 7.4 Confirm no changes landed in `libs/conversation-input/src/components/Input/ModelSelectorControl.tsx` or `libs/conversation-input`'s public models (`git diff --stat origin/development-1.0... -- libs/conversation-input` shows no output), verifying backward compatibility for the chat input trigger. Confirmed: empty diff.
