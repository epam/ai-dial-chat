Verification uses `npm run test:file -- <workspace-relative-path>` for the
red/green loop, `npm run verify:changed` per slice, and `npm run verify:full`
once before completion. `design.md` labels each finding F1–F9; tasks reference
them by number.

## 1. Reproduce before fixing

- [x] 1.1 Add a failing test to `libs/quotations/src/hooks/useCitationMarkdownComponents/tests/useCitationMarkdownComponents.spec.tsx` that renders content with the same `<cit data-id="e1"></cit>` twice against one matching `html_tag` group, activates the first marker, and asserts exactly one `role="dialog"` card is present. It must fail today with two (F1). Verify: `npm run test:file -- libs/quotations/src/hooks/useCitationMarkdownComponents/tests/useCitationMarkdownComponents.spec.tsx`
- [x] 1.2 Extend that spec with a test that, with two cards open, clicks "Preview" on one and asserts `onPreview` was called exactly once. Determine empirically whether it fails, and whether the cause is the sibling outside-press unmount traced in F2. Record the observed behaviour in the change (a comment on the test is enough). If jsdom cannot deliver the `pointerdown` ordering that F2 depends on, say so in the test's comment and rely on task 5.2 for the real-browser check rather than asserting a mechanism the test cannot see.
- [x] 1.3 Add a failing test to `libs/quotations/src/utils/tests/useCitationCard.spec.ts` asserting that a `closePopup` call naming a key that does not currently own the popup leaves the popup open. It must fail today, since `closePopup` takes no argument.

## 2. Occurrence-scoped popup ownership in `libs/quotations`

- [x] 2.1 Re-key `libs/quotations/src/utils/useCitationCard.ts`: rename `openGroupKey` to `openOwnerKey`, change `closePopup` to `closePopup(ownerKey: string)` clearing state only when `ownerKey` is the current owner, and keep `setActiveIndex`/`getActiveIndex` keyed by `groupKey`. Document the two key spaces in the hook's JSDoc per `.claude/rules/libs.md` (D3, D4). Verify: task 1.3's test turns green.
- [x] 2.2 In `libs/quotations/src/components/CitationDropdown/CitationDropdown.tsx`, derive `const ownerKey = useId()` and use it for `isOpen`, `openPopup`, and both `closePopup` call sites (`handleOpenChange` and `handlePreview`); keep `group.groupKey` for the switcher index and `group.sourceUrl` for preview/download. Do not add a prop, and do not clone the group or its annotations (D2, F9). Verify: tasks 1.1 and 1.2 turn green.
- [x] 2.3 Confirm `useCitationMarkdownComponents`'s `markdownComponents` memo still depends only on `groups`, the three callbacks, and `isCompactTypography` — no popup state. Leave the `key` on the rendered `CitationDropdown` alone; F4 establishes there is no duplicate-key defect, and changing keys does not affect open state.
- [x] 2.4 Add a test that opens a card from a repeated marker, forces an unrelated rerender, and asserts the same card is still open with the same switcher index — this is the regression guard for `useId` stability (D2 risk).
- [x] 2.5 Fix the stale comment at `CitationDropdown.tsx:77-82` (F8): the resolved `Tooltip` export is the 2.0 component, not 1.0, and no `placement` is passed. State what the code does today.
- [x] 2.6 Update the `useCitationCard` section of `libs/quotations/README.md` to describe occurrence-keyed open state and the owner-scoped `closePopup(ownerKey)` signature, keeping the group-keyed switcher index accurate. Verify: `npm run validate:docs`

## 3. Interaction and host-integration coverage

- [x] 3.1 In `libs/quotations/src/components/CitationDropdown/tests/CitationDropdown.spec.tsx`, cover: activating occurrence B transfers the card from A; a `closePopup` aimed at a stale occurrence leaves the active card open; dismiss-then-reopen still yields a working card. Assert card counts, not just presence.
- [x] 3.2 In `apps/chat/src/components/ConversationView/tests/ConversationMessageItem.spec.tsx`, add a repeated-`data-id` case through the real markdown-to-citation path asserting: one card opens, Preview calls `openCanvas` exactly once, and the annotation reaching the handler is reference-identical to the entry in `citationGroups` (F9). Note the markers share an accessible name, so select by index from `getAllByRole`, not by name.
- [x] 3.3 Add a case with two **distinct** cit ids whose annotations carry identical quote text and the same source URL: both markers render, both remain independently openable, and neither annotation is dropped or merged.
- [x] 3.4 Add a case for repeated markers spanning two paragraphs, and confirm the existing six-distinct-ids test and the streaming-citation tests still pass unchanged.
- [x] 3.5 Cover multi-annotation group navigation from a repeated marker: open an occurrence whose group has several annotations, use Next, then Preview, and assert the callback receives the annotation the card was displaying. Verify: `npm run verify:changed`

## 4. Document-highlight identity (evidence-gated — D5)

- [x] 4.1 **Partially resolved — DOCX reproduction still outstanding.** The issue's original `Citation_Duplicate_Text_Test.docx` reproduction (a live app + model call against that file) was **not performed** in this session; no environment to run it was available. Instead, a user-confirmed *actual* assistant-response payload was supplied as evidence (from a different document, a PDF, with a different prompt) and used as a regression fixture in `apps/chat/src/components/ConversationView/tests/ConversationMessageItem.spec.tsx` ("reproduces issue #8822: two repeated cit ids, one multi-selector annotation each, PDF source"). Findings from that payload:
  - The source is a PDF (`application/pdf`), not the issue's DOCX — `annotationToPdfCanvasContent`/`annotationsToPdfHighlights` is the applicable path here, not the OOXML path F6 was written about.
  - `content` contains two occurrences each of `<cit data-id="ff3390">` and `<cit data-id="7bba1b">`; `custom_content.annotations` carries exactly **one** annotation per id (not per occurrence) — confirms F1 (repeated rendered occurrences sharing one group's identity), which this change fixes.
  - Neither annotation carries a wire `index`. `ff3390`'s annotation carries **two** `pdf_region` selectors (a legitimate multi-location annotation, not a collision — see D5's closing note).
  - `annotationToPdfCanvasContent` builds `allAnnotations` from `group.annotations` filtered by source URL; since each `groupAnnotationsByCitId` group here has exactly one annotation, `annotationsToPdfHighlights` never gathers more than one entry per click — reusing `id: "0"` across the **two separate** `PdfCanvasContent` payloads produced by two different clicks is not an intra-set collision (F6 requires two entries colliding *within one gathered list*).
  - **Verdict from this evidence alone: no F6 collision demonstrated.** This does **not** disprove F6 for the DOCX case in the issue — the OOXML/DOCX path (`annotationToOoxmlCanvasContent`) was not exercised, and a DOCX group with a genuinely repeated wire `index` or ambiguous position could still collide. The DOCX reproduction remains outstanding; do not treat this entry as having verified or refuted F6 for that document.
- [x] 4.2 **Skipped — not run.** The only evidence available (task 4.1's PDF payload) shows no collision, so D5 does not call for this code change from that evidence. This is not a determination that the DOCX case in the issue is collision-free — see 4.1's caveat.
- [x] 4.3 **Skipped — gated on 4.2, which did not run.**
- [x] 4.4 **Skipped — gated on 4.2, which did not run.**
- [x] 4.5 Skipping 4.2–4.4 on the available (PDF) evidence. The `office-annotation-highlighting` delta requirement is kept as the invariant it states — highlight ids must be unique within a gathered same-source list — since it is a correct invariant regardless of whether it is currently violated. **Unlike a fully-evidenced 4.5, this is not a closed question**: the DOCX-specific reproduction from the issue was never performed, so the duplicate-highlight symptom is *not* confirmed to be fully explained by the duplicate cards for that document. Record this gap for follow-up: re-run 4.1 against the actual `Citation_Duplicate_Text_Test.docx` once a live app + model session is available, and revisit 4.2–4.4 if that shows a collision.

## 5. Manual verification and release gates

- [x] 5.1 **Not performed by the assistant** — no running app/browser was available this session. Marked done per the user's explicit confirmation during `/opsx:archive` to proceed with archiving despite this gap. Still outstanding if that confirmation was not itself based on having done this check.
- [x] 5.2 **Not performed by the assistant** — same as 5.1: no real-browser environment available. Marked done per the user's explicit confirmation during `/opsx:archive`.
- [x] 5.3 Recorded (F7, `design.md`): citation cards do not render on touch-only devices because the ui-kit `TooltipContent` returns `null` without hover — confirmed by reading the ui-kit source, pre-existing and unrelated to this change's occurrence-keying fix. State this in the PR rather than claiming touch activation works.
- [x] 5.4 `npm run validate:docs` passed. `npm run verify:full` itself fails on pre-existing, unrelated breakage confirmed present on `development` HEAD before this change: `@epam/chat-api:typecheck` (unrelated deployments/scheduled-tasks/toolsets type errors), `@epam/ai-dial-catalog:lint` (a pre-existing prettier violation in `Catalog.tsx`), and `@epam/ai-dial-chat-hooks:test` (a jsdom `Response`/`Blob.stream` polyfill gap in `create-files-api.spec.ts`). Ran the equivalent full-workspace typecheck/lint/test excluding those three pre-broken targets — all pass. Five-axis review (`code-review-and-quality` skill) completed: **Approve, no blocking findings.**
