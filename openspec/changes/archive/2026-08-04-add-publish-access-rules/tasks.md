## 1. Backend contract: publish DTOs gain `rules`, config-registry gains the source list

- [x] 1.1 Create `apps/chat-api/src/publish/dto/publish-rule.dto.ts` exporting `PublishRuleFunction` enum (`Equal = 'EQUAL'`, `Contain = 'CONTAIN'`, `Regex = 'REGEX'`) and `PublishRuleDto` class (`source: string` with `@IsString`/`@IsNotEmpty`/`@MaxLength(200)`; `function: PublishRuleFunction` with `@IsEnum`; `targets: string[]` with `@IsArray`/`@ArrayMinSize(1)`/`@ArrayMaxSize(20)`/`@IsString({ each: true })`/`@IsNotEmpty({ each: true })`/`@MaxLength(200, { each: true })`), each field annotated with `@ApiProperty`, following `apps/chat-api/AGENTS.md` DTO conventions.
- [x] 1.2 Add `rules?: PublishRuleDto[]` to `apps/chat-api/src/conversations/dto/publish-conversation.dto.ts` (`@IsOptional`, `@IsArray`, `@ArrayMaxSize(20)`, `@ValidateNested({ each: true })`, `@Type(() => PublishRuleDto)`, `@ApiPropertyOptional({ type: [PublishRuleDto] })`).
- [x] 1.3 Add the identical `rules?: PublishRuleDto[]` field to `apps/chat-api/src/publish/dto/publish-catalog-entity.dto.ts`.
- [x] 1.4 In `apps/chat-api/src/conversations/conversation-publish.service.ts`, change the `requestBody.rules` line (currently `rules: []` at line 104) to `rules: dto.rules ?? []`, threading `rules` through the service's existing parameter list (mirroring how `folderPath`/`author` are already passed).
- [x] 1.5 In `apps/chat-api/src/publish/publish.service.ts`, make the equivalent change to `requestBody.rules` (currently `rules: []` at line 99).
- [x] 1.6 Update `apps/chat-api/src/conversations/conversation-publish.controller.ts` and `apps/chat-api/src/publish/publish.controller.ts` to destructure `rules` from the request body and pass it through to the corresponding service call.
- [x] 1.7 Add a `publish.publicationFilterSources` entry to `apps/chat-api/src/app-config/config-registry/config-registry.constants.ts` (`type: 'config'`, `valueType: 'json'`, `visibility: 'client'`, `defaultValue: ['title', 'role', 'dial_roles']`, `envVar: 'PUBLICATION_FILTER_SOURCES'`), and declare `PUBLICATION_FILTER_SOURCES` as an optional string in `apps/chat-api/src/config/environment.config.ts`'s `EnvironmentVariables`, following the exact pattern of the existing `CUSTOM_VISUALIZERS`/`ENABLED_UI_FEATURES` entries (comma-separated parsing, empty-string-falls-back-to-default handling).
- [x] 1.8 Add `publicationFilterSources!: string[]` (with `@ApiProperty({ type: [String], example: ['title', 'role', 'dial_roles'] })`) to `ClientConfigDto` in `apps/chat-api/src/app-config/dto/client-config-response.dto.ts`, and wire it through whichever existing service resolves `CONFIG_DEFINITIONS` entries into `ClientConfigDto` fields (the same code path that already populates `fileManagerTabs`/`customVisualizers`).
- [x] 1.9 Write/extend unit tests: `apps/chat-api/src/conversations/tests/conversation-publish.service.spec.ts` and `apps/chat-api/src/publish/tests/publish.service.spec.ts` (rules passed through unchanged, `rules` defaults to `[]` when omitted); DTO validation tests for `PublishRuleDto`/`PublishConversationDto`/`PublishCatalogEntityDto` covering every rejection scenario in `specs/catalog-publish-api/spec.md` and `specs/conversation-publish-api/spec.md` (invalid enum, empty source, empty targets array, empty target string, >20 rules, >20 targets, >200-char string, malformed nested object, unrecognized-but-well-formed source is accepted); config-registry test asserting the new entry's shape and default-fallback behavior; `ClientConfigDto`/client-config controller test asserting `publicationFilterSources` appears in the response with the correct default and operator-configured value.
- [x] 1.10 Verify: `npm exec nx test @epam/chat-api`, `npm exec nx lint @epam/chat-api`, `npm exec nx build @epam/chat-api`.
- [x] 1.11 Regenerate and check the OpenAPI contract: `npm run openapi`, `npm run openapi:check`, then `npm exec nx build chat-api-client` and `npm exec nx lint chat-api-client` — confirm `PublishConversationDto`/`PublishCatalogEntityDto`/`PublishRuleDto`/`PublishRuleFunction`/`ClientConfigDto` all appear correctly typed in the generated client, and that `operationId`s `publishConversation`/`publishCatalogEntity`/`getClientConfig` are unchanged.

## 2. Backend: rules-lookup endpoint (`GET /api/v1/publish/rules`)

- [x] 2.1 Extract the shared target-folder helpers (`public/{folderPath}/` builder, its inverse, resource-type-prefix/name extraction) currently private to `apps/chat-api/src/publish/publish.service.ts` into `apps/chat-api/src/publish/publish-target.util.ts`, per `conversation-publish-api`'s "Shared publish-target utilities are extracted, not duplicated" requirement, and update `publish.service.ts` to import from it with no behavior change.
- [x] 2.2 Create `apps/chat-api/src/publish/dto/publish-rules-result.dto.ts` exporting `PublishRulesResultDto { rules: PublishRuleDto[] }` (reusing `PublishRuleDto` from task 1.1), and a query DTO (`folderPath: string`, `@IsString`/`@IsValidFilePath`) for the new endpoint.
- [x] 2.3 Create `apps/chat-api/src/publish/publish-rules.service.ts`: `getRules(accessToken, folderPath)` builds `url: "public/{folderPath}/"` via `publish-target.util.ts`, calls `this.dialClient.client.getPublicationRules({ headers, body: { url } })`, decodes the response's `rules` map keys, and returns only the entry whose decoded/normalized key equals the requested `folderPath` (`[]` if absent) — discarding every ancestor-folder entry.
- [x] 2.4 Create `apps/chat-api/src/publish/publish-rules.controller.ts` exposing `GET /api/v1/publish/rules?folderPath=...` (operationId `getPublishRules`), thin controller per `apps/chat-api/AGENTS.md`, registered in `apps/chat-api/src/publish/publish.module.ts`.
- [x] 2.5 Write unit tests `apps/chat-api/src/publish/tests/publish-rules.service.spec.ts` covering every scenario in `specs/publish-rules-lookup-api/spec.md` (folder with rules, folder without rules returns `[]` not 404, ancestor-folder entries discarded, invalid folder path rejected with 400, upstream failure mapped, unauthenticated rejected with 401).
- [x] 2.6 Verify: `npm exec nx test @epam/chat-api`, `npm exec nx lint @epam/chat-api`, `npm exec nx build @epam/chat-api`.
- [x] 2.7 Regenerate and check the OpenAPI contract: `npm run openapi`, `npm run openapi:check`, `npm exec nx build chat-api-client`, `npm exec nx lint chat-api-client` — confirm `getPublishRules`/`PublishRulesResultDto` appear correctly typed in the generated client.

## 3. Shared controlled editor in `libs/publish-panel`

- [x] 3.1 Add `PublicationRuleFunction` enum and `PublicationRule` interface to `libs/publish-panel/src/models/publish.ts`, each member/field with JSDoc per `.claude/rules/libs.md`.
- [x] 3.2 Extend `libs/publish-panel/src/utils/use-publish-flow.ts`: add `rules`/`setRules`/`isRulesLoading`/`hasRulesLoadError` state (parallel to `selectedFolderPath`), change `UsePublishFlowOptions.onPublish` to `(item: TItem, folderPath: string[], rules: PublicationRule[]) => Promise<void>`, add an optional `onFetchExistingRules?: (folderPath: string[]) => Promise<PublicationRule[]>` option, pass `rules` through in `handleSubmit`, add a `useEffect` keyed on `selectedFolderPath` that calls `onFetchExistingRules` (when provided and a folder is selected) and overwrites `rules` with the result on success or sets `hasRulesLoadError` on failure (leaving `rules` unchanged), reset `rules` to `[]` when `selectedFolderPath` becomes `undefined` or `reset()` is called. Update `libs/publish-panel/src/utils/use-publish-flow.spec.ts` to cover: rules are forwarded to `onPublish`; `reset()` clears rules; `setRules` updates state independent of folder selection; folder-selection triggers `onFetchExistingRules` and overwrites `rules` on success; a failed fetch sets `hasRulesLoadError` and leaves `rules` unchanged; deselecting the folder clears `rules` to `[]`; omitting `onFetchExistingRules` skips the fetch entirely (backward-compatible for any other consumer).
- [x] 3.3 Create `libs/publish-panel/src/components/PublishAccessRuleEditor/PublishAccessRuleEditor.tsx` — the single-rule editor: source `Select` (options from `ruleSourceOptions`, `searchable` with matched text rendered via `Highlight` from `@epam/ai-dial-ui-kit`), function `Select` (`EQUAL`/`CONTAIN`/`REGEX`), a `TagInput`-based multi-target field for `EQUAL`/`CONTAIN` (trim on add, reject exact-duplicate trimmed target), and a single-value text field for `REGEX` with native `RegExp` try/catch validation and a localized inline error tied via `aria-describedby`. Save disabled until structurally complete. Desktop renders inline; mobile renders as a full-screen step using responsive Tailwind classes only (no `apps/chat` breakpoint hook import). Escape cancels only the in-progress rule. Props named `PublishAccessRuleEditorProps` per `.claude/rules/libs.md`.
- [x] 3.4 Create `libs/publish-panel/src/components/PublishAccessRules/PublishAccessRules.tsx` — the chip list: renders `rules` as removable chips (`source`, translated `function` label, `targets` joined with an "Or" separator), "Add rule" trigger opening `PublishAccessRuleEditor`, "Clear all" (shown only when `rules.length > 0`), a shared `aria-live="polite"` `role="status"` region announcing add/remove/clear/pre-fill-from-folder-selection, a non-blocking loading indicator wired to `isRulesLoading` and a non-blocking inline notice wired to `hasRulesLoadError` (neither disables "Add rule"), and a `disabled` prop (wired to `isSubmitting`) that disables every control in the section. Enforces the 20-rule / 20-target-per-rule UI ceilings from design.md D6.
- [x] 3.5 Wire `PublishAccessRules` into `libs/publish-panel/src/components/PublishPanel/PublishPanel.tsx` between the folder-selection block (after the current line ~257) and the history section (before the current line ~259); add `rules: PublicationRule[]`, `onRulesChange: (rules: PublicationRule[]) => void`, `ruleSourceOptions: string[]`, `isRulesLoading?: boolean`, `hasRulesLoadError?: boolean` as `PublishPanelProps`, and the new label fields to `PublishPanelLabels`. Thread the same props through `libs/publish-panel/src/components/PublishPanel/StandalonePublishPanel.tsx`'s props down to `PublishPanel`.
- [x] 3.6 Update `libs/publish-panel/src/index.ts` to export `PublicationRule`, `PublicationRuleFunction`, `PublishAccessRules`, `PublishAccessRuleEditor`, and their prop/label interfaces.
- [x] 3.7 Component tests: `libs/publish-panel/src/components/PublishAccessRuleEditor/tests/PublishAccessRuleEditor.spec.tsx` and `libs/publish-panel/src/components/PublishAccessRules/tests/PublishAccessRules.spec.tsx` covering every scenario in `specs/publish-access-rules-editor/spec.md` (add/remove/clear, EQUAL/CONTAIN target validation and dedup, REGEX validation, disabled-during-submit, 20-item ceilings, aria-live announcements including the pre-fill announcement, Escape-cancels-in-progress-rule, keyboard operability of remove/add/clear, loading/error indicator visibility without blocking "Add rule"). Update `libs/publish-panel/src/components/PublishPanel/tests/PublishPanel.spec.tsx` and `.../StandalonePublishPanel.spec.tsx` for the new required props and reset-on-close behavior (rules clear when `reset()`/close fires).
- [x] 3.8 Verify: `npm exec nx test @epam/ai-dial-publish-panel`, `npm exec nx lint @epam/ai-dial-publish-panel`, `npm exec nx typecheck @epam/ai-dial-publish-panel`, `npm exec nx build @epam/ai-dial-publish-panel`.

## 4. Conversation publish integration

- [x] 4.1 Add `publicationFilterSources: string[]` to `AppConfigState.config` in `apps/chat/src/context/AppConfigContext.tsx`, a `DEFAULT_PUBLICATION_FILTER_SOURCES = ['title', 'role', 'dial_roles']` constant, initial-state/fallback wiring, and the `loadConfig` mapping line. Update `apps/chat/src/context/tests/AppConfigContext.spec.tsx` for the new field and its fetch-failure fallback.
- [x] 4.2 Update `apps/chat/src/server-api/conversation-publish.api.ts`'s `publishConversation` to accept and forward a `rules: PublicationRule[]` parameter into the generated client call's request body.
- [x] 4.3 Create `apps/chat/src/server-api/publish-rules.api.ts` exporting `getPublishRules(folderPath: string): Promise<PublicationRule[]>`, calling the generated client's `getPublishRules({ folderPath })` and returning `response.rules`.
- [x] 4.4 Add new i18n keys to a `PublishAccessRulesI18nKeys` enum in `apps/chat/src/constants/translation-keys.ts` (section heading, add/clear/remove labels, "Or" separator, source/function/target field labels and placeholders, invalid-regex error, duplicate-target error, rules-loading label, rules-load-error notice) with English values in `apps/chat/src/i18n/locales/en.json` — reuse `ButtonsI18nKeys` for any generic label (e.g. Cancel) that already exists rather than duplicating it.
- [x] 4.5 In `apps/chat/src/components/PublishConversationPanelContainer/PublishConversationPanelContainer.tsx`: read `ruleSourceOptions` from `useAppConfig().config.publicationFilterSources`; pass `rules`/`setRules`/`isRulesLoading`/`hasRulesLoadError` (from `usePublishFlow`) and `ruleSourceOptions` into `StandalonePublishPanel`; supply `usePublishFlow`'s `onFetchExistingRules` option as a call to `getPublishRules` (task 4.3); update the `onPublish` closure to accept the third `rules` argument and forward it to `publishConversation`; add the new `PublishAccessRulesI18nKeys`-sourced strings to `panelLabels`/`labels`.
- [x] 4.6 Update `apps/chat/src/server-api/tests/conversation-publish.api.spec.ts`, a new `apps/chat/src/server-api/tests/publish-rules.api.spec.ts`, and `PublishConversationPanelContainer`'s existing test suite for the new `rules`/`onFetchExistingRules` wiring (rules reach the API call; empty rules send `[]`; rules reset when the panel closes; source options come from `useAppConfig`; selecting a folder triggers `getPublishRules` and pre-fills; a lookup failure does not block submission).
- [x] 4.7 Verify: `npm exec nx test @epam/chat`, `npm exec nx lint @epam/chat`, `npm exec nx typecheck @epam/chat`.

## 5. Catalog (application/toolset) publish integration

- [x] 5.1 Update `apps/chat/src/server-api/publish.api.ts`'s `publishCatalogEntity` to accept and forward a `rules: PublicationRule[]` parameter into the generated client call's request body.
- [x] 5.2 Add `ruleSourceOptions?: string[]` and `onFetchExistingRules?: (folderPath: string[]) => Promise<PublicationRule[]>` to `CatalogProps` in `libs/catalog/src/models/catalog-props.ts`, and thread `rules`/`onRulesChange`/`ruleSourceOptions`/`isRulesLoading`/`hasRulesLoadError` from `CatalogProps` down through `Catalog` into `DetailsPanel`'s existing `PublishPanel` render (`libs/catalog/src/components/Details/DetailsPanel.tsx:329-366`), using `DetailsPanel`'s own `usePublishFlow` instance (supplied with `onFetchExistingRules` from `CatalogProps`) for `rules`/`setRules`/`isRulesLoading`/`hasRulesLoadError`.
- [x] 5.3 In `apps/chat/src/components/CatalogView/CatalogView.tsx`: read `ruleSourceOptions` from `useAppConfig().config.publicationFilterSources`, pass it and an `onFetchExistingRules` calling `apps/chat/src/server-api/publish-rules.api.ts`'s `getPublishRules` into `<Catalog>`'s new props, and update `handlePublish` (`CatalogView.tsx:503-515`) to accept the third `rules` argument (now supplied by `usePublishFlow`'s extended `onPublish` signature) and forward it to `publishCatalogEntity`.
- [x] 5.4 Update `libs/catalog/src/components/Details/tests/DetailsPanel.spec.tsx` (or equivalent) and `apps/chat/src/server-api/tests/publish.api.spec.ts`/`CatalogView`'s existing test suite for the new `rules`/`onFetchExistingRules` wiring, mirroring the conversation-flow test additions in task 4.6.
- [x] 5.5 Verify: `npm exec nx test @epam/ai-dial-catalog`, `npm exec nx lint @epam/ai-dial-catalog`, `npm exec nx typecheck @epam/ai-dial-catalog`, `npm exec nx test @epam/chat`, `npm exec nx lint @epam/chat`.

## 6. Accessibility, i18n, and RTL verification pass

- [x] 6.1 Confirm every string introduced in tasks 3–5 is routed through `PublishPanelLabels`/i18n (no hardcoded English in `libs/publish-panel`, no raw string literal passed to `t()` in `apps/chat`) and that duplicate-value keys reuse `ButtonsI18nKeys` per `.claude/rules/all-ts.md`'s i18n rule.
- [x] 6.2 Manually re-derive RTL correctness by grepping the new component files for physical-direction classes (`ml-`, `mr-`, `pl-`, `pr-`, `left-`, `right-`, `text-left`, `text-right`) and confirm none were introduced; confirm the chip-remove icon is not mirrored and no directional icon was added without a `rtl:scale-x-[-1]` counterpart.
- [x] 6.3 Add/extend accessibility-focused test assertions (`aria-live` region announces add/remove/clear/pre-fill; remove/add/clear controls have accessible names; regex error `aria-describedby` association; Escape cancels only the in-progress rule, not the whole panel; focus management when the rule editor opens/closes; loading/error indicators are announced without being intrusive) across the component tests from task 3.7 — these are additive assertions on the same spec files, not a new test file.
- [x] 6.4 Verify: `npm exec nx test @epam/ai-dial-publish-panel`, `npm exec nx lint @epam/ai-dial-publish-panel`.

## 7. Final regression verification

- [x] 7.1 Run the full affected suite against the base branch: `npm exec nx affected --target=lint --base=origin/development-1.0`, `npm exec nx affected --target=test --base=origin/development-1.0`, `npm exec nx affected --target=typecheck --base=origin/development-1.0`, `npm exec nx affected --target=build --base=origin/development-1.0`.
- [x] 7.2 Re-run `npm run openapi:check` to confirm the committed OpenAPI spec matches the final DTO/endpoint shapes after all slices.
- [x] 7.3 Confirm no regression in existing conversation/catalog publish behavior by re-reading the existing scenarios in `openspec/specs/conversation-publish-flow/spec.md`, `openspec/specs/catalog-publish-flow/spec.md`, `openspec/specs/conversation-publish-api/spec.md`, and `openspec/specs/catalog-publish-api/spec.md` against the updated test suites — folder selection, replace/no-replace submit-button gating, loading/error callouts, and successful-publish panel-close/notification behavior must all still pass unchanged.
- [x] 7.4 Confirm all delta spec files under this change's `specs/` directory accurately reflect what was actually implemented (adjust wording if implementation diverged from the plan, e.g. final file names for `PublishAccessRuleEditor`/`PublishAccessRules`/`publish-rules.service.ts`) — the specs are finalized/merged into `openspec/specs/*` at archive time via `/opsx:archive`, not here.
