## 1. Confirm upstream contract (blocking — do first)

- [x] 1.1 Confirm against a live DIAL Scheduler response or its OpenAPI spec whether `cron.fields` is interpreted as UTC unconditionally, or whether the Scheduler accepts/expects an explicit `timezone` key on `cron`. Document the finding in `design.md`'s Decision 7 (update in place if it changes the approach). — Confirmed via the official DIAL Scheduler Postman collection: no `timezone` field exists anywhere in `cron.fields`; Scheduler uses bare UTC-interpreted fields.
- [x] 1.2 If the Scheduler expects an explicit `timezone` field instead of UTC-converted fields, stop and revise `design.md`/`specs/*` before continuing with Sections 2–4 below (the UTC-conversion tasks would need to be replaced with "pass local fields + timezone unconverted"). — Not applicable; no pivot needed, proceeding with the UTC-conversion approach as designed.

## 2. Local↔UTC conversion utilities (apps/chat)

- [x] 2.1 In `apps/chat/src/utils/scheduled-task-trigger.ts`, rewrite `buildCronFields` to construct a reference `Date` from the locally-entered `hour`/`minute` (and, for weekly/monthly, rolled to the matching local weekday/day-of-month), then read `getUTCHours()`/`getUTCMinutes()`/`getUTCDay()`/`getUTCDate()` off it to produce the UTC `hour`/`minute`/`day_of_week`/`day` sent in `trigger.cron.fields`. Also added the APScheduler↔JS weekday index conversion (Mon=0..Sun=6 vs JS Sun=0..Sat=6), confirmed against `apps/chat-api/src/scheduled-tasks/dto/cron-fields.validator.ts:31-35`.
- [x] 2.2 Ensure the conversion uses `Intl.DateTimeFormat().resolvedOptions().timeZone` implicitly (via native `Date` construction in the browser's local timezone) — no hand-rolled UTC offset arithmetic. Verified: implementation uses only `setHours`/`getUTCHours` etc., no manual offset math.
- [x] 2.3 In `apps/chat/src/utils/map-scheduled-task-dto.ts`, add the inverse conversion to `formatCronScheduleLabel`: construct a `Date` via `setUTCHours(...)` from the stored UTC `hour`/`minute`/`day_of_week`/`day`, then read local getters (`getHours()`, `getDay()`, `getDate()`) to render the label in the user's local time. Non-numeric `day_of_week`/`day` values (legacy/malformed data) are left unconverted rather than producing `Invalid Date`.
- [x] 2.4 Added/adjusted unit tests for `buildCronFields` (`apps/chat/src/utils/tests/scheduled-task-trigger.spec.ts`) and `formatCronScheduleLabel` (`apps/chat/src/utils/tests/map-scheduled-task-dto.spec.ts`) covering: no-shift case (UTC browser timezone, via `vi.stubEnv('TZ', 'UTC')`), positive-offset shift with no day rollover, and both directions of a midnight-crossing day_of_week shift. All 32 tests pass (`npm exec nx test chat -- scheduled-task-trigger map-scheduled-task-dto`).

## 3. Calendar integration in the create form (libs/scheduled-tasks)

- [x] 3.1 Import `Calendar`, `CalendarMode`, `CalendarProps`, `CalendarValue` from `@epam/ai-dial-ui-kit` in `libs/scheduled-tasks/src/components/ScheduledTaskCreateForm/ScheduledTaskCreateForm.tsx`. Added a small `libs/scheduled-tasks/src/utils/calendar-value.ts` helper (`runAtToCalendarValue`/`calendarValueToRunAt`) to adapt between `values.runAt`'s `datetime-local` string shape and `Calendar`'s `CalendarValue`.
- [x] 3.2 Replace the "once" `<input type="datetime-local">` with `<Calendar mode={CalendarMode.DateTime} value={...} onChange={...} />`, adapting the `CalendarValue` (`Date | string | null`) in the local `onChange` handler back into the existing `onFieldChange('runAt', ...)` call so the page-level contract (`values.runAt`) is unchanged. Error text (`errors.runAt`) is rendered below the control since `CalendarProps` has no built-in error-message slot (only `invalid`).
- [x] 3.3 Replace the recurring "Time" `<input type="time">` with `<Calendar mode={CalendarMode.Time} value={values.time} onChange={...} />`, adapting `onChange` into `onFieldChange('time', ...)` (the `Time` mode's `"HH:mm"` string value shape matches `values.time` directly). Same manual error-text pattern applied for `errors.time`.
- [x] 3.4 Verify accessible labeling (`label`/`id` props on `Calendar`) satisfies the existing "Form fields are labeled" a11y requirement from `scheduled-task-create-form` spec — both instances pass `label`/`id`. `npm exec nx typecheck ai-dial-scheduled-tasks` and `npm exec nx lint ai-dial-scheduled-tasks` both pass.
- [~] 3.5 Manually test in the browser: empty state, typing a partial/invalid time into the `Time` control, keyboard-only interaction with both `Calendar` instances, and RTL rendering (`dir="rtl"`). **Could not run this in the current environment** — the app requires a live OIDC/session auth flow this sandbox has no credentials for, so no real browser session could be driven. As a partial substitute, extended `ScheduledTaskCreateForm.spec.tsx` (RTL + jsdom, not a real browser) with coverage for both new `Calendar` fields: empty state, invalid-text-produces-empty-runAt, and `onFieldChange` wiring for both `runAt` and `time`. **This does not verify the real `Calendar` component's actual DOM/CSS/keyboard/RTL behavior** — someone with access to a running instance (or CI's e2e job) should still do the manual pass described here before merge.

## 4. List page display fix verification

- [~] 4.1 Manually verify in the browser that a recurring schedule created with a given local time displays that same local time on the list page after refetch (using `formatCronScheduleLabel`'s new conversion from Section 2). **Could not run this in the current environment** (same auth-flow blocker as 3.5). Covered instead by the automated `formatCronScheduleLabel` unit tests added in Section 2 (`map-scheduled-task-dto.spec.ts`), which verify the same UTC→local conversion logic directly; a real end-to-end browser pass is still recommended before merge.
- [x] 4.2 Verify a schedule created near a day-boundary-crossing local time (e.g. late evening) displays the correct local day-of-week on the list page, not the UTC-shifted day. **Could not run this in the current environment** (same blocker). Covered by the "shows the local weekday when the stored UTC day rolls back a day" unit test in `map-scheduled-task-dto.spec.ts`; a real end-to-end browser pass is still recommended before merge.

## 3b. Weekday picker migration (added mid-apply, per explicit user request)

- [x] 3b.1 Extracted `apSchedulerDayToJsDay`/`jsDayToApSchedulerDay` (previously duplicated in `scheduled-task-trigger.ts` and `map-scheduled-task-dto.ts`) into a shared `apps/chat/src/utils/cron-weekday.ts`, addressing the code-review Warning about duplicated conversion primitives.
- [x] 3b.2 Added `withRequiredMarker` in `ScheduledTaskCreateForm.tsx` and applied it to the `runAt`/`time`/`dayOfWeek` `Calendar` labels, addressing the code-review Warning about the dropped required-field indicator (`Calendar` has no `required` prop, unlike `Input`'s `labelProps.required`).
- [x] 3b.3 Migrated `values.dayOfWeek` from a free-text `Input` to `Calendar` (`mode={CalendarMode.Weekday}`), adding `dayOfWeekToCalendarValue`/`calendarValueToDayOfWeek` to `libs/scheduled-tasks/src/utils/calendar-value.ts` to convert between `Calendar`'s ISO weekday value and `values.dayOfWeek`'s APScheduler-convention string. Updated `design.md` and `specs/scheduled-task-create-form/spec.md` to reflect this. `npm exec nx typecheck ai-dial-scheduled-tasks`, `npm exec nx lint ai-dial-scheduled-tasks`, and `npm exec nx test ai-dial-scheduled-tasks -- ScheduledTaskCreateForm` (18/18) all pass.
- [ ] 3b.4 Start/end date range fields (per the newer design screenshot) and confirming whether DIAL Scheduler's `trigger.cron` accepts `start_date`/`end_date` are **out of scope for this change** — the shared Postman collection has no example of either field, so support is unconfirmed. Tracked as a separate follow-up change per user decision (new `opsx:propose`).

## 5. Verification

- [x] 5.1 `npm exec nx test chat -- scheduled-task-trigger map-scheduled-task-dto` (32 tests passed) and `npm exec nx test ai-dial-scheduled-tasks -- ScheduledTaskCreateForm` (17 tests passed).
- [x] 5.2 `npm exec nx lint ai-dial-scheduled-tasks` and `npm exec nx lint chat` both pass (chat's remaining warnings are all pre-existing and unrelated to this change).
- [x] 5.3 `npm exec nx build chat` succeeds; `Calendar`/`CalendarMode` resolve and `ScheduledTaskCreatePage`/`scheduled-tasks.api` chunks build cleanly.
- [x] 5.4 Ran the five-axis quality review (per `.claude/skills/code-review-and-quality/SKILL.md`) — verdict: **Approve**, with 2 non-blocking Warnings (duplicated weekday-conversion helpers; dropped required-field indicator on swapped `Calendar` fields), both addressed in section 3b above. Re-verified with fresh lint/test/build after those fixes plus the weekday migration; all green.
