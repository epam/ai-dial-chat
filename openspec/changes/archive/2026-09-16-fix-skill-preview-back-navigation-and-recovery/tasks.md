## 1. Diagnosis gate — reproduce and attribute both failures before changing code

Every step here is browser work; label each result as a browser check, and keep **isolated** CSS reproduction distinct from **application-level** reproduction of issue #8842 — they are not interchangeable evidence.

The CSS *mechanism* is now browser-confirmed in isolation (see `design.md`'s "Evidence update" and `notes.md`): at 1440px the installed vendor stylesheet collapses the desktop Files panel and header actions to `display: none`, and the same sheet inside `@layer pdf-vendor` preserves `block`/`flex`; 768px is unaffected in all three states. That evidence is what group 5 proceeds on. It does **not** close 1.3, 1.4, or 1.6, which are application scenarios, and it does not promote H1 to a confirmed application-level root cause.

- [ ] 1.1 Start the app (`npm run start:all`) and reproduce case 1: open a skill in the editor, select a supporting PDF, activate the header Back arrow. Record whether it leaves the editor, whether the unsaved-changes prompt appears, and where it lands — for a clean form and for a dirty one.
- [ ] 1.2 Repeat 1.1 with a **successful** preview and with a preview still loading. Confirm C1's prediction that Back exits in every preview state, independent of any error.
- [ ] 1.3 (application-level, still pending) Establish the control: preview a supporting PDF that renders successfully, return to `SKILL.md` by re-selecting it in the Files tree, leave the editor, and open another skill. Record the layout (DevTools computed `display` for the desktop Files panel at `SkillEditor.tsx:422` and the header actions at `EditorLayout.tsx:73`) at a desktop width. **H1 predicts the layout is already damaged here, with no failure involved** — if it is intact, H1 is disproved and 1.6 decides the branch.
- [ ] 1.4 (application-level, still pending) Run H1's second prediction: in a fresh session, open a PDF attachment in a **conversation**, then go to the Skill Editor without touching a preview. Record the same computed values. Intact layout disproves H1.
- [ ] 1.5 For each failure mode — induced download fault, induced worker fault (block `configurePdfWorker`'s asset), induced render fault (corrupt PDF bytes) — record whether the `PdfContent` chunk and `@epam/ai-dial-react-pdf-highlighter/styles.css` actually load (Network + `document.styleSheets`), and the resulting preview state. Note explicitly that per C5 the editor's in-memory path always reaches `AttachmentContentType.Pdf`, so a resolver-level download fault is not reachable there.
- [ ] 1.6 (application-level, still pending — the isolated check confirms only the mechanism) In the damaged state, identify in DevTools which stylesheet rule wins for the desktop Files panel and the header actions, and confirm/deny that a vendor `.hidden { display: none }` outranks the app's `desktop:block`/`desktop:flex`. Record the verdict on H1 and, if H1 fails, the observed region and winning rule instead.
- [ ] 1.7 Trace the preview-state defects independently of H1: rapid A→B selection with a throttled resolve (C2), the same relative path across two skills and two buckets (C4), and a failed open leaving the pane spinning (C3). Record each as confirmed or not, and whether any of them — not the cascade — accounts for the persistent symptom.
- [ ] 1.8 Write the findings into this change's notes as two separate root-cause statements, one for navigation and one for the persistent layout failure, each labelled confirmed-in-browser or still-hypothesis, and state which of design.md's branches (D5 vs. the H2/H3 branch, D3's shared provider vs. its isolation fallback) the implementation takes.

## 2. Regression coverage for today's behavior (red before green)

- [x] 2.1 Add failing tests to `apps/chat/src/pages/SkillEditor/tests/SkillEditor.spec.tsx` for Back while a supporting file is selected: it must not navigate and must not prompt, for clean and dirty forms.
- [x] 2.2 Add failing tests for dirty preservation across a preview round trip: edit Description, upload a file, select the file, activate Back, assert the Description value, the Files list, and the still-dirty state.
- [x] 2.3 Add passing tests that pin what must not change: Back from `SKILL.md` (prompt when dirty, immediate `returnUrl` navigation when clean) and Cancel from both the `SKILL.md` view and a supporting-file preview (prompt when dirty, exit either way).
- [x] 2.4 Create `apps/chat/src/hooks/attachment/tests/useSkillFilePreviewSync.spec.ts` with failing cases for the late-completion orderings the existing spec already requires: A resolves after B is selected; A resolves after returning to `SKILL.md`; A resolves after the hook unmounted.
- [x] 2.5 Add failing tests for resource-scoped identity: the same relative path in two skills, and in two buckets, must not be treated as the same canvas selection.
- [x] 2.6 Add failing tests to `SkillEditorPreview.spec.tsx` for a failed open: an error with a retry control is rendered and no indefinite loading state remains.
- [x] 2.7 Verify the new suites fail for the expected reasons: `npm run test:file -- apps/chat/src/pages/SkillEditor/tests/SkillEditor.spec.tsx`, `... SkillEditorPreview.spec.tsx`, `... apps/chat/src/hooks/attachment/tests/useSkillFilePreviewSync.spec.ts`.

## 3. Navigation fix (D1)

- [x] 3.1 In `apps/chat/src/pages/SkillEditor/SkillEditor.tsx`, add a `handleBack` that returns `selectedPath` to `SKILL_MANIFEST_FILE` when a supporting file is selected and otherwise delegates to `handleCancel`; wire `onBack={handleBack}` and leave `onCancel={handleCancel}` unchanged. Memoise with `useCallback` over `selectedPath` and `handleCancel`.
- [x] 3.2 Add the `skillEditor.backToManifestAriaLabel` key to `apps/chat/src/i18n/locales/en.json` and `SkillEditorI18nKeys`, and pass it as `backAriaLabel` while a supporting file is selected.
- [x] 3.3 Announce the return to `SKILL.md` through a polite `role="status"` region. *(Implemented. Focus retention across the transition is **not** verified — it needs a browser; tracked by 3.6.)*
- [x] 3.4 Confirm no library change was needed: `libs/skill-editor` and `libs/builder-form` still forward Back to the host callback, and `skill-editor-library`'s documented contract is untouched.
- [x] 3.5 Green 2.1–2.3: `npm run test:file -- apps/chat/src/pages/SkillEditor/tests/SkillEditor.spec.tsx`.
- [ ] 3.6 Browser-verify Back in all three preview states (loading, rendered, failed), at desktop and mobile widths, by mouse and by keyboard, and in an RTL locale (`ar`) to confirm the mirrored glyph and the changed accessible name.

## 4. Preview state correctness (D2, D3, D4)

- [x] 4.1 Derive a resource-scoped canvas id in the page (bucket + skill path or a stable create-mode marker + relative path) and pass the same value to `useSkillFilePreviewSync` and to `SkillFilePreview`. Leave `skillFileToAttachment`'s `Attachment.id` as the relative path.
- [x] 4.2 Check request generation and ownership at the **canvas commit boundary** (`design.md` D3): number requests monotonically in `useSkillFilePreviewSync`, pass a per-request `shouldCommit` guard into `openAttachmentCanvas`, and have `libs/attachment-canvas` consult it immediately before every post-await canvas write. A stale request writes nothing, closes nothing, reports no failure, and releases its payload's object URL. Retire all in-flight requests on unmount.
- [x] 4.3 Replace `SkillFilePreview`'s `isLoading={!isCurrent || isLoading}` with explicit Loading/Ready/Error states from a `SkillFilePreviewState` string enum; have the hook record the failed id and expose a retry that re-runs the open for the current selection, clearing the failure on selection change or retry.
- [x] 4.4 Render the failure in a `role="alert"` region with a real `<button>` retry, reusing `attachmentCanvas.loadErrorLabel` and `buttons.retry`; add a Skill-specific key only if the generic message reads wrong in context.
- [x] 4.5 Enumerate and update every consumer of `SkillFilePreview` for the changed props; confirm the catalog's isolated `SkillDetailsFilePreview` path is unaffected.
- [x] 4.6 Green 2.4–2.6: `npm run test:file -- apps/chat/src/hooks/attachment/tests/useSkillFilePreviewSync.spec.ts` and `... apps/chat/src/pages/SkillEditor/tests/SkillEditorPreview.spec.tsx`.
- [ ] 4.7 Browser-verify rapid file switching with a throttled resolve, a failed preview plus retry, and the same relative path across two skills and two buckets.
- [x] 4.9 Add the `ShouldCommitCanvas` argument to `libs/attachment-canvas`'s public contract: export the type, document it in the lib README, cover it in the lib's own spec (guard discards the write, does not close another owner's canvas, releases the discarded object URL, is evaluated at commit time, and is a no-op when omitted), and record it as a MODIFIED requirement in `specs/attachment-canvas-workflow/spec.md`.
- [x] 4.10 Guard against the re-entrancy the failure path introduces: committing a failure closes the canvas, which re-runs the reconciliation effect before the failure is recorded. Track the in-flight id so the effect does not reopen the same file in a loop.
- [x] 4.8 *(Condition not met — not taken.)* Only if 1.7/1.8 showed shared canvas state contributing: take D3's isolation fallback instead of 4.2's guard alone, and add the corresponding MODIFIED requirement to `specs/skill-file-preview/spec.md` for the route-level provider the current spec forbids.

## 5. Style containment (D5) — proceeding on the browser-confirmed isolated CSS reproduction

- [x] 5.1 Replace `PdfContent.tsx`'s two direct vendor CSS imports with a co-located wrapper stylesheet that pulls both in via `@import … layer(pdf-vendor)`.
- [x] 5.2 Build `attachment-canvas` and then `chat`, and inspect the emitted CSS: confirm the layer statement survives the library build with the specifier externalized (`vite-external-matcher.ts`) and that the vendor rules resolve inside the layer in the app's output. If it does not survive, stop and take D5's eager-import fallback, and amend `specs/attachment-canvas-package-loading/spec.md`'s lazy-load requirement in this change rather than leaving it violated.
- [x] 5.3 Add the build-output assertion required by `attachment-preview-style-containment` ("Build output carries the containment mechanism"), alongside the existing package-boundary tests.
- [x] 5.4 Confirm the lazy boundary still holds: no PDF vendor stylesheet is requested until a PDF opens.
- [ ] 5.5 (application-level, still pending) Browser-verify containment: computed `display` of the desktop Files panel and header actions before and after the engine loads, at desktop and mobile widths, in light and dark themes, with the winning rule recorded. Re-run 1.3 and 1.4 and confirm both now pass.
- [ ] 5.6 (application-level, still pending) Browser-verify the PDF viewer itself is undamaged by layering: document render, thumbnails, highlights, and toolbar, in both themes and both layouts, in the conversation canvas and in the Skill Editor.
- [x] 5.7 Apply `.claude/rules/libs.md` and `.claude/rules/lib-styling.md` to the touched `libs/attachment-canvas` files, confirm the `./styles.css` export still matches build output, run `npm run validate:docs`, and record the new vendor-CSS styling tier in `docs/architecture.md`.

## 6. Defence in depth (D6) — not applied

Containment removes the mechanism that made the `hidden desktop:*` pair fragile, and the isolated check confirms the layered sheet preserves both the desktop and the mobile case. Editing four files across two libraries needs evidence that something still gets through **after** containment; there is none. See `design.md` D6.

- [x] 6.1 *(Decided against — see 6.3 and `design.md` D6.)* Replace `hidden desktop:block` / `hidden desktop:flex` with `mobile:hidden desktop:block` / `mobile:hidden desktop:flex` in `libs/builder-form/src/components/EditorLayout/EditorLayout.tsx`, `libs/skill-editor/src/components/SkillEditor/SkillEditor.tsx`, `libs/builder-form/src/components/BuilderFormBody/BuilderFormBody.tsx`, and `libs/builder-form/src/components/BuilderFormHeader/BuilderFormHeader.tsx`.
- [x] 6.2 *(Moot — 6.1 not taken.)* Verify mobile and desktop rendering of every touched surface — the Skill Editor, the builder form, and any other `EditorLayout` consumer — and confirm exactly one presentation shows at each width.
- [x] 6.3 Decide against applying this group on present evidence, and record the reasoning rather than making a cosmetic change. *(Revisit only with post-containment evidence.)*

## 7. Cross-surface regression and final verification

The user ran the change in the running application and confirmed the behavior they exercised works correctly — recorded in `notes.md` as a **user-confirmed application-level smoke check**. No scenario matrix, viewport, or theme was reported, so it closes no individual task below; each stays marked with the evidence it actually has.

- [ ] 7.1 (application-level, still pending) Browser-verify shared-canvas consumers outside the Skill Editor: conversation attachment previews (PDF, image, markdown, code, OOXML, HTML), the sources panel, citation previews, and the catalog's `SkillDetailsFilePreview`.
- [ ] 7.2 (application-level, still pending) Browser-verify the full reported sequences end to end: failed preview → Back → `SKILL.md`; failed preview → exit → reopen the same skill; failed preview → open another skill; failed preview → create a new skill — each with no page reload, at desktop and mobile widths.
- [ ] 7.3 Confirm the preserved #8837 behavior: a large supporting PDF still previews (no 256 KB cap on this path) and the worker request still goes to the app's bundled `pdf.worker.min.mjs`, not a CDN. *(Source inspection done: this change has an empty diff against `libs/chat-hooks` and `apps/chat/src/utils/pdf.ts`, the worker is still `pdfjs-dist/build/pdf.worker.min.mjs?url`, and no size cap exists on this path. The **browser** confirmation is still pending.)*
- [ ] 7.4 Confirm object URLs are still revoked across preview replacement, return-to-manifest, and editor exit — including the new discard path, where content is resolved but never reaches the canvas. *(Unit-level done: the provider still revokes on content replacement and unmount, and a lib test asserts a discarded payload's URL is revoked. The **browser** confirmation is still pending.)*
- [x] 7.5 Run `npm run verify:changed`, then `npm run verify:full` once before completion; run `npm run validate:docs` if any README, `docs/**`, or lib manifest was touched. *(Done. Two pre-existing failures on `development` are unrelated and reproduce with this change stashed — see `notes.md`.)*
- [x] 7.6 Run the five-axis quality review (`.claude/skills/code-review-and-quality/SKILL.md`) including the responsive-parity and documentation-accuracy gates. *(Run. Approve-with-comments on the code; not merge-ready while the browser gate is open. Acted on: the misleading catalog comment, `docs/architecture.md`, the resolver object-URL contract in the lib README, the canvas-key separator comment. Recorded and not closed: the catalog's own indefinite spinner, the missing-bytes path, and the re-upload refresh case — all in `notes.md`.)*
- [x] 7.7 Write the change's final verification note distinguishing, per claim, source inspection vs. mocked test vs. isolated browser check vs. build check vs. application-level check — and state plainly any regression scenario that was only covered by a mocked test. *(`notes.md`.)*
