## 1. Slice 1 — `usePageFileDrag`

- [x] 1.1 Create `libs/chat-hooks/src/usePageFileDrag/usePageFileDrag.ts` by moving `apps/chat/src/hooks/usePageFileDrag.ts` verbatim (unchanged signature: `usePageFileDrag(isAttachmentsAllowed?, isEnabled?)`), keeping the existing JSDoc.
- [x] 1.2 Move `apps/chat/src/hooks/tests/usePageFileDrag.spec.ts` to `libs/chat-hooks/src/usePageFileDrag/tests/usePageFileDrag.spec.ts` unchanged.
- [x] 1.3 Re-export `usePageFileDrag` from `libs/chat-hooks/src/index.ts`.
- [x] 1.4 Add a `usePageFileDrag` subsection to `libs/chat-hooks/README.md` following the `useConversationScroll` subsection's structure (description, usage example, params/returns tables).
- [x] 1.5 Update `apps/chat/src/hooks/usePageFileDrag.ts` consumers (`ConversationView.tsx`, `NewConversationComposer.tsx`) to import `usePageFileDrag` from `@epam/ai-dial-chat-hooks`.
- [x] 1.6 Delete `apps/chat/src/hooks/usePageFileDrag.ts` and `apps/chat/src/hooks/tests/usePageFileDrag.spec.ts`.
- [x] 1.7 Run `npm exec nx build ai-dial-chat-hooks`, `npm exec nx test ai-dial-chat-hooks`, and `npm exec nx affected --target=test,lint,build --base=origin/development`; fix any fallout before moving to slice 2.

## 2. Slice 2 — `useViewportWidth` and `usePanelMaxWidth`

- [x] 2.1 Create `libs/chat-hooks/src/useViewportWidth/useViewportWidth.ts` by moving `apps/chat/src/hooks/use-viewport-width.ts` verbatim.
- [x] 2.2 Create `libs/chat-hooks/src/usePanelMaxWidth/usePanelMaxWidth.ts` by moving `apps/chat/src/hooks/usePanelMaxWidth.ts`, changing its signature to `usePanelMaxWidth(minContentAreaWidth: number)` — remove the hardcoded `MIN_CONTENT_AREA_WIDTH` constant from the hook body.
- [x] 2.3 Author new unit tests for both hooks under `libs/chat-hooks/src/useViewportWidth/tests/` and `libs/chat-hooks/src/usePanelMaxWidth/tests/` (resize-event simulation for `useViewportWidth`; max-width math, including the boundary where the panel would be narrower than a sensible minimum, for `usePanelMaxWidth`).
- [x] 2.4 Re-export both hooks from `libs/chat-hooks/src/index.ts`.
- [x] 2.5 Add `useViewportWidth` and `usePanelMaxWidth` subsections to `libs/chat-hooks/README.md`.
- [x] 2.6 Update `apps/chat`'s call sites (`app.tsx`, `ConversationSourcesPanel.tsx`) to import `usePanelMaxWidth` from `@epam/ai-dial-chat-hooks` and pass `apps/chat`'s existing `MIN_CONTENT_AREA_WIDTH = 400` constant explicitly as the argument (added as `apps/chat/src/constants/layout.ts`, reused by both call sites).
- [x] 2.7 Delete `apps/chat/src/hooks/use-viewport-width.ts` and `apps/chat/src/hooks/usePanelMaxWidth.ts`.
- [x] 2.8 Run `npm exec nx build ai-dial-chat-hooks`, `npm exec nx test ai-dial-chat-hooks`, and `npm exec nx affected --target=test,lint,build --base=origin/development`; verify the two call sites render with identical max-width behavior before moving to slice 3. (211 test files / 3047 tests passed, lint clean, build clean.)

## 3. Amend the library-isolation rule (widened boundary)

- [x] 3.1 Update `AGENTS.md` §"Library isolation" to name `libs/chat-hooks` as a second, narrowly-scoped generated-client exception alongside the existing `libs/chat-api-client/` exception: it may depend on `@epam/ai-dial-chat-api-client` (the generated client's actual package name) types/operation signatures and on the already-published host-agnostic packages `@epam/ai-dial-chat-shared`, `@epam/ai-dial-share`, `@epam/ai-dial-quotations`, `@epam/ai-dial-source-panel`, `@epam/ai-dial-attachment-canvas`; it must still never construct/configure a client instance, and must still never import app contexts, routing, auth/session, env vars, feature flags, i18n, or UI-kit component rendering.
- [x] 3.2 Add `@epam/ai-dial-chat-api-client` as a peer dependency in `libs/chat-hooks/package.json`, alongside the existing `react` peer dependency; add the matching TS project reference to `../chat-api-client/tsconfig.lib.json` in `libs/chat-hooks/tsconfig.lib.json`.
- [x] 3.3 Verify `@nx/enforce-module-boundaries` in `eslint.config.mjs` allows `ai-dial-chat-hooks` to import the newly-permitted packages — confirmed: the current config uses `onlyDependOnLibsWithTags: ['*']` for every `sourceTag`, i.e. no tag-based restriction blocks this; no lint config change needed.

## 4. Slice 3 — `useShareLink` and `useShareRecipientsCount`

- [x] 4.1 Implement `libs/chat-hooks/src/useShareLink/useShareLink.ts`: same state machine as `apps/chat/src/hooks/useShareLink/useShareLink.ts` (loading/error, stale-response guard via a request-id ref, `setAccess` triggering re-fetch), accepting `Pick<ShareApi, 'createShareLink'>` (an already-configured `ShareApi` instance from `@epam/ai-dial-chat-api-client`) as a parameter, and importing `CreateShareLinkDtoResourceKindEnum`/`ShareLinkResponseDtoAccessEnum` from `@epam/ai-dial-chat-api-client` and `ShareLinkAccess`/`ShareLinkData` from `@epam/ai-dial-share` directly.
- [x] 4.2 Wrote a fresh `libs/chat-hooks/src/useShareLink/tests/useShareLink.spec.ts` (ported behavior from the deleted app spec) using a fake `Pick<ShareApi, 'createShareLink'>` instead of mocking `getShareLink`.
- [x] 4.3 Implement `libs/chat-hooks/src/useShareRecipientsCount/useShareRecipientsCount.ts`: same dedup/invalidate/idle-loading-resolved-unknown behavior as `apps/chat/src/hooks/useShareRecipientsCount/useShareRecipientsCount.ts`, accepting `Pick<ShareApi, 'getShareRecipientsCount'>`, with a library-owned `RecipientsCountStatus` enum (identical members/values to the app's — app now imports the library's enum instead of keeping its own).
- [x] 4.4 Wrote a fresh `libs/chat-hooks/src/useShareRecipientsCount/tests/useShareRecipientsCount.spec.ts` using a fake `Pick<ShareApi, 'getShareRecipientsCount'>`; the "unknown on error" case is preserved exactly.
- [x] 4.5 Updated `apps/chat`'s call sites (`ShareConversationPopoverContainer.tsx`, `SharePopoverContainer.tsx`, `ConversationPanelView.tsx`) to import the app's existing `shareApi` singleton from `server-api/api-client` and pass it into both hooks, imported from `@epam/ai-dial-chat-hooks`; `apps/chat/src/types/share-recipients.ts` deleted, call site now imports `RecipientsCountStatus` from the library (identical values). Also deleted the now-unused `apps/chat/src/utils/share-link.ts` (its `getShareLink`/`toShareLinkAccess`/`withCurrentOrigin` logic moved into the library hook). `server-api/share.api.ts`'s `createShareLink`/`getShareRecipientsCount` wrapper functions were **kept** — `CatalogView.tsx` still calls `getShareRecipientsCount` directly outside the hook.
- [x] 4.6 Deleted `apps/chat/src/hooks/useShareLink/` and `apps/chat/src/hooks/useShareRecipientsCount/`; updated the 3 affected component test files' mocks accordingly (`ConversationPanelView.spec.tsx` now mocks `server-api/api-client`'s `shareApi.getShareRecipientsCount`; the two popover-container specs mock `@epam/ai-dial-chat-hooks`'s `useShareLink` via `importOriginal` partial mock).
- [x] 4.7 Re-exported both hooks (and their public types) from `libs/chat-hooks/src/index.ts`; added README subsections for both.
- [x] 4.8 **Found and fixed a real bug during verification**: `vite.config.mts`'s `rollupOptions.external` is a hardcoded array, not derived from `package.json` peers — adding `@epam/ai-dial-chat-api-client`/`@epam/ai-dial-share` there without updating `external` inlined their entire dependency trees (bundle went ~7 kB → ~2 MB). Fixed by adding both to `external`; confirmed bundle back to ~7 kB. Also ran `nx sync` to add the `chat-api-client`/`share` TS project references. `npm exec nx build/test ai-dial-chat-hooks` green (32 tests); full affected verification run in progress.

## 5. Slice 4 — `useAttachmentUpload`

- [x] 5.1 Implemented `libs/chat-hooks/src/useAttachmentUpload/useAttachmentUpload.ts`: same debounced-failure-batching behavior as `apps/chat/src/hooks/conversation/useAttachmentUpload.ts`, accepting a configured `FilesApi` instance, `onNetworkError`, and a `debounceMs` param (default `700`); `buildUploadPath`'s DIAL bucket/path logic moved in as a library-owned pure function (the app's own copy in `utils/build-upload-path.ts` was kept — still used by import/export flows).
- [x] 5.2 Wrote a fresh `libs/chat-hooks/src/useAttachmentUpload/tests/useAttachmentUpload.spec.ts` (ported behavior from the deleted app spec) using a fake `Pick<FilesApi, 'uploadFile'>` instead of mocking `uploadFile`/`buildUploadPath`.
- [x] 5.3 Updated `apps/chat`'s call sites (`NewConversationComposer.tsx`, `useConversationHandlers.ts`) to import `filesApi` from `server-api/api-client` and pass it plus `apps/chat`'s existing `NETWORK_ERROR_DEBOUNCE_MS` constant into the hook, imported from `@epam/ai-dial-chat-hooks`.
- [x] 5.4 Deleted `apps/chat/src/hooks/conversation/useAttachmentUpload.ts` and its test.
- [x] 5.5 Re-exported the hook from `libs/chat-hooks/src/index.ts`; added a README subsection.
- [x] 5.6 Ran `npm exec nx build/test ai-dial-chat-hooks` and `npm exec nx affected --target=test,lint,build --base=origin/development`; found and fixed two fallout issues: (1) two apps/chat unit-test import-order/prettier lint errors in the edited files, (2) `ConversationRoute.spec.tsx`'s and `ConversationRoute.integration.spec.tsx`'s `uploadFile` mocks and assertions targeted the now-unused `server-api/files.api` wrapper — retargeted to mock `server-api/api-client`'s `filesApi.uploadFile` with object-shaped call assertions. Full affected run green after fixes (208 test files / 3036 tests, 0 lint errors).

## 6. Slice 5 — `useConversationSources` and `useAttachmentAction`

- [x] 6.1 Moved `apps/chat/src/hooks/conversation-sources/useConversationSources.ts` to `libs/chat-hooks/src/useConversationSources/useConversationSources.ts`, importing `Message`/`DisplayAttachment`/`MessageRole`/`AttachmentDisplayResolvers` from `@epam/ai-dial-chat-shared` directly and calling `messageAttachmentToDisplayAttachment` (also from chat-shared) instead of `apps/chat`'s `attachmentDtosToDisplayAttachments` wrapper — the app's wrapper baked in app-owned URL resolvers, so the lib hook now accepts an optional `resolvers: AttachmentDisplayResolvers` parameter (defaulted to a stable empty-object constant to preserve `useMemo` reference stability) instead.
- [x] 6.2 Ported `apps/chat/src/hooks/conversation-sources/useConversationSources.spec.ts` to `libs/chat-hooks/src/useConversationSources/tests/` unchanged (no resolvers needed by the existing fixtures).
- [x] 6.3 **Corrected during implementation** (see `design.md` §Risks): only `isDialFileId` (a pure `files/`-prefix check) moved into `libs/chat-hooks/src/useAttachmentAction/useAttachmentAction.ts` as protocol-level. `resolveDialFileDownloadUrl` hardcodes `apps/chat`'s own `/api/v1/files/download` BFF route — an app-owned REST path — so it stays in `apps/chat/src/utils/dial-file.ts`; the hook and its exported `downloadAttachment` helper now take a required `resolveDownloadUrl` parameter instead. Also moved `referenceAttachmentToPdfCanvasContent`/`openAnnotationAttachment`'s logic in as internal (non-exported) helpers parameterized the same way — the app's own copies in `utils/attachment-canvas.ts`/`utils/annotation.ts` are kept as-is since `ConversationMessageItem.tsx`'s citation-click path still uses them directly.
- [x] 6.4 Ported `apps/chat/src/hooks/attachment/tests/useAttachmentAction.spec.ts` to `libs/chat-hooks/src/useAttachmentAction/tests/`, replacing the real `resolveDialFileDownloadUrl` with a fake `resolveDownloadUrl` fixture. Also added `libs/chat-hooks/src/test-setup.ts` (mocks `@epam/pdf-highlighter-kit`, matching `apps/chat`'s own global test-setup) and a `resolve.alias` in `vite.config.mts` pointing `@epam/ai-dial-attachment-canvas` at its source for tests, both needed because the built dist eagerly touches `DOMMatrix`, which jsdom lacks.
- [x] 6.5 Updated `apps/chat`'s call sites (`ConversationSourcesPanel.tsx`, `ConversationMessageItem.tsx`) to import both hooks from `@epam/ai-dial-chat-hooks`, passing `resolveDialFileDownloadUrl`/`resolveCatalogIconUrl` (both still app-owned) as the injected callbacks.
- [x] 6.6 Deleted `apps/chat/src/hooks/conversation-sources/` and `apps/chat/src/hooks/attachment/useAttachmentAction.ts` (plus its test). `apps/chat/src/utils/attachment-dto-to-display.ts`, `utils/annotation.ts`, and `utils/attachment-canvas.ts`'s `referenceAttachmentToPdfCanvasContent` were **kept** — all three are still used directly by app-owned code (`ConversationMessageItem.tsx`'s citation path, `CatalogView.tsx`, etc.) outside the moved hooks.
- [x] 6.7 Re-exported both hooks from `libs/chat-hooks/src/index.ts`; added README subsections for both. Added `@epam/ai-dial-attachment-canvas`, `@epam/ai-dial-quotations`, `@epam/ai-dial-source-panel` to `package.json` peerDependencies and the first two to `vite.config.mts`'s `external` (source-panel is type-only, no runtime import).
- [x] 6.8 Ran `npm exec nx build ai-dial-chat-hooks` (11.07 kB, confirmed no bundle bloat) and `npm exec nx test ai-dial-chat-hooks` (57/57 tests passing). Full `npm exec nx affected --target=test,lint,build --base=origin/development` found and fixed one more fallout: `ConversationSourcesPanel.spec.tsx`'s `../../../utils/dial-file` mock didn't export the newly-imported `resolveDialFileDownloadUrl`, failing 17 tests in that file. Fixed; full affected run green (206 test files / 3013 tests, 0 lint errors) after the fix.

## 7. Documentation and validation

- [x] 7.1 Ran `npm run validate:docs` — passed (39 markdown files checked).
- [x] 7.2 Grepped `apps/chat/src` for references to all eight deleted app hook files (`usePageFileDrag`, `use-viewport-width`/`usePanelMaxWidth`, `useShareLink`, `useShareRecipientsCount`, `conversation/useAttachmentUpload`, `conversation-sources/useConversationSources`, `attachment/useAttachmentAction`, `types/share-recipients`) — none remain.
- [x] 7.3 Confirmed `libs/chat-hooks/package.json` peerDependencies: `react`, `@epam/ai-dial-chat-api-client`, `@epam/ai-dial-chat-shared`, `@epam/ai-dial-share`, `@epam/ai-dial-quotations`, `@epam/ai-dial-source-panel`, `@epam/ai-dial-attachment-canvas` — matches every hook's actual imports; `react` is the only *runtime* dependency requiring host configuration, the rest are DIAL-Core-shape packages per the widened boundary (D6).
