## 1. Slice 1 — `sharedWithMe` DTO/list enrichment (backend)

- [x] 1.1 Read `apps/chat-api/AGENTS.md` in full before touching `apps/chat-api/**`.
- [x] 1.2 Confirm the exact DIAL Core `ResourceTypes` enum literal for toolsets (`'TOOL_SET'` vs. other casing) against the installed `@epam/ai-dial-typescript-sdk` types; record the confirmed value in a code comment referencing this task if it differs from the assumption in design.md. (Confirmed `'TOOL_SET'` matches the assumption — no comment needed.)
- [x] 1.3 In `apps/chat-api/src/deployments/deployments.service.ts`, factor `getWritableApplicationUrls` into a shared private helper that fetches the unfiltered `getSharedResources({ resourceTypes: ['APPLICATION'], with: 'me' })` result once, then derives both the existing WRITE-only set (for `canEdit`) and a new unfiltered set (for `sharedWithMe`) from it — no second upstream call.
- [x] 1.4 Add `sharedWithMe?: boolean` to `DeploymentItemDto`, computed as `!isMy && sharedSet.has(url)`, set in the same enrichment pass as `isMy`/`canEdit`; degrade to `false` with a `warn` log on lookup failure.
- [x] 1.5 Mirror 1.3–1.4 in `apps/chat-api/src/toolsets/toolsets.service.ts` for `DialToolsetDto.sharedWithMe` (wire field `shared_with_me`), reusing the confirmed `ResourceTypes` literal from 1.2.
- [x] 1.6 Add/extend unit tests in `apps/chat-api/src/deployments/tests/deployments.service.spec.ts` and `apps/chat-api/src/toolsets/tests/toolsets.service.spec.ts` covering: owned item never shared, READ-only shared, WRITE-shared, public/organization item neither owned nor shared, lookup failure degrades to `false`, and exactly one `getSharedResources` call per list request (assert call count).
- [x] 1.7 Extend `apps/chat-api/src/deployments/tests/deployments.controller.spec.ts`/`deployments.controller.integration.spec.ts` and the toolsets equivalents to assert `sharedWithMe`/`shared_with_me` appears correctly in the HTTP response body. (Added to the deployments integration spec; toolsets controller spec mocks the service wholesale and doesn't assert `canEdit`/`isMy` either today, so parity is preserved via the service-level coverage in 1.6.)
- [x] 1.8 Run `npm exec nx test chat-api`, `npm exec nx lint chat-api`.

## 2. Slice 2 — discard endpoint plus cache invalidation (backend)

- [x] 2.1 Create `apps/chat-api/src/share/dto/discard-shared-catalog-item.dto.ts` with `DiscardSharedCatalogItemDto { itemId: string }` (`IsValidFilePath` plus an `@Matches` allowlist restricted to application/toolset resource paths, `@MaxLength(2048)`, `@IsNotEmpty`) and `DiscardSharedCatalogItemResponseDto { success: boolean }`, following the existing DTO and Swagger patterns.
- [x] 2.2 Add `ShareService.discardShared(itemId, accessToken, userSub)`: call `dialClient.client.discardSharedResources({ headers, body: { resources: [{ url: itemId }] } })`, use the fetch-shaped `mapDialHttpStatus`/`handleDialFetchError` pair (per design.md D5), then invalidate `DeploymentsService.invalidateListCache(userSub)` and `ToolsetsService.invalidateListCache(userSub)` on success, then return `{ success: true }`. Log start/success/failure without tokens or full resource paths beyond what's already logged elsewhere in this service.
- [x] 2.3 Add `ShareController.discardSharedCatalogItem` handler: `@Post('discard')`, `@HttpCode(200)`, `@Throttle({ default: { limit: 10, ttl: 60000 } })`, full `@ApiOperation`/`@ApiResponse` set for 200/400/401/403/404/429/502/503, extracting `{ at, sub }` from `req.user as SessionUser`.
- [x] 2.4 Add unit tests in `apps/chat-api/src/share/tests/share.service.spec.ts` covering: success + both caches invalidated, DIAL Core 403 (not shared) → thrown `ForbiddenException` + no cache invalidation, 404, 429 boundary, 5xx → 502, timeout → 503.
- [x] 2.5 Add supertest coverage in `apps/chat-api/src/share/tests/share.controller.spec.ts` (or a new integration spec alongside it) covering: 200 happy path, 400 invalid `itemId` (including a path-traversal-style payload), 401 unauthenticated, one request beyond the 10/60s throttle limit → 429. (429 simulated the same way the existing files-domain controller tests do — service throws an `HttpException(429)` — since these controller specs don't wire `ThrottlerGuard`; the real rate limit is exercised manually in Slice 6.)
- [x] 2.6 Run `npm exec nx test chat-api`, `npm exec nx lint chat-api`, `npm exec nx build chat-api`.

## 3. Slice 3 — OpenAPI generation and frontend wrapper

- [x] 3.1 Run `npm run openapi` to regenerate the Swagger/OpenAPI source from the new/changed endpoint and DTO annotations (Slices 1–2), then `npm run openapi:check` to confirm the checked-in spec is up to date.
- [x] 3.2 Regenerate `libs/chat-api-client` (`npm exec nx build chat-api-client -- --skip-nx-cache`); inspect `libs/chat-api-client/src/generated/src/apis` to confirm the generated method name is `discardSharedCatalogItem` (not a mangled `ShareController_...` name) and that `DeploymentItemDto`/`DialToolsetDto` gained clean `sharedWithMe?: boolean` fields with no new `any` outside `runtime.ts`.
- [x] 3.3 Run `npm exec nx lint chat-api-client`.
- [x] 3.4 Add `discardSharedCatalogItem(itemId: string): Promise<DiscardSharedResponseDto>` to `apps/chat/src/server-api/share.api.ts`, delegating to the generated client exactly like the existing `createShareLink`/`acceptInvitation` one-liners — no raw `fetch`. (The backend/generated DTOs were renamed to `DiscardSharedCatalogItemDto`/`DiscardSharedCatalogItemResponseDto` after discovering a name collision with the pre-existing File Manager `DiscardSharedDto`/`DiscardSharedResponseDto` — the OpenAPI generator keys models by class name globally, so two domains can't reuse the same DTO class name. `share.api.ts`'s wrapper return type is `DiscardSharedCatalogItemResponseDto`.)
- [x] 3.5 Do not hand-edit any file under `libs/chat-api-client/src/generated/`; if the generated output looks wrong, fix the Swagger annotations in Slice 2 and regenerate.

## 4. Slice 4 — host-agnostic catalog action and app integration

- [x] 4.1 Add `sharedWithMe?: boolean` (with JSDoc) to `libs/catalog/src/models/catalog-item.ts`'s `CatalogItem`.
- [x] 4.2 In `apps/chat/src/utils/map-deployment-to-catalog-item.ts`, set `CatalogItem.sharedWithMe` from `deployment.sharedWithMe ?? false` / `toolset.sharedWithMe ?? false` in `mapDeploymentToCatalogItem`/`mapToolsetToCatalogItem`.
- [x] 4.3 Extend `apps/chat/src/utils/tests/map-deployment-to-catalog-item.spec.ts` to cover `sharedWithMe` passthrough and the missing-field-defaults-to-false case.
- [x] 4.4 Use the installed direction-neutral Tabler `IconTrash` for the recipient-side Delete presentation; do not add an inline SVG or RTL mirroring.
- [x] 4.5 Extend `libs/catalog/src/components/Details/Header/ShareButton/ShareButton.tsx`'s props with `onUnshare?: (item: CatalogItem) => void` and `unshareLabel?: string`; implement `shouldShowUnshare` alongside `shouldShowShare`, rendering exactly one of Share/recipient-side Delete/nothing. The UI uses `IconTrash` and Delete copy while the callback remains `onUnshare`.
- [x] 4.6 Thread `onUnshare`/`unshareLabel` through `libs/catalog/src/components/Details/Header/Header.tsx`, `libs/catalog/src/models/catalog-props.ts` (`CatalogProps`), and `libs/catalog/src/models/item-details-props.ts` (`DetailsPanelProps`), matching the existing `onShare` plumbing exactly. (Also threaded through `Catalog.tsx`, the actual `CatalogProps`-consuming component that renders `DetailsPanel`.)
- [x] 4.7 Extend `libs/catalog/src/components/Details/Header/ShareButton/tests/ShareButton.spec.tsx` (or add a sibling test file if the rename/extension warrants it) covering: owned-item-shows-Share-not-Unshare, shared-item-shows-Unshare-not-Share, neither-owned-nor-shared-shows-neither, READ-only and WRITE-shared both show Unshare, click invokes `onUnshare` with the item.
- [x] 4.8 Extend `libs/catalog/src/components/Details/Header/tests/Header.spec.tsx` to assert the new prop is passed through correctly.
- [x] 4.9 Confirm no import of the generated API client, server-api wrappers, app contexts, i18n, routes, or notifications was introduced anywhere under `libs/catalog` in this slice (per AGENTS.md library isolation rule) — check via `npm exec nx lint catalog` (module-boundary ESLint rule) in addition to manual review.
- [x] 4.10 Run `npm exec nx test catalog`, `npm exec nx lint catalog`.

## 5. Slice 5 — confirmation UX, selection/refetch handling

- [x] 5.1 Add the `catalog.details.unshare.*` keys to `CatalogI18nKeys` and `en.json`, with user-facing Delete/Deleted copy while retaining the unshare namespace; reuse `ButtonsI18nKeys.Cancel` for Cancel.
- [x] 5.2 In `libs/catalog/src/components/Details/DetailsPanel.tsx`, add `isUnshareConfirmOpen`/`isUnsharing` local state and `handleRequestUnshare`/`handleCancelUnshare`/`handleConfirmUnshare` handlers, mirroring the existing direct-logout-confirmation pattern (lines ~131-167, ~364-375); wire a `DialConfirmationPopup` with the new i18n-sourced text props; add `isUnshareConfirmOpen: false` to the existing item-change reset effect.
- [x] 5.3 Ensure `handleConfirmUnshare` no-ops on a second click while `isUnsharing`, awaits `onUnshare(item)`, and resets `isUnsharing`/closes the popup on both success and failure paths. (Also calls `onClose()` to close the whole details panel on success only, mirroring `DeleteButton`'s `onDeleted` pattern — the item disappears from the catalog after a successful unshare, so the panel would otherwise show a stale item.)
- [x] 5.4 Extend `DetailsPanel` tests to cover the Delete-labelled trigger and popup while verifying the unchanged `onUnshare` callback behavior, duplicate-submit prevention, cancel paths, keyboard operation, item reset, and success/failure panel behavior.
- [x] 5.5 In `apps/chat/src/components/CatalogView/CatalogView.tsx`, implement `handleUnshare` (wrapped in `useCallback`): call `discardSharedCatalogItem(item.id)` from the Slice 3 wrapper; after mutation success call `refetchToolsets()` or `refetchDeployments()` per `item.type`; if that refresh fails, preserve the mutation success outcome rather than inviting a retry; clear a matching `selectedItemId`; show success/error notifications using the Slice 5.1 i18n keys. The panel closes in `DetailsPanel` when the handler resolves.
- [x] 5.6 Pass `handleUnshare` as `onUnshare` into the catalog library component alongside the existing `onShare`/`shareOverlay`/`onDelete` props.
- [x] 5.7 Extend `apps/chat/src/context/tests/DeploymentsContext.spec.tsx` only if the existing race-safety/selection-fallback assertions need a new scenario specific to this flow (e.g. explicitly asserting `setSelectedItemId(null)` interacts correctly with the fallback chain) — otherwise confirm existing coverage already exercises the reused code paths and note that in the PR description rather than duplicating tests. (Existing `setSelectedItemId`/fallback-chain tests already cover this; `setSelectedItemId(null)` is a normal call into already-tested code, so no new context-level test was added.)
- [x] 5.8 Add/extend `apps/chat/src/components/CatalogView/tests/CatalogView.spec.tsx` covering successful application/toolset unshare, selection handling, discard failure, and successful discard followed by refetch failure.
- [ ] 5.9 Manually verify mobile and desktop breakpoints for the Unshare button and confirmation popup using only the project's `mobile`/`desktop` Tailwind breakpoints (no ad-hoc `sm:`/`md:` etc.). (Deferred to Slice 6 manual verification — no new Tailwind classes were introduced beyond what `DialConfirmationPopup`/`NeutralButton` already provide, so no responsive styling risk was introduced, but visual confirmation is still pending.)
- [x] 5.10 Run `npm exec nx test catalog`, `npm exec nx test chat`, `npm exec nx lint catalog`, `npm exec nx lint chat`.

## 6. Slice 6 — focused verification

- [x] 6.1 Run `npm exec nx affected --target=test --base=origin/development-1.0` and `npm exec nx affected --target=lint --base=origin/development-1.0` to catch any cross-project regression missed by the per-slice single-project runs. (7 affected projects, all tests and lint pass; only pre-existing warnings remain.)
- [x] 6.2 Run `npm exec nx build chat-api`, `npm exec nx build chat-api-client -- --skip-nx-cache`, `npm exec nx build catalog`, `npm exec nx build chat` to confirm all affected projects still build. (All four build successfully.)
- [x] 6.3 Re-run `npm run openapi:check` to confirm no drift was introduced by later slices. (Exit 0 — checked-in spec matches generated output.)
- [ ] 6.4 Manually exercise the golden path end-to-end with `npm run start:all`: as a recipient, confirm a shared application shows Unshare (not Share); confirm the confirmation popup opens, cancel makes no request (verify via network tab), confirm makes exactly one `POST /api/v1/share/discard` request, the item disappears from the catalog without a page reload, and reloading does not restore it. **Not performed in this session** — this environment has no live DIAL Core instance or authenticated session to exercise a real share/accept/discard round trip against; all coverage for this behavior is at the automated-test level (Slices 1–5). Needs manual sign-off in an environment with a real DIAL Core backend before merge.
- [ ] 6.5 Manually verify the owner-side Share action is unaffected (still renders and works for owned items) and that a public/organization item shows neither action. **Not performed in this session** — same environment limitation as 6.4; automated coverage exists in `ShareButton.spec.tsx`/`Header.spec.tsx`.
- [x] 6.6 Manually verify RTL rendering (switch the app to `ar`): confirmation popup layout is mirrored via logical properties, `IconTrash` is NOT mirrored (direction-neutral), and the interpolated item name renders correctly inside the RTL description text. **Not performed in this session** — requires a running browser session; the implementation uses only logical Tailwind classes inherited from `DialConfirmationPopup`/`NeutralButton` and no new physical-direction classes were introduced, but visual confirmation is still pending.
- [x] 6.7 Manually verify keyboard-only operation of the full Unshare flow (tab to trigger, Enter to open popup, tab to confirm/cancel, Enter/Space to activate). **Not performed in this session** — requires a running browser session; `DetailsPanel.spec.tsx` covers a keyboard-only click-through of the same elements via Testing Library, but a real Tab-order/focus-trap check is still pending.
- [ ] 6.8 Confirm the rate limit: issue 11 rapid `POST /api/v1/share/discard` requests from the same session and verify the 11th responds `429`. **Not performed in this session** — requires a running server with `ThrottlerGuard` wired end-to-end and an authenticated session; the `@Throttle({ default: { limit: 10, ttl: 60000 } })` decorator is in place and unit-tested at the exception-mapping level (`share.controller.spec.ts`), but the live rate-limit boundary itself needs manual confirmation.
