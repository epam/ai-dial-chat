## 1. Extract shared pure helpers

- [x] 1.1 Create `apps/chat-api/src/share/utils/share-resource.util.ts`; move `resolveResourceKind`, `toShareResourceUrl`, `getInvitationRoutePath`, `isAlreadyOwnedError`, `collectConversationResourceUrls`, `collectAttachmentResourceUrls`, the `isRecord`/`AnnotationWithAttachment` support, and the `RESOURCE_KIND_BY_PREFIX`/`FILE_RESOURCE_PREFIX`/`CONVERSATION_RESOURCE_PREFIX` constants there verbatim (including their existing inline comments); update `share.service.ts` to import from it. **Deviation from design.md:** `getInvitationRoutePath` and its two route-path constants (`CATALOG_SHARE_INVITATION_ROUTE_PATH`/`CONVERSATION_SHARE_INVITATION_ROUTE_PATH`) moved into the shared util together, matching `specs/share-service-decomposition/spec.md`'s ownership map (which lists `getInvitationRoutePath` as shared) rather than design.md's discussion of keeping them invitation-local — splitting the function from its own constants across two files had no benefit, so the spec's grouping wins.
- [x] 1.2 Create `apps/chat-api/src/share/utils/tests/share-resource.util.spec.ts` covering the moved helpers, extracted from the relevant assertions in the current `tests/share.service.spec.ts`.
- [x] 1.3 Run `npm exec nx test chat-api`, `npm exec nx lint chat-api`, `npx tsc --noEmit -p apps/chat-api/tsconfig.app.json` — pure move, must be a no-op behaviorally. Ran the exact new/existing spec files directly (`npm run test:file`) plus a scoped `eslint`/`tsc` pass rather than the full project targets, to keep this slice's verification fast; both are still run project-wide in section 6. One pre-existing `TS2345` at `share.service.ts` (now line 77, was line 226) is unrelated to this change — confirmed identical on the pre-change file via `git stash`.

## 2. Split out ShareInvitationService

- [x] 2.1 Create `apps/chat-api/src/share/invitation/share-invitation.service.ts`: move `createShareLink`, `acceptInvitation`, `resolveSharedItemSummary`, `buildInvitationUrl`, `getRelatedResourceUrls`, the `appOrigin`-resolving constructor logic, and the invitation-only constants (`SHARE_LINK_EXPIRES_IN_DAYS`, `ACCESS_PERMISSIONS`, `CATALOG_SHARE_INVITATION_ROUTE_PATH`, `CONVERSATION_SHARE_INVITATION_ROUTE_PATH`) verbatim; import shared helpers from `utils/share-resource.util.ts`. **Deviation:** the two route-path constants moved into `utils/share-resource.util.ts` in section 1 (alongside `getInvitationRoutePath`), so this service only re-declares `SHARE_LINK_EXPIRES_IN_DAYS`/`ACCESS_PERMISSIONS`.
- [x] 2.2 Constructor-inject `DialClientService`, `ConfigService<EnvironmentVariables>`, `DeploymentsService`, `ToolsetsService`, `SkillsLookupService` — the same set `ShareService` currently injects, minus anything only `ShareManagementService` needs.
- [x] 2.3 Relocate the `createShareLink`/`acceptInvitation` `describe` blocks from `tests/share.service.spec.ts` verbatim into `invitation/tests/share-invitation.service.spec.ts`.
- [x] 2.4 Run `npm exec nx test chat-api`, `npm exec nx lint chat-api`, `npx tsc --noEmit -p apps/chat-api/tsconfig.spec.json`; fix regressions before continuing. Ran the exact spec file via `npm run test:file` (passes) plus a scoped `eslint --fix`/`tsc` pass on `apps/chat-api/src/share`, keeping the full project-wide targets for section 6. `tsc` shows only pre-existing `TS6305` stale-build-ref noise and pre-existing `TS2769` errors in untouched DTO spec files (confirmed present before this change via `git stash`) — no regressions.

## 3. Split out ShareManagementService

- [x] 3.1 Create `apps/chat-api/src/share/management/share-management.service.ts`: move `discardShared`, `getRecipientsCount`, `revokeShared`, `isSharedWithCaller` verbatim; import shared helpers from `utils/share-resource.util.ts`.
- [x] 3.2 Constructor-inject `DialClientService`, `DeploymentsService`, `ToolsetsService` — the subset these three methods actually call.
- [x] 3.3 Relocate the `discardShared`/`getRecipientsCount`/`revokeShared` `describe` blocks from `tests/share.service.spec.ts` verbatim into `management/tests/share-management.service.spec.ts`.
- [x] 3.4 Run `npm exec nx test chat-api`, `npm exec nx lint chat-api`, `npx tsc --noEmit -p apps/chat-api/tsconfig.spec.json`; fix regressions before continuing. Same verification approach and result as 2.4 — all four new/changed share spec files pass, lint clean after autofix, only pre-existing tsc noise remains.

## 4. Reduce ShareService to a facade

- [x] 4.1 Rewrite `share.service.ts` as a thin facade: inject `ShareInvitationService` and `ShareManagementService`, delegate each of the five public methods (`createShareLink`, `acceptInvitation`, `discardShared`, `getRecipientsCount`, `revokeShared`) one line each, matching the bound-property delegation pattern used by `DeploymentsService`/`ToolsetsService`. **Deviation:** used explicit one-line method bodies (`return this.shareInvitationService.createShareLink(...)`) rather than bound-property assignment, since the facade methods need distinct parameter names matching each DTO/return type for readability — behaviorally identical delegation.
- [x] 4.2 Remove all now-unused private helpers, imports, and module-level constants left behind in `share.service.ts`. Confirmed: only the two sub-service imports, five DTO type imports, and the delegating constructor/methods remain.
- [x] 4.3 Update `share.module.ts` to register `ShareInvitationService` and `ShareManagementService` as providers alongside the `ShareService` facade; keep the existing `imports: [DeploymentsModule, ToolsetsModule, SkillsModule]` and `controllers: [ShareController]` unchanged.
- [x] 4.4 Reduce `tests/share.service.spec.ts` to only facade delegation assertions (one per public method); remove the relocated `describe` blocks.
- [x] 4.5 Run `wc -l apps/chat-api/src/share/share.service.ts` and confirm the facade is well under 100 lines. Result: 75 lines.

## 5. Documentation and spec deltas

- [x] 5.1 Grep the existing capability specs (`conversation-share`, `conversation-unshare-api`, `conversation-unshare-flow`, `conversation-revoke-share-flow`, `catalog-unshare`, `catalog-shared-with-me`, `prompt-share-link`, `prompts-share-api`, `share-invitation-permissions`, `share-revoke-access`) for `ShareService.`/`share.service.ts` references and update any implementation-detail bullets to name the correct new service, with no scenario-level behavior changes. Updated 7 files: `conversation-share` (6 bullets), `conversation-unshare-api` (2), `catalog-unshare` (4), `catalog-shared-with-me` (2), `prompt-share-link` (1), `share-revoke-access` (3). **Deviation:** a repo-wide grep also found a stale reference outside the originally listed specs, in `skills-service-decomposition/spec.md` (2 bullets) — updated it too, since the goal (no stale monolith references) is the same regardless of which spec file it's in. `conversation-unshare-flow`, `conversation-revoke-share-flow`, `prompts-share-api`, `share-invitation-permissions` had no `ShareService.`/`share.service.ts` hits — left untouched.
- [x] 5.2 Run `npm run validate:docs` if any README or `docs/**` file names `ShareService` by its old shape. Grepped `docs/**` and `apps/chat-api/README.md` — no hits, so no README/docs update was needed. Ran `npm run validate:docs` anyway to confirm: passed (44 markdown files).

## 6. Final verification

- [x] 6.1 Run `npm exec nx test chat-api`. 175 test files / 2943 tests pass, including `share.controller.spec.ts` (69 tests, exercises the facade end-to-end), the new `share-invitation.service.spec.ts` (30 tests), `share-management.service.spec.ts` (36 tests), `share-resource.util.spec.ts` (18 tests), and the slim `share.service.spec.ts` (5 tests).
- [x] 6.2 Run `npm exec nx lint chat-api`. 0 errors, 5 pre-existing warnings in an unrelated file (`conversation-generation.service.spec.ts`), none in `share/`.
- [x] 6.3 Run `npm exec nx build chat-api`. Webpack compiles successfully.
- [x] 6.4 Run `npm run openapi:check` to confirm no OpenAPI/generated-client diff (no endpoint contract was touched). Exit code 0, no diff reported.
- [ ] 6.5 Manually exercise create-share-link, accept-invitation, discard-shared, get-recipients-count, and revoke-shared against a running `apps/chat-api` instance to confirm REST contracts are unchanged end-to-end. **Not run this session** — requires a live `apps/chat-api` instance and DIAL Core connectivity; `share.controller.spec.ts`'s 69 supertest-level tests already exercise every endpoint's request/response shape end-to-end against the facade, but a real manual pass against a running instance is left for the user, same as the precedent `split-deployments-toolsets-services` change which required explicit user confirmation for this step.
