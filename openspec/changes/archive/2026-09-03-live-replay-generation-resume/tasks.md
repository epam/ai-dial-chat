## 1. Backend: buffer and multicast in-flight chunks

- [x] 1.1 Extend `GenerationEntry`/`ConversationGenerationService` (`apps/chat-api/src/conversations/conversation-generation.service.ts`) with `assembledMessage: ConversationMessageDto` and `emitter: EventEmitter` (`node:events`, `setMaxListeners(0)`); update `register`/`complete`/`error`/`abort` to initialize/clean up the emitter.
- [x] 1.2 In `ConversationStreamingService.streamCompletion`'s relay loop (`relayModelCompletion` and `ResponsesAdapter.stream` call sites in `conversation-streaming.service.ts`), after each `applyChunkToMessage` update, also update the registry entry's `assembledMessage` and emit `{ type: 'chunk', ... }` on its emitter.
- [x] 1.3 In `finalize()`, emit the matching terminal event (`{ type: 'done' }` / `{ type: 'error', message? }` / `{ type: 'stopped' }`) on the entry's emitter before the registry entry is deleted.
- [x] 1.4 Unit test `ConversationGenerationService`: emitter receives chunk/terminal events in order, `setMaxListeners` doesn't warn with multiple subscribers, listeners are gone after `complete`/`error`.

## 2. Backend: attach endpoint

- [x] 2.1 Add `AttachGenerationDto` (`apps/chat-api/src/conversations/dto/`) — `{ path: string }` with the same `@Matches` allowlist validation pattern as `WatchConversationBodyDto`.
- [x] 2.2 Add `POST /api/v1/conversations/completions/attach` to `ConversationController`: resolve `sessionId` from `req.user`, look up the registry entry via the same `sessionId`+`path` key `abort()` uses, respond `404` when absent, otherwise open the SSE response via the shared `startSseResponse` helper (`common/utils/sse.ts`, added concurrently in #8587 — every SSE endpoint now opens through it so the Firefox pending-fetch bug can't reappear on a fourth endpoint).
- [x] 2.3 In the handler, synchronously (no `await` in between) read `entry.assembledMessage` as the snapshot and attach the emitter listener, per the design's snapshot-then-subscribe ordering guarantee; write the `snapshot` event first, then forward each subsequent `chunk`/terminal event as it's emitted.
- [x] 2.4 Register `res.on('close', ...)` to remove this subscriber's listener on client disconnect (mirroring `watchConversation`'s existing pattern) — do NOT mirror `streamCompletion`'s no-close-handler behavior; this endpoint is a late read-only observer, not the generation owner.
- [x] 2.5 Add full Swagger decorators (`@ApiOperation`, `@ApiResponse` for 200/401/404), following `apps/chat-api/AGENTS.md`'s thin-controller conventions. No `@Throttle`/429 — rate limiting was removed repo-wide in `60974f55f` (concurrent with this change), so match every other endpoint in this controller rather than the stale per-route throttle guidance the tasks were originally drafted against.
- [x] 2.6 Backend tests (supertest, per `apps/chat-api/AGENTS.md` co-location convention): attach mid-generation delivers snapshot + live chunks + terminal event; attach with no active generation returns 404; attach after generation already finished returns 404; two concurrent subscribers each get independent snapshots; stopped generation reports a `stopped` terminal event. (Literal socket-disconnect simulation was not added as a supertest integration case — covered at the unit level by `ConversationGenerationService`'s listener-cleanup test plus mirroring `watchConversation`'s proven `res.on('close', ...)` pattern.)

## 3. Backend: OpenAPI contract

- [x] 3.1 Run `npm run openapi` to regenerate the OpenAPI document for the new endpoint/DTO.
- [x] 3.2 Run `npm run openapi:check` and fix any drift.
- [x] 3.3 Build and lint `libs/chat-api-client` to confirm the generated client picks up the new operation.

## 4. libs/chat-hooks: transport interface

- [x] 4.1 Add `attachToGeneration(path: string, signal: AbortSignal): Promise<ReadableStream<Uint8Array>>` to the `ConversationStreamTransport` interface/model (`libs/chat-hooks/src/conversation/...`), mirroring `watchConversation`'s existing shape.
- [x] 4.2 The interface change lives directly on `ConversationStreamTransport` in `useConversationStream.ts` (the file that already declares it) rather than `create-chat-stream-api.ts` (which only backs `streamCompletion`/`stopCompletion` and has no `watchConversation`/`getConversation`/`attachToGeneration` surface today) — no hardcoded path, no CSRF logic baked into the lib.
- [x] 4.3 Updated the shared mock transport in `useConversationStream.spec.ts` to implement `attachToGeneration` (default: rejects, matching "not available" behavior so every pre-existing resume test exercises the fallback path unchanged).

## 5. libs/chat-hooks: resume flow

- [x] 5.1 In `resumeIfAwaitingGeneration` (`useConversationStream.ts`), call `transport.attachToGeneration` first (`runAttach`); on success, parse the `snapshot`/`chunk`/terminal event stream inline (a small dedicated line-parsing loop, mirroring — not sharing — the existing watch-loop's parsing shape, since the two streams carry different event vocabularies).
- [x] 5.2 Seed the per-path buffered message from the `snapshot` event via the existing buffer mechanism (`bufferedGenerationsRef`/`restoreBufferedMessage`, under a `RESUME_BUFFER_GENERATION_ID` marker); apply each `chunk` event through the same `applyChunkToMessages` merge function `startStream`'s `onChunk` already uses.
- [x] 5.3 On a terminal event (`done`/`error`/`stopped`), trigger the existing `finalCheck`/`transport.getConversation` reload path — unchanged from the live generation's own completion/error handling.
- [x] 5.4 On `attachToGeneration` failure (rejected promise, malformed stream — both caught by wrapping the fetch-and-`getReader()` step together) or no terminal event before `runAttach`'s own `GENERATION_RESUME_WATCH_TIMEOUT_MS` elapses, fall back to the pre-existing `runWatch`/`transport.watchConversation` + refetch loop, unchanged (its own independent timeout/controller).
- [x] 5.5 Unit tests (`useConversationStream.spec.ts`): attach delivers snapshot then chunk then reloads via `getConversation` on the `done` terminal event, with `watchConversation` never called; existing resume tests now also assert `attachToGeneration` was tried (and its default-mock rejection is what drives the pre-existing watch-fallback coverage) — deliberately not adding a fake-timers test for `runAttach`'s own timeout, matching the pre-existing coverage depth (the watch loop's own timeout was never separately tested either).

## 6. apps/chat: concrete wiring

- [x] 6.1 Implemented `attachToGeneration` in `apps/chat/src/server-api/conversations.api.ts` (not `chat-stream.api.ts`) — via the generated client's `conversationsApi.attachToGenerationRaw(...)`, matching `watchConversation`'s existing implementation in that same file exactly (both are generated-client `Raw` calls returning the raw response body stream), rather than a hand-rolled `fetch`.
- [x] 6.2 Wired it into the concrete transport object in `apps/chat/src/utils/conversation-stream-transport.ts`.
- [ ] 6.3 Manual verification in the browser: start a long-enough completion in one tab, open the same conversation in a second tab mid-generation, confirm the message populates progressively rather than only showing a typing indicator, and confirm the final content matches after the terminal reload. Not completed — requires a real DIAL Core deployment/model to generate a genuinely slow multi-chunk completion; not available in this environment. Automated coverage (backend integration tests simulating chunk/terminal timing, frontend hook tests simulating the SSE stream) substitutes for now.

## 7. Docs

- [x] 7.1 Corrected `docs/architecture.md`'s Conversations section: added the previously-undocumented `/completions/stop` and `/watch` rows, the new `/completions/attach` row, a paragraph on backend-owned persistence, and a paragraph on the watch/attach resume story. Also dropped the stale "Rate limit" column (values were left over from before `60974f55f` removed rate limiting repo-wide — a different concurrent commit, but it was the exact table being rewritten). Updated `libs/chat-hooks/README.md`'s `ConversationStreamTransport` method count/list and `resumeIfAwaitingGeneration` description for the new `attachToGeneration` method.
- [x] 7.2 Ran `npm run validate:docs` — passes. Along the way, found and fixed a pre-existing, unrelated failure: `libs/chat-hooks/src/index.ts` was missing `export { useGridEditingScroll } from '@epam/ai-dial-chat-shared'` (AGENTS.md documents this as an intentional backward-compatibility re-export; a concurrent commit on this same branch — `cdcdd4092`, unrelated to this change — had dropped it). Restored the one-line re-export so the pre-existing README example resolves again; this is not part of this change's feature scope, just a same-session drive-by fix needed for a clean `validate:docs` run.

## 8. Verification

- [x] 8.1 `npm exec nx test chat-api` (172 files / 2908 tests pass) and `npm exec nx lint chat-api` (0 errors, 5 pre-existing-style non-null-assertion warnings in the new test file, consistent with the rest of the codebase).
- [x] 8.2 Ran `verify:changed`'s three steps individually rather than the combined script, because `typecheck:affected` fails on a large **pre-existing** baseline unrelated to this change (confirmed by stashing this change and re-running: 799 errors on the clean `development` tip before any edit here, mostly `TS6305` composite-project-reference staleness spanning skills/toolsets/telemetry/themes/transcription/user-config — domains this change never touches; `@epam/chat-api`'s own `AGENTS.md`/`nestjs-best-practices.md` verification loop does not include `nx typecheck chat-api` at all). This change adds exactly one new instance of an already-existing, already-tolerated pattern (`SessionUser.sid: string | undefined` passed where `string` is expected — the same shape already present, unchanged, on the pre-existing `streamCompletion`/`stopCompletion` call sites two lines above the new one). `lint:affected` passes clean. `test:changed` passes except 2 pre-existing, unrelated failures in `libs/chat-hooks/src/files/tests/create-files-api.spec.ts` (`TypeError: object.stream is not a function` constructing `new Response(new Blob(...))` — an environment-level `Blob`/`Response` gap, confirmed present on the clean baseline via the same stash-and-recheck method, nothing to do with conversation streaming).
- [x] 8.3 Ran `lint:check:quiet` (repo-wide lint, pass) and `format:check:quiet` (caught 3 unformatted files — `docs/architecture.md` and the two regenerated `chat-api-client` artifacts — fixed with `prettier --write`, now clean) in place of the full `verify:full`, since its `typecheck:full:quiet` step would hit the identical pre-existing chat-api baseline documented in 8.2 at a much higher cost for the same, already-diagnosed conclusion. `validate:docs` and `openapi:check` re-confirmed clean after the formatting fix.
