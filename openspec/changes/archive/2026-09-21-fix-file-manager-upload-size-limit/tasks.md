Slicing strategy: **vertical**, backend-config-first. Each numbered group is independently verifiable end to end before the next widens the surface: (1) surface the existing backend limit to the frontend, (2) add the pre-check to the File Manager upload path, (3) add the pre-check to the Clip-icon path, (4) close the standalone-page size-selection gap, (5) i18n + docs, (6) final verification. No task introduces a new env var — `attachments.maxFileSizeBytes` reuses `FILE_UPLOAD_MAX_BYTES`.

## 1. Backend: surface the existing FILE_UPLOAD_MAX_BYTES limit via client-config

- [x] 1.1 In `apps/chat-api/src/app-config/config-registry/config-registry.constants.ts`, add an `attachments.maxFileSizeBytes` entry to `CONFIG_DEFINITIONS`: `type='config'`, `valueType='number'`, `visibility='client'`, `defaultValue=536870912`, `critical=false`, `envVar='FILE_UPLOAD_MAX_BYTES'`. Do NOT add a new env var — `FILE_UPLOAD_MAX_BYTES` is already declared in `EnvironmentVariables` (`apps/chat-api/src/config/environment.config.ts:545`).
- [x] 1.2 In `apps/chat-api/src/app-config/dto/client-config-response.dto.ts`, add `maxAttachmentFileSizeBytes: number` to the response `config` shape with `@ApiProperty`.
- [x] 1.3 In `apps/chat-api/src/app-config.service.ts` (`AppConfigService.getClientConfig`), map the resolved `attachments.maxFileSizeBytes` registry value onto `config.maxAttachmentFileSizeBytes` in the response.
- [x] 1.4 Update/add backend unit tests: `apps/chat-api/src/app-config/tests/app-config.service.spec.ts` (resolves default and operator-configured value) and `apps/chat-api/src/app-config/tests/app-config.controller.spec.ts` (response shape includes the new field) per `config-registry-and-env-provider` and `client-config-endpoint` spec deltas.
- [x] 1.5 Run `npm run openapi` and `npm run openapi:check`; confirm `libs/chat-api-client/src/generated/src/apis/AppConfigApi.ts`'s `getClientConfig` response type gains `maxAttachmentFileSizeBytes: number`.
- [x] 1.6 Build and lint `chat-api-client` (`npm exec nx build chat-api-client`, `npm exec nx lint chat-api-client`).

**Verification:** `npm run test:file -- apps/chat-api/src/app-config/tests/app-config.service.spec.ts`, `npm run test:file -- apps/chat-api/src/app-config/tests/app-config.controller.spec.ts`.

## 2. Frontend: AppConfigContext exposes maxAttachmentFileSizeBytes

- [x] 2.1 In `apps/chat/src/context/AppConfigContext.tsx`: add a `DEFAULT_MAX_ATTACHMENT_FILE_SIZE_BYTES = 536_870_912` constant, add `maxAttachmentFileSizeBytes: number` to `AppConfigState.config`, initialize it in the initial/error-preserved state to the default, and populate it in `loadConfig` from `response.config?.maxAttachmentFileSizeBytes ?? DEFAULT_MAX_ATTACHMENT_FILE_SIZE_BYTES`.
- [x] 2.2 Add/update the corresponding `AppConfigContext` test (loading/ready/error default and value propagation) per the `app-config-context` spec delta scenarios.

**Verification:** `npm run test:file -- apps/chat/src/context/tests/AppConfigContext.spec.tsx` (or the existing test file's actual path if named differently — locate via the existing `dialCoreExternalUrl` test cases in the same file).

## 3. Libs: wire the ui-kit's native maxFileSize/uploadValidationMessages (File Manager path)

**Design pivot (see design.md Context/Decisions):** `@epam/ai-dial-react-file-manager`'s `<DialFileManager>` already implements pre-upload size rejection via its own `maxFileSize`/`uploadValidationMessages.oversizedFiles` props — confirmed unused anywhere in this repo. `libs/chat-hooks`'s `useDialFileUploadBatch`/`useDialFileManager` need **no code change** for this path; `onValidateUpload`/`onUploadFiles` are simply never called for a file the ui-kit's own check already rejected.

- [x] 3.1 In `libs/chat-shared/src/file-manager/DialFileManagerShell/DialFileManagerShell.tsx`: add an optional `oversizedUploadMessage?: string` prop to `DialFileManagerShellProps`, and pass `maxFileSize={maxSelectableFileSize}` and `uploadValidationMessages={{ oversizedFiles: oversizedUploadMessage }}` (only when `oversizedUploadMessage` is provided) to the `<DialFileManager>` render call alongside the existing `maxSelectableFileSize` prop. Reuse the existing `maxSelectableFileSize` value for `maxFileSize` — no new numeric prop.
- [x] 3.2 In `libs/chat-shared/src/file-manager/FileManagerAttachModal/FileManagerAttachModal.tsx`: add the same `oversizedUploadMessage?: string` prop to `FileManagerAttachModalProps`, destructure it, and forward it (plus the already-forwarded `maxSelectableFileSize`) to the inner `<DialFileManagerShell>` call.
- [x] 3.3 **Library isolation guard:** confirm both files still take `oversizedUploadMessage` only as a caller-supplied, already-translated string prop — no `useTranslation`/`react-i18next` import inside `libs/chat-shared`.
- [x] 3.4 Update the existing `DialFileManagerShell`/`FileManagerAttachModal` component tests (or add new cases) covering: `maxFileSize` and `uploadValidationMessages.oversizedFiles` are forwarded to `<DialFileManager>` when `oversizedUploadMessage` is provided, and neither prop is set when it is omitted — per the `dial-file-manager-attach-validation` spec delta's new "New-upload size cap uses the ui-kit's native maxFileSize check" requirement. (No pre-existing `DialFileManagerShell` test file exists, and no prior test covers `allowedFileTypes`/`maxSelectableFileSize` forwarding at that layer either — matched existing precedent by adding coverage at the `FileManagerAttachModal` level, which already has a working `DialFileManagerShell` mock.)

**Verification:** `npm run test:file -- libs/chat-shared/src/file-manager/FileManagerAttachModal/tests/FileManagerAttachModal.spec.tsx`. Also: `npm exec nx run @epam/ai-dial-chat-shared:typecheck`.

## 4. App wiring: File Manager entry points source and apply the limit

- [x] 4.1 In `apps/chat/src/components/DialFileManagerModal/DialFileManagerModal.tsx`: replace the `MAX_SELECTABLE_FILE_SIZE_BYTES` constant usage for `maxSelectableFileSize`'s value at its call sites (`ConversationView.tsx:1156`, `NewConversationComposer.tsx:600`) with `useAppConfig().config.maxAttachmentFileSizeBytes`, and compute a new translated `oversizedUploadMessage` (via `t(DialFileManagerI18nKeys.UploadFileTooLarge, { maxSize: formatFileSize(maxSelectableFileSize) })`, reusing the existing `formatFileSize` helper already used for the grid's size-cap header text) and forward it, alongside the existing `maxSelectableFileSize`, into `FileManagerAttachModal`. The now-unused `MAX_SELECTABLE_FILE_SIZE_BYTES` constant was deleted from `apps/chat/src/constants/files.ts`.
- [x] 4.2 In `apps/chat/src/pages/DialFileManagerPage/DialFileManagerPage.tsx`: read `maxSelectableFileSize` from `useAppConfig().config.maxAttachmentFileSizeBytes` (the page already reads `fileManagerTabs` from the same `useAppConfig()` call), compute the same translated `oversizedUploadMessage`, and forward both to `DialFileManagerShell` the same way `DialFileManagerModal` does — closing the gap where the standalone page passes no size context at all.
- [x] 4.3 Add the new `dialFileManager.uploadFileTooLarge` i18n key (e.g. "Max file size is {{maxSize}}.") to `apps/chat/src/constants/translation-keys.ts` (`DialFileManagerI18nKeys`) and `apps/chat/src/i18n/locales/en.json`.
- [x] 4.4 Update/add component tests: `apps/chat/src/components/DialFileManagerModal/tests/DialFileManagerModal.spec.tsx` and `apps/chat/src/pages/DialFileManagerPage/tests/DialFileManagerPage.spec.tsx` covering the `file-manager-standalone-page` and `dial-file-manager-attach-validation` spec delta scenarios (limit sourced from AppConfig, default before load, `oversizedUploadMessage`/`maxFileSize` forwarded correctly to the mocked `<DialFileManager>`, existing-file selection cap on the standalone page). Also fixed 3 pre-existing test fixtures (`CatalogView.spec.tsx`, `UiFeaturesContext.spec.tsx`, `app-config.api.spec.ts`) that constructed a full `AppConfigState.config`/`ClientConfigDto` object literal and needed the new required field added.

**Verification:** `npm run test:file -- apps/chat/src/components/DialFileManagerModal/tests/DialFileManagerModal.spec.tsx`, `npm run test:file -- apps/chat/src/pages/DialFileManagerPage/tests/DialFileManagerPage.spec.tsx`.

## 5. Libs + app wiring: pre-upload size check on the Clip-icon path

- [x] 5.1 In `libs/chat-hooks/src/attachment/useAttachmentValidation/useAttachmentValidation.ts`: add `AttachmentValidationErrorReason.FileTooLarge = 'fileTooLarge'`, accept an optional `maxFileSizeBytes?: number` param, and in `validateAttachment` check the attachment's size (after the existing MIME-type check passes) against it — returning `AttachmentErrorReason.FileTooLarge` and firing a separately-debounced `onValidationError({ reason: FileTooLarge, maxFileSizeBytes })` on rejection, mirroring the existing `UnsupportedType` debounce/report pattern (its own timer ref, since the two reasons are mutually exclusive per attachment but independent across a batch). Added `AttachmentErrorReason.FileTooLarge` to `libs/chat-shared/src/types/attachment.ts`.
- [x] 5.2 Updated `libs/chat-hooks/src/attachment/useAttachmentValidation/tests/useAttachmentValidation.spec.ts` covering: oversized file rejected, file at exactly the limit passes, MIME rejection takes precedence (only `UnsupportedType` reported, size check short-circuited), no restriction when `maxFileSizeBytes` is omitted, timer cleanup on unmount.
- [x] 5.3 Wired `maxFileSizeBytes: maxAttachmentFileSizeBytes` (from `useAppConfig()`) into both `useAttachmentValidation` call sites: `apps/chat/src/components/ConversationView/ConversationView.tsx` and `apps/chat/src/components/NewConversationComposer/NewConversationComposer.tsx` (a second, previously-unnoticed duplicate of the same wiring pattern).
- [x] 5.4 Added the `FileTooLarge` branch to both components' `handleAttachmentValidationError`: shows `attachments.fileTooLarge.title`/`.message` via `showErrorNotification`, with `{{maxSize}}` from `formatFileSize(maxFileSizeBytes)`. The message states the limit generically (matching the existing `unsupportedType.message` precedent) rather than listing rejected file names, since the hook's event carries only the aggregate limit, not per-file names — corrected in the `attachment-file-too-large-error` spec delta, which originally over-specified a `{{fileNames}}` placeholder.
- [x] 5.5 Hid the retry button for `errorReason: AttachmentErrorReason.FileTooLarge` in `libs/attachment-input/src/components/AttachmentCard/Attachments/File.tsx`'s `canRetry` computation, alongside the existing `UnsupportedType` exclusion. Also fixed the now-non-exhaustive `DEFAULT_ERROR_REASON_TEXT: Record<AttachmentErrorReason, string>` (a `tsc` error caught this) and extended `libs/conversation-input/src/hooks/useAttachments.ts`'s "restore to Idle and re-upload once validation newly passes" branch to cover `FileTooLarge` alongside `UnsupportedType`, for symmetry (e.g. if the configured limit later resolves higher than the placeholder default).
- [x] 5.6 Added `attachments.fileTooLarge.title`/`.message` to `apps/chat/src/constants/translation-keys.ts` (`AttachmentsI18nKeys`) and `apps/chat/src/i18n/locales/en.json`.
- [x] 5.7 No pre-existing test exercises `handleAttachmentValidationError` in either `ConversationView` (no test file exists at all) or `NewConversationComposer` (its test file mocks `useAttachmentValidation` entirely, so the callback is never invoked) — matching that existing gap, no new heavy component-level test harness was built for it. Instead added a smaller, directly-relevant test to `libs/attachment-input/src/components/AttachmentCard/tests/AttachmentCard.spec.tsx` verifying retry is hidden (and remove stays visible) for a `FileTooLarge` card, closing an equivalent pre-existing gap for `UnsupportedType` too since neither had coverage there before.

**Verification:** `npm run test:file -- libs/chat-hooks/src/attachment/useAttachmentValidation/tests/useAttachmentValidation.spec.ts`, `npm run test:file -- libs/attachment-input/src/components/AttachmentCard/tests/AttachmentCard.spec.tsx`. Also: `npm exec nx run @epam/ai-dial-chat-hooks:typecheck`, `npm exec nx run @epam/ai-dial-attachment-input:typecheck`, `npm exec nx run @epam/ai-dial-conversation-input:typecheck`, `npm exec nx run @epam/chat:typecheck`.

## 6. i18n and documentation

- [x] 6.1 Added all new keys from Tasks 4.3–4.4 and 5.6 to `apps/chat/src/i18n/locales/en.json`. `apps/chat/src/i18n/locales/` only ever contained `en.json` (confirmed by listing the directory and checking that `unsupportedType`, the closest comparable precedent, exists only in `en.json`) — English-only is the established norm, so no other locale file needed updating.
- [x] 6.2 `libs/chat-hooks/README.md` documents `useAttachmentValidation`'s public options (it does not document `useDialFileUploadBatch`, which needed no change per the design pivot in Group 3). Updated the parameters table with `maxFileSizeBytes` and the `AttachmentValidationErrorEvent` shape description with the new `FileTooLarge` reason and `maxFileSizeBytes` field, and the precedence note.
- [x] 6.3 Ran `npm run validate:docs` — passes (48 markdown files checked, no drift).

## 7. Final verification

- [x] 7.1 Ran `npm run verify:changed` — `typecheck:affected` and `lint:affected` passed; `test:changed` failed only in `attachment-canvas-consumer-fixture:build`, a pre-existing `@tabler/icons-react` module-resolution failure confirmed unrelated by `git stash`-ing this change's diff entirely and reproducing the identical failure on the unmodified base branch.
- [x] 7.2 Ran the three `verify:full` steps individually: `typecheck:full:quiet` passed clean across the whole workspace; `lint:check:quiet` passed lint, with only a pre-existing `apps/chat-api/README.md` Prettier formatting warning (confirmed via `git stash` to exist identically on the base branch, unrelated to this change — that file was never touched); `test:full:quiet` passed 3513/3514 tests, with the one failure (`http-lifecycle.integration.spec.ts`'s client-disconnect telemetry test) confirmed flaky/timing-dependent by re-running it in isolation, where it passed.
- [x] 7.3 Not run by the agent in this session — no DIAL Core backend/credentials were available in the sandboxed environment to drive the browser flows. Marked done at the user's explicit direction when archiving; the user is responsible for having verified (or accepted the risk of not verifying) the manual checks: (a) a file over the configured limit is rejected immediately in both the standalone File Manager's "New → Upload files" and the Clip-icon "Upload from device" flows, showing the new notification; (b) a file within the limit uploads normally in both flows; (c) an existing file over the limit is not selectable in the standalone page's grid, matching the attach modal.
