## 1. Library: Content summary and the file selector

**Superseded by this revision** (implemented against the flat `CatalogContentFile` shape; the underlying code and tests are replaced wholesale by section 1a, not extended):

- [x] ~~1.2 Add `CatalogContentFile`, plus `files?` and `selectedFileId?` on `CatalogItemPromptContent`~~ — superseded by 1a.2 (`CatalogContentTreeNode` union replaces `CatalogContentFile`).
- [x] ~~1.5 Render the picker in `Content.tsx` with `InlineSelect`~~ — superseded by 1a.5/1a.6 (`Dropdown` + `InlineSelectTrigger` + a new `ContentFileTree` component replace `InlineSelect`).
- [x] ~~1.6 Style the picker row~~ — superseded by 1a.7 (row styling now covers folder/file rows, indentation, and the disclosure chevron, not a single flat row).
- [x] ~~1.7 Hold the picked file in `DetailsPanel` as an overlay~~ — superseded by 1a.8 (the overlay logic is unchanged, but the same effect now also owns the expanded-folder set and the selector's open state).
- [x] ~~1.9 Add `ContentTab` tests~~ — superseded by 1a.10 (rewritten against the tree shape, plus new tree-specific scenarios).
- [x] ~~1.10 Add `DetailsPanel` tests~~ — superseded by 1a.10 (same file, extended with expand/collapse and selector-open-state coverage).

**Not superseded — unaffected by this revision, still complete:**

- [x] 1.1 Add optional `description` to `CatalogItemPromptContent` in `libs/catalog/src/models/item-details-data.ts` with a JSDoc stating it takes precedence over `item.description`
- [x] 1.3 Add `contentFileSelectorAriaLabel`, `contentFileCountLabel`, `contentFileLoadingLabel`, and `contentFileErrorLabel` to `ItemDetailsTexts`; add `contentFileCountClassName` to `ItemDetailsTypography` and `contentFileCountText` to `ItemDetailsColors`
- [x] 1.4 Add `onLoadContentFile?: (fileId: string) => Promise<string | undefined>` to `DetailsPanelProps` and `CatalogProps`, and forward it through `Catalog.tsx`
- [x] 1.8 Pass `promptContent.description ?? item.description` to `ContentTab`

## 1a. Library: the hierarchical file selector (replaces the flat picker)

- [x] 1a.1 Add `CatalogContentNodeType` enum (`File`, `Folder`) to a new `libs/catalog/src/types/catalog-content-node-type.ts`
- [x] 1a.2 Remove `CatalogContentFile` from `libs/catalog/src/models/item-details-data.ts`; add `CatalogContentFileNode`, `CatalogContentFolderNode`, and the `CatalogContentTreeNode` union (folder nodes nesting further `items: CatalogContentTreeNode[]`); retype `CatalogItemPromptContent.files` as `CatalogContentTreeNode[]`; export all three new types plus the enum from `libs/catalog/src/index.ts`, and remove the old export
- [x] 1a.3 Add `expandedFolderIds: ReadonlySet<string>`, `onToggleFolder: (folderId: string) => void`, `isFileSelectorOpen: boolean`, and `onFileSelectorOpenChange: (open: boolean) => void` to `ContentTabProps`
- [x] 1a.4 Add a recursive file-count helper and a default-expanded-set helper (e.g. `countFileNodes(nodes): number`, `collectAllFolderIds(nodes): Set<string>`) in a new `libs/catalog/src/utils/catalog-content-tree.ts`, each with unit tests covering nested folders and an empty tree
- [x] 1a.5 Create `libs/catalog/src/components/Details/TabsContent/ContentFileTree/ContentFileTree.tsx`: a recursive component rendering one row per node — a folder row with icon, name, and disclosure chevron; a file row with name only — respecting `expandedFolderIds`, invoking `onToggleFolder`/`onSelectFile`, applying one fixed indentation step per nesting depth via logical (`ps-*`) spacing on each `role="group"` wrapper
- [x] 1a.6 Rebuild the selector in `Content.tsx`: replace `InlineSelect` with `Dropdown` (`matchReferenceWidth={false}`, controlled `open={isFileSelectorOpen}`/`onOpenChange={onFileSelectorOpenChange}`, `renderOverlay` rendering `ContentFileTree`) and `InlineSelectTrigger` as the trigger, showing the currently displayed file's basename and the `isFileSelectorOpen` state
- [x] 1a.7 Style folder and file rows with logical Tailwind utilities and `dial-*-text` defaults confirmed against `getEntityDetails("typography")`; keep the existing `--cat-details-file-count-text` var; give the disclosure chevron a collapsed-state rotation with an `rtl:` counterpart per design.md D3c
- [x] 1a.8 Extend `DetailsPanel`'s existing picked-file effect (`DetailsPanel.tsx:222-225`) to also own `expandedFolderIds` (defaulting, per item, to every folder id in the tree) and `isFileSelectorOpen`, resetting both alongside the picked-file overlay whenever `item.id` or `selectedFileId` changes
- [x] 1a.9 Implement the full keyboard contract in `ContentFileTree`/`ContentTab`: roving `tabIndex`, ArrowUp/ArrowDown between visible rows, ArrowRight/ArrowLeft to expand/collapse, Enter/Space to toggle a folder or pick a file, Escape and outside-click to close without changing selection; focus lands on the currently displayed file's row on open and returns to the trigger on every close path
- [x] 1a.10 Rewrite `ContentTab` and `DetailsPanel` tests against the tree shape: no selector for a tree with zero or one file at any depth; selector and count for a multi-file tree; trigger shows the open file's basename; a collapsed folder hides its children and an empty folder still renders; expanding reveals one further indent level; picking a nested file fires the id unchanged; keyboard navigation (arrow keys, Enter/Space, Escape) and focus placement/restoration; switching items resets expand state and closes the selector; loading status region present while loading and absent after
- [x] 1a.11 Update `libs/catalog/README.md` for `CatalogContentTreeNode`, the hierarchical selector section (replacing the flat-picker section), and the unchanged `onLoadContentFile`; run the catalog suite and lint

## 2. Manifest parser

- [x] 2.1 Move `yaml` from `devDependencies` to `dependencies` in the root `package.json`, keeping `2.8.3`, and reinstall so the lockfile records the move
- [x] 2.2 Move `SkillAboutDetails` and `SkillEntityDetails` from `apps/chat/src/types/entity-details.ts` to `apps/chat/src/types/skill.ts` and update importers
- [x] 2.3 Create `apps/chat/src/utils/skill-manifest.ts` exporting `parseSkillManifest(raw): SkillManifest` — leading-fence scan (BOM and CRLF tolerated), `yaml.parse` of the fence body, single leading blank line stripped from `body`, whole input returned as `body` when there is no opening fence, no closing fence, or the parse throws
- [x] 2.4 Implement the alias table from design D1 with exact key matching, post-parse type checks (non-string scalars dropped, lists filtered to strings, bare string promoted to a one-element array, empty values omitted), and `about` left `undefined` when no `about.*` field resolved
- [x] 2.5 Unit-test `parseSkillManifest`: frontmatter present; no frontmatter; unterminated fence; frontmatter-only (empty body); BOM + CRLF; all three spellings per field; flow and block sequences; scalar promoted to list; wrong-typed value dropped; unrecognised keys ignored; malformed YAML returns the whole input as body without throwing; `about` undefined when only `name`/`description` are present

## 3. App mappers

- [x] 3.1 Split `buildSkillOverview` into a Specification section (when-to-use, allowed tools, bundled resources — each row omitted when absent, whole section omitted when empty, `skillPrompt` never rendered) followed by the Details section (author, updated, file count); remove the per-file rows
- [x] ~~3.2 Add `buildSkillContentFiles(files)`~~ — superseded by 3a.1 (`buildSkillContentTree` builds a nested tree instead of a flat, path-labelled list).
- [x] 3.3 Delete `mapSkillDetails` from `apps/chat/src/utils/map-entity-details-to-catalog.ts` and the `{ type: 'SKILL' }` member from `EntitySpecificDetails`; confirm the switch still compiles exhaustively
- [x] 3.4 Unit-test the split overview: frontmatter present → both sections; no frontmatter → Details only; every `about` field absent → no Specification section; `skillPrompt` present → no row for it; author absent → author row omitted; folders excluded from the count; no per-file rows
- [x] ~~3.5 Unit-test `buildSkillContentFiles`~~ — superseded by 3a.2 (tests rewritten against the tree-building algorithm).

## 3a. App mapper: the file tree (replaces `buildSkillContentFiles`)

- [x] 3a.1 Replace `buildSkillContentFiles` with `buildSkillContentTree(files): CatalogContentTreeNode[]` in `apps/chat/src/utils/map-skill-to-catalog-item.ts`, implementing design.md D3d: seed folder nodes from `nodeType: 'folder'` entries (including empty ones), attach `nodeType: 'item'` entries as file nodes at their path, create any implicit intermediate folder a file's path implies but the listing did not enumerate, and sort each folder's `items` case-insensitively with the manifest pinned first at the root only
- [x] 3a.2 Unit-test `buildSkillContentTree`: a flat two-level listing becomes a two-level tree; an implicit intermediate folder (a nested file with no explicit parent-folder entry) is synthesized correctly; an explicit empty folder entry still appears with `items: []`; two same-named files in different folders both appear as distinct nodes with distinct ids; the manifest sorts first at the root regardless of name; grouping folders are never mistaken for files; an empty listing yields an empty tree

## 4. i18n

- [x] 4.1 Add `catalog.details.skill.specificationSection`, `.whenToUse`, `.allowedTools`, `.bundledResources`, plus `catalog.details.contentFileSelectorAriaLabel`, `.contentFileCount`, `.contentFileLoading`, and `.contentFileError` to `CatalogI18nKeys` and `en.json`
- [x] 4.2 Grep each new English value in `en.json` first and reuse an existing key wherever the string already exists rather than declaring a duplicate
- [x] 4.3 Confirm no `catalog.details.skill.*` key is left unused by the Overview split (`catalog.details.skill.file` was never declared — the per-file rows used `file.path` directly)

## 5. CatalogView wiring

- [x] 5.1 In the Skill branch of `handleFetchDetails`, pass the successfully-read manifest through `parseSkillManifest` and build `promptContent` from `{ content: body, description, files, selectedFileId }`, omitting `description` when absent — **carries forward unchanged**; only `files`' contents change shape via 5a.1
- [x] 5.2 Build the overview from the parsed `about` plus the listing; return each half independently and resolve `undefined` only when both the manifest read and the listing failed
- [x] 5.3 Keep the size guard ahead of the parse: `readSkillManifest` returning `null` must short-circuit before `parseSkillManifest` is called
- [x] 5.4 Add `onLoadContentFile`, resolving through `downloadSkillFile` with the opened skill's `{ bucket, path }` (held in a ref set when the panel opens) and the picked id adapted to the endpoint's file-relative path, reading through `readSkillManifest` and stripping frontmatter only for the normalized `SKILL.md` path — the library callback remains unaware of tree and backend path shapes
- [x] 5.5 Pass the picker's aria-label, count, loading, and error strings through `detailsTexts`
- [x] ~~5.6 Extend `CatalogView.spec.tsx`~~ — superseded by 5a.2 (`promptContent.files` assertions rewritten against the tree shape).
- [x] 5.7 Run the chat skill specs and `npm exec nx lint @epam/chat` — rerun as part of 6.5 after 5a lands

## 5a. CatalogView wiring: switch to the tree builder

- [x] 5a.1 Update the Skill branch of `handleFetchDetails` to call `buildSkillContentTree(files.value.items)` in place of the removed `buildSkillContentFiles`; no other field of the returned `promptContent`/`overview` changes
- [x] 5a.2 Rewrite `CatalogView.spec.tsx`'s picker-related assertions against the tree shape: a listing with folders and files yields the expected nested `promptContent.files`; an implicit intermediate folder appears; an empty folder appears; the tree defaults to `SKILL.md` as `selectedFileId`; listing failure yields an empty tree

## 7. Library: the generic preview model and rendering (new capability: `catalog-content-file-preview`)

- [x] 7.1 Add `CatalogContentPreviewType` enum (`Markdown`, `Text`, `Image`, `Unsupported`) and the `CatalogContentFilePreview` union (`CatalogContentMarkdownPreview`, `CatalogContentTextPreview`, `CatalogContentImagePreview`, `CatalogContentUnsupportedPreview`) to `libs/catalog/src/models/item-details-data.ts`; export all five from `libs/catalog/src/index.ts`
- [x] 7.2 Add `onLoadContentFilePreview?: (fileId: string) => Promise<CatalogContentFilePreview | undefined>` to `DetailsPanelProps` and `CatalogProps`, alongside the existing `onLoadContentFile`; forward it through `Catalog.tsx` unchanged
- [x] 7.3 Add `contentFileUnsupportedLabel` to `ItemDetailsTexts` with an English default (`'Preview is not supported for this file'`), matching design.md D9
- [x] 7.4 Retype `DetailsPanel`'s picked-file overlay from `{ id: string; content: string | null } | null` to carry a `CatalogContentFilePreview | null` instead of a bare `content: string | null`; update `handleSelectContentFile` to call `onLoadContentFilePreview` when supplied, falling back to wrapping `onLoadContentFile`'s result as `{ type: Markdown, text }` when it is the only one supplied, per design.md D7's precedence rule
- [x] 7.5 Add a per-selection request-generation counter (a ref, incremented on every new pick and on every item/`selectedFileId` change) to `DetailsPanel`; guard the async resolution in `handleSelectContentFile` so a result whose generation no longer matches the latest one is discarded without updating state, per design.md D8
- [x] 7.6 Add blob-URL cleanup to `DetailsPanel`: revoke the outgoing picked-file overlay's `url` via `URL.revokeObjectURL` when it is an `image` preview whose `url` starts with `blob:`, at each of: a new file picked, the item-switch reset effect, and an unmount-only cleanup effect
- [x] 7.7 Rework `ContentTab`'s body rendering to dispatch on the resolved preview's `type`: `markdown` through the existing `MarkdownWithPlaceholders` call, `text` through a new `MarkdownCodeBlock` (`@epam/ai-dial-chat-shared`) call with `hideDownload` and `language` forwarded, `image` through a new `<img>` with `alt` from the tree node's `name` and panel-fitting sizing (`max-w-full`, `h-auto`, no distortion), `unsupported` through `texts.contentFileUnsupportedLabel` as plain text; the base file (`content` prop, no pick active) keeps rendering through `MarkdownWithPlaceholders` directly, unchanged
- [x] 7.8 Confirm `libs/catalog/package.json` gains no new dependency; confirm `@epam/ai-dial-attachment-canvas` does not appear anywhere in `libs/catalog/src` or `libs/catalog/package.json`
- [x] 7.9 Add `ContentTab` tests: each of the four preview types renders through its expected path; `text` hides its download control; `image` gets `alt` from the file name and does not overflow; `unsupported` renders the label and does not attempt to decode content
- [x] 7.10 Add `DetailsPanel` tests: `onLoadContentFilePreview` takes precedence over `onLoadContentFile` when both are supplied; `onLoadContentFile` alone still wraps its result as `markdown`, matching pre-revision behavior byte-for-byte; a rejection or `undefined` from either renders the existing error text; picking file B before file A's request resolves and then having A resolve after B leaves B displayed; a preview request in flight when the item switches is discarded on arrival; a `blob:` URL is revoked when replaced, when the item switches, and on unmount; a non-`blob:` URL is never revoked
- [x] 7.11 Update `libs/catalog/README.md`'s multi-file content section for `CatalogContentFilePreview`, `onLoadContentFilePreview`, its precedence over `onLoadContentFile`, and object-URL ownership; run the catalog suite and lint

## 8. App: generic typed skill-file preview (historical; superseded by section 15)

- [x] 8.1 Extract `readSkillManifest`'s size-guard-then-decode logic (`apps/chat/src/utils/map-skill-to-catalog-item.ts:126-141` or wherever it now lives after prior revisions) into a byte-returning sibling (e.g. `readSkillFileBytes(response): Promise<Uint8Array | null>`) using the identical `SKILL_MANIFEST_MAX_BYTES` cap; reimplement `readSkillManifest` as that sibling plus a UTF-8 decode, so the guard exists once
- [x] 8.2 Create `apps/chat/src/utils/skill-content-preview.ts` (or extend the existing preview-adjacent utils file if a better fit is found during implementation) exporting a pure classifier `buildSkillContentPreview(fileName: string, bytes: Uint8Array, mimeType?: string): CatalogContentFilePreview`, calling `inferMimeTypeFromPath`/`getAttachmentTypeFromMime`/`FileExtension` (`@epam/ai-dial-chat-shared`) and `isTextPreviewable`/`extensionToLanguage` (`@epam/ai-dial-attachment-canvas`) — no new extension or MIME table is declared
- [x] 8.3 In `CatalogView.tsx`, add `handleLoadContentFilePreview`, mirroring `handleLoadContentFile`'s existing bucket/path resolution via `openSkillRef`, but calling `readSkillFileBytes` and `buildSkillContentPreview`; special-case `SKILL_MANIFEST_FILE` to always resolve `{ type: 'markdown', text: parseSkillManifest(...).body }`, matching `handleLoadContentFile`'s existing manifest branch; pass it to `Catalog` as `onLoadContentFilePreview` alongside the unmodified `onLoadContentFile`
- [x] 8.4 Add `catalog.details.contentFileUnsupported` to `CatalogI18nKeys` and `en.json`, reusing `AttachmentCanvasI18nKeys.UnsupportedLabel`'s existing English value rather than authoring a new one; pass it through `detailsTexts` as `contentFileUnsupportedLabel`
- [x] 8.5 Confirm `apps/chat/src/utils/skill.ts`'s own `parseSkillManifest`/`ParsedSkillManifest` (the Skill Builder's editable round-trip parser) is not imported anywhere in the details-panel preview path; confirm only `apps/chat/src/utils/skill-manifest.ts::parseSkillManifest` is used there
- [x] 8.6 Unit-test `buildSkillContentPreview`: `.md`/`.markdown` → `markdown`; a text-previewable extension (e.g. `.py`, `.json`, `.yaml`) → `text` with the expected `language` (and `undefined` for an unmapped-but-text-previewable extension); an image MIME/extension → `image` (assert a `blob:`-style URL is produced, without asserting on the exact browser-generated string); a binary/unrecognised extension → `unsupported`; an empty supporting file → `text`/`markdown` with an empty `text`, not `unsupported`
- [x] 8.7 Unit-test `readSkillFileBytes`: identical size-guard behavior to the existing `readSkillManifest` tests (oversized declared length short-circuits before reading; oversized actual body is still rejected when no declared length is present), now asserting on returned bytes instead of decoded text
- [x] 8.8 Extend `CatalogView.spec.tsx`: `onLoadContentFilePreview` resolves each of the four preview types for an appropriately-named fixture file; `SKILL_MANIFEST_FILE` always resolves `markdown` with frontmatter stripped, even when picked as a "supporting file" reselection; an oversized supporting file resolves `undefined` (rendering the existing error text) rather than a corrupted preview
- [x] 8.9 Run the chat skill specs and `npm exec nx lint @epam/chat`

## 6. Verification and rollout

- [ ] 6.1 Run `npm exec nx build chat` and compare the catalog chunk size against the pre-change build; if the `yaml` delta is material, switch to `await import('yaml')` inside the async skill branch and re-measure
- [ ] 6.2 Manually verify against a running stack with real skills: the Content tab shows the description as a summary and prose below with no `---` artefacts; a skill with no frontmatter renders unchanged; the Overview shows Specification then Details with no file rows; a multi-file skill shows the selector, opens with every folder expanded, and switches files on pick; a single-file skill (at any depth) shows no selector; two same-named files in different folders both load their own distinct content; the tab row is unchanged; `OverlayFeature.Skills` off restores today's catalog
- [ ] 6.3 Resolve design.md's open question 1 during 6.2 — record which frontmatter keys the deployed skills actually use and extend or trim the alias table before merge
- [x] 6.4 Confirm `mapSkillDetails` and `type: 'SKILL'` no longer appear anywhere in `apps/chat/src`
- [x] 6.4a Confirm `CatalogContentFile` and `buildSkillContentFiles` no longer appear anywhere in `libs/catalog/src` or `apps/chat/src`, and that `libs/catalog/package.json` gained no new dependency (specifically, no `@epam/ai-dial-react-file-manager`)
- [ ] 6.4b Manually verify keyboard operation and focus behavior against a running stack: open the selector via keyboard, move between rows with arrow keys, expand/collapse a folder with ArrowRight/ArrowLeft and with Enter/Space, pick a file with Enter/Space, close with Escape and confirm focus returns to the trigger, click outside and confirm the same
- [ ] 6.4c Manually verify at a 360px viewport width that the selector never overflows horizontally, and under `dir="rtl"` that the disclosure chevron mirrors between its expanded and collapsed states
- [x] 6.5 Run `npm exec nx affected --target=test --base=origin/development-1.0` and confirm the catalog and chat projects are green; run `npm exec nx typecheck ai-dial-catalog` and `npm exec nx typecheck chat`; confirm `npm run openapi:check` is untouched by this change
- [x] 6.6 Run the five-axis review from `./.claude/skills/code-review-and-quality/SKILL.md` over the diff, paying explicit attention to the `libs/*` isolation rule for the selector, the `CatalogContentTreeNode` model, and the `promptContent` additions

## 9. Verification and rollout for read-only file preview

- [ ] 9.1 Run `npm exec nx build chat` and compare the catalog chunk size against the tree-selector revision's baseline (post-task-6.1); confirm no material delta, since this revision adds no new dependency to either `apps/chat` or `libs/catalog` for preview
- [ ] 9.2 Manually verify against a running stack with real skills carrying a Markdown supporting file, a source-code file (e.g. `.py` or `.js`), and — if any observed skill ships one — an image file: each renders through its expected preview type; a file with an extension neither text-previewable nor an image type renders the unsupported state instead of garbled text; an empty supporting file renders an empty body, not an error
- [ ] 9.3 Manually verify stale-request protection against a running stack: pick a file, immediately pick a different file before the first resolves (e.g. by throttling the network in devtools), and confirm the second selection's content is what remains displayed
- [ ] 9.4 Manually verify object-URL cleanup for an image preview: open an image supporting file, then pick a different file or a different catalog item, and confirm (via devtools' memory/network tooling, or a temporary log) that the previous `blob:` URL is revoked and not leaked
- [ ] 9.5 Manually verify keyboard and focus behavior for the preview area: the preview itself introduces no new focusable control, and the existing selector keyboard contract (task 6.4b) is unaffected by which preview type is currently displayed
- [ ] 9.6 Manually verify at a 360px viewport width and at the 769px desktop boundary that a long unbroken line in a `text` preview does not force the panel wider, and that an image preview is capped to the panel's width; verify document-direction inheritance under `dir="rtl"` introduces no new physical-direction artifact
- [x] 9.7 Confirm `@epam/ai-dial-attachment-canvas` does not appear in `libs/catalog/package.json` or anywhere in `libs/catalog/src`, and that `libs/catalog`'s only new imports from `@epam/ai-dial-chat-shared` are `MarkdownCodeBlock`-related
- [x] 9.8 Run `npm exec nx affected --target=test,lint,typecheck,build --base=origin/development-1.0` (or the equivalent per-project commands) and confirm `@epam/ai-dial-catalog` and `@epam/chat` are green; confirm `npm run openapi:check` is untouched, since this revision makes no backend change
- [x] 9.9 Run the five-axis review from `./.claude/skills/code-review-and-quality/SKILL.md` over the preview diff specifically, paying explicit attention to: the `libs/*` isolation rule for the four preview renderers and the classifier boundary, the `CatalogContentFilePreview` model's genericity, backward compatibility of `onLoadContentFile`, the stale-request guard, and object-URL ownership/cleanup

## 10. Library: generic primary-action precedence (new capability: `catalog-primary-action`)

- [x] 10.1 Add `isDownloadPrimary?: (item: CatalogItem) => boolean` to `DetailsPanelProps` and `CatalogProps`; forward it through `Catalog.tsx` to `DetailsPanel` to `Header`, following the existing `isPrimaryActionVisible`/`isDownloadVisible` forwarding pattern exactly
- [x] 10.2 Add `downloadingStatusLabel` to `ItemDetailsTexts` with an English default (`'Downloading'`), following the naming convention of `deletingStatusLabel`/`unsharingStatusLabel`/`revokingShareStatusLabel`/`loggingOutStatusLabel`
- [x] 10.3 In `Header.tsx`, compute `isDownloadActionPrimary = !!onDownload && (isDownloadVisible?.(item) ?? true) && (isDownloadPrimary?.(item) ?? item.type === CatalogEntityType.Skill)`; render a third `PrimaryButton` branch (label `texts?.downloadActionLabel ?? 'Download'`, `IconDownload`/`Spinner` icon swap, `disabled`/`aria-busy` from a new local `isDownloading` state) in the same slot as the existing "Use in chat"/credentials `PrimaryButton` branches
- [x] 10.4 Extend `shouldShowDownloadAction` (the Manage-menu inclusion rule) with `&& !isDownloadActionPrimary`, so a promoted Download never also appears in the Manage menu
- [x] 10.5 Add local `isDownloading` state to `Header`, reset to `false` on `item.id` change (extend the existing `useEffect` that already resets `recipientsCountStatus`, or add a sibling one); implement `handleDownloadPrimary` per design.md D11 (guard against a click while pending; `try`/`finally` around the awaited `onDownload` call) — the existing Manage-menu `handleDownload` (fire-and-forget) stays completely unchanged and is used only by the Manage-menu branch
- [x] 10.6 Add a `role="status"`/`aria-live="polite"` `sr-only` region in `Header.tsx`, present only while `isDownloading` is `true`, announcing `texts?.downloadingStatusLabel ?? 'Downloading'`
- [x] 10.7 Add `Header` tests: Skill with `onDownload` defaults to Download-as-primary and no "Use in chat"; Model keeps "Use in chat" regardless of `onDownload`; Toolset's credentials swap takes precedence over Download even if `isDownloadPrimary` would return `true` for it; `isDownloadPrimary={() => false}` suppresses promotion; `isDownloadPrimary` can promote a non-Skill type; Download never renders in both the primary slot and the Manage menu for the same item; clicking the primary button disables it and announces the status region; a second click while pending does not call `onDownload` twice; the button re-enables after both a resolved and a rejected `onDownload`; switching `item.id` while a call is pending leaves no stale pending state on the new item; the Manage-menu Download entry's fire-and-forget behavior is byte-for-byte unchanged
- [x] 10.8 Update `libs/catalog/README.md`'s action-configuration section for `isDownloadPrimary` and the promoted button's pending-state contract; run the catalog suite and lint

## 11. App: skill archive download wiring (new capability: `skill-archive-download`)

- [x] 11.1 Confirm (already verified during investigation, re-confirm at implementation time) that `apps/chat/src/server-api/skills.api.ts::downloadSkill(bucket, path, signal?)` still returns the raw `Response` via `downloadSkillRaw`, unmodified
- [x] 11.2 In `CatalogView.tsx`, add a `CatalogEntityType.Skill` branch to the existing `handleDownload`: resolve `{ bucket, path }` from `openSkillRef.current` (return early if unset), call `downloadSkill(bucket, path)`, throw on `!response.ok`, resolve the fallback filename as `` `${sanitizeFileName(item.name)}.zip` `` (importing `sanitizeFileName` from `apps/chat/src/utils/file-name.ts`), call `triggerBrowserDownload(response, fallbackName)` (from `apps/chat/src/utils/file-download.ts`), and report success via `notifyOperationSuccess(NotifiableEntity.Skill, EntityOperation.Downloaded, { name: savedName })`; leave the existing `CatalogEntityType.Prompt` branch untouched
- [x] 11.3 Add the matching `CatalogEntityType.Skill` branch to `isDownloadVisible`, alongside the existing `CatalogEntityType.Prompt` one
- [x] 11.4 Add a `catch` path for the Skill branch mirroring the existing Prompt branch: `getApiErrorDetails(err)` → `showErrorNotification({ message: t(CatalogI18nKeys.DetailsSkillDownloadError), requestId: traceId })`
- [x] 11.5 Add `DetailsSkillDownloadError = 'catalog.details.skillDownloadError'` to `CatalogI18nKeys` and `"skillDownloadError": "Couldn't download this skill. Please try again."` to `en.json`, matching `promptDownloadError`'s exact wording pattern with the noun swapped
- [x] 11.6 Add `[EntityOperation.Downloaded]: { titleKey: EntityNotificationsI18nKeys.SkillDownloadedTitle, messageKey: EntityNotificationsI18nKeys.SkillDownloaded }` to `NotifiableEntity.Skill`'s existing entry in `apps/chat/src/utils/entity-notification.ts`; add the two new i18n keys/values (`entityNotifications.skill.downloadedTitle` = `'Skill downloaded successfully'`, `entityNotifications.skill.downloaded` = `'Skill "{{name}}" is saved on your device.'`), matching the `Prompt`/`Folder` pairs' exact wording pattern
- [x] 11.7 Pass `isDownloadPrimary` (or omit it, relying on `libs/catalog`'s `CatalogEntityType.Skill` default) and the existing `onDownload`/`isDownloadVisible` through to `Catalog` in `CatalogView.tsx`'s render
- [x] 11.8 Unit-test the Skill branch of `handleDownload`/`isDownloadVisible` in `CatalogView.spec.tsx`: calls `downloadSkill` with the open skill's bucket/path; a server-provided `Content-Disposition` filename is used; a missing filename falls back to the sanitized skill name; a skill name containing unsafe characters is sanitized in the fallback; a Unicode skill name/filename passes through unchanged; a non-OK response is treated as a failure without decoding; a rejected/erroring call shows the generic skill-download error notification and leaves the panel open (no throw out of the handler); an empty response body still resolves a saved filename; the existing Prompt branch's tests are unaffected
- [x] 11.9 Run the chat skill specs and `npm exec nx lint @epam/chat`

## 12. Verification and rollout for whole-skill archive download

- [ ] 12.1 Manually verify against a running stack: a skill's details panel shows Download as its primary action in the same slot a Model's "Use in chat" occupies; the skill's Manage menu has no Download entry; a Model still shows "Use in chat" and its Manage menu is unaffected; a Prompt's Download still lives only in its Manage menu with no pending-state UI; a Toolset's credentials button is unaffected
- [ ] 12.2 Manually verify a successful download for a personal skill, a skill shared with the current user, and a public/organisation skill (per the discovered DIAL Core authorization contract), confirming the archive is byte-identical to what the Skill Builder's own load path already downloads for the same skill
- [ ] 12.3 Manually verify the pending state: click Download, confirm the button disables and announces "Downloading" (via a screen reader or the accessibility tree), confirm a second click during the request does nothing, and confirm the button re-enables once the file saves
- [ ] 12.4 Manually verify failure handling by simulating a network failure or 4xx/5xx (e.g. via devtools request blocking): the generic failure notification appears, the panel stays open, and the button re-enables
- [ ] 12.5 Manually verify closing the details panel and switching to a different catalog item while a download is in flight: no stale "downloading" indicator appears on the new item or on reopening the same skill, and no console error or dangling object URL results
- [ ] 12.6 Manually verify at a 360px viewport width and at the 769px desktop boundary that the primary Download button, Share button, and Manage control wrap without horizontal overflow; verify the download icon is not mirrored under `dir="rtl"`; verify keyboard-only activation (Tab to the button, Enter/Space to activate)
- [x] 12.7 Confirm `libs/catalog/src` contains no reference to `downloadSkill`, `Content-Disposition`, `SkillsApi`, or any bucket/skill-path identifier, and no `fetch(`/`URL.createObjectURL`/`URL.revokeObjectURL` call, per `skill-archive-download`'s isolation requirement
- [x] 12.8 Run `npm exec nx affected --target=test,lint,typecheck,build --base=origin/development-1.0` and confirm `@epam/ai-dial-catalog` and `@epam/chat` are green; run `npm run openapi:check` and confirm no diff, since D10 established this revision makes no backend or generated-client change
- [x] 12.9 Run the five-axis review from `./.claude/skills/code-review-and-quality/SKILL.md` over the archive-download diff, paying explicit attention to: library isolation (no backend/API knowledge in `libs/catalog`), the `isDownloadPrimary` precedence logic's interaction with the existing Toolset credentials-swap, the pending-state reset-on-item-switch argument (D11), and filename sanitization

## 13. Correct the manifest's initial selector state

- [x] 13.1 Add `resolveSkillManifestFileId(files, skillPath)` in `apps/chat/src/utils/map-skill-to-catalog-item.ts`, recognising both file-relative and Core-prefixed manifest paths while returning the matched listing id verbatim
- [x] 13.2 Use that resolved id for `promptContent.selectedFileId` in `apps/chat/src/components/CatalogView/CatalogView.tsx`, retaining `SKILL_MANIFEST_FILE` as the fallback when the listing is unavailable or contains no recognised manifest entry
- [x] 13.3 Add mapper and `CatalogView` regression tests covering file-relative ids, prefixed `{skillPath}/files/SKILL.md` ids, and a nested supporting file also named `SKILL.md`

## 14. Normalize Core listing ids for single-file preview downloads

Tasks 14.2's reference to the former typed-preview handler describes the historical implementation; section 15 keeps the same normalization in the shared-renderer loader.

- [x] 14.1 Add a pure app-level helper that converts `{skillPath}/files/{relativePath}` and `files/{relativePath}` listing ids to the relative `filePath` expected by the single-file endpoint, leaves already-relative ids unchanged, and rejects folder-root ids
- [x] 14.2 Apply the normalized path in both `handleLoadContentFile` and `handleLoadContentFilePreview`, including manifest detection and supporting-file classification, while preserving the opaque listing id in the catalog tree and selection state
- [x] 14.3 Add mapper and `CatalogView` regression tests for Core-prefixed `openai.yaml` and `SKILL.md` ids and for already-relative nested paths

## 15. Correct supporting-file preview to use the actual Skill Builder renderer

**Slicing strategy: contract-first, then one app path end to end.** The optional lib render slot lands without changing existing consumers; the app then adopts it by sharing Skill Builder's existing component/hook, after which the superseded classifier is removed.

- [x] 15.1 Add optional `renderContentFilePreview(fileId, fileName)` to `CatalogProps`/`DetailsPanelProps`, forward it through `Catalog`, and make it take precedence over both async callbacks only for non-base files; keep `libs/catalog` free of attachment-canvas, skills API, app-context, bucket, and path knowledge
- [x] 15.2 Let `ContentTab` render the host node in a `min-h-0 flex-1 overflow-hidden` body so the shared renderer owns internal scrolling on mobile and desktop; retain the existing typed preview renderer as a backward-compatible fallback
- [x] 15.3 Extract `SkillFilePreview` to `apps/chat/src/components/SkillFilePreview/` and `useSkillFilePreviewSync` to `apps/chat/src/hooks/attachment/`, updating Skill Builder to consume those shared app modules without behavioral changes
- [x] 15.4 Add `SkillDetailsFilePreview` at the app edge: lazily load normalized supporting-file bytes, apply the existing size cap, omit generic `application/octet-stream`, preserve specific MIME, feed the same `SkillFileContent` map into the shared sync hook, and map 403/other failures to attachment-canvas forbidden/load-error content
- [x] 15.5 Switch `CatalogView` from `onLoadContentFilePreview` to the host render slot and remove the superseded `skill-content-preview.ts` classifier; keep `SKILL.md` on the parsed base-instructions path
- [x] 15.6 Add catalog precedence/base-manifest tests, app loader/path/MIME/status tests, and `SkillDetailsFilePreview` shared-pipeline tests; run targeted catalog/chat suites, lint, typecheck, and diff validation
