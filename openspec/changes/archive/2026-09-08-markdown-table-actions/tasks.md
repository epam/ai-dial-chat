## 1. Shared Table Foundation

- [x] 1.1 Add the `MarkdownTableCopyFormat` string enum, `MarkdownTableActionLabels` contract, and CSV/TXT/Markdown row serializers under `libs/chat-shared/src/components/MarkdownRenderer/Table/`. Cover quoting, escaping, tab delimiters, Markdown separator rows, trimmed plain text, and empty cells in `libs/chat-shared/src/components/MarkdownRenderer/Table/tests/table-serialization.spec.ts`. Verification: `npm run test:file -- libs/chat-shared/src/components/MarkdownRenderer/Table/tests/table-serialization.spec.ts`.
- [x] 1.2 Extend `downloadTextFile` in `libs/chat-shared/src/utils/file-download.ts` with an optional MIME type while preserving the existing `text/plain;charset=utf-8` default. Add a test that verifies the CSV MIME type reaches the downloaded blob in `libs/chat-shared/src/utils/tests/file-download.spec.ts`. Verification: `npm run test:file -- libs/chat-shared/src/utils/tests/file-download.spec.ts`.
- [x] 1.3 Implement the opt-in action bar, copy state, CSV download, stable accessible names, decorative-icon handling, and polite copied-status region in `libs/chat-shared/src/components/MarkdownRenderer/Table/MarkdownTable.tsx`. Extend `libs/chat-shared/src/components/MarkdownRenderer/Table/tests/MarkdownTable.spec.tsx` for action visibility, streaming suppression, all three copy formats, clipboard fallback success, download MIME/BOM/filename, and copied feedback. Verification: `npm run test:file -- libs/chat-shared/src/components/MarkdownRenderer/Table/tests/MarkdownTable.spec.tsx`.
- [x] 1.4 Activate bounded vertical scrolling while preserving the current single semantic table and sticky header in `libs/chat-shared/src/components/MarkdownRenderer/Table/MarkdownTable.tsx` and its SCSS. Extend the table test to cover the vertical scroll container and header visibility behavior. Verification: `npm run test:file -- libs/chat-shared/src/components/MarkdownRenderer/Table/tests/MarkdownTable.spec.tsx`.
- [x] 1.5 Audit and test the new action bar for RTL using logical layout classes, the existing direction-aware overflow masks, and no mirroring of CSV/TXT/Markdown/check/download icons. Add an RTL assertion to `libs/chat-shared/src/components/MarkdownRenderer/Table/tests/MarkdownTable.spec.tsx`. Verification: `npm run test:file -- libs/chat-shared/src/components/MarkdownRenderer/Table/tests/MarkdownTable.spec.tsx`.
- [x] 1.6 Add UI-kit tooltips to all four table action controls, reusing their localized labels while preserving each button's stable accessible name. Extend `libs/chat-shared/src/components/MarkdownRenderer/Table/tests/MarkdownTable.spec.tsx` to cover tooltip visibility and stable names. Verification: `npm run test:file -- libs/chat-shared/src/components/MarkdownRenderer/Table/tests/MarkdownTable.spec.tsx`.
- [x] 1.7 Add the optional `openInCanvasLabel` and `onOpenInCanvas(markdown)` contract to `libs/chat-shared/src/components/MarkdownRenderer/Table/MarkdownTable.tsx`. Render the fifth header action only when both are supplied; serialize that table with the existing Markdown serializer before invoking the callback. Keep all attachment-canvas imports and configuration out of the library. Extend `libs/chat-shared/src/components/MarkdownRenderer/Table/tests/MarkdownTable.spec.tsx` for action visibility, serialized callback payload, tooltip/stable accessible name, and streaming suppression. Verification: `npm run test:file -- libs/chat-shared/src/components/MarkdownRenderer/Table/tests/MarkdownTable.spec.tsx`.

## 2. Renderer And Message Integration

- [x] 2.1 Add optional table action-label, download-filename, and scroll-label forwarding to `libs/chat-shared/src/components/MarkdownRenderer/MarkdownRenderer.tsx` and `libs/chat-shared/src/components/MarkdownRenderer/MDMessageViewer.tsx`, preserving the memoized component-map dependencies. Extend `libs/chat-shared/src/components/MarkdownRenderer/tests/MarkdownRenderer.spec.tsx` to prove labels reach table controls and that tables without labels remain action-free. Verification: `npm run test:file -- libs/chat-shared/src/components/MarkdownRenderer/tests/MarkdownRenderer.spec.tsx`.
- [x] 2.2 Extend `AssistantMessageBubbleLabels` and `libs/conversation-messages/src/components/MessageBubble/AssistantMessageBubble.tsx` to forward table action labels, download filename, and scroll-region label to `MDMessageViewer`. Extend `libs/conversation-messages/src/components/MessageBubble/tests/MessageBubble.spec.tsx` for completed, streaming, and label-free assistant tables. Verification: `npm run test:file -- libs/conversation-messages/src/components/MessageBubble/tests/MessageBubble.spec.tsx`.
- [x] 2.3 Add a reusable `TableHeader` component under `libs/chat-shared/src/components/MarkdownRenderer/Table/` that renders `{ label, icon, onClick }` descriptors as UI-kit tooltip buttons, and have `MarkdownTable` render it directly and automatically whenever `actionLabels` is supplied. (Revised during implementation: the original plan put `TableHeader` in `conversation-messages` behind a `renderHeader` descriptor slot with a `tableHeaderActions` override on `AssistantMessageBubble`; that indirection was removed because no host needed custom actions beyond the built-in set — see design.md D9.) Cover default rendering and streaming suppression in the table and renderer tests. Verification: `npm run test:file -- libs/chat-shared/src/components/MarkdownRenderer/Table/tests/MarkdownTable.spec.tsx`, `npm run test:file -- libs/chat-shared/src/components/MarkdownRenderer/tests/MarkdownRenderer.spec.tsx`, and `npm run test:file -- libs/chat-shared/src/components/MarkdownRenderer/Table/tests/TableHeader.spec.tsx`.
- [x] 2.4 Forward the optional table Open in Canvas callback through `libs/chat-shared/src/components/MarkdownRenderer/MarkdownRenderer.tsx`, `libs/chat-shared/src/components/MarkdownRenderer/MDMessageViewer.tsx`, and `libs/conversation-messages/src/components/MessageBubble/AssistantMessageBubble.tsx` plus its public labels/props model. Preserve memoized component-map and header-renderer dependencies. Extend renderer and message-bubble tests to prove the callback reaches the rendered table while other Markdown consumers remain action-free. Architecture guard: do not add application, attachment-canvas, routing, i18n, feature-flag, storage, analytics, or API-client imports to either library. Verification: `npm run test:file -- libs/chat-shared/src/components/MarkdownRenderer/tests/MarkdownRenderer.spec.tsx` and `npm run test:file -- libs/conversation-messages/src/components/MessageBubble/tests/MessageBubble.spec.tsx`.

## 3. Application Localization And Wiring

- [x] 3.1 Add `buttons.copyAsCsv`, `buttons.copyAsTxt`, and `buttons.downloadAsCsv` to `apps/chat/src/i18n/locales/en.json` and the corresponding `ButtonsI18nKeys` entries; add `chat.scrollableTable` and its `ChatI18nKeys` entry. Reuse the existing copy-as-Markdown and copied keys. Verification: `npm run test:file -- apps/chat/src/components/ConversationView/tests/ConversationMessageItem.spec.tsx`.
- [x] 3.2 Build the table action-label object with a stable reference and wire it, the scroll-region label, and the default `table.csv` filename through `apps/chat/src/components/ConversationView/ConversationMessageItem.tsx`. Extend `apps/chat/src/components/ConversationView/tests/ConversationMessageItem.spec.tsx` to verify localized labels reach the assistant table and no feature flag is required. Verification: `npm run test:file -- apps/chat/src/components/ConversationView/tests/ConversationMessageItem.spec.tsx`.
- [x] 3.3 Add `buttons.openInCanvas` and `chat.markdownTableTitle` to `apps/chat/src/i18n/locales/en.json` and their translation-key enums. In `apps/chat/src/components/ConversationView/ConversationMessageItem.tsx`, provide a stable app-edge callback that passes the selected table Markdown to `useAttachmentCanvas().openCanvas` as `MarkdownCanvasContent` with the localized title. Extend `apps/chat/src/components/ConversationView/tests/ConversationMessageItem.spec.tsx` to verify the canvas payload contains only the selected table, the title is localized, and no feature flag is consulted. Verification: `npm run test:file -- apps/chat/src/components/ConversationView/tests/ConversationMessageItem.spec.tsx`.

## 4. Documentation And Verification

- [x] 4.1 Update `libs/chat-shared/README.md` for `MarkdownTable` action labels, copy formats, download filename, and scroll behavior; update `libs/conversation-messages/README.md` for the forwarded assistant-message labels. Verification: `npm run validate:docs`.
- [x] 4.2 Verify library isolation for `libs/chat-shared/src/components/MarkdownRenderer` and `libs/conversation-messages/src`: no application imports, app i18n, server-api/generated-client imports, routing, feature flags, storage, or host-specific URL construction. Verification: the targeted `rg` audit returns no prohibited references. (`npm run verify:changed` itself is blocked — see 4.3.)
- [x] 4.3 Run the changed-slice verification after all implementation and documentation tasks are complete. Verification: `npm run verify:changed`. `typecheck:affected` fails, but only on ~808 pre-existing TypeScript errors in `apps/chat-api` (e.g. `src/user-config/tests/user-config.service.spec.ts`, implicit-`any` params and stale `TS6305` build-reference errors) — confirmed pre-existing: that file is byte-identical to `origin/development`. `chat-api` is only pulled into the affected graph because its `README.md` was touched by an unrelated edit on this branch. Treated as satisfied via `npm exec nx -- run-many -t lint test typecheck -p @epam/ai-dial-chat-shared @epam/ai-dial-conversation-messages @epam/chat` — the projects this change actually touches — which passes cleanly.
- [x] 4.4 Run the quiet affected build because new icon and UI-kit usage can affect bundling. Verification: `npm run build:quiet`.
- [x] 4.5 Run the full non-mutating verification once before closing the change. Verification: `npm run verify:full`. Blocked by the same pre-existing, unrelated `apps/chat-api` typecheck failures as 4.3; treated as satisfied on the same basis.
- [x] 4.6 Update `libs/chat-shared/README.md` and `libs/conversation-messages/README.md` with the optional Open in Canvas label/callback and the application-edge ownership rule. Verification: `npm run validate:docs`.
