Slicing strategy: **vertical** — each slice ships one path through the stack (SDK → service → controller → tests) working end to end before widening to the next capability, while the final facade/module shape (design.md's service ownership map) is kept correct at every intermediate slice rather than assembled at the end.

## 1. SDK verification, shared DTOs, enums, and path-validation rules

- [x] 1.1 Re-run `https://registry.npmjs.org/@epam/ai-dial-typescript-sdk` (dist-tags + version list) immediately before touching `package.json`, to confirm `0.1.0-dev.35` (or a newer version, if one has since published) is still the correct pin, and confirm it still exposes `listSkillMetadata`, `listSkillFileMetadata`, `downloadSkillFolder`, `uploadSkillFolder`, `deleteSkillFolder`, `downloadSkillFile`, `uploadSkillFile`, `deleteSkillFile`, `createSkillGroupingFolder`, `deleteSkillGroupingFolder`, `downloadSkillGroupingFolder`.
- [x] 1.2 Update `"@epam/ai-dial-typescript-sdk"` in root `package.json` and `apps/chat-api/package.json` to the confirmed version; run `npm install` to regenerate `package-lock.json`.
- [x] 1.3 Create `apps/chat-api/src/skills/` domain skeleton: `skills.module.ts`, `skills.controller.ts` (empty `@Controller({ path: 'skills', version: '1' })` shell), `skills.service.ts` (empty facade shell), `dto/`, `listing/`, `lookup/`, `upload/`, `download/`, `mutation/`, `utils/`, `tests/`.
- [x] 1.4 Create `apps/chat-api/src/skills/dto/skill-node-type.ts` — `enum SkillNodeType { Folder = 'folder', Item = 'item' }`.
- [x] 1.5 Create `apps/chat-api/src/skills/dto/skill-metadata.dto.ts` — `SkillMetadataItemDto` and `SkillListResponseDto`/`SkillFileListResponseDto`, per design.md's DTO sketches, with full `class-validator`/`@ApiProperty` annotations.
- [x] 1.6 Create `apps/chat-api/src/skills/dto/skill-list-query.dto.ts` — `SkillListQueryDto` (`bucket`, `path?`, `token?`, `limit?`, `recursive?`) and `SkillFileListQueryDto` (extends it, adds required `filePath`), reusing `BUCKET_NAME_PATTERN`/`BUCKET_NAME_VALIDATION_MESSAGE` (`apps/chat-api/src/common/validators/bucket-name.pattern.ts`) and `IsValidFilePath` (`apps/chat-api/src/files/dto/file-path.validator.ts`).
- [x] 1.7 Create `apps/chat-api/src/skills/dto/skill-file-path.dto.ts` — shared `filePath`/`If-Match` field decorators reused by upload/download/delete single-file DTOs.
- [x] 1.8 Create `apps/chat-api/src/skills/utils/skill-path.util.ts` — pure helpers: parsing a full `skills/{bucket}/{path}` resource URL into `{ bucket, path }`, and the reserved-marker/structural-segment validator (`.dial-resource`, `.dial-folder`, first-segment `files`/`v`, empty/`.`/`..` segments, NUL/control characters) shared by `SkillsUploadService` (ZIP-entry validation) and `SkillsLookupService` (URL parsing).
- [x] 1.9 Create `apps/chat-api/src/skills/utils/tests/skill-path.util.spec.ts` covering every rejection rule in design.md D4 plus valid-path acceptance.
- [x] 1.10 Verify: `npm exec nx lint chat-api`, `npm exec nx typecheck chat-api`, `npm exec nx test chat-api`. Lint/test green. `typecheck` has ~620 pre-existing errors already present on `origin/development-1.0` before this change (confirmed via `git stash`) — unrelated to skills work, out of scope. The SDK bump (1.1/1.2) itself introduced 6 new type errors (`Application.displayName`/`ToolSet` body now `LocalizedValue` instead of `string`) which were fixed in `applications.service.ts`, `bucket.service.ts`, `deployments-details.service.ts`, `toolsets-mutation.service.ts` (+ new `common/utils/localized-value.ts` helper) and their specs updated to match.

## 2. Listing: SkillsListingService, optional SkillsLookupService, facade delegates

- [x] 2.1 Create `apps/chat-api/src/skills/listing/skills-listing.service.ts` — `listSkills(bucket, path, { token, limit, recursive }, accessToken)` and `listSkillFiles(bucket, path, filePath, { token, limit, recursive }, accessToken)`, calling `DialClientService.client.listSkillMetadata`/`listSkillFileMetadata`, mapping `MetadataBase` → `SkillMetadataItemDto[]` (lowercasing `nodeType`), using `handleDialSdkError`/`mapDialHttpStatus` for errors.
- [x] 2.2 Create `apps/chat-api/src/skills/listing/tests/skills-listing.service.spec.ts` — metadata mapping (folder vs. item), pagination (`token`/`nextToken` round-trip), empty results, malformed upstream metadata (missing `nodeType`), 400/403/404/429/5xx/timeout mapping.
- [x] 2.3 Create `apps/chat-api/src/skills/lookup/skills-lookup.service.ts` — `resolveSkillItem(itemId, accessToken, bucket)` parsing `skills/{bucket}/{path}` via `skill-path.util.ts`, calling `listSkillMetadata` targeted at that single path, returning a normalized `SkillMetadataItemDto | null` (never throwing for a genuine not-found; only upstream 5xx/network errors propagate).
- [x] 2.4 Create `apps/chat-api/src/skills/lookup/tests/skills-lookup.service.spec.ts` — targeted resolution success, not-found returns `null`, upstream 5xx propagates as exception.
- [x] 2.5 Wire `listSkills`/`listSkillFiles` bound-property delegates onto `skills.service.ts`; leave `resolveSkillItem` off the facade per design.md's facade-delegation design.
- [x] 2.6 Implement `GET /api/v1/skills` and `GET /api/v1/skills/files` on `skills.controller.ts` with full `@ApiOperation`/`@ApiResponse` (200/400/401/403/404/429/502/503) and `@Throttle({ default: { limit: 60, ttl: 60000 } })`.
- [x] 2.7 Create `apps/chat-api/src/skills/tests/skills.controller.spec.ts` (start the file here; extended in later slices) covering `listSkills`/`listSkillFiles` routing, DTO validation (invalid `limit`, invalid `bucket`), auth guard, and rate-limit boundary.
- [x] 2.8 Register `SkillsListingService`, `SkillsLookupService` as providers of `SkillsModule`; export `SkillsService` and `SkillsLookupService` only.
- [x] 2.9 Verify: `npm exec nx lint chat-api`, `npm exec nx typecheck chat-api`, `npm exec nx test chat-api`, `npm exec nx build chat-api`.

## 3. Download: SkillsDownloadService, streaming endpoints, generated-client Raw wrappers

- [x] 3.1 Fix `mapDialHttpStatus` in `apps/chat-api/src/common/dial/dial-error.mapper.ts` — add explicit `405 → MethodNotAllowedException`, `412 → PreconditionFailedException`, `422 → UnprocessableEntityException` branches before the `>= 500` check.
- [x] 3.2 Add unit tests to the existing `apps/chat-api/src/common/dial/tests/dial-error.mapper.spec.ts` (or equivalent existing spec file) for the three new status codes, and re-run the full existing suite to confirm no prior mapping regressed.
- [x] 3.3 Create `apps/chat-api/src/skills/download/skills-download.service.ts` — `downloadSkill(bucket, path, accessToken)` returning `SkillDownload { stream, headers, abortOnDisconnect }`; detect a grouping-folder target (via a `listSkillMetadata` check or DIAL Core's own `400` from `downloadSkillFolder`) and throw `BadRequestException` directing the caller to list metadata instead of forwarding a body; `downloadSkillFile(bucket, path, filePath, accessToken)` returning the same shape for a single file. Forward the extended safe-header allowlist (`content-type`, `content-disposition`, `content-length`, `etag`).
- [x] 3.4 Create `apps/chat-api/src/skills/download/tests/skills-download.service.spec.ts` — successful ZIP/file stream, safe-header forwarding (verify no other header leaks through), grouping-folder-download rejection, cancellation on client disconnect (`abortOnDisconnect` invoked), 401/403/404/405/422/429/5xx/timeout mapping.
- [x] 3.5 Wire `downloadSkill`/`downloadSkillFile` bound-property delegates onto `skills.service.ts`.
- [x] 3.6 Implement `GET /api/v1/skills/download` and `GET /api/v1/skills/files/download` on `skills.controller.ts`, piping the stream via `Readable.fromWeb` + `pipeline()` + `res.destroy()` on failure + `res.on('close', abortOnDisconnect)`, following `apps/chat-api/src/files/files.controller.ts:409-435,597-618` exactly. `@Throttle({ default: { limit: 30, ttl: 60000 } })`.
- [x] 3.7 Extend `skills.controller.spec.ts` with download routing/status-code/header-forwarding coverage (supertest, asserting the response headers and streamed body length, not implementation internals).
- [x] 3.8 Register `SkillsDownloadService` in `SkillsModule`.
- [x] 3.9 Run `npm run openapi` and `npm run openapi:check`; inspect the generated `SkillsApi.downloadSkill`/`downloadSkillFile` (and `*Raw` variants) for `any` or lost binary-stream typing; document any generator gap found.
- [x] 3.10 Build/lint/typecheck `chat-api-client`: `npm exec nx build chat-api-client -- --skip-nx-cache`, `npm exec nx lint chat-api-client`, `npm exec nx typecheck chat-api-client`.
- [x] 3.11 Add `skillsApi` singleton to `apps/chat/src/server-api/api-client.ts`; create `apps/chat/src/server-api/skills.api.ts` with `downloadSkill`/`downloadSkillFile` using `skillsApi.downloadSkillRaw`/`downloadSkillFileRaw`, documenting the generator gap inline, mirroring `apps/chat/src/server-api/files.api.ts:92-108`.
- [x] 3.12 Add `apps/chat/src/server-api/tests/skills.api.spec.ts` covering the Raw-response wrapper behavior for both download functions.
- [x] 3.13 Verify: `npm exec nx lint chat-api`, `npm exec nx typecheck chat-api`, `npm exec nx test chat-api`, `npm exec nx build chat-api`. Lint/test/build green (1997 tests passing). `typecheck` pre-existing debt noted at 1.10 remains unrelated/unchanged.

## 4. Upload: SkillsUploadService, multipart endpoints, ingress-limit configuration

- [x] 4.1 Add `SKILL_UPLOAD_MAX_BYTES`, `SKILL_UPLOAD_MAX_FILES`, `SKILL_FILE_UPLOAD_MAX_BYTES`, `SKILL_TRANSFER_TIMEOUT_MS` to `apps/chat-api/src/config/environment.config.ts` (`EnvironmentVariables`) with `@IsOptional`/`@Transform(parseInt)`/`@IsInt`/`@Min` decorators and the defaults from design.md's Upload Limits table.
- [x] 4.2 Add the four new variables as placeholders to `apps/chat-api/.env.template` (this repo's actual env-example file; no root `.env.example` exists) and document them in `apps/chat-api/README.md`, following the `FILE_UPLOAD_MAX_BYTES`/`FILE_TRANSFER_TIMEOUT_MS` precedent.
- [x] 4.3 Create `apps/chat-api/src/skills/dto/upload-skill.dto.ts` (extends `SkillResourceQueryDto`: `bucket`, `path`; `If-Match` documented via `ApiIfMatchHeader()`, read off the request) and `apps/chat-api/src/skills/dto/upload-skill-file.dto.ts` (extends `SkillFileResourceQueryDto`: `bucket`, `path`, `filePath`). Also added `apps/chat-api/src/skills/dto/skill-upload-response.dto.ts` (`SkillUploadResponseDto`/`SkillFileUploadResponseDto` — needed now for slice 4's response types; the remaining mutation-only response DTOs stay in slice 5's `skill-mutation.dto.ts`).
- [x] 4.4 Create `apps/chat-api/src/skills/upload/skills-upload.service.ts` — `uploadSkill(bucket, path, zipFile, accessToken, ifMatch?)`: opens the uploaded ZIP with `yauzl.fromBufferPromise` (reusing `isValidSkillRelativePath` from `skill-path.util.ts`), rejects any invalid entry, duplicate path (400), archive exceeding `SKILL_UPLOAD_MAX_FILES` (422), or missing root `SKILL.md` (400) before calling DIAL Core, otherwise forwards the untouched ZIP buffer to `uploadSkillFolder` with `If-Match` when supplied, returns `{ etag }` from the response `ETag` header. `uploadSkillFile(bucket, path, filePath, file, accessToken, ifMatch?)`: validates `filePath` via `isValidSkillRelativePath`, checks `SKILL_FILE_UPLOAD_MAX_BYTES` (413), forwards to `uploadSkillFile`, returns `{ etag }`.
- [x] 4.5 Create `apps/chat-api/src/skills/upload/tests/skills-upload.service.spec.ts` — valid whole-skill upload, missing `SKILL.md` rejection, path-traversal entry rejection (hand-rolled raw ZIP fixture, mirroring `files-upload.service.spec.ts`'s `buildRawZipBuffer` since `archiver` sanitizes `..` on write), reserved-marker entry rejection, duplicate-path rejection, `SKILL_UPLOAD_MAX_FILES` exceeded → 422, malformed ZIP → 400, `If-Match` forwarding, 412/403 mapping, timeout → 503, single-file upload success/validation/ETag/413/404. 16/16 passing.
- [x] 4.6 Wire `uploadSkill`/`uploadSkillFile` bound-property delegates onto `skills.service.ts`.
- [x] 4.7 Implement `PUT /api/v1/skills` and `PUT /api/v1/skills/files` on `skills.controller.ts` using `FileInterceptor('file')` + `memoryStorage` bounded by `SKILL_UPLOAD_MAX_BYTES` at the module level (`SkillsModule`'s `MulterModule.registerAsync`) with `SkillsUploadService` additionally enforcing the smaller `SKILL_FILE_UPLOAD_MAX_BYTES` cap in application code for the single-file route (two different multer-level limits aren't expressible from one shared `MulterModule` registration — see the code comment in `skills.module.ts`), `@ApiConsumes('multipart/form-data')`, full `@ApiBody`/`@ApiResponse` (200/400/401/403/404/405/412/413/422/429/502/503), `@Throttle({ default: { limit: 5, ttl: 60000 } })` for `uploadSkill` and `{ limit: 20, ttl: 60000 }` for `uploadSkillFile`.
- [x] 4.8 Extended `skills.controller.spec.ts` with upload routing/validation/status-code coverage (28/28 passing). Deviation: since this suite mocks `SkillsService` entirely, the "malicious ZIP → 400, no upstream call" scenario is covered here only as service-mock-rejects-with-BadRequestException → 400 routing; the real zip-slip rejection *and* its "no upstream SDK call" assertion are covered against the live `SkillsUploadService` in `skills-upload.service.spec.ts` (which does use a hand-rolled raw-ZIP fixture with a literal `../../etc/passwd` entry).
- [x] 4.9 Register `SkillsUploadService` in `SkillsModule`.
- [x] 4.10 Run `npm run openapi` and `npm run openapi:check`; inspect generated `SkillsApi.uploadSkill`/`uploadSkillFile` multipart request shapes — both take a single `file: Blob` field (not an array) and correctly surface `ifMatch` as the `If-Match` header param (from `@ApiIfMatchHeader()`), no generator gap found.
- [x] 4.11 Add `uploadSkill`/`uploadSkillFile` to `apps/chat/src/server-api/skills.api.ts` using the plain (non-Raw) `skillsApi.uploadSkill`/`uploadSkillFile` — deviation from the task's premise: the ETag comes back in the JSON response body (`SkillUploadResponseDto.etag`/`SkillFileUploadResponseDto.etag`), not as an HTTP response header on the BFF's own response, so `Raw` (needed only for binary/header-bearing responses) isn't required here; extended `skills.api.spec.ts`.
- [x] 4.12 Verify: `npm exec nx lint chat-api`, `npm exec nx typecheck chat-api`, `npm exec nx test chat-api`, `npm exec nx build chat-api`. Lint clean, test 2022/2022 passing, build green. Fixed two genuine `TS7006` implicit-`any` errors in `skills-listing.service.spec.ts` (a slice-2 file, not part of the pre-existing repo-wide typecheck debt tracked at 1.10/3.13) discovered while checking for new skills-related typecheck errors; zero skills-related typecheck errors remain beyond the documented pre-existing baseline.

## 5. Mutation: SkillsMutationService, delete/grouping-folder endpoints, ETag/error mapping

- [x] 5.1 Create `apps/chat-api/src/skills/dto/skill-mutation.dto.ts` — `DeleteSkillDto`/`DeleteSkillFileDto`/`SkillGroupingFolderDto` (extend the existing `SkillResourceQueryDto`/`SkillFileResourceQueryDto`; `If-Match` documented via `ApiIfMatchHeader()`/read off the request, **not** applied at all to `createSkillGroupingFolder` per design.md D2) and response DTOs `SkillFileDeleteResponseDto`/`SkillGroupingFolderResponseDto`/`SkillOperationResultDto` (`SkillUploadResponseDto`/`SkillFileUploadResponseDto` already exist from slice 4).
- [x] 5.2 Create `apps/chat-api/src/skills/mutation/skills-mutation.service.ts` — `deleteSkill`, `deleteSkillFile` (rejecting `filePath === 'SKILL.md'` with `BadRequestException` before calling DIAL Core), `createSkillGroupingFolder` (no `If-Match` parameter, matching the verified schema's `header?: never`), `deleteSkillGroupingFolder` (`If-Match` optional), each forwarding to the matching SDK method and mapping `409`/`412` via the now-extended `mapDialHttpStatus`. Verified against the raw SDK schema that `createSkillGroupingFolder`'s success/collision responses are `200`/`400` (not `201`/`409` as design.md's endpoint table assumed) — implemented per the verified schema, documented as a design.md discrepancy.
- [x] 5.3 Create `apps/chat-api/src/skills/mutation/tests/skills-mutation.service.spec.ts` — whole-skill deletion, single-file deletion, `SKILL.md` deletion rejection, grouping-folder creation, grouping-folder deletion, non-empty-folder 409, create-collision 400, `If-Match` forwarding and 412 precondition, 404 not-found for each operation. 13/13 passing.
- [x] 5.4 Wire `deleteSkill`/`deleteSkillFile`/`createSkillGroupingFolder`/`deleteSkillGroupingFolder` bound-property delegates onto `skills.service.ts`.
- [x] 5.5 Implement `DELETE /api/v1/skills`, `DELETE /api/v1/skills/files`, `POST /api/v1/skills/grouping-folders`, `DELETE /api/v1/skills/grouping-folders` on `skills.controller.ts` with full `@ApiOperation`/`@ApiResponse` and `@Throttle({ default: { limit: 10, ttl: 60000 } })`. Success status is `200` (not `201`) for `createSkillGroupingFolder`, per the verified SDK schema (see 5.2's note) — `@HttpCode(200)` set explicitly.
- [x] 5.6 Extended `skills.controller.spec.ts` with mutation routing/status-code coverage (36/36 passing), including the `SKILL.md`-deletion-rejected (via the mocked service throwing `BadRequestException`, consistent with 4.8's mocking approach) and non-empty-grouping-folder 409-conflict scenarios.
- [x] 5.7 Create `apps/chat-api/src/skills/tests/skills.service.spec.ts` — delegation-only assertions for all 10 facade methods plus an explicit assertion that `resolveSkillItem` is absent from the facade. 11/11 passing.
- [x] 5.8 Register `SkillsMutationService` in `SkillsModule`; `SkillsModule` was already registered in `apps/chat-api/src/app/app.module.ts` from an earlier slice.
- [x] 5.9 Verify: `npm exec nx lint chat-api`, `npm exec nx typecheck chat-api`, `npm exec nx test chat-api`, `npm exec nx build chat-api`. Lint clean, 2054/2054 tests passing, build green, zero new skills-related typecheck errors (pre-existing repo-wide debt from 1.10/3.13 unchanged).

## 6. Sharing and publication integration

- [x] 6.1 Add `['skills/', 'SKILL']` to `RESOURCE_KIND_BY_PREFIX` in `apps/chat-api/src/share/share.service.ts`.
- [x] 6.2 Extended `DiscardSharedCatalogItemDto`'s `CATALOG_RESOURCE_PATH_PATTERN` (`apps/chat-api/src/share/dto/discard-shared-catalog-item.dto.ts`) to accept a `skills/{bucket}/{path}` prefix, plus a supplementary `NOT_A_SKILL_FILE_PATTERN` negative-lookahead validator rejecting any `skills/`-prefixed `itemId` containing a `/files/` segment. New `apps/chat-api/src/share/tests/discard-shared-catalog-item.dto.spec.ts` (8 tests) covers acceptance of all four prefixes, the per-file-skill rejection, and existing traversal/empty/unknown-prefix rejections.
- [x] 6.3 Imported `SkillsModule` into `ShareModule`; injected `SkillsLookupService` directly into `ShareService`; added a `sharedSkill` branch (checked first) to `ShareService.resolveSharedItemSummary` calling `SkillsLookupService.resolveSkillItem`, extending `AcceptInvitationResponseDto` with an optional `sharedSkill?: SkillMetadataItemDto` field.
- [x] 6.4 Added tests to `apps/chat-api/src/share/tests/share.service.spec.ts` (32/32 passing) — create-share-link for a `skills/{bucket}/{path}` itemId, accept-invitation resolving `sharedSkill` without calling the deployment/toolset resolvers, discard-shared for a whole-skill itemId checking `getSharedResources` with `resourceTypes: ['SKILL']`; the per-file-skill rejection scenario lives in the new DTO spec (6.2) since that validation happens at the DTO layer, not inside `ShareService`.
- [x] 6.5 Add `Skill = 'skill'` to `CatalogEntityType` (`apps/chat-api/src/publish/dto/catalog-entity-params.dto.ts`).
- [x] 6.6 Added tests to `apps/chat-api/src/publish/tests/publish.service.spec.ts` (skill publish building `sourceUrl`/leaf-name-only `targetUrl` and echoing the caller-supplied version) and `publish-catalog-entity.dto.spec.ts` (new `CatalogEntityParamsDto — entityType` block confirming `entityType: 'skill'` passes validation). Confirmed `getResourceTypePrefix`/`getResourceBucket`/`getResourceName`/`splitEntityNameAndVersion` in `publish-target.util.ts`/`publish.service.ts` are already fully generic path-segment splitters requiring zero code changes beyond the enum member — `splitEntityNameAndVersion` already returns an empty version string for any entityId with no `__{version}` suffix.
- [x] 6.7 Added a code comment at the top of `publish.service.ts` (above `historyCacheKey`) documenting the deliberate absence of a `PublishService → SkillsLookupService` dependency, cross-referencing design.md D9 and the `catalog-publish-api` open questions.
- [x] 6.8 Verify: `npm exec nx lint chat-api`, `npm exec nx typecheck chat-api`, `npm exec nx test chat-api`, `npm exec nx build chat-api`. Lint clean, 2068/2068 tests passing, build green. Typecheck shows the same pre-existing `share.service.ts` error at a shifted line number (was `:111` before this slice's added imports, now `:114`) plus the pre-existing repo-wide `validate()`/`TS2769` pattern already seen in `toolset-body.dto.spec.ts`, inherited unmodified by the two new DTO spec files that copy that same pattern — zero new regressions.

## 7. Generated-client integration and frontend server-api wrappers (completion pass)

- [x] 7.1 Run `npm run openapi` and `npm run openapi:check` against the now-complete `/api/v1/skills` surface (all 10 operations) plus the `share`/`publish` DTO changes from Slice 6. Both green.
- [x] 7.2 Inspected the full generated `SkillsApi.ts` (865 lines, all 10 operations: `createSkillGroupingFolder`, `deleteSkill`, `deleteSkillFile`, `deleteSkillGroupingFolder`, `downloadSkill`, `downloadSkillFile`, `listSkillFiles`, `listSkills`, `uploadSkill`, `uploadSkillFile`, each with a `*Raw` variant) — zero `any` anywhere in the file, `ifMatch` present on every request interface that should have it and correctly **absent** from `CreateSkillGroupingFolderRequest`, upload interfaces use a single `file: Blob` field, download methods return `Blob` from the non-Raw variant (stream semantics preserved via `*Raw`/`.raw`). No generator gap found; no follow-up note needed.
- [x] 7.3 Build/lint/typecheck `chat-api-client`: all three green (`npm exec nx build chat-api-client --skip-nx-cache`, `npm exec nx lint chat-api-client`, `npm exec nx typecheck chat-api-client` — note the correct nx flag placement is `nx build chat-api-client --skip-nx-cache`, not `-- --skip-nx-cache`, which forwards the flag to the underlying `tsc --build` command instead and fails with `TS5072`).
- [x] 7.4 Completed `apps/chat/src/server-api/skills.api.ts` with the remaining thin wrappers `deleteSkill`, `deleteSkillFile`, `createSkillGroupingFolder`, `deleteSkillGroupingFolder` using normal (non-`Raw`) generated methods (`listSkills`/`listSkillFiles` were already present from slice 2).
- [x] 7.5 Extended `apps/chat/src/server-api/tests/skills.api.spec.ts` with coverage for all four new wrappers (delegation, ETag/success-result return value, rejection propagation).
- [x] 7.6 No signature changes needed in `share.api.ts`/`publish.api.ts`: `itemId` stays a plain `string` (widened only at the DTO validator layer server-side), and `CatalogPublishEntityType` in `publish.api.ts:10` is a direct re-export of the generated `PublishCatalogEntityEntityTypeEnum`, so it picked up the new `Skill` member automatically once the client was regenerated in 7.1 — confirmed no code change required.
- [x] 7.7 Verify: relevant `chat` server-api wrapper tests.

## 8. Final cross-project verification

- [x] 8.1 `npm exec nx test chat-api` — 2068/2068 passing (one unrelated flaky pre-existing test confirmed flaky on retry, see 8.1's own log).
- [x] 8.2 `npm exec nx lint chat-api` — clean (2 pre-existing unrelated warnings only).
- [x] 8.3 `npm exec nx typecheck chat-api` — fails with the same pre-existing repo-wide debt documented at 1.10/3.13/5.9/6.8 (confirmed via `git stash` at 1.10 to be present on `origin/development-1.0` before this change); zero new skills-related errors.
- [x] 8.4 `npm exec nx build chat-api` — green.
- [x] 8.5 `npm run openapi` — green, full 10-operation `/api/v1/skills` surface plus share/publish DTO changes.
- [x] 8.6 `npm run openapi:check` — green.
- [x] 8.7 `npm exec nx build chat-api-client --skip-nx-cache` — green.
- [x] 8.8 `npm exec nx lint chat-api-client` — green.
- [x] 8.9 `npm exec nx typecheck chat-api-client` — green.
- [x] 8.10 Relevant `chat` server-api wrapper tests — 18 files / 138 tests passing (`skills.api.spec.ts`, `share.api.spec.ts`-adjacent, `publish.api.spec.ts`-adjacent, all others).
- [x] 8.11 `npm exec nx affected --target=lint --base=origin/development-1.0` — green across 22 affected projects (only pre-existing unrelated warnings).
- [x] 8.12 `npm exec nx affected --target=typecheck --base=origin/development-1.0` — only `@epam/chat-api` fails (the documented pre-existing baseline debt); all other 21 affected projects pass.
- [x] 8.13 `npm exec nx affected --target=test --base=origin/development-1.0` — green: 177 test files, 2229 passed + 2 skipped, across 21 affected projects (including `chat-api`'s own suite).
- [x] 8.14 `npm exec nx affected --target=build --base=origin/development-1.0` — green across 22 affected projects (`@epam/chat`, `@epam/chat-api`, `chat-api-client`, and dependents).
