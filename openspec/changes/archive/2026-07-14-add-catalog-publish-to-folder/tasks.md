## 1. Lib: DialFoldersTree wrapper (Slice 1)

- [x] 1.1 Confirm pinned `@epam/ai-dial-ui-kit` version now exports `DialFoldersTree` with the props documented in design.md (confirmed at `0.12.0-dev.28`: `DialFoldersTree` is exported from package root `dist/src/index.d.ts`; props unchanged from design.md)
- [x] 1.2 Add `PublishFoldersTree` component in `libs/catalog` wrapping `DialFoldersTree` (`showFiles={false}`, no `getContextMenuItems`), mapping `PublishFolderNode[]` ↔ `DialFile[]` via new `toDialFileTree`/`toFolderPathKey`/`fromFolderPathKey` helpers in `publish-folder-tree.ts`
- [x] 1.3 Wire single-selection: `selectedPath`/`onItemClick` mapped to `usePublishFlow`'s `selectedFolderPath`/`onSelectedFolderPathChange` (toggle-off on re-click preserved)
- [x] 1.4 Wire inline folder creation: `createdFolderPath`, `onCreateFolderSave`, `onCreateFolderCancel` mapped to `usePublishFlow`'s optimistic create flow, via a placeholder `DialFile` node (`insertPlaceholderDialFile`) and duplicate-name check (`getSiblingFolderNames`)
- [x] 1.5 Adapt `filterFolderTree` (from `publish-folder-tree.ts`) to filter `PublishFolderNode[]` by the host-owned search query before conversion to `DialFile[]`
- [x] 1.6 Replaced `PublishFolderPicker` usage in `PublishPanel` directly with `PublishFoldersTree` and deleted the old picker + its tests immediately (no side-by-side flag — kept per project convention against feature flags/half-finished states; parity verified via the new unit test suite instead of manual A/B)
- [x] 1.7 Verify RTL: logical properties only in wrapper markup (no `ml/mr/pl/pr/left/right`); `DialFoldersTree` owns its own RTL chevrons internally, so no app-level icon mirroring was added
- [x] 1.8 Add/update unit tests for `PublishFoldersTree` (selection, search filter, inline create, duplicate rejection, cancel, empty state, disabled state) and for the new `publish-folder-tree.ts` converters
- [x] 1.9 Ran `npm exec nx test ai-dial-catalog` and `npm exec nx lint ai-dial-catalog` (project name is `@epam/ai-dial-catalog`, not `catalog`) — all new/changed tests pass; only a pre-existing unrelated `Header.spec.tsx` failure remains (confirmed via `git stash` to predate this change)

## 2. App: real folder data hook (Slice 2)

- [x] 2.1 Create `apps/chat/src/hooks/catalog/useCatalogPublishFolders.ts` with `cache`/`expandedPaths`/derived `loadedPaths`/`loadingPaths`/`onExpandedPathsChange` (scoped-down, purpose-built version per design.md D2 — not a generalization of `useDialFileManager`)
- [x] 2.2 Call `listPublicFiles` from `apps/chat/src/server-api/files.api.ts` for folder listing; map API items to `PublishFolderNode[]` via `buildFolderNodes`
- [x] 2.3 Implement `onCreatePublishFolder` using the existing `createFolder` server-api call; merge created folder into the hook's local cache optimistically (bucket resolved from the parent folder's already-listed items — see code comment on the known root-creation edge case tied to design.md's Open Questions)
- [x] 2.4 Implement `hasPublishWriteAccess(folderPath)` heuristic per design.md D2, isolated behind a single function boundary for later replacement
- [x] 2.5 Replace `MOCK_PUBLISH_FOLDERS` usage in `CatalogView` with `useCatalogPublishFolders` output; also wired `onCreatePublishFolder` now (natural fit with the folder-data source, rather than deferring to Slice 4) and deleted the now-dead `MOCK_PUBLISH_FOLDERS` export from `mock-catalog-items.ts`
- [x] 2.6 Added `useCatalogPublishFolders.spec.ts` (root load, lazy expand/merge, folder creation, write-access heuristic) and updated `CatalogView.spec.tsx` (mocks the hook; new tests assert `publishFolderItems`/`onCreatePublishFolder` pass-through)
- [x] 2.7 Ran `npm exec nx test chat` and `npm exec nx lint chat` — lint caught a real bug (`setState` inside `useMemo` for the initial root fetch, flagged by `react-hooks/set-state-in-render`), fixed by moving the initial fetch into `useEffect`; all tests pass after the fix

## 3. Backend: catalog-publish API — proxies DIAL Core Publication API (Slice 3)

> **Revised** after confirming `apps/chat-api` has no database/ORM anywhere in the stack: this domain proxies DIAL Core's Publication API (`createPublication`/`getPublications`, already typed in `@epam/ai-dial-typescript-sdk`) via `DialClientService`, the same pattern `ShareService` already uses — it does **not** persist `PublishHistoryEntry` records itself. See design.md D3 (revised) and `specs/catalog-publish-api/spec.md`.

- [x] 3.1 Scaffolded `apps/chat-api/src/catalog-publish/` domain: `catalog-publish.module.ts`, `catalog-publish.controller.ts`, `catalog-publish.service.ts`, `dto/` — service injects `DialClientService` + `CACHE_MANAGER` (no repository/persistence layer); registered in `app.module.ts`
- [x] 3.2 Defined `CatalogEntityParamsDto` (`entityType` enum `model|toolset|application`, `entityId` via `IsValidFilePath`) and `PublishCatalogEntityDto` (`folderPath` via `IsValidFilePath`, `version` — made **required**, not optional: Core's Publication schema has no version field to derive one from)
- [x] 3.3 Defined `PublishResultDto` and `PublishHistoryEntryDto` response DTOs with `@ApiProperty` Swagger metadata, populated from Core's `Publication` fields (`createdAt`/`author`/`targetFolder`); `version` round-trips through `Publication.name` since Core has no native version field
- [x] 3.4 Implemented `POST /api/v1/catalog/{entityType}/{entityId}/publish` — thin controller, `@ApiOperation`/`@ApiResponse` per status, `@Throttle({ limit: 10, ttl: 60000 })` (mirroring `ShareController`); service calls `this.dialClient.client.createPublication({...})` with `entityId` as `sourceUrl`, `folderPath` as `targetFolder`, `` `${folderPath}/${entityId}` `` as `targetUrl`, `version` as `Publication.name`
- [x] 3.5 Implemented `GET /api/v1/catalog/{entityType}/{entityId}/publish-history` — service calls `getPublications({ body: { url: entityId } })` **scoped by `entityId`, not `folderPath`** (fixed a bug caught during implementation: history must span every folder the entity was ever published to, since `PublishPanel` filters by folder client-side), filters by `resources[].sourceUrl`, maps to `PublishHistoryEntryDto[]`; cached via `withCachedDialRequest` (key `catalog-publish-history:{entityType}:{entityId}`, TTL 60s), invalidated via `cacheManager.del` on successful publish
- [x] 3.6 Mapped errors via `mapDialHttpStatus`/`handleDialFetchError` (`apps/chat-api/src/common/dial/dial-error.mapper.ts`), matching the exact pattern `ShareService`/`ModelsService` already use; no token/body logging
- [x] 3.7 Added 16 backend unit tests (7 service + 9 controller integration via `supertest`): happy path asserting exact `createPublication`/`getPublications` call shape, 403→`ForbiddenException`, unexpected throw→`BadGatewayException`, cache hit/miss/invalidation, entityType/folderPath/version validation — all mock `DialClientService`, no repository
- [x] 3.8 Ran `npm run openapi` and `npm run openapi:check` — first pass failed the repo's `check-client.mjs` naive-`any`-word scanner because Swagger prose used the English word "any" (false positive, not an actual `any` type); reworded the two `@ApiOperation` descriptions and re-ran clean; `CatalogPublishApi.ts` generated with `publishCatalogEntity`/`getCatalogPublishHistory`; `nx build chat-api-client` passes
- [x] 3.9 Ran `npm exec nx test chat-api` (16/16 pass), `npm exec nx lint chat-api` (0 errors, 1 pre-existing unrelated warning), `npm exec nx build chat-api` (webpack compiles successfully)

## 4. App: wire CatalogView end-to-end (Slice 4)

- [x] 4.1 Added `apps/chat/src/server-api/catalog-publish.api.ts` thin wrapper around the generated `publishCatalogEntity`/`getCatalogPublishHistory` client methods; registered `catalogPublishApi` in `server-api/api-client.ts`
- [x] 4.2 Replaced mock `handlePublish` in `CatalogView` with a real call to `publishCatalogEntity`; `usePublishFlow` already maps a thrown error to `hasSubmitError`/the submit-error callout, so no extra mapping was needed in `CatalogView` itself
- [x] 4.3 Replaced mock `getPublishHistory` with the real history call — this required making `getPublishHistory` **async** across the lib contract (`catalog-props.ts`, `item-details-props.ts`) since a real API call can't be synchronous; `DetailsPanel` now fetches on publish-open via `useEffect` with loading/error state, threaded through `PublishPanel` → `PublishHistoryList` (new `isLoading`/`hasError` props)
- [x] 4.4 Replaced mock `hasPublishWriteAccess` in `CatalogView` with `useCatalogPublishFolders`'s heuristic (from task 2.4) — simple destructure rename, no new code needed
- [x] 4.5 Implemented `isPublishVisible` in `CatalogView` as `(item) => Boolean(item.isMyApp) && toPublishEntityType(item.type) != null` — used `isMyApp` ("belongs to the current user"), not `isEditable` (which is Edit-button-specific per its own doc comment); did **not** touch `Header.tsx`'s internal default fallback, since `isPublishVisible` fully overrides it and a pre-existing, unrelated baseline test failure lives in that default path (confirmed via `git stash` in an earlier slice)
- [x] 4.6 Added `CatalogI18nKeys.PublishFolderSearchPlaceholder`/`PublishFolderEmptyState`/`PublishHistoryLoading`/`PublishHistoryError` to `en.json`, wired via a new `publishTexts` prop and new `PublishPanelTexts` fields (`folderEmptyStateText`/`historyLoadingText`/`historyErrorText`) threaded to `PublishFoldersTree`/`PublishHistoryList`. **Arabic parity skipped**: no Arabic (or any non-English) locale file exists anywhere in this repo yet — only `en.json` is registered in `i18n/config.ts` — so full-catalog Arabic translation is a separate, app-wide initiative, not something this change can scope
- [x] 4.7 Success notification wiring unchanged and still fires through the real `onPublishSuccess` path (verified by existing + new tests); removed the now-dead local `publishHistory`/`MOCK_PUBLISH_HISTORY` state it used to mutate, since the real backend (with server-side cache invalidation) is the source of truth
- [x] 4.8 Added tests: `apps/chat/src/utils/tests/catalog-publish.spec.ts` (6 tests for the pure `toPublishEntityType`/`mapPublishHistoryEntryDto` mappers) + 6 new `CatalogView.spec.tsx` tests (publish success call shape, non-publishable entity type rejection, publish failure propagation, history mapping, history failure propagation, `isPublishVisible` gating) — 40 total new/changed tests across chat this slice
- [x] 4.9 Ran `npm exec nx test chat` (all pass), `npm exec nx lint chat` (0 errors, pre-existing warnings only), `npm exec nx build chat` (succeeds); manual QA deferred to task 5.3's checklist (no running backend/DIAL Core available in this session to click through the live flow — see Slice 5 notes)

## 5. Cleanup, docs, and QA (Slice 5)

- [x] 5.1 ~~Delete `PublishFolderPicker`... once Slice 1–4 parity is confirmed~~ — done early as part of task 1.6 (direct replacement, no flag); `publish-folder-tree.ts` was kept and extended (not deleted) since its `filterFolderTree`/`collectFolderKeys` utilities are reused by the new `PublishFoldersTree`
- [x] 5.2 Deleted `MOCK_PUBLISH_FOLDERS` (Slice 2) and `MOCK_PUBLISH_HISTORY` + the mock `handlePublish`/write-access logic (Slice 4) from `CatalogView`/`mock-catalog-items.ts` as each became dead code, rather than deferring — consistent with the same "no half-finished/dead state" approach used in task 1.6
- [x] 5.3 Wrote `docs/catalog-publish-manual-qa.md` (mirroring `docs/filemanager-7501-test-cases.md`) covering: folder tree render/expand/select/search/create (Slice 1), RTL layout, publish success/no-access/replace-warning, history load + load-failure, and Publish-button visibility (Slices 2-4); documents known gaps (no Arabic locale, placeholder write-access heuristic, root-folder-creation bucket edge case)
- [x] 5.4 Ran the full affected suite against `origin/development-1.0` (fetched fresh): `nx affected --target=test` (112 chat test files / 1013 passed + 2 skipped; `chat-api` 67 files / 1077 passed; only the pre-existing, confirmed-unrelated `ai-dial-catalog` `Header.spec.tsx` failure), `nx affected --target=lint` (0 errors, pre-existing warnings only), `nx affected --target=build` (all succeed)
- [x] 5.5 Searched `docs/` for references to the prototype's mock publish flow (`grep -rl "MOCK_PUBLISH\|mock publish"`) — none found; nothing to update

## 6. Post-completion fixes (found via user testing after all slices above)

- [x] 6.1 **Expand-to-fetch wiring gap**: `useCatalogPublishFolders`'s `expandedPaths`/`onExpandedPathsChange`/`loadingPaths` were never threaded past `CatalogView` — `PublishFoldersTree` managed expand state purely internally, so clicking a folder never fetched its children. Fixed by making these props optional and controllable on `PublishFoldersTree` (falls back to internal state when omitted) and threading them through `PublishPanelProps`, `DetailsPanelProps` (as `publishExpandedPaths`/`onPublishExpandedPathsChange`/`publishLoadingPaths`), `CatalogProps`, `Catalog.tsx`, `DetailsPanel.tsx`, down to `CatalogView.tsx`. Also fixed a related bug in `usePublishFlow`: it snapshotted the `folderItems` prop into local state only once on mount, so even with expand wired, newly-fetched children would never reach the tree — added a `useEffect` to re-sync on prop change. Added regression tests in `PublishFoldersTree.spec.tsx`, `use-publish-flow.spec.ts`, and `CatalogView.spec.tsx`.
- [x] 6.2 **Publish 400 on nested folders**: `catalog-publish.service.ts` built `targetUrl` as `` `${folderPath}/${entityId}` ``, but `entityId` is the entity's full DIAL Core resource path (e.g. `applications/{bucket}/{name}`), so the destination ended up with the entire source path nested inside it, and DIAL Core rejected it with 400. Fixed via a `getResourceName(entityId)` helper (`entityId.split('/').pop()`) so `targetUrl` uses only the resource's own name, matching what design.md D3 always specified. Added a regression test reproducing the real reported payload (`applications/{bucket}/My App Name` → `DK Test with nested/Level 1`).
- [x] 6.3 Re-ran `nx test`/`nx lint`/`nx build` for `ai-dial-catalog`, `chat-api`, and `chat` — all pass (same single pre-existing `Header.spec.tsx` failure, confirmed unrelated); updated `design.md` and `docs/catalog-publish-manual-qa.md` to document both fixes and flag TC-1.1/TC-2.1 for explicit retesting
- [x] 6.4 **Publish still 400'd after fix 6.2 — "lost public bucket id"**: `targetFolder`/`targetUrl` were built from the plain `folderPath` with no bucket identity, so Core couldn't resolve which bucket the destination lived in. Investigated the legacy pre-BFF frontend publication feature on `origin/development` (`git show`, read-only) and confirmed DIAL Core's Publication API addresses the shared Organization/public area via the **literal segment `public`** (`PUBLIC_URL_PREFIX` in `apps/chat/src/constants/publication.ts`), not an opaque bucket-id hash. Added `getPublicTargetFolder`/`getResourceTypePrefix`/`stripPublicTargetFolder` helpers: `targetFolder` → `public/{folderPath}` (bare `public` at the root); `targetUrl` (at this point) → `{resourceTypePrefix}/public/{folderPath}/{resourceName}`. Still wrong — see 6.5.
- [x] 6.5 **Publish still 400'd after fix 6.4 — confirmed against a live DIAL Core instance via temporary debug logging**: added debug logs of the exact `createPublication` request body and Core's actual error body (not just the status code). Core's error was explicit: `` Publication "targetUrl" must start with: public and ends with: / ``. Initially (incorrectly) interpreted as "`targetUrl` must be folder-shaped, no resource-type prefix/name" — set `targetUrl = `${publicTargetFolder}/``. This produced a *different* 400 on the next live attempt (`Bad resource url: public/{folderPath}`), see 6.6.
- [x] 6.6 **Publish still 400'd after fix 6.5 — root-caused against DIAL Core's own published OpenAPI spec**: fetched https://dialx.ai/dial_api#tag/Publications/operation/createPublication (via a headless browser, since the page is a JS-rendered SPA — `WebFetch` alone only returns the loading shell; found the underlying spec at `https://dialx.ai/universal_chat_api.yaml`). The spec's own documented example for `/v1/ops/publication/create` is `targetFolder: public/folder/` (**trailing slash required**) and `targetUrl: conversations/public/folder/conversation` (a full file path: `resourceTypePrefix/targetFolder/resourceName`, no trailing slash) — the opposite of fix 6.5's folder-shaped `targetUrl`. Root cause: fix 6.4's `targetFolder` was missing its required trailing slash, and Core's "must start with public and ends with /" error was actually about `targetFolder`, not `targetUrl` as read at the time. Final fix: `targetFolder` always trailing-slashed (`public/{folderPath}/`, bare `public/` at the root); `targetUrl` restored to `{resourceTypePrefix}/{targetFolder}{resourceName}` (re-added `getResourceName`/`getResourceTypePrefix`, removed in 6.5). Updated all tests, `design.md`, and `specs/catalog-publish-api/spec.md` to match the spec-confirmed shape exactly. Temporary debug logging left in place (marked `TODO(debug): remove...`) pending a live end-to-end retest against DIAL Core; all `chat-api` tests (19/19)/lint/build re-verified green.
- [x] 6.7 **Confirmed working live**: user tested a real publish end-to-end — `POST .../publish` returned `201`. Added full debug logging of the `createPublication`/`getPublications` success response bodies (not just error bodies) per user request, to help diagnose a follow-up "publication not visible in admin" question. All tests/lint/build re-verified green.
- [ ] 6.8 **Found while adding the debug logs above, not yet fixed**: DIAL Core's OpenAPI spec defines `getPublications`'s `url` field as the **list scope** (`publications/{bucket}/` for a user's own submissions, `publications/public/` for admins), not a resource filter — `getPublishHistory` currently sends `{ url: entityId }` (the entity's own resource path), which doesn't match this contract. The in-app publish-history list may not actually return results even though publish itself now works; needs the calling user's own bucket threaded into `CatalogPublishService` (not currently available — only `accessToken` is). Deferred pending user confirmation this is worth fixing now vs. later.
- [x] 6.9 **Users couldn't choose the bucket root itself as a publish destination**: `PublishFoldersTree`/`PublishPanel`/`use-publish-flow` treated `selectedFolderPath: []` as "nothing selected" (`Boolean(path?.length)` truthiness checks throughout), so the root of the Organization/public tree — a valid publish target per `getPublicTargetFolder`'s bare `public/` case (fix 6.6) — had no way to be selected in the UI. Redesigned the sentinel: `undefined` now means nothing selected, `[]` means the bucket root is explicitly selected. Replaced every `Boolean(selectedFolderPath?.length)`/`selectedFolderPath?.[…]` truthiness check with `selectedFolderPath != null` in `PublishPanel.tsx` and `DetailsPanel.tsx`; added a `rootFolderLabel` text override (`PublishPanelTexts`, default `'Organization'`) used as the `{folder}` substitution in callouts when root is selected. `use-publish-flow.ts`'s `setSelectedFolderPath` type widened to `(path: string[] | undefined) => void`; its `hasExistingVersionInFolder`/`hasWriteAccess`/`handleSubmit` logic needed no changes since `![]` and `[] ? x : y` already treat empty arrays as truthy in JS — only `undefined` short-circuits them.
  - **Initial implementation (superseded by 6.10 below)**: rendered a separate "Organization root" `<button>` above `DialFoldersTree`, not part of the tree itself.
- [x] 6.10 **Corrected the root-selection UI per user feedback, comparing against the file manager's own pattern**: (1) the file manager represents its bucket root as a real node *inside* the folder tree (`useDialFileManager.ts`'s `items` memo builds a top-level `DialFile` named after the active tab — `"Organization"`, `"Shared with me"`, `"My files"` — wrapping the rest of the tree as its children), not as a floating control outside the tree. Reworked `PublishFoldersTree.tsx` to match: removed the standalone root button (`isRootSelected`/`handleRootClick`, the `IconCheck`/`IconFolder` row) and instead wrap the tree's actual folder nodes (`childFiles`) as the `items` of a single synthetic root `DialFile` (`path: ''`, `name: rootLabel`), which `DialFoldersTree` renders like any other expandable node — click-to-select maps `path: ''` to `selectedFolderPath: []` via the existing `fromFolderPathKey`/`toFolderPathKey` round-trip, so no new selection logic was needed. The root node is always kept expanded (unioned into `expandedPaths` at render time) so its children stay visible by default as before, and the root path key (`''`) is stripped before being forwarded to the host's `onExpandedPathsChange` so `useCatalogPublishFolders` (which has no concept of a `''` folder) never sees it. The root wrapper is omitted entirely while a search matches nothing, so the existing empty-state message still renders correctly. Renamed the default label from `"Organization root"` to `"Organization"` (`PublishFoldersTree`'s `rootLabel` and `PublishPanel`'s `rootFolderLabel`), matching the file manager's exact tab label. (2) Simplified `PublishFooter`'s submit button: it previously read `"Publish to {folder}"` with the destination folder name interpolated, which overflowed the button for long folder/entity names (reported via screenshot). Removed the `folderName`/`publishLabel` prop and interpolation entirely — the button now always reads plain `"Publish"` (or `"Update version {version}"` when replacing an existing version), regardless of the destination; `DetailsPanel.tsx` no longer computes/passes a `folderName` to `PublishFooter`. Updated `PublishFoldersTree.spec.tsx` (23 tests, mock now recurses into nested `DialFile.items` so nested/root rows are clickable), `PublishPanel.spec.tsx`, and `PublishFooter.spec.tsx` accordingly. Re-verified `ai-dial-catalog` (224/225 passing, same pre-existing unrelated `Header.spec.tsx` failure), lint (0 errors), build (succeeds), and `chat` (typecheck/test/lint/build all green) — confirms no downstream impact on `apps/chat`.
- [x] 6.11 **Publish requests succeeded (201) but the request title was wrong, per a live debug log comparison against the legacy frontend's own request-creation form**: `createPublication`'s `name` field was set to the bare version string (e.g. `"0.0.1"`) — a misreading of design D3's "version has nowhere else to go" note as "put it in `name`". Compared against `origin/development`'s `CreatePublicationHandler.tsx`/`PublicationHandler.tsx`/`translatePublicationName.ts`, which default the request title to `"New request by {author}"` and separately send `displayAuthor`/`rules` fields that this service's request body was missing entirely. First correction attempt matched that convention literally (`name: `New request by ${author}``); per follow-up user feedback the title should instead include **the entity's own name and version**, with no author in the title at all (the separate `displayAuthor` field already carries that). Final request body: `name: `${entityName} ${version}`` (e.g. `"My App Name 0.0.1"`), `displayAuthor: author`, `rules: []`. `entityName`/`version` are both recovered from `entityId`'s own `{name}__{version}` suffix (new `splitEntityNameAndVersion` helper, URL-decoding the name) rather than from `Publication.name` — this also let `getPublishHistory`'s per-entry `version` field switch from reading `publication.name` (never reliable, since Core's schema has no version field) to the same `entityId`-derived value. `author` (a human display name, e.g. `"Valery Dluski"`) is a new required param on `CatalogPublishService.publish`, sourced from a new `getUserDisplayName(claims)` util (`apps/chat-api/src/common/utils/user-display-name.ts`, 6 tests) that reads the session's allowlisted OIDC claims (`name` → `preferred_username` → local part of `email` → `'Unknown Author'` fallback, matching the legacy frontend's own `LEGACY_NEW_REQUEST_BY_UNKNOWN` convention) — wired through `CatalogPublishController.publish` from `req.user.claims`. Updated `catalog-publish.service.spec.ts` (entity IDs now carry a real `{name}__{version}` suffix throughout, e.g. `toolsets/bucket-123/tool-abc123__1.2.0`) and `catalog-publish.controller.spec.ts` (`TEST_USER.claims`, expects the 6th `author` arg). Re-verified `chat-api` (1089/1089 passing — one unrelated flaky `files.controller.spec.ts` "socket hang up" network test, confirmed to pass on rerun), lint (0 errors), build (succeeds).
- [x] 6.12 **Publish 400'd on a folder name containing a space, per a live debug log**: Core's error was `` Bad resource url: public/test 14.04/ `` — `folderPath` arrives as plain, unencoded text from the request body (e.g. `"test 14.04"`), but `getPublicTargetFolder` interpolated it directly into `targetFolder`/`targetUrl` with no percent-encoding, unlike `entityId`, which is always already URL-encoded by the generated client. Fixed by encoding each `folderPath` segment through the existing `encodeDialResourcePath` helper (`apps/chat-api/src/common/utils/encode-dial-path.ts`) — the same one `toolsets.service.ts`/`conversation.service.ts` already use for every other DIAL resource path built from user-supplied text (decode-then-encode, so it's idempotent whether or not the input was already encoded). `stripPublicTargetFolder` (used when reading publish history back) now decodes each segment symmetrically, so `Publication.targetFolder` round-trips back to the exact plain folder path the frontend sent. Added a regression test reproducing the exact reported folder name (`'test 14.04'` → `targetFolder: 'public/test%2014.04/'`) plus a decode-back test for `getPublishHistory`. Re-verified `chat-api` (1091/1091 passing), lint (0 errors), build (succeeds).
- [x] 6.13 **Renamed "catalog-publish" naming to "publish"**, scoped to the backend module and frontend API wrapper only (per explicit user confirmation — `useCatalogPublishFolders`/`CatalogPublishEntityType` intentionally left unchanged, since they're a distinct app-level hook/domain-type name, not the module's own identity): `apps/chat-api/src/catalog-publish/` → `apps/chat-api/src/publish/`, with `catalog-publish.controller/service/module.ts` → `publish.controller/service/module.ts` and `CatalogPublishController`/`CatalogPublishService`/`CatalogPublishModule` → `PublishController`/`PublishService`/`PublishModule`; `@ApiTags('catalog-publish')` → `@ApiTags('publish')`; cache-key prefix `catalog-publish-history:` → `publish-history:`. Ran `npm run openapi` to regenerate the client — `CatalogPublishApi.ts` → `PublishApi.ts` (the class name derives from the `ApiTags` string) — and `npm run openapi:check` to confirm the regenerated client is in sync. `operationId`s (`publishCatalogEntity`/`getCatalogPublishHistory`) were intentionally left unchanged, since renaming the generated SDK method names is a separate concern from the module's own naming. `apps/chat/src/server-api/catalog-publish.api.ts` → `publish.api.ts` and `apps/chat/src/utils/catalog-publish.ts` → `publish.ts` (plus their spec files), with the `catalogPublishApi` client instance in `api-client.ts` renamed to `publishApi`; updated every importer (`CatalogView.tsx` and its spec). Re-verified `chat-api` (1091/1091 tests, lint 0 errors, build succeeds) and `chat` (1014/1014 tests, lint 0 errors, build succeeds). `nx typecheck chat-api` was also checked, but surfaced ~330 pre-existing `TS7006`/`TS6305` errors spread across nearly every unrelated spec file in the project — confirmed present on the pre-rename tree too (via a throwaway `git stash`/`stash pop` comparison), so it's an existing environment/tooling gap out of scope for this task, not a regression from this rename.
