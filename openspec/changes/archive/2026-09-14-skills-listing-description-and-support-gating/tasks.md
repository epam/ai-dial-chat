# Tasks: skills-listing-description-and-support-gating

## 1. Skill description from listing (Core PR #1970)

- [x] 1.1 Add optional `description?: string` to `SkillMetadataItemDto` (`apps/chat-api/src/skills/dto/skill-metadata.dto.ts`) with `@ApiProperty({ required: false })` — implemented as `@ApiPropertyOptional`, the file's existing convention for every optional field (identical Swagger output)
- [x] 1.2 In `mapToSkillMetadataItem` (`apps/chat-api/src/skills/utils/skill-metadata.util.ts`), read `attributes` defensively off the raw `DialMetadataBase` (`'attributes' in item`) and map `description` only when it is a string. The description is nested — confirmed against a live Core `listSkillMetadata` response, items carry it at `attributes.description` (alongside `attributes.name`); the top-level item has no `description` field. Folders and absent/non-string values map to `undefined` (the item-only guard makes the DTO's "item only" doc true even against a folder entry with a stray `attributes` map)
- [x] 1.3 Regenerate the OpenAPI spec and client (`npm run openapi`, `npm run openapi:check`), then build/lint `chat-api-client` — regen run by the user, output verified by inspection (spec + generated client carry the field); `openapi:check` + build/lint awaiting user approval
- [x] 1.4 Extend the skills listing service tests: item with `attributes.description` forwards it; item without `attributes` omits it; non-string description is ignored; folders never carry it (covers `GET /api/v1/skills` and `GET /api/v1/skills/catalog` paths)
- [x] 1.5 Catalog mapping: in `mapSkillToCatalogItem` (`libs/chat-hooks/src/catalog/map-skill-to-catalog-item.ts`), replace the hardcoded `description: ''` (and its "Skill metadata carries no description" comment) with `description: skill.description ?? ''` — the catalog grid card and the details panel's header/Content summary show the listing description before the manifest fetch lands
- [x] 1.6 Correct the stale `CONTENT_FIRST_ENTITY_TYPES` rationale comment in `libs/catalog/src/components/Details/DetailsPanel.tsx` ("a skill has no description at all in its metadata") — behavior unchanged: skills stay content-first with no About tab, only the comment's fact is updated
- [x] 1.7 Extend `libs/chat-hooks/src/catalog/tests/map-skill-to-catalog-item.spec.ts`: a listing `description` is forwarded to `CatalogItem.description`; an absent description still maps to `''` (the old combined "leaves description and version empty" test narrowed to version/topics — the description half moved to the two dedicated tests)

## 2. Backend — deployments skillsSupported flag (Core PR #1976)

- [x] 2.1 Add `skillsSupported?: boolean` to `DeploymentFeaturesDto` and `DeploymentFeaturesDetailsDto` (`apps/chat-api/src/deployments/dto/`)
- [x] 2.2 Map it defensively in `mapToDeploymentItem` (listing, from `features.skills_supported`) and `mapDeploymentFeatures` (details) in `apps/chat-api/src/deployments/utils/deployment-mapper.util.ts` — same pattern as `responsesApi`/`chatCompletion`
- [x] 2.3 Regenerate the OpenAPI spec and client (`npm run openapi`, `npm run openapi:check`), then build/lint `chat-api-client` — regen + check done; build/lint awaiting user approval
- [x] 2.4 Extend the deployment-mapper tests: `features.skills_supported: true` → `skillsSupported: true`; absent/non-boolean → `undefined`; no caching/filtering change

## 3. Frontend — lazy description cleanup

- [x] 3.1 Delete `fetchSkillDescription` (and `fetchSkillManifestDescription` if now unused) from `libs/chat-hooks/src/catalog/useSkillItemDetails.ts` — the details-panel manifest + file-listing fetch stays
- [x] 3.2 In `libs/skills` `useSkillSelectorOverlay`: remove `onFetchSkillDescription`, the per-session `skillDescriptions` cache, `pendingDescriptionIds`, `handleItemTooltipOpen`, and all `isDescriptionLoading` plumbing; shrink `buildFavoriteSkillItem` to map `description` straight from the listing entry
- [x] 3.3 Update `libs/skills` models: `SkillListingEntry` and `FavoriteSkillItem` gain `description?` from the listing; drop `FavoriteSkillItem.isDescriptionLoading`
- [x] 3.4 Remove `isDescriptionLoading`/`onTooltipOpen` from the shared tooltip content component, `FavoriteSkillsPanel`, and `ChatSkill`; the tooltip renders the listing description or the "View details" button alone
- [x] 3.5 Update the app wrapper `apps/chat/src/components/SkillSelector/useSkillSelectorOverlay.tsx`: drop `handleFetchSkillDescription` and its wiring
- [x] 3.6 Update `libs/skills` README (props and model changes) and run `npm run validate:docs` — both READMEs updated (`libs/skills` and the `libs/chat-hooks` `fetchSkillDescription` section, which documented the removed export); `validate:docs` run held for user approval with the verification batch. Also fixed pre-existing `ChatSkill` README drift surfaced by the rewrite: an `onRemove`/`removeLabel` remove control that never existed in the component, and "kit-default Right placement" where the code places the tooltip Top

## 4. Frontend — skillsSupported gate and ChatSkill error state

- [x] 4.1 Add `isUnsupported?: boolean` and `unsupportedLabelClassName?: string` (default `text-error`) to `ChatSkill`; when unsupported, the label carries the error color and the tooltip content is the unsupported message alone (no description, no "View details") — the color class joins `labelClassName` via `mergeClasses` so the whole visible `/{name}` label reads as the error. Follow-up styling tweak (user request): added `unsupportedClassName?: string` (default `bg-error`, the repo's established error-tint pairing with `text-error`) applied to the chip button via `mergeClasses` so the error state also carries an error background; README's ChatSkill section now documents the full error state
- [x] 4.2 Add the unsupported-message label (English default) to the lib's labels type and thread it through the tooltip content component — `ChatSkillLabels.unsupportedTooltipLabel` (default matching the i18n value) and `SkillInfoTooltipContentProps.unsupportedMessage` (while set, the content is the message alone); `onViewDetails` on the tooltip content became optional since the unsupported branch renders no button
- [x] 4.3 Add `isSkillsSupported` as a parameter of the lib `useSkillSelectorOverlay`; the effective entry-point gate becomes `isEnabled && isSkillsSupported` (menu overlay and command menu configs are omitted when unsupported); compute `isSkillUnsupported = selectedSkill != null && !isSkillsSupported` and pass it to the `ChatSkill` element; expose `isSkillUnsupported` on the hook result — the browse modal, details panel, selected chip, and removal gesture survive an unsupported deployment per the delta spec (only the ways in are hidden); the flag-off early return reports `isSkillUnsupported: false`
- [x] 4.4 Update the app wrapper to accept `isSkillsSupported: boolean`, drop the removed fetch wiring, and forward the new label from i18n — fetch wiring was already dropped in 3.5; the wrapper now takes `{ isSkillsSupported }` and forwards `t(SkillSelectorI18nKeys.UnsupportedTooltipLabel)` as `unsupportedTooltipLabel`
- [x] 4.5 Wire the hosts: `ConversationView` and `NewConversationComposer` pass `selectedDeployment?.features?.skillsSupported === true`; `AppPreviewChat` passes `appDeployment?.features?.skillsSupported === true`; each folds the hook's `isSkillUnsupported` into the `isSendDisabled` value it already passes to the input — note: the new-conversation surface's hook caller is `ConversationRoute` (not the composer component itself, which receives the slot/commandMenu via props), so `ConversationRoute` computes the boolean and `NewConversationComposer` gained an `isSkillUnsupported` prop folded into its internal `isSendDisabled` (matching `ConversationView`'s own fold); in `ConversationView` and `ConversationRoute` the `useDeployments`/`selectedDeployment` block moved above the hook call because the hook now takes the support value as a parameter. Also fixed stale wording in `AppPreviewChat`'s hook comment ("lazy description fetch" left over from group 3)
- [x] 4.6 Confirm `libs/conversation-input` needs no change (send gating rides the existing `isSendDisabled` + `canSend` path) and that `libs/skills` imports no deployment types — verified by grep: `libs/skills` has no deployment DTO or `chat-api-client` imports (the support signal arrives as a plain boolean; the only "deployment" matches are comments and the generic shared `DeploymentIcon`), and `libs/conversation-input` is untouched (no skill references; its deployment matches are its own model-selector utilities)

## 5. i18n

- [x] 5.1 Grep `apps/chat/src/i18n/locales/en.json` for the message value (duplicate-value rule), then add `skillSelector.unsupportedTooltipLabel` to `en.json`, `SkillSelectorI18nKeys.UnsupportedTooltipLabel` in `translation-keys.ts`, and the key to every locale file — grep found no existing value ("doesn't support"/"not supported" hits are unrelated file-preview strings), so the feature-scoped key is correct; `en.json` is the only locale file; the value matches the lib's English default exactly (wording later updated at user request to "Selected model does not support skills. Remove the skill or select different model to proceed." — lib defaults, `en.json`, proposal/design/spec wording all carry the new sentence)

## 6. Tests and verification

- [x] 6.1 Frontend tests: favorites tooltip renders the listing description with no fetch; ChatSkill error state (error class, message-only tooltip); entry points hidden when `isSkillsSupported` is false and restored on true; hook returns `isSkillUnsupported` correctly across model switches; send disabled while unsupported
- [x] 6.2 Run `npm run verify:changed` per slice during implementation, then `npm run verify:full` once before completion (after user approval, per project workflow)

## 7. Docs

- [x] 7.1 Update `libs/skills` README for the changed hook/component props (already covered by 3.6 — verify it reflects the 4.x changes too) and re-run `npm run validate:docs` — README now covers all 4.x changes: the hook example passes the required `isSkillsSupported` and destructures `commandMenu`/`isSkillUnsupported` (the `commandMenu` result was previously undocumented — drift from the archived change, now fixed), the prose documents the entry-point gate (`undefined` while `isEnabled` or `isSkillsSupported` is false) and the `isSkillUnsupported` result with the `labels.unsupportedTooltipLabel` default, the overview paragraph names the deployment-support boolean, and `SkillInfoTooltipContent` documents `unsupportedMessage` and the now-optional `onViewDetails`. Also added the listing-description clause to `libs/chat-hooks` README's `mapSkillToCatalogItem` section. `npm run validate:docs` passed (43 markdown files)
- [x] 7.2 Check `docs/architecture.md` — no new lib/app/context/route is added, so no update is expected; confirm nothing described there changed — no described mechanism changed (no new endpoints; `SkillsContext`, `/api/v1/skills`, and the module-boundary sections are untouched), but the check surfaced two pre-existing gaps in the libs table, both fixed: `@epam/ai-dial-skills` was missing entirely (drift from the archived change that created the lib — row added between `prompts` and `skill-editor`), and `@epam/ai-dial-attachment-canvas` was listed twice (dropped the older duplicate, kept the row that includes CSV spreadsheets)
