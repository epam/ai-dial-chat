**Slicing strategy:** contract-first — agree the `isActive` field and the pause/resume request/response contract (Slice 1), regenerate the client (Slice 2), then build backend and frontend against that fixed contract in parallel-safe order (Slices 3–7). Each slice ends with its own verification command; do not start the next slice while the current one is red.

## 1. Contract: `isActive` field and pause/resume DTO shapes (backend)

- [x] 1.1 In `apps/chat-api/src/scheduled-tasks/dto/scheduled-task.dto.ts`, add `isActive?: boolean` to `ScheduledTaskDto` with `@ApiPropertyOptional({ example: true }) @IsOptional() @IsBoolean()`, placed alongside the existing `nextRunTime`/`triggerType` optional fields.
- [x] 1.2 In `apps/chat-api/src/scheduled-tasks/scheduled-tasks.mapper.ts`, extend `fromUpstreamSchedule` to compute `isActive` per design.md "Decision 1": `upstream.next_run_time != null` when `upstream.trigger != null || upstream.trigger_type != null`, else `undefined`. Add a short inline comment marking this as the documented assumption pending upstream contract confirmation (do not silently invent a different heuristic later without updating this comment and design.md).
- [x] 1.3 Add/extend `apps/chat-api/src/scheduled-tasks/tests/scheduled-tasks.mapper.spec.ts` (or the existing mapper spec file) covering the three "Scheduled task active state field" scenarios from `openspec/changes/add-scheduled-task-active-toggle/specs/scheduled-tasks-api/spec.md`: future `next_run_time` → `true`, paused cron (`next_run_time: null` with `trigger_type`/`trigger.cron` present) → `false`, missing `trigger`/`trigger_type` → `undefined`, and confirm mapping never throws.
- [x] 1.4 Verify: `npm exec nx test chat-api`, `npm exec nx lint chat-api`.

## 2. Contract: pause/resume DTOs, controller routes, and Swagger (backend)

- [x] 2.1 In `apps/chat-api/src/scheduled-tasks/scheduled-tasks.controller.ts`, add `pauseScheduledTask` and `resumeScheduledTask` handlers on `POST :scheduleId/pause` and `POST :scheduleId/resume`, reusing `GetScheduledTaskDto` for `@Param()`, no `@Body()`. Set `@ApiOperation({ operationId: 'pauseScheduledTask', ... })` / `resumeScheduledTask` respectively, `@HttpCode(200)`, `@Throttle({ default: { limit: 10, ttl: 60000 } })` (matching `updateScheduledTask`), and full `@ApiResponse` coverage for 200 (`type: ScheduledTaskDto`), 400, 401, 403, 404, 429, 502, 503. (409 omitted — no evidence design.md's Open Question 3 needs it; not flagged with a TODO since the requirement text already says "only if... later confirmed.")
- [x] 2.2 Delegate both handlers to new `ScheduledTasksService.pauseScheduledTask(sub, at, scheduleId)` / `resumeScheduledTask(sub, at, scheduleId)` methods, reading `sub`/`at` from `req.user as SessionUser` exactly like `updateScheduledTask`. (Implemented ahead of Slice 4 since the controller does not compile without them; see Slice 4 for the cache/rollback contract details.)
- [x] 2.3 **Deviation, flagged for review:** `npm run openapi` could not complete in this sandbox — `node -r @swc-node/register apps/chat-api/src/openapi-spec.ts` (which bootstraps the full `AppModule` via `NestFactory.create`) crashes silently (no stdout/stderr, exit code 1) before reaching `SwaggerModule.createDocument`, reproduced identically via `nx run chat-api:openapi-spec --skip-nx-cache` and via `tsx`; root cause not isolated (looks like a native-binary/environment issue unrelated to this change's code, since it occurs immediately inside `NestFactory.create` for the *entire* app graph). Worked around by **hand-editing** `libs/chat-api-client/openapi.json` to add the `pauseScheduledTask`/`resumeScheduledTask` paths and the `isActive` property on `ScheduledTaskDto`/`CreatedScheduledTaskDto`/`UpdatedScheduledTaskDto`, mirroring the exact JSON shape of the sibling `updateScheduledTask`/`getScheduledTask` entries byte-for-byte. `npm run openapi:check` (`node tools/openapi/check-client.mjs`) passes against the result. **A real dev/CI environment must re-run `npm run openapi` before merge** to confirm the hand-written spec matches Swagger's actual output and replace it with the generator's output.
- [x] 2.4 Verify: `npm exec nx lint chat-api`, `npm exec nx build chat-api`. (Both green.)

## 3. Generated client regeneration

- [x] 3.1 **Same deviation as 2.3:** `libs/chat-api-client/src/generated/src/apis/ScheduledTasksApi.ts` and `.../models/index.ts` were hand-edited (not run through `openapi-generator-cli`) to add `pauseScheduledTask`/`resumeScheduledTask` methods and `isActive` fields, mirroring the exact generated-code pattern (JSDoc, `*Raw`/plain method pairs, `RequiredError` checks, `JSONApiResponse<T>`) already present for `updateScheduledTask`/`getScheduledTask`. **A real environment must re-run `npm run openapi` (which regenerates both the spec and the client) before merge** and diff the result against this hand-written version — this task is not a substitute for that regeneration, only a way to keep downstream slices (4–9) unblocked and verifiable in this environment.
- [x] 3.2 Inspected `libs/chat-api-client/src/generated/src/apis/ScheduledTasksApi.ts`: `pauseScheduledTask`/`resumeScheduledTask` (and their `*Raw` counterparts) use clean names, return `ScheduledTaskDto`, and take no request-body parameter (`PauseScheduledTaskRequest`/`ResumeScheduledTaskRequest` carry only `scheduleId`). No stray `: any` in the new code.
- [x] 3.3 Verify: `npm exec nx build chat-api-client -- --skip-nx-cache` (tsc build succeeded), `npx nx run chat-api-client:lint --skip-nx-cache` (0 errors/warnings on the new code).

## 4. Backend service implementation: pause/resume + cache invalidation

- [x] 4.1 In `apps/chat-api/src/scheduled-tasks/scheduled-tasks.service.ts`, add a private `buildScheduleActionUrl(scheduleId, action: 'pause' | 'resume')` helper (or extend `buildSchedulesUrl`) producing `.../schedules/{scheduleId}/{action}`.
- [x] 4.2 Implement `pauseScheduledTask(userSub, accessToken, scheduleId)`: call `fetchUpstream(actionUrl('pause'), 'POST', accessToken, 'pause scheduled task "<id>"')` with no body; on success, call `getScheduledTask(accessToken, scheduleId)` as the follow-up refresh (catch its failure per design.md Decision 5 — on refresh failure, still return a `ScheduledTaskDto` with `isActive: false` merged in, do not throw); then call `invalidateListCache(userSub)`; return the resulting DTO. (Implemented via a shared private `performScheduleAction` helper.)
- [x] 4.3 Implement `resumeScheduledTask(userSub, accessToken, scheduleId)` symmetrically with `isActive: true`.
- [x] 4.4 Add `apps/chat-api/src/scheduled-tasks/tests/scheduled-tasks.service.spec.ts` cases (or extend the existing service spec) per the "Pause a scheduled task" / "Resume a scheduled task" spec scenarios: exact upstream URL called, bearer token forwarded, list cache invalidated only on action success, list cache NOT invalidated on action failure, follow-up-`GET` failure after a successful action still returns 200 with the requested `isActive` and still invalidates the cache (no rollback), and 404/502/503 mapping via the existing `mapDialHttpStatus`/`handleDialFetchError` helpers.
- [x] 4.5 Add `apps/chat-api/src/scheduled-tasks/tests/scheduled-tasks.controller.spec.ts` (or extend it) supertest cases per "Feature disabled rejects the request" / "Unauthenticated request is rejected" / rate-limit-exceeded scenarios for both new routes.
- [x] 4.6 Verify: `npm exec nx test chat-api`, `npm exec nx lint chat-api`. (1945/1945 tests pass; 0 lint errors, 2 pre-existing unrelated warnings.)

## 5. Frontend API wrappers

- [x] 5.1 In `apps/chat/src/server-api/scheduled-tasks.api.ts`, add `pauseScheduledTask(scheduleId: string): Promise<ScheduledTaskDto>` and `resumeScheduledTask(scheduleId: string): Promise<ScheduledTaskDto>`, delegating to `scheduledTasksApi.pauseScheduledTask({ scheduleId })` / `scheduledTasksApi.resumeScheduledTask({ scheduleId })` from the regenerated `@epam/chat-api-client`, matching the existing `getScheduledTask`/`updateScheduledTask` wrapper style — no direct `fetch`, no `base.ts` helper.
- [x] 5.2 Add a unit test file (e.g. `apps/chat/src/server-api/tests/scheduled-tasks.api.spec.ts` or extend the existing one) asserting both wrappers pass `scheduleId` through to the correct generated-client method and return its resolved `ScheduledTaskDto`.
- [x] 5.3 Verify: `npm exec nx test chat` (7/7 new tests pass), `npm exec nx lint chat` (0 errors; pre-existing unrelated warnings only).

## 6. Library: Active switch in `ScheduledTaskDetailView` (host-agnostic)

- [x] 6.1 In `libs/scheduled-tasks/src/models/scheduled-task-detail-view-props.ts` (or the file defining `ScheduledTaskDetailViewProps`), add `isActive?: boolean`, `isActiveUpdating?: boolean`, `isActiveDisabled?: boolean`, `onActiveChange?: (nextActive: boolean) => void`, and extend the labels type with the Active switch's accessible-name string and any status-announcement strings the component itself renders (e.g. `activeStatusLabel`). Document each new field's JSDoc per `.claude/rules/libs.md` (one-line summary, default if any). (Also added `activeStatusAnnouncement?: string` for the aria-live region, wired in Slice 9.)
- [x] 6.2 In `libs/scheduled-tasks/src/components/ScheduledTaskDetailView/ScheduledTaskDetailView.tsx`, import `DialSwitch` from `@epam/ai-dial-ui-kit` and render it in the header's end-side slot, before the existing `onEdit && <NeutralButton .../>` block, only when `isActive !== undefined`. Rendered without `label` on `DialSwitch` itself — the visible "Active" text is a sibling `<span>` the component renders directly, avoiding a duplicate/conflicting `aria-label="switch-title"` quirk in the vendor label markup (see comment in source). `disabled={isActiveUpdating || isActiveDisabled}`, `onChange={(value) => onActiveChange?.(value)}`. No API client, routing, i18n, or feature-flag import added.
- [x] 6.3 `DialSwitch`'s own root renders `role="switch"` but does **not** expose `aria-checked` (confirmed by reading the compiled UI-kit bundle — a vendor gap, out of scope to patch per AGENTS.md a11y "Scope boundary"). Added `aria-checked`/`aria-label` explicitly on the component's own wrapping `role="group"` element around `<DialSwitch>`, documented inline with a comment explaining why.
- [x] 6.4 Extended `ScheduledTaskDetailView.spec.tsx` with an `"Active switch"` describe block: absent when `isActive` undefined; checked/unchecked matching `isActive`; `onActiveChange` called exactly once with the requested value, no navigation/network call; disabled+non-interactive under `isActiveUpdating`/`isActiveDisabled`; DOM order before Edit; aria-live announcement rendering.
- [x] 6.5 Added a `"ScheduledTaskDetailView — library isolation"` describe block that reads the component source and asserts no `apps/chat`, `@epam/chat-api-client`, `react-router`, or `react-i18next` imports; confirmed by inspection that the props model file also has no such imports.
- [x] 6.6 Verify: `npx nx run ai-dial-scheduled-tasks:test --skip-nx-cache` (117/117 pass), `npx nx run ai-dial-scheduled-tasks:lint --skip-nx-cache` (0 errors/warnings), `npx nx run ai-dial-scheduled-tasks:build --skip-nx-cache` (succeeds).

## 7. Frontend: wire the switch into `ScheduledTaskDetailPage`

- [x] 7.1 In `apps/chat/src/pages/ScheduledTaskDetailPage/ScheduledTaskDetailPage.tsx`, add `isActiveUpdating` state and an `activeChangeRequestRef` counter, plus `currentScheduleIdRef`/`isMountedRef` refs (mirroring the existing `taskFetchToken` pattern) to guard against a stale pause/resume response applying after navigation, a `scheduleId` change, or unmount.
- [x] 7.2 Compute `isActiveDisabled` as `task?.triggerType === 'date' && task?.nextRunTime == null` (per the "Active switch is disabled, not hidden, for a completed one-time schedule" spec requirement).
- [x] 7.3 Implemented `handleActiveChange(nextActive: boolean)` per the described optimistic-update/rollback/token-guard contract, using `getApiErrorDetails` (from `apps/chat/src/server-api/api-error.ts`) and `useNotification`/`NotificationVariant` (the established app-wide notification pattern, confirmed against `ScheduledTaskCreatePage.tsx`'s create-success/error flow) for the success/error toasts.
- [x] 7.4 Passed `isActive={task?.isActive}`, `isActiveUpdating={isActiveUpdating}`, `isActiveDisabled={isActiveDisabled}`, `onActiveChange={handleActiveChange}` to `<ScheduledTaskDetailView />`.
- [x] 7.5 Extended `ScheduledTaskDetailPage.spec.tsx` with an "Active switch" describe block covering: pause called exactly once on toggle-off / resume exactly once on toggle-on; switch disabled during the in-flight call; success updates `isActive`/shows a success notification; failure rolls back `isActive` and shows an error notification with the trace id, while the rest of the page stays visible; a pause response resolving after unmount does not throw/update state; a completed one-time schedule renders unchecked+disabled without calling either API; a paused-but-resumable recurring schedule stays togglable.
- [x] 7.6 Verify: `npx nx run chat:test --skip-nx-cache -- ScheduledTaskDetailPage` (16/16 pass), `npx nx run chat:lint --skip-nx-cache` (0 errors; pre-existing unrelated warnings only).

## 8. i18n

- [x] 8.1 Added to `apps/chat/src/i18n/locales/en.json` under `scheduledTasks.detail`: `activeStatusLabel: "Active"`, `pauseSuccess: "Task paused"`, `resumeSuccess: "Task resumed"`, `activeStatusUpdateError: "Failed to update the task's active status"` (grepped `en.json` first — no existing generic success/error string matched; these name the specific pause/resume action, so they stay feature-scoped per `.claude/rules/all-ts.md`).
- [x] 8.2 Added the matching members to `ScheduledTasksI18nKeys` in `apps/chat/src/constants/translation-keys.ts`: `DetailActiveStatusLabel`, `DetailPauseSuccess`, `DetailResumeSuccess`, `DetailActiveStatusUpdateError`. Confirmed distinct from `DetailActiveWindowLabel`/`DetailActiveWindowValue` (different key, different meaning).
- [x] 8.3 Updated `ScheduledTaskDetailPage.tsx`'s `labels` `useMemo` to include `activeStatusLabel: t(ScheduledTasksI18nKeys.DetailActiveStatusLabel)` and `activeStatusAnnouncement` (the live aria-live message state), passed to `ScheduledTaskDetailView`.
- [x] 8.4 Verify: `npx nx run chat:lint --skip-nx-cache` and `npx nx run chat:test --skip-nx-cache -- ScheduledTaskDetailPage` — both green (see 7.6).

## 9. RTL and accessibility verification

- [x] 9.1 Confirmed no physical-direction Tailwind classes were introduced by this change (`grep -n "ml-\|mr-\|pl-\|pr-\|left-\|right-"` over `ScheduledTaskDetailView.tsx` returns nothing new) — the switch sits in a plain `flex items-center gap-2` group, which is direction-agnostic and needs no `rtl:` mirroring since a switch carries no inherent left/right meaning.
- [x] 9.2 DOM order (switch before Edit) is source-order and independent of `dir`; the end-side group's placement itself is governed by the header's existing `justify-between` layout (unchanged by this feature). Covered by the "renders before the Edit button in DOM order" test in `ScheduledTaskDetailView.spec.tsx`.
- [x] 9.3 Implemented in Slice 6/8: an `aria-live="polite"` `role="status"` region in `ScheduledTaskDetailView.tsx` renders `labels.activeStatusAnnouncement` (set by the host page after a successful pause/resume), separate from the switch's own accessible name (`labels.activeStatusLabel`, stable as "Active" in both states). Covered by the "announces a status message via an aria-live region, separate from the switch label" test.
- [x] 9.4 **Documented gap, not fixed (per AGENTS.md a11y "Scope boundary" — vendor internals out of scope):** reading the compiled `@epam/ai-dial-ui-kit` bundle shows `DialSwitch`'s hit area is `w-[36px] h-[18px]` (36×18 CSS px), well under the 44×44 touch-target minimum, and its root exposes `role="switch"` but no `aria-checked` (worked around at the call site in Slice 6.3). Focus-visible treatment is entirely vendor-controlled and not inspected further, for the same reason. Both are flagged here for a future UI-kit-side fix, not patched in this change.
- [x] 9.5 Verify: `npx nx run ai-dial-scheduled-tasks:test --skip-nx-cache` (117/117 pass), `npx nx run chat:test --skip-nx-cache -- ScheduledTaskDetailPage` (16/16 pass).

## 10. Documentation

- [x] 10.1 No `docs/` files describe the Scheduled Task detail-page header or the scheduled-tasks-api contract today (confirmed during investigation — `docs/` covers architecture/auth, not this feature); no docs update task is required. If a reviewer later finds a `docs/` page describing this header, add a follow-up task then rather than guessing at one now.

## 11. Final verification

- [x] 11.1 Ran the full affected check against `origin/development-1.0`: `npx nx affected --target=lint --base=origin/development-1.0` (0 errors across all affected projects, pre-existing unrelated warnings only), `npx nx affected --target=test --base=origin/development-1.0` (176 test files / 2227 passed, 2 pre-existing skipped), `npx nx affected --target=build --base=origin/development-1.0` (5 affected projects + 37 dependency tasks, all green). Two `typecheck` tasks (`ai-dial-sidebar`, `ai-dial-conversation-input`) were flagged "flaky" by Nx and unrelated to this change's files.
- [x] 11.2 Re-ran `node tools/openapi/check-client.mjs` (the `openapi:check` script) — exits 0 against the hand-patched `libs/chat-api-client/openapi.json` and generated client. **Reminder:** per Slices 2.3/3.1, `openapi.json` and the generated client were hand-edited (not machine-generated) because `npm run openapi`'s underlying `NestFactory.create(AppModule)` bootstrap crashes silently in this sandbox for reasons unrelated to this change's code. A real dev/CI environment must re-run `npm run openapi` before merge to replace the hand-written artifacts with the generator's actual output and confirm no drift.
- [x] 11.3 All 45 tasks in this file are checked. `design.md`'s three "Open Questions" remain explicitly open in the merged code, each with its narrowing mitigation intact and cited from the source: Decision 1's `next_run_time`-based `isActive` heuristic is isolated to and commented in `deriveIsActive` (`scheduled-tasks.mapper.ts`); Decision 2's BFF follow-up-`GET` design is implemented in `performScheduleAction` (`scheduled-tasks.service.ts`), which also implements Decision 5's "no rollback on refresh failure" rule; Decision 4's disabled-not-hidden rule for a completed one-time schedule is implemented as `isActiveDisabled` in `ScheduledTaskDetailPage.tsx` and covered by a dedicated test. None of the three questions were silently resolved with an unverified assumption presented as fact.

## 12. Catalog card: read-only "Paused" badge

- [x] 12.1 Added `isActive?: boolean` to `ScheduledTaskItem` (`libs/scheduled-tasks/src/models/scheduled-task-item.ts`) — `false` shows the "Paused" badge; `true`/`undefined` keeps the schedule pill.
- [x] 12.2 Added `pausedBadgeLabel` (labels), `pausedBadgeBackground`/`pausedBadgeBorder`/`pausedBadgeText` (colors), `pausedBadgeClassName` (typography) to `ScheduledTaskCardProps` (`scheduled-task-card-props.ts`), matching the lib's existing color/typography prop conventions.
- [x] 12.3 `ScheduledTaskCard.tsx`: when `item.isActive === false`, render a rounded-full badge with `IconPlayerPause` + the "Paused" label in place of the schedule pill (same slot, mutually exclusive). Added matching `.pausedPill`/`.pausedLabel` SCSS classes with CSS-var fallbacks mirroring `.schedulePill`/`.scheduleLabel`.
- [x] 12.4 Extended `ScheduledTaskCard.spec.tsx`: badge renders and schedule pill is absent when `isActive: false`; schedule pill renders and badge is absent when `isActive: true` or omitted.
- [x] 12.5 `apps/chat/src/utils/map-scheduled-task-dto.ts`'s `mapScheduledTaskDtoToItem` now sets `isActive: task.isActive` (straight passthrough, no re-derivation — the BFF mapper owns that). Extended `map-scheduled-task-dto.spec.ts` with `isActive: false` / `isActive: true` / missing-`isActive` cases.
- [x] 12.6 Confirmed by reading `ScheduledTaskCardGrid.tsx` and `ScheduledTasks.tsx` that both forward each `ScheduledTaskItem` (including `isActive`) unchanged into `ScheduledTaskCard` — no intermediate layer narrows the item shape, so no changes were needed there.
- [x] 12.7 Verify: `npx nx run ai-dial-scheduled-tasks:test --skip-nx-cache` (119/119 pass), `npx nx run ai-dial-scheduled-tasks:lint --skip-nx-cache` (0 errors), `npx nx run chat:test --skip-nx-cache -- map-scheduled-task-dto` (18/18 pass), `npx nx run chat:lint --skip-nx-cache` (0 errors, pre-existing warnings only).
