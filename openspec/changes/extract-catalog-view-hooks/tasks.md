## 1. Baseline

- [x] 1.1 Read `CatalogView.tsx` and `CatalogView.spec.tsx` end to end; map each existing test case to the hook group (per `specs/catalog-view-hook-decomposition/spec.md`'s ownership map) it will move to.
- [x] 1.2 Run `npm run verify:changed` (or `npm run test:file -- apps/chat/src/components/CatalogView/tests/CatalogView.spec.tsx`) once before any change to confirm a clean green baseline.

## 2. Extract `useCatalogItems`

- [x] 2.1 Create `apps/chat/src/hooks/useCatalogItems/useCatalogItems.ts` owning `quickAppSchemaId`, `quickAppDeploymentIds`, `promptOverviewLabels`, `skillOverviewLabels`, `deploymentLimitsLabels`, `catalogDetailsApi`, `combinedSkills`, the `useCatalogItemDetails` call, `catalogItems`, `visibleCatalogItems`, `reconciledFilterTopics`, `availableTabIds`, `favorites` — same inputs/dependency arrays as today.
- [x] 2.2 Update `CatalogView.tsx` to call `useCatalogItems` and consume its outputs in place of the inline declarations.
- [x] 2.3 Move the corresponding test cases from `CatalogView.spec.tsx` into `apps/chat/src/hooks/useCatalogItems/tests/useCatalogItems.spec.ts` (using `renderHook`, reusing existing mocked `server-api`/context fixtures). *(Verified via `npm run test:file` on both `useCatalogItems.spec.ts` (28 tests) and `CatalogView.spec.tsx`, both green; `nx run @epam/chat:typecheck` clean.)*
- [x] 2.4 Run `npm run verify:changed`; fix any regression before moving on. *(Verified 2026-09-06 via fresh `nx run @epam/chat:typecheck --skip-nx-cache` + `npm run test:file -- apps/chat/src/components/CatalogView/tests/CatalogView.spec.tsx`, both green.)*

## 3. Extract `useCatalogToolsetCredentials`

- [x] 3.1 Create `apps/chat/src/hooks/useCatalogToolsetCredentials/useCatalogToolsetCredentials.ts` owning `getLevelStatus`, `showLoginSuccess`, `showLogoutSuccess`, `handleLogin`, `handleLogout`.
- [x] 3.2 Update `CatalogView.tsx` to call the new hook and pass its `handleLogin`/`handleLogout` through to `Catalog`.
- [x] 3.3 Move the corresponding test cases into `apps/chat/src/hooks/useCatalogToolsetCredentials/tests/useCatalogToolsetCredentials.spec.ts`, preserving the silent-cancel-outcome assertion. *(Verified via `npm run test:file` on both `useCatalogToolsetCredentials.spec.ts` (10 tests, including a dedicated Cancelled-outcome unit test against a mocked `useToolsetLogin`) and `CatalogView.spec.tsx`, both green.)*
- [x] 3.4 Run `npm run verify:changed`. *(Same verification pass as 2.4 — both hooks were in place together when checked.)*

## 4. Extract `useCatalogPublishing`

- [x] 4.1 Create `apps/chat/src/hooks/useCatalogPublishing/useCatalogPublishing.ts` owning `getPublishHistory`, `handlePublish`, `handleUnpublish`, `handlePublishSuccess`, `handlePublishError`, `handleFetchExistingRules`, `isPublishVisible`.
- [x] 4.2 Update `CatalogView.tsx` to call the new hook, preserving the `showPublishError`-then-rethrow ordering in `handleUnpublish`.
- [x] 4.3 Move the corresponding test cases into `apps/chat/src/hooks/useCatalogPublishing/tests/useCatalogPublishing.spec.ts`. *(Verified via `npm run test:file` on both `useCatalogPublishing.spec.ts` (17 tests, including the moved `CatalogView — unpublish` describe with its notify-then-rethrow assertion) and `CatalogView.spec.tsx`, both green.)*
- [x] 4.4 Run `npm run verify:changed`. *(Same verification pass as 2.4 — all three hooks were in place together when checked.)*

## 5. Extract `useCatalogSharing`

- [x] 5.1 Create `apps/chat/src/hooks/useCatalogSharing/useCatalogSharing.ts` owning `isShareVisible`, `isUnshareVisible`, `isRevokeShareVisible`, `handleUnshare`, `handleRevokeShare`, `handleFetchRecipientsCount`. *(`isApplicationsSharingEnabled`/`isToolsetsSharingEnabled` moved in too — they were used exclusively by `isShareVisible`.)*
- [x] 5.2 Update `CatalogView.tsx` to call the new hook, preserving the swallowed-refetch-error behavior in `handleUnshare`.
- [x] 5.3 Move the corresponding test cases into `apps/chat/src/hooks/useCatalogSharing/tests/useCatalogSharing.spec.ts`. *(Verified via `npm run test:file` on both `useCatalogSharing.spec.ts` (21 tests) and `CatalogView.spec.tsx` (92 tests), both green.)*
- [x] 5.4 Run `npm run verify:changed`. *(Verified 2026-09-06 via fresh `nx run @epam/chat:typecheck --skip-nx-cache` (clean) and `nx run @epam/chat:lint` (clean, one pre-existing unrelated warning in `ScheduledTaskDetailPage.spec.tsx`).)*

## 6. Extract `useCatalogItemActions`

- [x] 6.1 Create `apps/chat/src/hooks/useCatalogItemActions/useCatalogItemActions.tsx` owning `fetchPromptDto`, `handleUseInChat`, `handleCardSelect`, `handleDownload`, `isDownloadVisible`, `isPrimaryActionVisible`, `onToggleFavorite`, `renderContentFilePreview`. *(`.tsx` extension: `renderContentFilePreview` returns JSX, matching the existing `useSettingsTabConfig.tsx`/`useNavigationMenuGroups.tsx` precedent.)*
- [x] 6.2 Update `CatalogView.tsx` to call the new hook, passing `catalogItems`/`isLoading` from `useCatalogItems` and `onLoadSkillDetailsFile` through, and threading its eight outputs into the same `Catalog` props as before.
- [x] 6.3 Moved the corresponding test cases into `apps/chat/src/hooks/useCatalogItemActions/tests/useCatalogItemActions.spec.ts` (27 tests, `renderHook`-based); left in `CatalogView.spec.tsx`: `onFetchDetails`-owned prompt-fetch cases (`useCatalogItems`'s own `getPrompt`/`getPublicPrompt` wiring), `isRevokeShareVisible`/`isShareVisible` cases, `handleEdit`/`handleDelete`/`createOptions` cases (section 7, not yet extracted), and the skill archive-download filename-derivation/error-path cases (cross-hook integration through the skill-details-open flow).
- [x] 6.4 Run `npm run verify:changed`. *(Verified 2026-09-06 via `npm run test:file` on both `useCatalogItemActions.spec.ts` (27 tests) and `CatalogView.spec.tsx`, both green; fresh `nx run @epam/chat:typecheck --skip-nx-cache` clean; `eslint` on touched/new files clean except the pre-existing repo-wide `@nx/enforce-module-boundaries` lazy-load warning already present on every other file that statically imports `@epam/ai-dial-chat-hooks`/`@epam/ai-dial-chat-shared`, e.g. `useCatalogSharing.ts`/`useCatalogPublishing.ts`.)*

## 7. Extract `useCatalogEditNavigation`

- [x] 7.1 Create `apps/chat/src/hooks/useCatalogEditNavigation/useCatalogEditNavigation.ts` owning `buildEditorUrl`, `handleEdit`, `handleDelete`, `createOptions`.
- [x] 7.2 Update `CatalogView.tsx` to call the new hook.
- [x] 7.3 Move the corresponding test cases into `apps/chat/src/hooks/useCatalogEditNavigation/tests/useCatalogEditNavigation.spec.ts`. *(24 tests, `renderHook`-based; the isEditable-gated "opens the editor for a writable shared prompt/skill" and "keeps public prompts/skills read-only" cases stayed in `CatalogView.spec.tsx` — they exercise `useCatalogItems`' `isEditable` derivation together with this hook's navigation, i.e. cross-hook, not this hook's own contract.)*
- [x] 7.4 Run `npm run verify:changed`. *(Verified via `npm run test:file` on both `useCatalogEditNavigation.spec.ts` (24 tests) and `CatalogView.spec.tsx`, both green; fresh `nx run @epam/chat:typecheck --skip-nx-cache` clean; `nx run @epam/chat:lint` clean except the pre-existing unrelated warning in `ScheduledTaskDetailPage.spec.tsx`.)*

## 8. Finalize composition

- [x] 8.1 Re-read the resulting `CatalogView.tsx`; confirm it only calls data-source hooks, the six new hooks, and renders the unchanged JSX — no leftover inline state/handlers from the six groups. *(Confirmed by the orchestrating session prior to this slice; re-verified here incidentally while tracing prop wiring for the 8.2/8.3 test audit — no leftover inline state/handlers found.)*
- [x] 8.2 Trim `CatalogView.spec.tsx` down to composition/integration cases that exercise more than one hook together; remove now-duplicated fully-isolated cases already covered by the per-hook specs. *(46 tests → 27. Moved to `useCatalogItems.spec.ts`: 4 `getPrompt`/`getPublicPrompt` fetch-wiring cases. Moved to `useCatalogSharing.spec.ts`: 3 skill-specific `isShareVisible` cases (`isMyApp`-only gate, previously untested for the Skill branch) plus the owned-skill assertion folded out of a combined test. Moved to `useCatalogItemActions.spec.ts`: 3 `handleDownload` fallback-filename-construction cases (sanitized name, unsafe chars, Unicode). Moved to `apps/chat/src/utils/tests/file-download.spec.ts`: 2 cases exercising the real (unmocked) `triggerBrowserDownload` Content-Disposition/fallback-filename behavior, which previously had zero direct coverage. Moved to `useCatalogEditNavigation.spec.ts`: 1 create-menu label case. Deleted as pure duplicates already covered upstream (verified by reading the covering spec): 2 `isEditable`-read-only cases (already exhaustively covered by `map-prompt-to-catalog-item.spec.ts`/`map-skill-to-catalog-item.spec.ts` in `libs/chat-hooks`), 5 "content file preview" cases (already covered by `libs/chat-hooks`'s `useCatalogItemDetails.spec.ts` plus this app's own `downloadSkillFile`-wiring assertions in `useCatalogItems.spec.ts`), 1 combined isPublishVisible/isShareVisible-for-skill test (isPublishVisible's `isMyApp` gate is type-agnostic and already covered), and 3 archive-download tests that duplicated existing `useCatalogItemActions.spec.ts` assertions verbatim. Kept as genuine composition/integration or composition-root cases (27 remaining): publish-folder passthrough from `usePublishFolders` (3), `ruleSourceOptions` sourcing from `useAppConfig` (1), the `itemId` search-param one-shot effect (3), sort/filter and active-tab persistence wiring (8), the UI-feature-gated early return (2), Props-contract equivalence — `isFullWidth`/`onSelect`/`visibleTypes` passthrough, added to close a spec.md coverage gap (4), the favourite+unshare and edit-navigation cross-hook cases spanning `useCatalogItems` + `useCatalogSharing`/`useCatalogEditNavigation` (4), the skill hides-Use-in-chat cross-hook case spanning `useCatalogItemActions` + `useCatalogSharing` (1), and the inline `skillsError` notification effect that lives in `CatalogView.tsx` itself, owned by no single hook (1).**
- [x] 8.3 Confirm every scenario in `specs/catalog-view-hook-decomposition/spec.md` has a corresponding passing test (existing or newly added). *(All four requirements' scenarios verified covered: "Every extracted field maps to exactly one owning hook" — confirmed structurally via 8.1's re-read, not a runtime scenario. "Consumers require no changes" — was previously untested for `isFullWidth`/`onSelect`/`visibleTypes`; added 4 tests to `CatalogView.spec.tsx`'s new "Props contract equivalence" describe. "Feature-disabled early return is preserved" — `CatalogView.spec.tsx`'s "UI feature gates" describe. "Toolset login cancellation stays silent" — `useCatalogToolsetCredentials.spec.ts`. "Unshare refetch failure does not surface as an error" — `useCatalogSharing.spec.ts`'s "preserves mutation success when the subsequent refetch rejects". "Publish/unpublish notification and rethrow ordering is preserved" — `useCatalogPublishing.spec.ts`'s "raises no success notification when the request fails, notifying then rethrowing". "catalogItems recomputes on the same input changes only" — was untested; added a new memoization test to `useCatalogItems.spec.ts` asserting reference stability across an unrelated rerender and recomputation on a `deployments` change.)*

## 9. Verification

- [x] 9.1 Run `npm run verify:full` once before completion. *(`typecheck:full` failed only on pre-existing, unrelated `apps/chat-api` errors — identical failure set present in a `tmp/agent-logs` typecheck log from 2026-09-02, before this change touched anything, so not introduced here; `@epam/chat:typecheck` itself matched cache clean (freshly re-verified with `--skip-nx-cache` after every slice). `lint:check` passed; only `format:check` flagged 6 pre-existing unformatted files (`apps/chat-api/README.md`, `libs/chat-api-client/openapi.json`, `libs/deployment-creation-form/README.md`, `refactoring-*.md`), none touched by this change. `test:full` failed only two unrelated pre-existing tasks — `@epam/ai-dial-conversation-input:test` (a flaky keyboard/focus timing test in `AddAttachmentButton`) and `@epam/ai-dial-chat-hooks:test` (`create-files-api.spec.ts`'s `downloadFile` cases fail with `object.stream is not a function`, a Blob/Response polyfill issue in that file's own test setup) — neither lib was touched by this change. `@epam/chat:test` itself: 149 test files, 2041 passed / 3 skipped, fully green.)*
- [x] 9.2 Run `npm run lint:check` (or the project's lint target) on all touched/new files. *(Covered by the `verify:full` run above — `lint:check` passed clean across the whole workspace, including all six new hook files/specs and the trimmed `CatalogView.tsx`/`CatalogView.spec.tsx`.)*
- [ ] 9.3 Manually smoke-test the Catalog view in the running app (browse, open details, toolset login/logout, publish/unpublish, share/unshare, edit, delete, create menu) to confirm no regression beyond what automated tests cover. *(Skipped — no running dev server available in this environment. Left unchecked; needs a manual pass before merge.)*
