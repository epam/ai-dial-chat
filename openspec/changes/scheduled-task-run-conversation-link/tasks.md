## 1. Backend: map conversation_id through the BFF

- [x] 1.1 Add `conversation_id?: string | null` to `UpstreamScheduleRun` in `apps/chat-api/src/scheduled-tasks/scheduled-tasks.mapper.ts`
- [x] 1.2 Extend `fromUpstreamRun` to map `conversation_id` → `conversationId`, normalizing absent/`null` to `undefined`
- [x] 1.3 Add `conversationId?: string` to `ScheduledTaskRunDto` (`apps/chat-api/src/scheduled-tasks/dto/scheduled-task-run.dto.ts`) with `@ApiPropertyOptional()`, `@IsOptional()`, `@IsString()`
- [x] 1.4 Update `scheduled-tasks.mapper.spec.ts` with cases for `conversation_id` present, absent, and `null` (using the sample payload from the proposal)
- [x] 1.5 Update `scheduled-tasks.service.spec.ts` if it asserts the full `ScheduledTaskRunDto` shape (no change needed — it only asserts `status`, not the full DTO shape)
- [x] 1.6 Run `npm run openapi` then `npm run openapi:check`; verify the regenerated `@epam/ai-dial-chat-api-client` only adds `conversationId` and nothing else changed
- [x] 1.7 Verify: `npm exec nx test chat-api`, `npm exec nx lint chat-api`

## 2. Lib: conditional row click + unread dot in ScheduledTaskRunHistoryList

- [x] 2.1 Add `conversationId?: string` and `isUnread?: boolean` to `ScheduledTaskRunItem` (`libs/scheduled-tasks/src/models/scheduled-task-run-item.ts`), with JSDoc per `.claude/rules/libs.md`
- [x] 2.2 Change `ScheduledTaskRunHistoryListProps`'s `onRunClick` signature to `(run: ScheduledTaskRunItem) => void` if not already, and confirm it's optional
- [x] 2.3 Add `unreadIndicatorLabel` to `ScheduledTaskRunHistoryListLabels` and export the type if not already exported from `index.ts` (already exported)
- [x] 2.4 Add `unreadDotColor` to the list's `*Colors`/styles interface and wire it through `buildCssVars` (default `--text-accent`)
- [x] 2.5 In `ScheduledTaskRunHistoryList.tsx`, derive per-row interactivity as `Boolean(item.conversationId) && Boolean(onRunClick)`: apply `role="button"`, `tabIndex={0}`, Enter/Space handling, `cursor-pointer` only when true; call `onRunClick(item)` only then
- [x] 2.6 Render the unread dot (reserved `size-3` slot, `size-[5.33px] rounded-full`, `aria-hidden`) before the timestamp when `item.isUnread === true`, using logical positioning (`ms`/`ps`/`start`) — implemented via normal flex row order, which is direction-agnostic under `dir`. The unread label itself is folded into the row's own `aria-label` (appended the same way `currentRunLabel` already is) rather than a nested `sr-only` span — the row's `aria-label` overrides all descendant text per the ARIA accessible-name algorithm, so a nested `sr-only` span would be unreachable to assistive technology (caught in code review, see 4.3)
- [x] 2.7 Update `ScheduledTaskDetailView`'s prop passthrough for the new list props if it narrows/re-declares them (added `unreadIndicatorLabel` label and `unreadDotColor` color passthrough)
- [x] 2.8 Add/extend tests in `ScheduledTaskRunHistoryList`'s `tests/` folder: click/keyboard fires `onRunClick` only with `conversationId` set; static row when `conversationId` missing even if `onRunClick` supplied; row's accessible name (`aria-label`) ends with `unreadIndicatorLabel` only when `isUnread` is `true`
- [x] 2.9 Update `libs/scheduled-tasks/README.md` examples showing `ScheduledTaskRunItem`/`onRunClick`/`unreadIndicatorLabel`
- [x] 2.10 Verify: `npm exec nx test @epam/ai-dial-scheduled-tasks`, `npm exec nx lint @epam/ai-dial-scheduled-tasks` — both pass; `npm run validate:docs` reports one pre-existing, unrelated failure in `libs/chat-hooks/README.md` (untouched by this change)

## 3. App: thread conversationId, compute isUnread, wire navigation

- [x] 3.1 Update `apps/chat/src/utils/map-scheduled-task-run-dto.ts` to copy `conversationId` from `ScheduledTaskRunDto` onto `ScheduledTaskRunItem`
- [x] 3.2 Add a helper (co-located with existing scheduled-tasks utils, or inline in the page) that resolves `isUnread` for a run by matching `run.conversationId` against `ConversationsContext` items via `conversationIdsMatch`, defaulting to `false` on no match/no id (`findMatchingConversation` in `map-scheduled-task-run-dto.ts`, used by `mapScheduledTaskRunDtoToItem`)
- [x] 3.3 In `ScheduledTaskDetailPage.tsx`, map each run item through that helper before passing items to `ScheduledTaskDetailView`
- [x] 3.4 Pass `onRunClick` to `ScheduledTaskDetailView`: on activation, `navigate(getConversationRoute(run.conversationId))` only — do NOT call `markConversationViewed` directly; the existing `useActiveConversationSync` (wired into the always-mounted `ConversationPanelView`) already marks the newly active conversation viewed once the URL changes
- [x] 3.5 Resolve `unreadIndicatorLabel` via `t(ConversationPanelI18nKeys.UnreadIndicatorLabel)` (or the app's existing resolved string for that key) and pass it through
- [x] 3.6 Add/extend tests in `ScheduledTaskDetailPage`'s `tests/` folder: navigation fires for a run with a conversation id (and no direct `markConversationViewed` call from the page); no navigation for a run without one; `isUnread` resolves correctly with and without a `conversations/` prefix mismatch between run id and list id
- [x] 3.7 Verify: `npm exec nx test @epam/chat` (2044 passed), `npm exec nx lint @epam/chat` — the lint target's `typecheck` dependency fails, but only on pre-existing, unrelated `cancelJob`/`ConversationTransferErrorCode` errors in `ConversationPanelView.tsx`/`.spec.tsx` (confirmed present on `development` before this change, in files this change never touches); direct `eslint` on every file this change touched (plus the two other `mapScheduledTaskRunDtosToItems` call sites it updated) is clean, and the one pre-existing `@nx/enforce-module-boundaries` warning reproduces identically on an untouched file

## 4. Cross-cutting verification

- [x] 4.1 Run `npm run verify:changed` for the full changed slice (chat-api, scheduled-tasks, chat) — `typecheck:affected`, `lint:affected`, and `test:changed` each report failures, but every failing file/error was verified (via `git diff --stat` showing no changes, and re-running the same check on `development` state / in isolation) to be pre-existing and unrelated to this change: chat-api's cascading `TS6305` composite-build errors stem from pre-existing type errors in `localized-value.ts`/`conversation.controller.ts`/`apply-chunk.server.ts`/`files-archive-download.service.ts`/`files-listing.service.ts`/`share.service.ts`; the lint failures are pre-existing Prettier drift in `ClientChannelContext.tsx` and a pre-existing `jsx-a11y` warning in this test file's untouched `ConfirmationPopup` mock; the test failures are a pre-existing `object.stream is not a function` environment issue in `create-files-api.spec.ts` and an unrelated pre-existing failure in `NegativeFeedbackModal.spec.tsx`. Every test file and lint target this change actually touches was independently run and passes (see 1.7, 2.10, 3.7). A full `npm run verify:full` was also attempted; it fails at the same pre-existing chat-api `tsc --build` composite-reference issue (unrelated files), confirming this is a repo-wide baseline issue rather than something scoped to `typecheck:affected`
- [ ] 4.2 Manually exercise in the browser: a run with a conversation id navigates and clears its dot; a run without one stays static; RTL (`dir="rtl"`) keeps the dot/timestamp order correct via logical properties — **not performed**: this sandbox has no live DIAL Scheduler/DIAL Core backend to exercise the real endpoint end-to-end; the component/page test suites added in groups 2–3 cover the equivalent behavior (conditional click, unread dot rendering and its folded-in accessible-name suffix, navigation, id-matching) at the unit/component level
- [x] 4.3 Run the five-axis quality review skill before merge — first pass found one blocking a11y defect: the row's `aria-label` overrode the nested `sr-only` unread label per the ARIA accessible-name algorithm, making the unread state unreachable by assistive technology. Fixed by folding the unread label into the row's `aria-label` (same pattern already used for `currentRunLabel`); tests and design.md/spec.md updated to match. Re-review confirmed the fix and found no further issues
