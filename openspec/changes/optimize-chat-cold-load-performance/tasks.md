## 1. Baseline and measurement tooling

- [x] 1.1 Reproduce the clean production build (`npx nx build chat --skip-nx-cache`) and record raw + gzip byte sizes for every file referenced by `apps/chat/dist/index.html` (script, `modulepreload` links, stylesheets) in a before-table; commit this table into the change's `design.md` "Baseline" or a working notes file.
- [x] 1.2 Write a small repeatable script (e.g. under `scripts/` or the scratchpad, not committed unless it becomes a permanent tool) that reads `apps/chat/dist/index.html`, resolves each referenced file, and prints raw/gzip byte sizes plus a total — used for every before/after comparison in this change so numbers are produced the same way every time.
- [x] 1.3 Confirm whether `@epam/ai-dial-ui-kit`'s published dist already contains per-module ESM boundaries (inspect `node_modules/@epam/ai-dial-ui-kit/dist/` structure) — resolves the Decision 1 open question before starting that slice.
- [x] 1.4 Confirm whether `@epam/ai-dial-attachment-canvas`'s `AttachmentCanvasProvider` eagerly touches `pdfjs-dist` internals independent of `apps/chat/src/main.tsx`'s import — resolves the Decision 2 open question before starting that slice.
- [x] 1.5 Trace all three `listConversations()` call sites in `ConversationsContext.tsx` and document which cold-load path(s) each is reachable from — resolves the Decision 4 open question before starting that slice.

## 2. Defer `pdfjs-dist` behind the PDF-preview feature boundary

- [x] 2.1 Remove the eager `pdfjs-dist` import and `GlobalWorkerOptions.workerSrc` assignment from `apps/chat/src/main.tsx:1,8-9,40`. Note (task 1.4 finding): this alone does not remove `pdfjs-dist` from the initial graph — task 2.2 is the load-bearing step.
- [x] 2.2 **Revised per task 1.4's finding**: add a lazy boundary inside `@epam/ai-dial-attachment-canvas` around the PDF-rendering path (`AttachmentCanvasBody` → `PdfContent`, which statically imports `@epam/ai-dial-react-pdf-highlighter` → `@epam/pdf-highlighter-kit` → `pdfjs-dist`) so that chain — not just `main.tsx`'s explicit import — loads only the first time an attachment resolves to `AttachmentContentType`'s PDF variant. `AttachmentCanvasContainer` is mounted unconditionally and eagerly from `apps/chat/src/app/app.tsx:550`, so a conditional *render* inside it does not by itself achieve code-splitting — the split must be a `lazy()`/dynamic `import()` boundary. Move worker URL setup (`GlobalWorkerOptions.workerSrc`) to run once inside that lazy boundary. See design.md's revised Decision 2.
  - Implemented as: `AttachmentCanvasBody.tsx` now imports `PdfContent` via `lazy(async () => { const module = await import('../PdfContent/PdfContent'); return { default: module.PdfContent }; })`, wrapped in `<Suspense fallback={<Spinner />}>` around the `AttachmentContentType.Pdf` case. `pdfjs-dist` added to `libs/attachment-canvas/package.json` peerDependencies and `vite.config.mts`'s rollup `external` list (it was previously resolved only via `apps/chat`'s implicit/hoisted dependency through `main.tsx`'s now-removed import).
  - **Revised per code review**: `PdfContent.tsx` originally owned `GlobalWorkerOptions.workerSrc = pdfWorkerUrl` at module scope, importing `pdfjs-dist` itself. Flagged as a library-isolation violation (a lib mutating a third-party runtime global that's shared across the host app, risking conflict with another `pdfjs-dist` consumer/version). Fixed: `PdfContent` no longer imports `pdfjs-dist` at all (removed from `libs/attachment-canvas/package.json` peerDependencies and `vite.config.mts`'s rollup `external` list — the lib genuinely no longer touches the package); it accepts an optional `configurePdfWorker?: () => void | Promise<void>` prop, threaded through `AttachmentCanvasContainer`/`AttachmentCanvas`/`AttachmentCanvasBody`, and calls it once (module-scope guard flag, invoked synchronously in the render body so it runs before `DocumentPreview` is created). `apps/chat/src/utils/pdf.ts` implements it (dynamic `import('pdfjs-dist')` + dynamic `import('pdfjs-dist/build/pdf.worker.min.mjs?url')`, memoized) and `app.tsx` passes it to `AttachmentCanvasContainer`. Omitting the prop falls back to `@epam/pdf-highlighter-kit`'s own CDN-hosted worker default.
  - **Also per code review**: `apps/chat/src/main.tsx` still eagerly imported `@epam/ai-dial-react-pdf-highlighter/styles.css` and `@epam/pdf-highlighter-kit/dist/pdf-highlight-viewer.css` (~20 KB raw) despite the PDF renderer itself being lazy. Moved both imports into `PdfContent.tsx`, so they're part of the same lazy chunk. Neither package declares these CSS files in its `exports` map, so strict `exports` resolution (this lib's own Vite build, and Vitest) rejects the subpath import; added the same `resolve.alias` workaround `apps/chat/vite.config.mts` already carried to `libs/attachment-canvas/vite.config.mts`. The same review comment noted `main.tsx` also eagerly imported `@uiw/react-markdown-preview/markdown.css` and `@uiw/react-md-editor/markdown-editor.css` for `@epam/ai-dial-ui-kit`'s `LazyMarkdownEditor`, used only inside `libs/skill-editor`, `libs/prompt-editor`, and `libs/scheduled-tasks` (all already route-lazy) — moved both imports into the three consuming components (`SkillEditor.tsx`, `PromptEditor.tsx`, `ScheduledTaskCreateForm.tsx`), adding `@uiw/react-markdown-preview`/`@uiw/react-md-editor` to each lib's `peerDependencies` and Rollup `external` list.
- [x] 2.3 Add/adjust a loading state for the first PDF open (spinner or equivalent) if the on-demand load introduces a perceptible delay; verify no broken UI on slow networks.
  - The `<Suspense fallback={<Spinner />}>` boundary added in 2.2 covers this — same `Spinner` already used for the body's own `isLoading` state.
- [x] 2.4 Add or update regression tests covering: PDF attachment opens and renders correctly; non-PDF conversations never trigger a `pdfjs-dist` network request.
  - `AttachmentCanvasBody.spec.tsx`: updated the PDF-content test to `await screen.findByRole(...)` (the mocked `PdfContent` module now resolves through `lazy()`, so rendering is asynchronous even though the mock itself is synchronous); added a test asserting the mocked `PdfContent` function is never called for `PlainText` content, i.e. the lazy import is never triggered for a non-PDF attachment. `PdfContent.spec.tsx`: added a minimal `vi.mock('pdfjs-dist', ...)` — jsdom has no `DOMMatrix`, which the real `pdfjs-dist` package touches at import time; the unit test only needs `GlobalWorkerOptions` to exist as an object.
- [x] 2.5 Rebuild (`npx nx build chat`), confirm no `pdfjs-dist` chunk is referenced by `dist/index.html`, and record the byte delta using the task 1.2 script.
  - Rebuilt clean (`npx nx build chat --skip-nx-cache`). `pdfjs-dist`'s payload now lives in its own `PdfContent-*.js` chunk (460,620 raw / 133,880 gzip bytes), which `dist/index.html` does **not** reference — confirmed by grep: 0 `pdfjs`/`GlobalWorkerOptions`/`pdf.worker` hits in any of the initial JS chunks, and the `PdfContent-*.js` filename itself doesn't appear in `index.html`. `scripts/measure-initial-bundle.mjs` before/after: JS gzip 1,656,352 → 1,524,068 (**−132,284 bytes**, ~8.0%); CSS unchanged (68,287 → 68,266, noise); grand total gzip 1,724,639 → 1,592,334 (**−132,305 bytes gzip, ~7.7%**). File count also dropped from 20 to 21... (see note below) — actually JS file count in the preload list went from 20 to 21 files but total bytes dropped, because one previously-separate small chunk (`toolsets-*.js`) merged differently across the two default-Rollup-heuristic builds; this is expected build-hash/default-chunking noise or a name-content check, not a regression — the measured byte totals are the authoritative before/after signal, not the raw file count.
- [x] 2.6 Run `npm run test:file` for touched test files, then `npm run verify:changed`.
  - `npm run test:file` for `AttachmentCanvasBody.spec.tsx` and `PdfContent.spec.tsx`: both pass. Full `@epam/ai-dial-attachment-canvas` suite (`nx test`): 163/163 tests pass. `npm run verify:changed` (typecheck:affected + lint:affected + test:changed against `origin/development`): all three pass.

## 3. Defer KaTeX and `react-syntax-highlighter` inside `MarkdownRenderer`

- [x] 3.1 In `libs/chat-shared/src/components/MarkdownRenderer/MarkdownRenderer.tsx`, add a lazy/dynamic-import boundary so `katex`/`rehype-katex` load only when the parsed markdown contains a math block.
  - Removed the eager top-level `import 'katex/dist/katex.min.css'` and `import rehypeKatex from 'rehype-katex'`. Added a cheap regex heuristic (`hasMathContent`, checked on the post-`preprocessLaTeX` content for `$$`/`\(`/`\[`) that gates a `useEffect` dynamic `Promise.all([import('rehype-katex'), import('katex/dist/katex.min.css')])`; the resolved plugin tuple is stored in state and merged into `rehypePlugins` via a new `effectiveRehypePlugins` memo. `remark-math` (lightweight, not a heavy dependency) stays eager so math spans are still parsed; only the KaTeX renderer/stylesheet is deferred. A message with no math never imports either module.
- [x] 3.2 In `libs/chat-shared/src/components/MarkdownRenderer/CodeBlock/CodeBlock.tsx`, add a lazy/dynamic-import boundary so `react-syntax-highlighter` loads only when a fenced code block is present.
  - Removed the eager `import { Prism as SyntaxHighlighter } from 'react-syntax-highlighter'`. Added `LazySyntaxHighlighter = lazy(async () => { const { Prism } = await import('react-syntax-highlighter'); return { default: Prism }; })`, used only in the `language`-truthy branch, wrapped in `<Suspense fallback={plainCode}>` where `plainCode` is the same unhighlighted `<pre><code>` markup already used for language-less blocks — so the code text is visible immediately either way, only the highlighting swaps in once the engine resolves. `MarkdownCodeBlock`'s own public export/contract is unchanged (still a synchronous component), so `libs/catalog`'s direct synchronous usages (`ApiDetails.tsx`, `Content.tsx`) are unaffected.
- [x] 3.3 Verify plain-text messages render immediately with no heavy-dependency network request, and messages with math/code render correctly once the deferred engine loads, including during active streaming (message starts as plain text, then gains a code fence or math block mid-stream).
  - Verified via the new tests in 3.4 (fast path, code-block path, and both streaming-transition cases using `rerender`).
- [x] 3.4 Add regression tests in `libs/chat-shared` covering: fast path (no heavy deps loaded for plain text), math path, code-block path, and the streaming transition case from 3.3.
  - `MarkdownRenderer.spec.tsx`: updated the two existing KaTeX tests to `await waitFor(...)` (math now renders asynchronously); added a `describe('deferred heavy dependencies', ...)` block with 4 new tests — fast path (plain text shows no `<math>` and no `data-language` marker, synchronously), code-block path (fenced code eventually shows the mocked highlighter's `data-language` attribute), and two streaming-transition tests (`rerender` from plain text to content containing a code fence / a math block, asserting the deferred engine loads and renders after the transition). `MarkdownCodeBlock.spec.tsx`: updated the one test that synchronously checked the mocked `data-language` attribute to `await waitFor(...)`; added a test asserting the syntax-highlighted output eventually appears. Full `@epam/ai-dial-chat-shared` suite: 325/325 pass.
- [x] 3.5 Update `libs/chat-shared/README.md` to document the async-loading behavior of `MarkdownRenderer`/`CodeBlock` (public prop contract itself is unchanged).
  - Added a paragraph to the `MarkdownRenderer` section describing the on-demand KaTeX load (and that `MarkdownCodeBlock` defers its own highlighter the same way), and a sentence to the `MarkdownCodeBlock` section describing the `Suspense`-boundary fallback behavior for `language`-truthy blocks.
- [x] 3.6 Run `npm run validate:docs`, `npm run test:file` for the touched lib, `npx nx build chat-shared`, and `npx nx build chat`; confirm no `katex`/`react-syntax-highlighter` chunk is referenced by `dist/index.html` when no test conversation contains math/code, and record the byte delta.
  - `npm run validate:docs`: fails on one **pre-existing, unrelated** finding (`libs/chat-hooks/README.md` importing `useGridEditingScroll`, not exported by `@epam/ai-dial-chat-hooks`) — confirmed via `git diff --stat -- libs/chat-hooks` showing zero changes to that lib in this session; not caused by this slice. `npm run test:file`/`nx test` for `@epam/ai-dial-chat-shared`: 325/325 pass. `nx lint @epam/ai-dial-chat-shared`: clean. `npx nx build @epam/ai-dial-chat-shared`: succeeds; the lib's own dist now has a separate `rehype-katex-*.js` chunk (369 KB raw / 87 KB gzip) that its `index.js` entry does not statically pull in.
  - **Discovery during the first `npx nx build chat --skip-nx-cache`**: after the `MarkdownRenderer`/`CodeBlock` fix alone, `dist/index.html` still referenced a small eager `esm-*.js` chunk (16.6 KB raw / 5.7 KB gzip) containing genuine `react-syntax-highlighter` code (the `react-syntax-highlighter-line-number` class-name string from its shared `create-element.js` helper) — grep confirmed zero `react-syntax-highlighter`/`refractor`/`Prism.`/`hljs` hits anywhere else in the eager graph, so the katex side was already fully clean, but this one small piece of the syntax-highlighter wasn't. Root cause traced to `libs/attachment-canvas/src/components/AttachmentCanvasBody/AttachmentCanvasBody.tsx`'s static `import { CodeContent } from '../CodeContent/CodeContent'` — `CodeContent.tsx` (a **different** consumer from `libs/chat-shared`, used to preview `AttachmentContentType.Code` attachments) statically imported `react-syntax-highlighter` directly, and `AttachmentCanvasBody` is reachable unconditionally and eagerly from the app root (the same `AttachmentCanvasContainer` mounting pattern task 1.4/Decision 2 found for `pdfjs-dist`). This is outside section 3's literal file list (`libs/chat-shared`) but was required to actually satisfy this task's acceptance check, so it was fixed here rather than left half-done: `libs/attachment-canvas/src/components/CodeContent/CodeContent.tsx` now lazy-loads `react-syntax-highlighter` the same way (`LazySyntaxHighlighter = lazy(...)` wrapped in `<Suspense fallback={<pre>{text}</pre>}>`), with a new `libs/attachment-canvas/src/components/CodeContent/tests/CodeContent.spec.tsx` (3 tests: plaintext/no-language fast paths stay synchronous with no highlighter load, language-tagged content shows the value immediately then the highlighted output once the engine loads). Full `@epam/ai-dial-attachment-canvas` suite: 166/166 pass; `nx lint @epam/ai-dial-attachment-canvas`: clean.
  - **Final clean `npx nx build chat --skip-nx-cache`** (after both fixes), measured with `node scripts/measure-initial-bundle.mjs`: `dist/index.html` now references **21 JS files** (down from 22) — the `esm-*.js` chunk is gone entirely, and neither `rehype-katex-*.js`/`katex-*.css` nor `PdfContent-*.js` are referenced. Grep of every referenced JS file for `react-syntax-highlighter-line-number`/`rehype-katex`/`katex.min`/`pdfjs-dist` finds only 2 hits in one chunk, both the compiled `import('./rehype-katex-*.js')` dynamic-import call itself (loader glue, not the library). Byte totals: JS gzip 1,524,068 → **1,440,689** (−83,379 bytes, ~5.5%); CSS gzip 68,266 → **59,834** (−8,432 bytes, ~12.3% — the on-demand `katex.min.css` import also dropped out of the eager CSS graph); **grand total gzip 1,592,334 → 1,500,523 (−91,811 bytes, ~5.8%)**, continuing from slice 2's 1,724,639 baseline (combined slices 2+3: −224,116 bytes gzip, ~13.0% off the original 1,724,639).

## 4. Remove Monaco/AG Grid from the eager initial graph

- [x] 4.1 **Revised per task 1.3's finding (mixed result, not a single yes/no)**: for Monaco, narrow `apps/chat/vite.config.mts:158`'s `manualChunks` matcher so it captures only `@epam/ai-dial-ui-kit`'s entry module (`dial-ui-kit.es.js`/`index-CIDIXPOX.js`), not every file whose path contains the package name — the current substring match re-merges the library's own already-lazy `JsonEditor-*.js` (Monaco) chunk back into the eager `ui-kit` chunk. Rebuild and verify Monaco is no longer in the initial preload list. This is app-side only, no library change.
- [ ] 4.2 For AG Grid: task 1.3 confirmed `ag-grid-community`/`ag-grid-enterprise` are statically imported inside `@epam/ai-dial-ui-kit`'s single monolithic `index-CIDIXPOX.js` with no internal split point — removing the manual chunk grouping does not split AG Grid out (Rollup has nothing to cut). Propose and implement additive subpath exports in `@epam/ai-dial-ui-kit` (e.g. a grid entry point) that isolate AG Grid-backed components from the base component set, keeping the existing root `.` export and its full re-export surface unchanged.
- [ ] 4.3 Update `apps/chat`'s imports of grid/editor-backed UI-kit components to the new subpath if 4.2 was required, and update the manual chunk config accordingly.
- [ ] 4.4 If 4.2 was required, add package-contract tests for `@epam/ai-dial-ui-kit` confirming both the root export and the new subpath export resolve correctly, and update its README to document the new subpath.
- [ ] 4.5 Verify every consumer of Monaco/AG Grid-backed components (catalog `ListView`, file manager, skill/prompt/apps editors, scheduled-task pages) still renders and functions correctly after the split — manual smoke pass plus existing automated tests for each.
- [ ] 4.6 Rebuild, confirm Monaco/AG Grid content is absent from `dist/index.html`'s referenced files (grep the remaining initial chunks for the telltale strings identified in `design.md`), and record the byte delta.
- [ ] 4.7 Run `npm run validate:docs` (if README changed), `npm run test:file` for touched projects, `npx nx build ui-kit` equivalent target if applicable, `npx nx build chat`, and `npm run verify:changed`.

## 5. CSS critical-path reduction

- [x] 5.1 Generate Tailwind output twice — once with `apps/chat/tailwind.config.js:13-17`'s two `node_modules` content globs as-is, once with them removed — and diff the resulting CSS class sets. **Done**: see design.md Decision 5 — 639 classes with both globs vs. 447 without (192 fewer), plus a third run isolating each glob's individual contribution.
- [x] 5.2 If the diff shows classes matched only by the `node_modules` globs that are never applied by any component actually rendered in `apps/chat`, remove those globs; if any matched class is genuinely used by `@epam/ai-dial-ui-kit`/`@epam/ai-dial-react-file-manager` components, keep the glob (or scope it more narrowly) and document the finding instead of cutting it. **Done**: both globs' matched classes are genuine design-system utility usages (`bg-control-*`, `divide-x-reverse`, `cursor-grab`, etc., plus a small real file-manager contribution) — no dead weight found, both globs kept unchanged. See design.md Decision 5.
- [x] 5.3 Perform a manual visual smoke pass (light/dark theme, LTR/RTL) over screens rendering `@epam/ai-dial-ui-kit` and file-manager components after any glob change, to catch a silent class-stripping regression. **N/A**: no glob change was made (5.2 kept both globs as-is), so there is no config change to smoke-test.
- [x] 5.4 Rebuild, remeasure CSS byte totals referenced by `dist/index.html` using the task 1.2 script, and record the delta. **Done**: no delta — `apps/chat/tailwind.config.js` is unchanged, so the CSS total remains the post-task-4.1 baseline (7 files, 301,683 raw / 54,137 gzip, measured via `node scripts/measure-initial-bundle.mjs`).
- [x] 5.5 Run `npm run verify:changed`.

## 6. Provider/request startup deduplication

- [x] 6.1 Based on task 1.5's trace, if two or more of `ConversationsContext.tsx`'s `listConversations()` call sites are proven reachable on the same cold-load path with no distinct guard, consolidate them into one call while preserving existing caching, error handling, and post-navigation availability. **N/A**: task 1.5/design.md Decision 4 already found none of the three call sites are redundant on the same path — no consolidation to make.
- [x] 6.2 If the call sites are not proven redundant, leave them unchanged and record the finding in the change's notes instead of forcing a consolidation. **Done**: left `ConversationsContext.tsx` unchanged; finding already recorded in design.md's Decision 4 (task 1.5's resolution) — re-verified against current source (`ConversationsContext.tsx:119-139` `refreshConversations`/`silentRefreshConversations`, `:252-275` mount/identity-change effect) with no `visibilitychange`/`focus` listener present in this file, confirming the mount/identity-change effect is the only cold-load call site.
- [x] 6.3 Confirm no other root provider under `apps/chat/src/context/**` was serialized as a side effect of this change — `AppConfigContext`, `UserConfigContext`, `DeploymentsContext`, `PromptsContext`, `SkillsContext` fetches must remain concurrent. **Confirmed**: no file under `apps/chat/src/context/**` was edited in this change (slices 1-6), so none of these providers' fetch timing changed.
- [x] 6.4 Add/adjust regression tests for `ConversationsContext` covering initial load, focus-revalidate, and post-mutation refresh scenarios to confirm behavior is unchanged after any consolidation. **No new tests needed**: `apps/chat/src/context/tests/ConversationsContext.spec.tsx` already covers initial load (every test's `waitFor(() => isLoading === false)`), the identity-change re-fetch trigger that stands in for "focus-revalidate" since `ConversationsContext` itself has no `visibilitychange` listener (`describe('ConversationsContext — identity-keyed refetch')`), and post-mutation refresh (`deleteAllConversations`, `renameConversation`, `markConversationViewed`, and the SSE-driven `silentRefreshConversations` in `watchForDisplayNameUpdate`) — since no code changed, no new coverage is required.
- [x] 6.5 Run `npm run test:file` for `ConversationsContext` tests and `npm run verify:changed`.

## 7. Verify existing lazy boundaries stay effective

- [x] 7.1 After all preceding slices, rebuild and confirm `dist/index.html`'s referenced files still exclude: `ConversationInput` (`@epam/ai-dial-conversation-input`), `DialFileManagerModal`, `CatalogView`, `DialFileManagerPage`, `SettingsPage`, scheduled-task pages, `AppsEditorPage`, `ToolsetEditorPage`, `CustomAppEditorPage`, `PromptEditorPage`, `SkillEditorPage`, invitation pages, and `NotFoundPage`.
  - Rebuilt clean (`npx nx build chat --skip-nx-cache`) and greped `dist/index.html`'s reference list (24 `modulepreload`/entry files) for every excluded name: `DialFileManagerModal`, `CatalogView`, `DialFileManagerPage`, `SettingsPage`, `ScheduledTask`, `AppsEditor`, `ToolsetEditor`, `CustomAppEditor`, `PromptEditor`, `SkillEditor`, `Invitation`, `NotFoundPage` — **zero matches for all of them**, and each still exists as its own separate lazy chunk under `dist/assets/` (confirmed by filename), so the boundary holds for all of these.
  - **Finding — `ConversationInput` boundary is NOT fully effective (pre-existing, not introduced by this change)**: although the filename `ConversationInput` doesn't appear in `dist/index.html`, the eagerly-preloaded chunk `src-Bm1Dqkrq.js` (50,604 raw / 15,746 gzip) contains the actual compiled `ConversationInput`/`EditMessageInput`/`Input`/`ChatSettingsModal`/`BottomSheetShell` component implementations from `@epam/ai-dial-conversation-input`'s flat `index.ts` barrel — confirmed by content grep (`Et=({isStreaming:e=!1,placeholder:t=`Type a prompt or use "/" t...`` is the real component body, not a re-export stub). Root cause: `apps/chat/src/hooks/keyboard-shortcut/useKeyboardShortcutPreference.ts:1` and `apps/chat/src/hooks/navigation/useNavigationMenuGroups.tsx:2` — both reachable eagerly from the app shell (navigation menu, not behind any route `lazy()`) — import the `SendOnEnter` enum as a **runtime value** from `@epam/ai-dial-conversation-input`'s package root. Since that root is a single flat `index.ts` re-exporting `ConversationInput`/`EditMessageInput`/`Input`/`ChatSettingsModal`/`BottomSheetShell` alongside `SendOnEnter`, and the same module is *also* the target of `NewConversationComposer.tsx`/`ConversationView.tsx`'s `lazy()` dynamic imports, Rollup resolves the whole module as statically reachable (because of the eager `SendOnEnter` import) and folds the dynamically-imported chunk into the eager graph instead of keeping it split — the exact same "flat-barrel-with-no-subpath-exports" defect class documented for `@epam/ai-dial-ui-kit` in Decision 1, just for a different package. Confirmed pre-existing (not caused by sections 1-6 of this change): `git log`/`git diff` show neither hook file has been touched in this change or in recent history relevant to it.
  - **Not fixed in this session**: cleanly fixing it would need the same "additive subpath export" treatment as Decision 1 (e.g. a `@epam/ai-dial-conversation-input/models` or similar lightweight subpath carrying just `SendOnEnter` and sibling model types, so the two eager hook files can import the enum's real runtime value without touching the barrel that also re-exports the heavy components) — TypeScript string enums cannot be safely worked around with a raw string-literal + type-only import (a string enum member is not structurally assignable from its underlying string literal without an unsafe cast), so an app-only fix isn't a clean option here. This is the same class of work as section 4's `@epam/ai-dial-ui-kit` packaging fix, which this session's slice selection (7-9 only) did not include. Recorded as a named follow-up per task 8.5 rather than worked around with an unsafe cast or expanded out-of-scope library change.
- [x] 7.2 Confirm the deliberate eager pre-fetch of the conversation page module (`apps/chat/src/app/app.tsx:110-116`) is preserved and still functions (no Suspense flash on first navigation to an existing conversation).
  - Source confirmed unchanged: `apps/chat/src/app/app.tsx:110-116`'s `const conversationPageModule = import('../pages/Conversation/Conversation');` still runs at module scope (before `App`'s render), and `ConversationRoute.tsx:333,365` still wraps its content in `<Suspense fallback={<RouteFallback />}>`. `ConversationRoute.spec.tsx` (full suite) passes unchanged, including its keyboard-shortcut-preference scenarios that exercise the same `SendOnEnter` code path noted in 7.1.
- [x] 7.3 Exercise each lazily-loaded feature (catalog navigation, prompt selection, file manager, settings, skill editor, sharing routes) manually or via existing e2e/integration coverage to confirm no regression from the chunk-graph changes in sections 2-6.
  - No e2e project exists in this repo and no live browser/server is available in this environment (same limitation design.md's Decision 6 documents) — used the existing RTL/Vitest integration suites as the available substitute: `CatalogView.spec.tsx`, `ListView.spec.tsx` (catalog navigation/grid), `DialFileManagerPage.spec.tsx` (file manager), `SettingsPage.spec.tsx` (settings), `ScheduledTasksPage.spec.tsx`, `AppsEditor.spec.tsx`, `ToolsetEditor.spec.tsx`, `PromptEditor.spec.tsx`, `SkillEditor.spec.tsx` (editors), `SharedInvitation.spec.tsx`, `ConversationSharedInvitation.spec.tsx` (sharing routes), `NotFoundPage.spec.tsx` — all pass. Combined with `@epam/ai-dial-attachment-canvas` (166/166) and `@epam/ai-dial-chat-shared` (325/325) suites already run in sections 2/3, every consumer of a chunk-graph-affected path has passing coverage; no functional regression found.

## 8. Budgets, before/after report, and lab measurement procedure

- [x] 8.1 Using the task 1.2 script, produce a final before/after byte table (raw + gzip, JS and CSS separately, and total) comparing the section 1 baseline to the post-implementation clean build.
  - Final clean `npx nx build chat --skip-nx-cache` + `node scripts/measure-initial-bundle.mjs` (this session, after sections 2, 3, 4.1, 5, 6, 7 — section 4.2-4.7 not implemented, per this session's slice-7/8/9-only scope):

    | | raw bytes | gzip bytes | vs. section 1 baseline (gzip) |
    |---|---|---|---|
    | JS (25 files) | 3,895,371 | 1,082,371 | 1,656,352 → 1,082,371 (**−573,981, −34.7%**) |
    | CSS (7 files) | 301,683 | 54,137 | 68,287 → 54,137 (**−14,150, −20.7%**) |
    | **Total** | **4,197,054** | **1,136,508** | 1,724,639 → 1,136,508 (**−588,131, −34.1%**) |

    File count rose from 20 (baseline) to 25 (now) even though total bytes dropped, because sections 2/3/6's split of `pdfjs-dist`/`katex`/`react-syntax-highlighter` out of monolithic chunks, plus 4.1's narrower `manualChunks` matcher, created more small independent chunks (`UserContext`, `AppConfigContext`, `DeploymentsContext`, `ThemeContext`, `hooks`, etc.) instead of merging them into `ui-kit`/`src-*` — expected, not a regression; the gzip byte totals are the authoritative before/after signal per this change's convention (see task 2.5's note).
- [x] 8.2 Confirm the measured post-change totals meet or beat the budgets stated in `design.md`'s Goals section; if any budget is missed, document why and whether it requires a follow-up (e.g. a library packaging fix not completed in this change) rather than silently lowering the target.
  - **CSS budget (≤66 KB gzip): MET.** 54,137 gzip is 12 KB under budget.
  - **JS budget (≤450 KB gzip): MISSED.** 1,082,371 gzip is ~632 KB over budget, and the overall ≤516 KB gzip total target is also missed (1,136,508 gzip). Root cause, both already named in `design.md` as known, tracked gaps rather than new discoveries:
    1. `ui-kit-vgu1A96y.js` (472,204 gzip) — task 4.1 (done, this session's earlier work) removed Monaco from this chunk, but AG Grid remains, because Decision 1's AG Grid fix (an additive `@epam/ai-dial-ui-kit` subpath export, tasks 4.2-4.7) was **not implemented in this session** — the user's slice selection for this session was sections 7, 8, 9 only, explicitly excluding section 4.2-4.7's library packaging work.
    2. `api-client-DLQCjIE0.js` (227,695 gzip) — the generated OpenAPI client, explicitly out of scope for this change per `proposal.md`'s "Explicitly out of scope" list and `design.md`'s Open Questions (flagged for a possible separate follow-up, not committed to here).
    - Both gaps are pre-identified, not silently-lowered targets: this task records that the JS/total budgets remain unmet **pending section 4.2-4.7**, which is a known, scoped, and separately-landable follow-up (see 8.5), not a new problem discovered here. Sections 2, 3, 5, 6, 7 (this session's and prior sessions' completed scope) are fully applied and their own byte contributions (PDF.js, KaTeX/syntax-highlighter, provider dedup investigation) are reflected in the 34.1% reduction achieved so far.
- [x] 8.3 Document the exact lab measurement profile from `design.md` Decision 6 (device/CPU throttle, network throttle, browser, server location, compression/cache verification, auth state, run count) as the procedure a human/CI runner must execute; if that run is executed as part of this change's validation, record its median/p95 LCP and `chat-ready` results and label them explicitly as lab-proxy results, not production RUM.
  - The procedure is already fully specified in `design.md`'s Decision 6 (device/CPU: Lighthouse mobile 4x throttle on a documented reference machine; network: Lighthouse "Slow 4G" plus one no-throttle baseline run; browser: headless Chrome latest stable via Lighthouse CLI; server location: client colocated with the `apps/chat-api` origin region; compression/cache: verify `Content-Encoding`/`Cache-Control: immutable` on the deployed target; auth: pre-seeded authenticated session cookie, interactive OIDC excluded from the timed navigation; runs: ≥7 per configuration, median + p95; `chat-ready` mark: authenticated shell committed + composer input attached/enabled; routes: `/` primary, `/conversations/:id` regression-only). **Not executed in this environment**: no live browser, no deployed target, and no Lighthouse CLI access exist here (same limitation `design.md`'s Context section and Decision 6 state explicitly) — this task's deliverable is the documented, ready-to-run procedure itself (already complete in `design.md`, re-confirmed current here), not a measured LCP/`chat-ready` number. A human or CI runner with real Chrome + deployed-target access must execute it and record results labeled "lab proxy, not production RUM" per the Requirement's scenario.
- [x] 8.4 Verify production build chunk loading still works under the deployed base path and existing CSP (manual check of `apps/chat-api`'s static-serving config and any CSP headers affecting script-src/style-src for the new/changed chunks — no `apps/chat-api` code change expected, verification only).
  - Confirmed no `apps/chat-api` file is touched by this change (`git diff --stat -- apps/chat-api` is empty across sections 1-7), so its static-serving config and any CSP header configuration are unchanged from their pre-change state — nothing to re-verify beyond that identity. The new/changed chunks (`PdfContent-*.js`, `rehype-katex-*.js`, the narrower `ui-kit-*.js`) are all still emitted under the same `apps/chat/dist/assets/` base path with the same content-hashed naming scheme as every other chunk `apps/chat-api` already serves today — no new path pattern, script-src origin, or file extension was introduced that a CSP `script-src`/`style-src` rule keyed on the existing asset path/origin would need to special-case.
- [x] 8.5 If any material bottleneck is found outside this change's scope (BFF delivery headers, generated API-client bulk, a repo-wide bootstrap orchestrator), write it up as a named follow-up with expected byte/performance benefit, owning project, and regression surface, per the proposal's closing requirement.
  - **Follow-up 1 — `@epam/ai-dial-ui-kit` AG Grid subpath export (tasks 4.2-4.7, deferred by this session's slice selection).** Expected benefit: removes the AG Grid engine's share of `ui-kit-*.js`'s 472 KB gzip from the eager graph — the single largest remaining lever, likely the majority of the ~632 KB JS-budget gap. Owning project: `@epam/ai-dial-ui-kit` (library change) + `apps/chat` (consumer import updates). Regression surface: every AG Grid/Monaco-backed consumer (catalog `ListView`, file manager, skill/prompt/apps editors, scheduled-task pages) — same list already enumerated in `design.md` Decision 1 and tasks.md 4.5.
  - **Follow-up 2 — `@epam/ai-dial-conversation-input` eager-value-import leak (new finding, task 7.1).** Expected benefit: ~15.7 KB gzip removed from the eager graph, plus closes a defect class (flat barrel with no subpath split) that could recur for any other lightweight enum/const the package exports. Owning project: `@epam/ai-dial-conversation-input` (additive subpath export for `SendOnEnter` and sibling model types) + `apps/chat`'s two hook files (`useKeyboardShortcutPreference.ts`, `useNavigationMenuGroups.tsx`). Regression surface: the keyboard-shortcut preference UI and the two lazy consumers of `ConversationInput` (`NewConversationComposer.tsx`, `ConversationView.tsx`) — low risk, already covered by `ConversationRoute.spec.tsx`.
  - **Follow-up 3 — generated API-client bulk (`api-client-*.js`, 227.7 KB gzip, already flagged in `design.md`'s Open Questions).** Expected benefit: unquantified pending investigation into what fraction is executable-at-bootstrap vs. type-only/schema literals; potentially large given it's the second-largest remaining eager chunk. Owning project: `libs/chat-api-client` (generated, would need an OpenAPI-generation or import-boundary change, out of scope per `AGENTS.md`'s library-isolation exception for that lib) + the root providers that import it eagerly (`PromptsContext.tsx`, `SkillsContext.tsx`, `DeploymentsContext.tsx`). Regression surface: broad — every feature that calls DIAL Core through the generated client.
  - **Follow-up 4 — BFF delivery headers (gzip/brotli + immutable cache, already flagged in `design.md`'s Open Questions).** Expected benefit: unverified — depends on whether the deployed target already serves these headers; if missing, correctly configuring them would reduce transfer time (not raw byte count) for every asset in this change's scope. Owning project: the BFF/deployment layer (`apps/chat-api` static-serving config or the reverse proxy in front of it), not `apps/chat`. Regression surface: none expected (headers-only change) but requires the lab profile (task 8.3) to confirm before/after transfer-time impact.
  - No repo-wide bootstrap orchestrator follow-up is recorded: `design.md` Decision 4 already concluded (task 1.5/6.1-6.3) that `ConversationsContext`'s three call sites are not redundant and the other root providers already fetch concurrently, so there is no uncoordinated-fan-out finding in this change to write up as a follow-up.

## 9. Final verification

- [x] 9.1 Run `npm run build:quiet` to confirm all bundle-affecting projects build cleanly.
  - `PASS build:affected (33.7s)` — all Nx-affected projects (relative to `origin/development`) build cleanly.
- [x] 9.2 Run `npm run validate:docs` (README/public-export changes were made in sections 3 and/or 4).
  - Fails on the same single **pre-existing, unrelated** finding already noted in task 3.6: `libs/chat-hooks/README.md:1158` imports `useGridEditingScroll`, not exported by `@epam/ai-dial-chat-hooks`. Re-confirmed here: `git diff --stat -- libs/chat-hooks` is empty (zero changes to that lib across this entire change, sections 1-9), and `git log` shows its README was last touched by unrelated commits (`cc441abc1`, `210c62e7f`) predating this change. No README touched by sections 2-6 of this change (`libs/attachment-canvas/README.md`, `libs/chat-shared/README.md`) reports any validation failure.
- [x] 9.3 Run the repository's required full verification (`npm run verify:full`) before considering the change complete.
  - `typecheck:full` **fails**, but only in two projects this change never touches: `@epam/chat-api` (795 errors — `TS6305` project-reference/incremental-build errors plus genuine `TS18046`/`TS7006`/`TS2769`/`TS2835`/`TS2322` type errors across `skills/`, `telemetry/`, `themes/`, `toolsets/`, `transcription/`, `user-config/`) and `mcp-app-sandbox`. Confirmed pre-existing and unrelated: `git diff --stat -- apps/chat-api` is empty (zero changes across this entire session/change), and re-running `npx nx run @epam/chat-api:build` then `npx nx run @epam/chat-api:typecheck` fresh (ruling out stale incremental `.tsbuildinfo`/`dist` state) reproduces the same 795 errors — this is a standing defect on `origin/development` itself, not something introduced here.
  - `lint:check` **passes** cleanly across the whole repo. Its `format:check` sub-step fails on 3 files, all pre-existing/unrelated: `apps/chat-api/README.md` and `libs/chat-api-client/src/generated/README.md` (both zero-diff — already unformatted on `origin/development`), and `.claude/skills/lean-verification/SKILL.md` (modified before this session started, not by this change — see the session's initial `git status`).
  - `test:full` **passes** — full repo test suite (`nx run-many -t test --all`) is green.
  - This change's own affected-scope gates are fully green: `typecheck:affected`, `lint:affected`, `test:changed` (task 7's `verify:changed` run), plus every touched project's individual `nx test`/`nx build` (sections 2, 3, 4.1, 9.4) all pass. The full-repo `verify:full` gate is blocked only by pre-existing, out-of-scope issues in `apps/chat-api`, `mcp-app-sandbox`, and 3 unrelated files — none introduced by, or fixable within, this change's scope (`apps/chat-api` is explicitly out of scope per `proposal.md`).
- [x] 9.4 Confirm all affected libraries (`libs/chat-shared`, and `@epam/ai-dial-ui-kit` if touched) remain independently buildable and publishable (`npx nx build <project>` for each).
  - `npx nx build @epam/ai-dial-chat-shared --skip-nx-cache`: succeeds (its `rehype-katex-*.js` chunk stays split from `index.js`, confirming task 3's lazy boundary is preserved in the lib's own standalone build, not just inside `apps/chat`'s bundle).
  - `npx nx build @epam/ai-dial-attachment-canvas --skip-nx-cache`: succeeds (its `PdfContent-*.js` chunk stays split from the lib's other exports, confirming task 2's lazy boundary likewise holds standalone).
  - `@epam/ai-dial-ui-kit` was not touched in this change (section 4.2-4.7, the only planned library-side change to it, was not implemented in this session's slice-7/8/9-only scope) — nothing to re-verify for it.
- [x] 9.5 Re-read `design.md`'s Open Questions section and confirm each was resolved (or explicitly deferred with reasoning) during implementation.
  - All 5 Open Questions in `design.md` are already marked resolved or explicitly deferred with reasoning (re-read and confirmed current, no drift from implementation):
    1. `@epam/ai-dial-ui-kit` ESM boundaries — resolved (task 1.3, mixed finding, drove the revised Decision 1).
    2. `AttachmentCanvasProvider`/PDF.js eager touch — resolved (task 1.4, drove the revised Decision 2, implemented in section 2).
    3. `ConversationsContext` triple call-site redundancy — resolved (task 1.5, no consolidation needed, section 6).
    4. `api-client-*.js` executable-vs-schema-literal fraction — explicitly deferred as out of scope, recorded as Follow-up 3 (task 8.5) rather than silently dropped.
    5. Production gzip/brotli + immutable-cache header verification — explicitly deferred (no deployment access in this environment), recorded as Follow-up 4 (task 8.5).
  - One additional item surfaced during this session's section-7 verification work that `design.md` did not originally anticipate — the `@epam/ai-dial-conversation-input` eager-value-import leak (task 7.1) — is recorded as Follow-up 2 (task 8.5); `design.md` itself was not amended with a 6th Open Question since the finding is already fully captured in tasks.md's own record, per this change's practice of keeping the byte-level narrative in tasks.md's task notes rather than duplicating it into `design.md`.
